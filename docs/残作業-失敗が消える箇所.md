# 残作業：失敗が静かに消える箇所の一覧（2026-09-10 更新）

このファイルは **作業用のチェックリスト**です。片付いたら行を消していき、
全部消えたらこのファイルごと削除してください。
（引き継ぎ本体に長い一覧を置くと、毎回それを読むことになるので分けています）

## 何が問題か

🚨 **supabase の `.update()` / `.delete()` は、RLS（権限）で弾かれても
error にならず「0件成功」で返る。** 戻り値を受けないと、
何も更新できていなくても先へ進んでしまう。

2026-09-09 に実際に起きていたこと：
- 受理が通っていないのに、申請者へ「受理されました」と通知が飛んでいた
- 更新できていないのに、Googleカレンダーの予定が削除されていた

## 直し方（決まった形）

```ts
import { describeUpdate } from '../lib/statusUpdate';   // パスは階層に合わせる

const res = await supabase.from('テーブル').update({ … }).eq('id', id).select('id');
const fail = describeUpdate(res, '受理', 'missing');   // 'competing' との使い分けは下記
if (fail) { setErrorMsg(fail); return; }
```

- `missing` … 権限が無い／対象が消えた（→ 管理者に相談。自分では直せない）
- `competing` … 他の人が先に処理した（→ 一覧を更新して確認）
- 🚨 この2つは**利用者が次にすべきことが正反対**なので混ぜないこと

【`.eq('status', …)` を足すかどうか】
　そのボタンが**特定の状態のときにしか画面に出ない**なら足してよい（挙動は変わらない）。
　管理者の是正操作など、どの状態からでも押せるものには足さない（管理者が詰む）。

【順序】
　🚨 **失敗したときに「もう一度押せば直る」順序にする。**
　　 例：関連する行を消すとき、一覧に出す元になっている行（＝対象）を**最後に**消す。
　　 先に消すと、残りが中途半端に残ったまま一覧から消えて、直す手段が無くなる。

【lint】
　`client/eslint.config.js` に「戻り値を受けない update/delete」を禁止するルールがある。
　🚨 片付いたファイルを `files:` に**足していく**こと（足す前に0件を確認）。

---

## 残り 0件（2026-09-10 に両方の領域とも完了）

### ✅ 場所予約（`pages/RoomBookingPage.tsx`）… 2026-09-10 に完了（場所予約担当）

🚨 **「13件」は過少だった。実測は 20 件**（コミット f9a98c0）。
・同じ ESLint ルールで 16 件。うち 1 件は `run()` が件数を見ていて安全（誤検知）
・この一覧から **`room_recurrences` の `.delete()`（2501行付近）が漏れていた**
・**ルールが拾えない 5 件**があった。`run()` という共通ラッパを経由していて、
  その `run()` 自体が error しか見ていなかった → ラッパを2本とも直した
・`eslint.config.js` の `files:` に追加済み。**追加後に0件を実測**
・詳細は `docs/引き継ぎ-場所予約.md` の (60)

🚨 **次に数える人へ**：このルールは `ExpressionStatement > AwaitExpression` しか見ないので、
　**共通ラッパ経由（`run(async () => … .update(…))`）と、戻り値を受けて error だけ見ている形は拾えない**。
　ファイルを `files:` に足して0件でも、「全部直った」とは言えない。ラッパの中身まで読むこと。

### ✅ 連絡板・設定・マスタ（28か所）… 2026-09-10 に完了

5ファイルとも ESLint の `files:` に追加し、**追加後に0件であることを実測済み**（tsc・vite build も通過）。

| 場所 | 直した内容 |
|---|---|
| `pages/BoardPage.tsx` 削除4連（2か所） | 共通の **`deleteNoticeRows()`** に集約。関連3表 → 本体 の順は維持し、本体だけ件数を見る。まとめて削除は**消せなかったものを選んだまま残す**（もう一度押せる） |
| `pages/BoardPage.tsx` お知らせの修正 | error だけ見て「保存しました」を出していた。件数まで見て、失敗したら編集を閉じない |
| `pages/BoardPage.tsx` アーカイブ・戻す・送信トレイの隠す | 消えていないのに一覧から消さない |
| `pages/BoardPage.tsx` お気に入り（実害小） | 失敗したら★の見た目を変えない。解除の0件は正常（二度押し）＝error だけ見る |
| `pages/BoardPage.tsx` 送信の宛先 insert | 🚨 **失敗すると誰にも届かないのに送信済みに見えていた**。止めて通知・メールも送らず、送信トレイを読み直して消せるようにした |
| `pages/BoardPage.tsx` CC の記録 | 🚨 **赤にしない**（お知らせ自体は送れている）。黄色の部分成功で断る |
| `components/admin/BoardSettingsTab.tsx` | チャンネルの権限保存／DMの権限保存／既読詳細の ON・OFF（消してから入れ直す作りなので、失敗したらボタンも戻す） |
| `components/admin/NotificationsTab.tsx` | メール定型文の追加・更新・削除／予約リマインドの保存・ON/OFF・削除／リマインド日数の保存 |
| `components/admin/OvertimeAdminTab.tsx` | 通常シフトの締め・削除（🚨 失敗すると新旧2本が並ぶので止める。止めたら一覧を読み直す＝もう一度押せる）／会社カレンダーの削除 |
| `components/admin/GroupsTab.tsx` | 🚨 **順序を入れ替えた**。人の所属を先に、一覧のもとになる `master_options` を最後に。先にグループを消す／改名すると、直せなかった人が残ったときに**探す入口が消える** |

🚨 **本番の実測で見つけたこと（次に触る人へ）**：
`board_reads` / `board_confirmations` / `board_message_recipients` には **`id` 列が無い**
（鍵は message_id と user_id の組）。件数を見るための `.select('id')` を書くと
「列が無い」エラーになり、**いつも失敗する削除**になる。`.select('message_id')` を使うこと。
同様に `company_calendar` は `date`、`reminder_days_settings` は `event_key`。
**書く前に列を実測すること**（`information_schema.columns` を1回引けば分かる）。

### ✅ 申請まわり（8か所）… 2026-09-09 夜に完了

6ファイルとも ESLint の `files:` に追加し、**追加後に0件であることを実測済み**。

| 場所 | 直した内容 |
|---|---|
| `pages/CalendarPage.tsx` 勤怠の取消 | 🚨 **段0**。取消できていないのに Googleカレンダーの予定が消え、リーダーへ取消通知まで飛んでいた。件数を見て**止める**ようにし、失敗は消えない赤で出す |
| `pages/OvertimePage.tsx` 締め後申請の取り下げ | 件数を見る＋`status='open'` を条件に（経理の判断を黙って上書きしない）。失敗は消えない赤 |
| `pages/OvertimePage.tsx` 残業の時間帯の入れ替え | 消し漏れの2経路（終日に変更＝insert が走らない／3本→2本＝3本目が残る）は unique 制約の網に掛からない。error で止める |
| `components/admin/LeaveRequestsTab.tsx` 有給奨励日 | 消せていないのに insert すると**承認済みの休暇が二重**になる。回答の update も件数を見る |
| `lib/faq.ts` 回答の保存 | 対象役職を消し漏れると**見えてはいけない役職に回答が出る**。本文の update も件数を見る |
| `components/OvertimeProposalResponse.tsx` 提案の紐づけ | 申請自体は成立するので**赤にしない**。黄色（部分成功）で断る |
| `App.tsx` 通知の既読 | 実害小。記録だけ残す（画面から消すのはそのまま＝閉じる手段を奪わない） |

🚨 **件数0を一律に失敗としない**、が今回の判断のポイント。
　 「その phase を初めて保存する」「まだ休暇申請が無い人を直す」「対象を1つも付けていない回答」は
　 **0件が正常**。そこを失敗にすると、正しい操作が止まる。これらは `error` だけを見ている。

---

## 別枠：`.then(null, () => {})`（54件）

🚨 **一括で置換しないこと。** 受け手によって意味が真逆です。

- 受け手が **supabase のビルダー** → エラーを拾えていない（直す対象）
- 受け手が **素の async 関数**（`dispatchEmail` など）→ **正当な守り**。外すと未処理エラーになる

判定は目視ではなく、受け手を機械で辿ること。
