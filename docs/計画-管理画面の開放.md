# 計画：管理画面をマネージャー以上に開く（PC のみ・タブは管理者が選ぶ）

2026-09-14 作成。ユーザーの要望と、一問一答で決めたことをまとめる。
🚨 **2人共通ファイル（App.tsx / AdminPanel.tsx / AdminPanelContext.tsx / FeaturePermissionsTab.tsx）の動きを変える**ので、
着手前に場所予約の担当へ一声かける（CLAUDE.md ②）。連絡文は末尾。

---

## 1. 要望（ユーザーの言葉を要約）

- シフトを今後はアプリで登録したい。前の版から何が変わったかを赤で出す。いつから適用かを入れる。
  修正ボタンで一気に入れて保存。Excel取込も残す。使いたいときから使えるようにする
- それを触れるのは **マネージャー以上が PC でログインしたときだけ**。管理画面に入れる
- 管理画面で（見せるタブを）追加できるようにしたい
- シフト・リマインド・FAQ、ほかに入れたほうがよいものは？

## 2. 決まったこと（2026-09-14 ユーザー確定）

| 項目 | 決定 |
|---|---|
| マネージャー以上に開けるタブ（候補） | **グループ／通常シフト／リマインド設定／FAQ管理／お知らせ・安否確認** |
| 見せるタブの決め方 | 管理者が「権限管理」でタブごとにチェック。**最初は全部オフ** |
| お知らせ | マネージャー以上も **作成も送信もできる**（🚨 2026-09-09 の「送れるのは管理者だけ（Q5=B）」を変える） |
| グループ | **スタッフの所属だけ**。グループそのものの追加・名前の変更・削除は管理者だけ |
| 安否確認の削除 | **管理者だけのまま** |
| シフトの一括編集で赤く出す比べ先 | **適用開始日の前日に効いている版** |
| 一括編集で保存したとき新しい版を作る人 | **表に出ている全員** |
| 作る順 | **① 入口 → ② シフトの一括編集 → ③ リマインド・FAQ**（お知らせ・グループ・安否は①に含めるかは下の段取りで） |

※「マネージャー以上」＝ `roles.is_manager_plus`（マネージャー5・社長1・管理者1。2026-09-14 実測）。役職名では判定しない。
  🚨 **2026-09-15 ユーザー再確認：このまま（リーダー・フロア責任者は入れない）**。
  リーダーにも開くなら、役職の印を付け替えるのではなく `can_manage_admin_tab` を直す（印は安否の発信など他でも使っているため）。
※「PC」＝ `lib/idleLogout.ts` の `isPointerDevice()`（マウスのある端末）。

## 3. いまの作り（調べた結果）

- `/admin` は `App.tsx` の `AdminPage` が `isAdmin` だけで判定。ナビの「⚙️ 管理」も `isAdmin` だけ
- `AdminPanel.tsx` のタブは `TAB_GROUPS` に固定。**タブごとの権限は無い**。`?tab=` を直接打てばどのタブにも入れる
  （`AdminPanelContext.tsx` の `initialTab` は `ADMIN_TABS` に含まれるかだけを見る）
- 管理画面を開くと、どのタブかに関係なく `fetchUsers()` と `fetchLeaveRequests()` が走り、承認待ちのバッジを出す
- 各タブの書き込み先と今の決まり（本番の RLS・関数の中身を読んで確認）

| タブ | 書き込み先 | 今の書き込み権限 |
|---|---|---|
| グループ | `profiles.group_names`（所属）／`master_options`（category='group'） | 管理者だけ（`profiles` の ALL・`master_options` の I/U/D） |
| 通常シフト | `weekly_shift_patterns`（画面から1行ずつ。RPC なし・トランザクションなし） | 管理者だけ（`patterns_admin_all`） |
| リマインド設定 | `board_scheduled_reminders`／`reminder_days_settings` | 管理者だけ |
| FAQ管理 | `faq_topics`／`faq_answers`／`faq_answer_targets`／`faq_topic_relations` | `can_edit_faq()`＝管理者 or `profiles.is_faq_editor` |
| お知らせ | `announcements`（`lib/announcements.ts`）＋ Edge Function `announcement-notify` | 表は `is_admin()`・関数も admin だけ |
| 安否確認 | `safety_check_templates`／Edge Function `safety-check-send`／RPC `submit_safety_response_proxy`／RPC `delete_safety_check` | 定型文・発信・代わりの回答は **もともとマネージャー以上**。削除だけ管理者 |

- 読み取りは概ね足りている：`profiles` は全員読める／安否の発信・回答はマネージャー以上が読める／`announcements`・`app_settings` は全員読める
- 🚨 リマインド設定が読む `board_channels` は「メンバー or 作成者 or 管理者」しか読めない（2026-09-14 実測）。
  **マネージャーは自分が入っていないチャンネル宛てのリマインドを選べない**＝画面の選択肢が欠ける。
  ① でリマインドを開くなら、チャンネル一覧を読む手段（`can_manage_admin_tab('scheduled_reminders')` の人は全チャンネルの id と名前だけ読める関数など）が要る
- ✅ FAQ の集計 RPC `faq_public_event_summary` は **SECURITY INVOKER**（呼んだ人の権限で読む・2026-09-14 実定義で確認）。
  読む表 `faq_public_event` の RLS が `can_edit_faq()` なので、**`can_edit_faq()` を広げれば集計も見られる**（関数を別に直す必要はない）

## 4. 設計（① 入口）

### 4-1. 設定の持ち方
- `app_settings` の key **`manager_admin_tabs`**＝マネージャー以上に開くタブの配列（例 `["faq","scheduled_reminders"]`）。
  既定は空配列＝**全部オフ**。読みは全員（既存の RLS）・書きは管理者（既存の RLS）
- 🚨 **DB 側でも同じ設定を見る**：新しい関数 `can_manage_admin_tab(p_tab text)`＝
  `is_admin() or (is_manager_plus() and manager_admin_tabs に p_tab が入っている)`。
  各タブの書き込みの決まりをこれで広げるので、**管理者がチェックを外せば DB の書き込みも止まる**（画面だけの制限にしない）
- 🚨 **「PC だけ」は画面の制限**。DB は端末を見分けられない（スマホから API を直接叩けば書ける）。ユーザーに説明済みの前提として明記する

### 4-2. 画面
- 新しい hook `useManagerAdminTabs()`（`app_settings` を1回読んで共有・`useRoles` と同じ形）
- 「管理画面に入れる」＝ `isAdmin || (isManagerPlus && isPointerDevice() && 開くタブが1つ以上)`
  - `App.tsx` の `AdminPage` の判定と、`NavBar` の「⚙️ 管理」ボタンの判定をこれにする
    （🚨 NavBar は15か所から呼ばれている。props を増やさず、NavBar の中で hook を呼ぶ）
- `AdminPanel.tsx`：管理者でなければ `TAB_GROUPS` を「開くタブ」だけに絞る（PC の3段・スマホのセレクトの両方）
- `AdminPanelContext.tsx`：`initialTab` と `setActiveTab` も「開くタブ」に含まれるかを見る（URL 直打ち対策）。
  管理者でなければ `fetchUsers`/`fetchLeaveRequests` の常時読み込みとバッジを止める（グループのタブを開いたときだけ `fetchUsers`）
- タブの中の出し分け（管理者でない人）
  - グループ：グループの追加・名前変更・削除のボタンを出さない。所属の追加・外すは新しい RPC 経由
  - FAQ：`canManageEditors` は管理者だけ（今のまま）
  - 安否：削除ボタンを出さない
  - 通常シフト（②）：残業管理の中の「通常シフト」だけを出す（受理済み一覧・打刻・締め後の許可・会社カレンダー・設定は出さない）
- 「権限管理」タブの末尾に「マネージャー以上に開く管理画面のタブ」（新しい部品・チェックボックス）。
  `FeaturePermissionsTab.tsx` には import と1行だけ足す（自動ログアウトの設定枠と同じ形）

### 4-3. DB（① で入れるもの）
| 対象 | 変更 |
|---|---|
| 共通 | `can_manage_admin_tab(text)`（SECURITY DEFINER・anon 実行不可） |
| グループ | RPC `add_profile_group(p_user_id, p_group)`／`remove_profile_group(...)`：`can_manage_admin_tab('groups')`、`p_group` が `master_options(category='group')` にあること、`group_names` のその1つだけを足す／外す（役職・雇用形態など他の列は触れない） |
| お知らせ | `announcements` の書き込みを `is_admin() or can_manage_admin_tab('announcements')`／Edge Function `announcement-notify` の判定を同じにする（`role_is_manager_plus` ＋ 設定を RPC で確認） |
| 安否 | 変更なし（もともとマネージャー以上。削除は管理者のまま） |
| リマインド | `board_scheduled_reminders`／`reminder_days_settings` の書き込みを `is_admin() or can_manage_admin_tab('scheduled_reminders')` |
| FAQ | `can_edit_faq()` に `or can_manage_admin_tab('faq')` を足す（FAQ の表すべてに効く）。🚨 本番の実定義から起こす |
| 通常シフト | ② で RPC を作るときに `can_manage_admin_tab('shift_patterns')` を使う（① では書き込みを広げない） |

- 🚨 RLS を置き換えるときは本番の実定義を読んでから `drop policy … create policy` で置き換え、名前を変えない
- 🚨 `can_edit_faq` など既存関数を変えるときは `pg_get_functiondef` から起こし、機械で突合
- 取り消し版で「管理者／マネージャー（タブON）／マネージャー（タブOFF）／リーダー／パート」になりすまして書き込みを試す

### 4-4. 出す順
DB（関数・RLS）→ Edge Function（announcement-notify・🚨 deploy 前に git pull）→ 画面。
最初は全部オフなので、出した瞬間に誰の画面も変わらない。管理者が確かめてからタブを1つずつオンにする。

## 5. 設計（② シフトの一括編集・概要のみ。① のあとで詳しく決める）

- 「通常シフト」に［修正］→ 表に出ている全員×月〜日を一度に編集できる表 → 適用開始日 → 前日に効いている版と違うマスを赤 → ［保存］
- 保存は **SECURITY DEFINER の RPC 1回**で全員分を書く（途中で失敗したら何も残らない）。Excel取込も同じ RPC に乗せ替える
  （今は取込も1人ずつの編集も、画面から1行ずつ書いていてトランザクションが無い）
- 「使い始め」は権限管理のスイッチ（オフの間は今の1人ずつの編集と取込だけ）
- 🚨 `weekly_shift_patterns` は同じ人×同じ曜日で期間が重なる行を DB が禁止（exclude 制約）。締め→入れる の順は RPC の中で守る
- 影響先：`sync_overtime_from_leave`（時間外調整休）／OvertimePage の通常シフト／シフト調整の候補／ShiftDirectoryPage ほか

### 5-1. ② で決まったこと（2026-09-15 ユーザー確定・一問一答）
| 項目 | 決定 |
|---|---|
| 置き場所 | 管理画面に**新しいタブ「通常シフト」**（スタッフの段）。取り込み・1人ずつ編集・一覧もまとめて移し、残業管理からは外す。タブの鍵は `shift_patterns` |
| 表の列 | **月〜日の7列だけ**（祝・出は1人ずつ編集で。2026-09-15 実測で祝・出の行は0件） |
| 表に出す人 | 対象（正社員だけ／パートも）＋**チームで絞って見られる**。保存は対象の全員を確かめ、確認に「表示していない人も含めて」と出す |
| 1日の中の区切り | 🚨 **時間帯ごとに校と部門**（レビュー R5・2026-09-15 ユーザー確定）。1日を「時間・校・部門」の行で区切って入れる（例：阿部さん 9:30〜12:00 四条本校・大人／12:00〜19:15 西陣校・こども）。表のマスは「本校(大)→西陣(子)」、PDF は紙と同じ「四条本校大人11～12→西陣校15:30～」の形で出す。今の `start_time2`（外出・戻り・テレワーク）と `location` 1列はこの形に移す（移し方は段取り1で決める） |
| 呼び名 | **持たない。表にはフルネームで出す**（2026-09-15 ユーザー確定「フルネームで入れるで良くないか」）。🚨 ④掃除担当表・⑤こどもシフト表は1マスに2〜4人入り紙面が詰まるので、見本で紙面を作って読みにくければその段で相談し直す。紙から取り込むときの別の書き方（渡辺→渡邊・尾上→森本 千佳子）の結び付けは別に要る |
| 書き添え | **曜日ごと**（マスの下：「無理な時は退勤早める」）と**人ごと**（見出し：「月1(木)下鴨夢 10:00～12:00」「固定給」）の**2つ**（2026-09-15 ユーザー確定）。どちらもシフトと一緒に「いつから」が切り替わり、PDF は紙と同じ場所に印刷。書き添えだけ直した人も「変更あり」に数える |
| 1人ずつ編集 | **一括編集にまとめる**（2026-09-15 ユーザー確定）。今の「スタッフを選択」の欄はなくし、一括編集で名前を押して開いた中に 祝・出 と 過去の履歴 も出す（直す場所を1か所に） |
| 部門の一覧 | 最初は**こども・大人・管理部・フロント・事務・テレワーク**の6つ。管理者が画面から追加・名前変更・隠すができる（2026-09-15 ユーザー確定）。人ごとのメインの部門もこの一覧から1つ選ぶ |
| 今日より前の適用開始日 | **確認を挟んで保存できる**（2026-09-15 ユーザー確定）。確認に黄色で「今日より前の日付です。受理済みの休暇の時間外調整休の記録は変わりません」、ボタンは［さかのぼって保存する］ |
| Excel 取り込み | **一括編集の表に値を入れるだけ**（レビュー R10・2026-09-15 ユーザー確定）。読み取った値が表に入り、変わるマスは赤字＋「前：」。直してから一括編集と同じ確認で保存。名前が一致しない人の結び付けは今までどおり。Excel に載っていない人は変えない。🚨 Excel には部門が無いので、取り込んだ時間帯の部門は**その人のメインの部門**で入れる（要確認） |
| PDF の紙面 | **A（1人1ブロック・紙と同じ並び・休憩まで入る）と B（週の一覧・A4横）を出すときに選べる**（2026-09-15 ユーザー確定・見本で比較）。部門は色つきの校名（白黒でも読めるよう文字も）／移りは「本校→西陣」／変わった所は赤字（有無を選べる）／休みの曜日は薄い字／労働時間・合計は自動 |
| 校ごとの時間帯（C） | PDF にはしない。🚨 **③ 勉強会を入力するときのプレビューとして出す**（誰と誰の時間が合うかを見て決める・ユーザー要望） |
| 使い始め | **出した日から切り替え**（スイッチは置かない・2026-09-15 ユーザー確定）。出した日から残業管理の「通常シフト」の欄はなくなり、新しいタブで取り込み・修正。マネージャー以上には管理者が権限管理でタブを開いたときから出る。出す前に取り込みと保存を取り消し版で確かめる |
| 先に登録してある版 | 🚨 **A 残す**（レビュー R2・2026-09-15 ユーザー確定）。新しい版は「先の版が始まる前日まで」。確認に「◯◯さんは10/1からのシフトがあるので9/30まで」と名前つきで出す。黙って消さない |
| 変更なしの人 | 🚨 **2026-09-15 見直し（レビュー R6）：前の版と同じ人には版を作らない**（「変更あり◯人／変更なし◯人（そのまま）」）。先日の「表に出ている全員に作る」は取り消し |
| 1マスの入力 | **C：名前を押すと、その人の月〜日の入力欄が表の中に開く**。変えたマスは赤字＋「前：」 |
| PDF | 出す人を選べる（**全員／変更があった人**など）。並びは**チームごと → 役職の高い順** |
| 業務部門 | 🚨 正社員は2部門で働く（大人とこども・管理部とこども など）。受理の宛先に使う「所属チーム」（`group_names`）とは**別に持つ**。**メインの部門は1人1つ**（2026-09-15 ユーザー確定）。PDF・絞り込みはメインの部門で並べる |

### 5-2. 表をつなげる全体の段取り（2026-09-15 ユーザー確定）
いただいた紙の表：勤務表（2026.8.16～）／毎日の掃除担当表（2026/9/16～）／こどもシフト表（2026年9月案）。
- **② 通常シフト**：一括編集に、日ごと・人ごとの**書き添え**（「洛西口18:45～20:20」「月1(木)下鴨夢…」など）と**表の呼び名**（恭子・貴子・晃平など）を足し、**勤務表と同じ形の PDF**
- **③ 勉強会**：1件ずつ登録（曜日・開始・長さ・校・参加者）→ 参加者全員の勤務表の欄とこどもシフト表に自動で出す。
  🚨 **決めるときに今のシフトを並べて、誰と誰の時間が合うかを見ながら決められる**こと（ユーザー要望）
- **④ 掃除担当表**：**掃除担当表を元にする**（勤務表の掃除欄は時刻だけで、場所・仕事を持たないため逆向きは作れない）。
  勤務表の掃除欄・「休み」「担当なし」は自動。休みの人・出勤前・別の校にいる人は印を付ける。PDF
- **⑤ こどもシフト表**：月ごとの案。曜日×校・階×時間帯に班・担当・P・打合せ＋マスの書き添え。「社員休み」は自動、出勤とのずれに印。PDF
- 通常シフトを直したら、掃除表・こどもシフト表の「その時間に出勤していない」マスに**印を付けるだけ**（黙って書き換えない）

| タブの名前 | **シフト管理**（2026-09-15 ユーザー確定）。中に［勤務表］［勉強会］［掃除担当表］［こどもシフト表］を並べる（② は勤務表だけ） |

### 5-4. ② の実装（2026-09-15・取り消し版の検算まで。本番DB・push はまだ）
- **段0（DB変更なし）**：週のシフトを読む3か所（`OvertimeAdminTab` の一覧／`OvertimeShiftImport`／`ShiftAdjustTab` の候補）に「その日に効いている行だけ」の条件を足した（R1）
- **DB（`supabase/migrations/20260915033000_shift_roster.sql`）**
  - `shift_work_areas`（6つ・読み全員・足す／直すは `can_manage_admin_tab('shift_patterns')`・消さずに隠す）
  - `staff_main_work_areas`（1人1つ・最初は所属チームと同じ名前の部門・書くのは保存の関数だけ）
  - `weekly_shift_patterns` に `segments`（時間・校・部門の区切り）・`note`・`saved_by`。既存308行は今までの列から `segments` を作った
  - `weekly_shift_person_notes`（人ごとの書き添え・版で切り替わる）
  - `shift_band_break` / `shift_pattern_fields`（`lib/breakCalc.ts` の休憩の表の写し。つながった区切りは1つの時間帯、校は「A→B」、時間帯は2つまで、本務は長いほう）
  - `shift_patterns_token`（開いた時点の目印）／`shift_patterns_save`（一括保存）
  - 読みの決まり `patterns_select_shift_admin_tab` を足した。🚨 書き込みの決まり `patterns_admin_all` は広げていない
- **取り消し版の検算 52項目一致**（本番の実データで実施し、取り消し後に表・列・関数が残っていないことを実測）
  - 🚨 既存308行：`segments` から作り直した時間・校・休憩・労働が保存値と**ずれ0件**
  - 1回目の検算で**休憩が2倍になる不具合**（2つ目の時間帯が無いとき `shift_band_break(null,null)` が60を返す）を見つけて直した
  - 変えた人だけ版／同じ内容は0行／同じ日付はその場で直す／stale で断る／今日より前は確認つき／先の版を残す（9/10〜9/15）／
    未登録の人に休みの行を作らない／途中で校が移る・テレワーク＋本務の計算／重なり・無い校・3つの時間帯・崩れた時刻・逆の時刻を断る／
    2人目で失敗したら1人目も残らない／書き添えの入れる・同じ・消す／メインの部門／タブを開いたマネージャーは保存できる・閉じると断る・リーダーは断る
- **画面側の計算（`lib/shiftRoster.ts`）**：既存308行すべてで保存値と一致（`tsx` で検算・ずれ0件）
- **画面**
  - 管理画面の「スタッフ」の段に **📑 シフト管理**（`components/admin/ShiftManagementTab.tsx`）。残業管理から「通常シフト」の欄と `OvertimeShiftImport.tsx` を外した
  - 表（月〜日・メインの部門ごと→役職順）／名前を押すと月〜日・祝・出・過去の履歴が開く／赤字＋「前：」／週合計／直した人だけ表示／
    保存の確認（変更あり・なし・表示していない人の変更・先のシフトを残す曜日・入力のまちがい）／stale のとき「読み込み直す」（直した内容は残す）
  - Excel取り込み（`ShiftExcelLoader.tsx`）：読み取った値を表に入れるだけ。部門はメインの部門
  - PDF（`lib/shiftRosterPrint.ts`）：別の窓に書き出して印刷。A（1人1ブロック・A4縦・8人）／B（週の一覧・A4横）・出す人（表示している全員／変更があった人）・赤字の有無
  - 部門の一覧（名前・短い名前・色・隠す／戻す・追加）
  - `lib/adminTabs.ts` に `shift_patterns`（権限管理でマネージャー以上に開ける）
- 社内FAQ：「通常シフト」を含む回答3件はスタッフの申請画面の話で、管理画面の置き場所には触れていない → 直すものなし
- tsc 0／vite build 成功／rules-of-hooks 0／新しいファイルの ESLint 0件／触った共通ファイルは HEAD と同じ件数

### 5-3. ②〜⑤ のレビュー（2026-09-15・UI/UX とシニアエンジニアの2体・重要な主張は自分で裏取り）
| # | 重さ | 指摘 | 裏取り |
|---|---|---|---|
| R1 | 致命的 | 週のシフトを読む3か所が件数・終わりの日で絞っていない。全員に版を作ると数回で1,000行を超え、黙って欠ける | `OvertimeAdminTab.tsx:656`／`OvertimeShiftImport.tsx:251`／`ShiftAdjustTab.tsx:490`（`lte valid_from` だけ）✅。本番308行 ✅ → **②の最初に「期間に効く行だけ返す読み取りRPC」へ置き換える** |
| R2 | 致命的 | 適用開始日以降に始まる版を削除する作りのため、先の版（パート9/16版126行）が黙って消える。赤字の比べ先（前日）にも出ない | `OvertimeAdminTab.tsx:730`／`OvertimeShiftImport.tsx:294` ✅・本番 126行 ✅ |
| R3 | 重要 | 古い画面のまま保存すると、別の人の変更を44人分上書き | 設計に決めごと無し ✅ → RPC にロック＋「開いた時点の目印」照合 |
| R4 | 重要 | 休憩・労働時間の計算が画面（`lib/breakCalc.ts`）だけ。一括保存で DB 側と2か所になる | ✅ → SQL の計算関数を作り、本番308行の保存値と一致を検算 |
| R5 | 重要 | 1日の途中の校・部門の移り（「本校大10～14:30→上桂」「(こども15:30～17:30)」「テレワーク6:30～7:15」）が文字でしか持てない。④⑤の印が不正確になる | 紙の勤務表 ✅・`location` 1列 ✅ |
| R6 | 重要 | 「対象」の切り替えが保存の範囲まで変える／変わっていない人にも版を作ると履歴が全員分並び、行の無い正社員2人が「未登録」→「全曜日休み」に変わる | 5-1 ✅・正社員28人中26人登録（(47)）✅ |
| R7 | 重要 | `patterns_admin_all` を広げると API からチェック無しで削除できる → 書き込みは RPC だけ。読みは `shift_pattern_directory` 頼みなので読み取り RPC にもタブ判定 | 本番 RLS ✅ |
| R8 | 重要 | 呼び名は手入力＋在籍者で重複禁止＋別名（渡辺→渡邊・尾上→森本 千佳子） | 紙 ✅・`overtime_name_aliases` に尾上のみ ✅・椿原さんは退職扱い ✅ |
| R9 | 重要 | 所属チーム（受理の宛先）と業務部門（2部門）の食い違い。業務部門は別に持ち、所属は触らない | 正社員26人とも所属1つ ✅・ユーザー「大人とこども、管理部とこども」 |
| R10 | 改善 | 取り込みと一括編集で差分の見せ方・保存の決まりが違う → 取り込みは「一括編集の表に値を入れるだけ」に | `OvertimeShiftImport.tsx:172` ✅ |
| — | 誤り | 「①は本番に入っていない」→ 入っている（設計書の見出しが古かった・直した）／「社員休みの小川・阿部・川井・東・尾上は大人所属」→ 5人とも所属はこども ✅ |

段取り（エンジニア案）：0 読み取りRPC（R1）→ 1 計算関数＋保存RPC（R2〜R4）→ 2 取り込み・1人ずつ編集を RPC へ → 3 一括編集の表と PDF → 4 マネージャーに開く → 5 ③④⑤。
**段取り1の前に決める**：保存の中身の形（時間帯ごとの校・部門）／呼び名／業務部門。

## 6. レビュー（CLAUDE.md の決まり：大きな設計は2体でレビュー・指摘は自分で裏取り）

2026-09-14 に「DB・権限」「画面・使い勝手」の2体でレビューし、**重要な主張は本番の定義とコードを読んで裏取りした**。

### 6-1. 裏取りして本当だった指摘（設計に反映する）
| # | 重さ | 指摘 | 裏取り |
|---|---|---|---|
| A | 致命的 | `announcement-notify` に送信済みの確認が無い。id を渡すたびに全員分のベル（notifications insert）とメールを作る | index.ts の取得→insert→send-email に既送の判定なし（✅） |
| B | 致命的 | `announcement-remind` の二重送信防止は `announcements.remind_last_sent_on` だけ。編集できる人が増えるとこれを null に戻して毎日送り直せる | index.ts:71-72, :125（✅） |
| C | 致命的 | お知らせの画面は保存・切り替え・削除の失敗を黙って捨て、通知の関数の結果も見ない（失敗しても「✓ 出しました」） | AnnouncementsTab.tsx:120-131, 161-176（✅） |
| D | 重要 | 所属（`profiles.group_names`）は受理依頼の宛先に効く。`resolve_role_recipients` が「同じ所属チームの上長」に絞っている | 本番の実定義 :39-51 `p.group_names && v_teams`（✅） |
| E | 重要 | リマインドの「対応状況」`scheduled_reminder_status` は本番でも管理者だけ → マネージャーは全行で失敗 | 本番の実定義 `role <> 'admin' → raise`（✅） |
| F | 重要 | リマインドの送り先チャンネル一覧は `board_channels` の RLS で「メンバー／作成者／管理者」だけ | 本番 RLS（✅） |
| G | 重要 | 安否の締め・取消は `is_manager_plus()` だけ → **タブをオフにしても DB では止まらない**（今までと同じ） | 本番 `cancel_safety_check`/`close_safety_check`（✅） |
| H | 重要 | 安否の削除ボタンは条件なしで出ている／発信者名は管理画面の `users` から引く（読み込みを止めると「—」） | SafetyChecksTab.tsx:592-595, :70, :290（✅） |
| I | 重要 | グループの名前変更・追加・削除のボタンがマネージャーにも出る／所属は `profiles` を直接更新 | GroupsTab.tsx:68, :116, :212, :43/:146/:168/:192（✅） |
| J | 重要 | 管理画面の枠が誰にでもストレージ・DB使用量の RPC、`useAdminSetupAlerts(true)`、修正依頼の件数を読む | AdminPanel.tsx の冒頭（✅） |
| K | 重要 | `?tab=` の初期値は `ADMIN_TABS` に含まれるかだけ・既定は `'approvals'` | AdminPanelContext.tsx:253（✅・4-2 で対策済みの方針） |
| L | 重要 | 設定のキーとタブの名前が合っていない（通常シフトは `overtime_admin` の一部、お知らせと安否は別タブ） | AdminPanelContext.tsx:16（✅） |
| M | 前例 | 設定を読む関数は `board_can_manage_groups()` と同じ形（`jsonb_typeof = 'array'` を確かめて読む） | 本番の実定義（✅） |

### 6-2. 設計に足すこと（① で必ずやる）
- **お知らせ**：`announcements.notified_at` を足し、`announcement-notify` の最初で「`notified_at is null` の行だけ更新できたら送る」。
  管理者以外は `remind_last_sent_on`／`notified_at`／`created_by` を変えられない（トリガー）。
  関数の中の権限確認は **利用者の JWT のまま** `rpc('can_manage_admin_tab')` を呼ぶ（service_role で呼ぶと auth.uid() が null で常に false）。
  画面は保存・切り替え・削除・通知の error を必ず出す（C）。**画面より先にこの直しを入れる**
- **グループ**：所属の RPC は1文の `array_append`／`array_remove`、0件は例外、相手が管理者・全社の役職・自分より上なら断る。
  所属チーム（`shift_report_group`）を触らせるかはユーザー判断（D）
- **リマインド**：`scheduled_reminder_status` の判定に `can_manage_admin_tab('scheduled_reminders')`（E）／
  チャンネルの id と名前だけを返す SECURITY DEFINER の関数（F）／insert の with check に `created_by = auth.uid()`
- **安否**：削除ボタンは管理者だけ（H）。発信者名のために安否タブでも `users` を読む。オフでも DB で止まらないことを明記（G）
- **グループの画面**：名前変更・追加・削除は管理者だけ。メンバーの「削除」は「外す」に（I）
- **管理画面の枠**：ストレージ・DB使用量・設定もれ・修正依頼の件数は `isAdmin` のときだけ（J）
- **判定を1か所に**：`useAdminAccess()` が `{ ready, canOpen, reason, visibleTabs, isTabAllowed }` を返し、
  NavBar・AdminPage・AdminPanelContext が同じものを使う。`TAB_GROUPS` と設定のキー一覧は lib の定数1つ（L）
- **読み込み中**：設定を読み終えるまで入口は「読み込んでいます...」（読み終える前にホームへ飛ばさない）
- **行き止まりにしない**：マネージャー以上がスマホで開いた／タブが0件 → 理由のカードと［← ホームへ］
- **オフにされたとき**：設定は `usePolling` で読み直し、開いているタブが外れたら案内して残りのタブへ
- **FAQ**：`can_edit_faq()` を広げると、タブがオンのマネージャーには下書きの回答と検索語のログも API で見える（画面は今までどおり）
- **設定の枠**：各タブで「できるようになること」「DB の書き込みも許可されるか」を出す。お知らせは「全員（約46名）に通知・メール」と二段階の確認
- **連絡文**：共通ファイルに `NotificationsTab.tsx`・`useAuth.ts` も足す

### 6-2b. レビューを受けてユーザーが決めたこと（2026-09-14）
| 項目 | 決定 | 設計への反映 |
|---|---|---|
| お知らせの編集・削除 | **他の人が作ったものも、マネージャー以上が直せる・消せる** | `announcements` の書き込みは `created_by` で絞らない。ただし `created_by`・`notified_at`・`remind_last_sent_on` は管理者以外は変えられない（トリガー）。二重送信の歯止め（`notified_at`）は必須のまま |
| 所属チーム（こども・大人・管理部） | **マネージャー以上も変えられる**（受理依頼の宛先が変わることは説明済み） | 所属の関数は `shift_report_group` の値も扱う。🚨 相手が管理者・全社の役職・自分より上の役職なら断る。画面に「所属チームを変えると、休暇・残業の受理依頼が届く上長が変わります」と出す |
| リマインド設定の「送るタイミング」 | **両方開く**（定期リマインドと、有給奨励日・連絡板未読の送る時刻） | `reminder_days_settings` の書き込みも `can_manage_admin_tab('scheduled_reminders')` |
| 安否・緊急 | **今までどおり**（タブのオン・オフは管理画面に出すかだけ） | 締め・取消・発信の関数は触らない。設計書 4-1 の「オフで DB も止まる」に安否は例外と明記 |

### 6-3. リハーサルに足す試験
anon は新しい関数を呼べない／リーダーは所属の関数で断られる／マネージャーは管理者・社長の所属を変えられない／
マネージャーは `remind_last_sent_on`・`created_by`・`notified_at` を変えられない／同じお知らせの通知は2回目に送られない／
`scheduled_reminder_status` がタブ ON のマネージャーで通り OFF で断られる／FAQ 編集専用アカウントが今までどおり使える／
設定が「行なし・null・文字列」のとき false。Edge Function は deploy 後に「タブ OFF のマネージャーで 403」「通知 OFF のお知らせで push:0,email:0」を実測

## 6-4. ① の実装（2026-09-15・✅ 本番DB適用・announcement-notify v6 deploy・push `b72b02b` まで完了。最初は全部オフ）

### DB（`supabase/migrations/20260915001444_manager_admin_tabs.sql`）
- `app_settings` 'manager_admin_tabs' ＝ `[]`（全部オフ）
- `can_manage_admin_tab(text)`：`is_admin()`、または `is_manager_plus()` かつ設定の配列にタブがある。配列でない値（null・文字列）・行なしは false
- お知らせ：`notified_at` を追加し、**いまある行は `created_at` で埋めた**（古いお知らせに作成時の通知を飛ばせないように）。
  トリガー `trg_announcements_guard_system_columns`：画面からの操作（`current_user = authenticated`）で管理者でなければ、
  作成者は自分・送信済みの印（`notified_at`／`remind_last_sent_on`）は変えられない。Edge Function（service_role）はそのまま通る。
  書き込みの決まり `announcements_admin_write` を `can_manage_admin_tab('announcements')` に（名前はそのまま）
- グループ：`set_profile_group(p_user_id, p_group, p_member)`（設計 4-3 の add/remove を1本にした）。
  `master_options` の group／shift_report_group にある値だけ。1文の `array_append`／`array_remove`。二度押しは今の値を返す。
  管理者以外は、相手が管理者（app_metadata）・全社の役職・自分より上（roles.sort_order）なら断る
- リマインド：`reminders_admin_write`／`管理者のみ編集可` を `can_manage_admin_tab('scheduled_reminders')` に（名前はそのまま）。
  作るときの作成者は自分（`reminders_insert_own_creator`・restrictive）。
  `scheduled_reminder_status` は本番の実定義から起こし、判定の1行だけ差し替え（機械で突合：違いは判定の行と見出しの大文字小文字だけ）。
  `reminder_channel_options()`（id と名前だけ）
- FAQ：`can_edit_faq()` に `or public.can_manage_admin_tab('faq')`（本番の実定義から起こした・欠けた行 0）
- 新しい関数3つとも anon の実行権限を外した

### 取り消し版の検算（65項目すべて一致）
anon は3つとも呼べない／設定が空・文字列・null・行なしで false／ON でマネージャー・社長は通りリーダー・パートは通らない／
Q&A管理専用は今までどおり／お知らせ：他の人の分を直せる・送信済みの印と作成者は変えられない・他の人の名前で作れない・
service_role の「送る印」は1回目1件・2回目0件・OFF で直せない作れない／リマインド：作れる・他の人の名前で作れない・
直せる・送るタイミングも直せる・対応状況とチャンネル26件が読める・リーダーと OFF は断る／
グループ：同じ役職の人に足す・二度押しで1つ・外す・他の所属と役職はそのまま・社長と管理者は断る・無いグループは断る・
リーダーと OFF は断る・管理者は社長も変えられる。取り消し後、本番に設定・関数・列が残っていないことを実測

### Edge Function `announcement-notify`
- 判定を `rpc('can_manage_admin_tab', { p_tab: 'announcements' })`（利用者の JWT のまま）に
- 送る前に `notified_at is null` の行だけ印を付け、付けられなければ 409「このお知らせの通知はすでに送っています」
- 送る相手を読めなかったときだけ印を戻す。push の失敗・メールの失敗数を返す（`push_failed`／`email_failed`）
- `deno check` は HEAD と同じ 0件

### 画面
- `lib/adminTabs.ts`（タブの鍵・説明・判定 `decideAdminAccess`）／`hooks/useAdminAccess.ts`（設定の共有と読み直し）
- `App.tsx`：`AdminPage` は設定を読み終えるまで「読み込んでいます...」、開けない理由はカード（`AdminAccessNotice`）。
  「⚙️ 管理」は `adminAccess.canOpen`
- `AdminPanel.tsx`／`AdminPanelContext.tsx`：開いているタブだけ・URL 直打ちも不可・閉じられたら残りのタブへ移して黄色で知らせる。
  使用量・設定もれ・修正依頼・承認待ちの読み込みは管理者だけ。安否タブでは発信者名のためにスタッフを読む
- グループ：追加・名前変更・削除は管理者だけ。所属は `set_profile_group`（管理者も同じ道）。「削除」→「外す」。所属チームには注意書き
- お知らせ：保存・停止・削除・通知の失敗を出す（`describeUpdate`／`describePartial`）。履歴を読めないときは「まだありません」と言わない
- リマインド：チャンネルは `reminder_channel_options`。読み込みの失敗を出す
- 安否：削除ボタンは管理者だけ／FAQ：`canManageEditors` は管理者だけ
- 権限管理の末尾に「マネージャー以上に開く管理画面のタブ」（`ManagerAdminTabsSection`）。お知らせを開くときだけ二段階
- tsc 0／vite build 成功／rules-of-hooks 0／判定の検算 16項目一致

### 出す順（許可をもらってから）
DB 適用 → 実測（関数・anon・列・決まり・設定 `[]`）→ `git pull` → `announcement-notify` deploy（🚨 新しい判定は DB の関数を使うので必ず DB の後）→ push。
全部オフで出るので、出した瞬間に誰の画面も変わらない。管理者が確かめてからタブを1つずつオンにする。

## 7. 場所予約の担当への連絡文（案）

> 管理画面をマネージャー以上（PC のみ）に開く作業を始めます。2人共通ファイルのうち
> `App.tsx`（/admin の入口と「⚙️ 管理」ボタンの判定）、`useAuth.ts`（判定の読み出し）、
> `AdminPanel.tsx`／`AdminPanelContext.tsx`（タブの出し分け）、`NotificationsTab.tsx`（リマインド設定の画面）、
> `FeaturePermissionsTab.tsx`（設定枠を1行足す）の動きが変わります。
> 最初は全部オフで、管理者がタブを選ぶまで誰の画面も変わりません。場所予約（/rooms）には触りません。
> DB は `app_settings` に設定を1つ、判定の関数、所属・リマインドのチャンネル一覧の関数、
> FAQ・リマインド・お知らせの書き込みの決まりを広げます。Edge Function は `announcement-notify` を変えます
> （2人共通の9本には入っていませんが、deploy 前に pull します）。
> 重なる作業があれば教えてください。
