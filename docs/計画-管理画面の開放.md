# 計画：管理画面をマネージャー以上に開く（PC のみ・タブは管理者が選ぶ）

2026-09-14 作成。ユーザーの要望と、一問一答で決めたことをまとめる。
🚨 **2人共通ファイル（App.tsx / AdminPanel.tsx / AdminPanelContext.tsx / FeaturePermissionsTab.tsx）の動きを変える**ので、
着手前に場所予約の担当へ一声かける（CLAUDE.md ②）。連絡文は末尾。

---

## 1. 要望（ユーザーの言葉を要約）

- シフトを今後はアプリで登録したい。前の版から何が変わったかを赤で出す。いつから適用かを入れる。
  修正ボタンで一気に入れて保存。Excel取込も残す。使いたいときから使えるようにする
- それを触れるのは **マネージャー以上が PC でログインしたときだけ**。管理画面に入れる
- 管理画面で（見せるタブを）追加できるようにしたい
- シフト・リマインド・FAQ、ほかに入れたほうがよいものは？

## 2. 決まったこと（2026-09-14 ユーザー確定）

| 項目 | 決定 |
|---|---|
| マネージャー以上に開けるタブ（候補） | **グループ／通常シフト／リマインド設定／FAQ管理／お知らせ・安否確認** |
| 見せるタブの決め方 | 管理者が「権限管理」でタブごとにチェック。**最初は全部オフ** |
| お知らせ | マネージャー以上も **作成も送信もできる**（🚨 2026-09-09 の「送れるのは管理者だけ（Q5=B）」を変える） |
| グループ | **スタッフの所属だけ**。グループそのものの追加・名前の変更・削除は管理者だけ |
| 安否確認の削除 | **管理者だけのまま** |
| シフトの一括編集で赤く出す比べ先 | **適用開始日の前日に効いている版** |
| 一括編集で保存したとき新しい版を作る人 | **表に出ている全員** |
| 作る順 | **① 入口 → ② シフトの一括編集 → ③ リマインド・FAQ**（お知らせ・グループ・安否は①に含めるかは下の段取りで） |

※「マネージャー以上」＝ `roles.is_manager_plus`（マネージャー5・社長1・管理者1。2026-09-14 実測）。役職名では判定しない。
  🚨 **2026-09-15 ユーザー再確認：このまま（リーダー・フロア責任者は入れない）**。
  リーダーにも開くなら、役職の印を付け替えるのではなく `can_manage_admin_tab` を直す（印は安否の発信など他でも使っているため）。
※「PC」＝ `lib/idleLogout.ts` の `isPointerDevice()`（マウスのある端末）。

## 3. いまの作り（調べた結果）

- `/admin` は `App.tsx` の `AdminPage` が `isAdmin` だけで判定。ナビの「⚙️ 管理」も `isAdmin` だけ
- `AdminPanel.tsx` のタブは `TAB_GROUPS` に固定。**タブごとの権限は無い**。`?tab=` を直接打てばどのタブにも入れる
  （`AdminPanelContext.tsx` の `initialTab` は `ADMIN_TABS` に含まれるかだけを見る）
- 管理画面を開くと、どのタブかに関係なく `fetchUsers()` と `fetchLeaveRequests()` が走り、承認待ちのバッジを出す
- 各タブの書き込み先と今の決まり（本番の RLS・関数の中身を読んで確認）

| タブ | 書き込み先 | 今の書き込み権限 |
|---|---|---|
| グループ | `profiles.group_names`（所属）／`master_options`（category='group'） | 管理者だけ（`profiles` の ALL・`master_options` の I/U/D） |
| 通常シフト | `weekly_shift_patterns`（画面から1行ずつ。RPC なし・トランザクションなし） | 管理者だけ（`patterns_admin_all`） |
| リマインド設定 | `board_scheduled_reminders`／`reminder_days_settings` | 管理者だけ |
| FAQ管理 | `faq_topics`／`faq_answers`／`faq_answer_targets`／`faq_topic_relations` | `can_edit_faq()`＝管理者 or `profiles.is_faq_editor` |
| お知らせ | `announcements`（`lib/announcements.ts`）＋ Edge Function `announcement-notify` | 表は `is_admin()`・関数も admin だけ |
| 安否確認 | `safety_check_templates`／Edge Function `safety-check-send`／RPC `submit_safety_response_proxy`／RPC `delete_safety_check` | 定型文・発信・代わりの回答は **もともとマネージャー以上**。削除だけ管理者 |

- 読み取りは概ね足りている：`profiles` は全員読める／安否の発信・回答はマネージャー以上が読める／`announcements`・`app_settings` は全員読める
- 🚨 リマインド設定が読む `board_channels` は「メンバー or 作成者 or 管理者」しか読めない（2026-09-14 実測）。
  **マネージャーは自分が入っていないチャンネル宛てのリマインドを選べない**＝画面の選択肢が欠ける。
  ① でリマインドを開くなら、チャンネル一覧を読む手段（`can_manage_admin_tab('scheduled_reminders')` の人は全チャンネルの id と名前だけ読める関数など）が要る
- ✅ FAQ の集計 RPC `faq_public_event_summary` は **SECURITY INVOKER**（呼んだ人の権限で読む・2026-09-14 実定義で確認）。
  読む表 `faq_public_event` の RLS が `can_edit_faq()` なので、**`can_edit_faq()` を広げれば集計も見られる**（関数を別に直す必要はない）

## 4. 設計（① 入口）

### 4-1. 設定の持ち方
- `app_settings` の key **`manager_admin_tabs`**＝マネージャー以上に開くタブの配列（例 `["faq","scheduled_reminders"]`）。
  既定は空配列＝**全部オフ**。読みは全員（既存の RLS）・書きは管理者（既存の RLS）
- 🚨 **DB 側でも同じ設定を見る**：新しい関数 `can_manage_admin_tab(p_tab text)`＝
  `is_admin() or (is_manager_plus() and manager_admin_tabs に p_tab が入っている)`。
  各タブの書き込みの決まりをこれで広げるので、**管理者がチェックを外せば DB の書き込みも止まる**（画面だけの制限にしない）
- 🚨 **「PC だけ」は画面の制限**。DB は端末を見分けられない（スマホから API を直接叩けば書ける）。ユーザーに説明済みの前提として明記する

### 4-2. 画面
- 新しい hook `useManagerAdminTabs()`（`app_settings` を1回読んで共有・`useRoles` と同じ形）
- 「管理画面に入れる」＝ `isAdmin || (isManagerPlus && isPointerDevice() && 開くタブが1つ以上)`
  - `App.tsx` の `AdminPage` の判定と、`NavBar` の「⚙️ 管理」ボタンの判定をこれにする
    （🚨 NavBar は15か所から呼ばれている。props を増やさず、NavBar の中で hook を呼ぶ）
- `AdminPanel.tsx`：管理者でなければ `TAB_GROUPS` を「開くタブ」だけに絞る（PC の3段・スマホのセレクトの両方）
- `AdminPanelContext.tsx`：`initialTab` と `setActiveTab` も「開くタブ」に含まれるかを見る（URL 直打ち対策）。
  管理者でなければ `fetchUsers`/`fetchLeaveRequests` の常時読み込みとバッジを止める（グループのタブを開いたときだけ `fetchUsers`）
- タブの中の出し分け（管理者でない人）
  - グループ：グループの追加・名前変更・削除のボタンを出さない。所属の追加・外すは新しい RPC 経由
  - FAQ：`canManageEditors` は管理者だけ（今のまま）
  - 安否：削除ボタンを出さない
  - 通常シフト（②）：残業管理の中の「通常シフト」だけを出す（受理済み一覧・打刻・締め後の許可・会社カレンダー・設定は出さない）
- 「権限管理」タブの末尾に「マネージャー以上に開く管理画面のタブ」（新しい部品・チェックボックス）。
  `FeaturePermissionsTab.tsx` には import と1行だけ足す（自動ログアウトの設定枠と同じ形）

### 4-3. DB（① で入れるもの）
| 対象 | 変更 |
|---|---|
| 共通 | `can_manage_admin_tab(text)`（SECURITY DEFINER・anon 実行不可） |
| グループ | RPC `add_profile_group(p_user_id, p_group)`／`remove_profile_group(...)`：`can_manage_admin_tab('groups')`、`p_group` が `master_options(category='group')` にあること、`group_names` のその1つだけを足す／外す（役職・雇用形態など他の列は触れない） |
| お知らせ | `announcements` の書き込みを `is_admin() or can_manage_admin_tab('announcements')`／Edge Function `announcement-notify` の判定を同じにする（`role_is_manager_plus` ＋ 設定を RPC で確認） |
| 安否 | 変更なし（もともとマネージャー以上。削除は管理者のまま） |
| リマインド | `board_scheduled_reminders`／`reminder_days_settings` の書き込みを `is_admin() or can_manage_admin_tab('scheduled_reminders')` |
| FAQ | `can_edit_faq()` に `or can_manage_admin_tab('faq')` を足す（FAQ の表すべてに効く）。🚨 本番の実定義から起こす |
| 通常シフト | ② で RPC を作るときに `can_manage_admin_tab('shift_patterns')` を使う（① では書き込みを広げない） |

- 🚨 RLS を置き換えるときは本番の実定義を読んでから `drop policy … create policy` で置き換え、名前を変えない
- 🚨 `can_edit_faq` など既存関数を変えるときは `pg_get_functiondef` から起こし、機械で突合
- 取り消し版で「管理者／マネージャー（タブON）／マネージャー（タブOFF）／リーダー／パート」になりすまして書き込みを試す

### 4-4. 出す順
DB（関数・RLS）→ Edge Function（announcement-notify・🚨 deploy 前に git pull）→ 画面。
最初は全部オフなので、出した瞬間に誰の画面も変わらない。管理者が確かめてからタブを1つずつオンにする。

## 5. 設計（② シフトの一括編集・概要のみ。① のあとで詳しく決める）

- 「通常シフト」に［修正］→ 表に出ている全員×月〜日を一度に編集できる表 → 適用開始日 → 前日に効いている版と違うマスを赤 → ［保存］
- 保存は **SECURITY DEFINER の RPC 1回**で全員分を書く（途中で失敗したら何も残らない）。Excel取込も同じ RPC に乗せ替える
  （今は取込も1人ずつの編集も、画面から1行ずつ書いていてトランザクションが無い）
- 「使い始め」は権限管理のスイッチ（オフの間は今の1人ずつの編集と取込だけ）
- 🚨 `weekly_shift_patterns` は同じ人×同じ曜日で期間が重なる行を DB が禁止（exclude 制約）。締め→入れる の順は RPC の中で守る
- 影響先：`sync_overtime_from_leave`（時間外調整休）／OvertimePage の通常シフト／シフト調整の候補／ShiftDirectoryPage ほか

### 5-1. ② で決まったこと（2026-09-15 ユーザー確定・一問一答）
| 項目 | 決定 |
|---|---|
| 置き場所 | 管理画面に**新しいタブ「通常シフト」**（スタッフの段）。取り込み・1人ずつ編集・一覧もまとめて移し、残業管理からは外す。タブの鍵は `shift_patterns` |
| 表の列 | **月〜日の7列だけ**（祝・出は1人ずつ編集で。2026-09-15 実測で祝・出の行は0件） |
| 表に出す人 | 対象（正社員だけ／パートも）＋**チームで絞って見られる**。保存は対象の全員を確かめ、確認に「表示していない人も含めて」と出す |
| 1日の中の区切り | 🚨 **時間帯ごとに校と部門**（レビュー R5・2026-09-15 ユーザー確定）。1日を「時間・校・部門」の行で区切って入れる（例：阿部さん 9:30〜12:00 四条本校・大人／12:00〜19:15 西陣校・こども）。表のマスは「本校(大)→西陣(子)」、PDF は紙と同じ「四条本校大人11～12→西陣校15:30～」の形で出す。今の `start_time2`（外出・戻り・テレワーク）と `location` 1列はこの形に移す（移し方は段取り1で決める） |
| 呼び名 | **持たない。表にはフルネームで出す**（2026-09-15 ユーザー確定「フルネームで入れるで良くないか」）。🚨 ④掃除担当表・⑤こどもシフト表は1マスに2〜4人入り紙面が詰まるので、見本で紙面を作って読みにくければその段で相談し直す。紙から取り込むときの別の書き方（渡辺→渡邊・尾上→森本 千佳子）の結び付けは別に要る |
| 書き添え | **曜日ごと**（マスの下：「無理な時は退勤早める」）と**人ごと**（見出し：「月1(木)下鴨夢 10:00～12:00」「固定給」）の**2つ**（2026-09-15 ユーザー確定）。どちらもシフトと一緒に「いつから」が切り替わり、PDF は紙と同じ場所に印刷。書き添えだけ直した人も「変更あり」に数える |
| 1人ずつ編集 | **一括編集にまとめる**（2026-09-15 ユーザー確定）。今の「スタッフを選択」の欄はなくし、一括編集で名前を押して開いた中に 祝・出 と 過去の履歴 も出す（直す場所を1か所に） |
| 部門の一覧 | 最初は**こども・大人・管理部・フロント・事務・テレワーク**の6つ。管理者が画面から追加・名前変更・隠すができる（2026-09-15 ユーザー確定）。人ごとのメインの部門もこの一覧から1つ選ぶ |
| 今日より前の適用開始日 | **確認を挟んで保存できる**（2026-09-15 ユーザー確定）。確認に黄色で「今日より前の日付です。受理済みの休暇の時間外調整休の記録は変わりません」、ボタンは［さかのぼって保存する］ |
| Excel 取り込み | **一括編集の表に値を入れるだけ**（レビュー R10・2026-09-15 ユーザー確定）。読み取った値が表に入り、変わるマスは赤字＋「前：」。直してから一括編集と同じ確認で保存。名前が一致しない人の結び付けは今までどおり。Excel に載っていない人は変えない。🚨 Excel には部門が無いので、取り込んだ時間帯の部門は**その人のメインの部門**で入れる（要確認） |
| PDF の紙面 | **A（1人1ブロック・紙と同じ並び・休憩まで入る）と B（週の一覧・A4横）を出すときに選べる**（2026-09-15 ユーザー確定・見本で比較）。部門は色つきの校名（白黒でも読めるよう文字も）／移りは「本校→西陣」／変わった所は赤字（有無を選べる）／休みの曜日は薄い字／労働時間・合計は自動 |
| 校ごとの時間帯（C） | PDF にはしない。🚨 **③ 勉強会を入力するときのプレビューとして出す**（誰と誰の時間が合うかを見て決める・ユーザー要望） |
| 使い始め | **出した日から切り替え**（スイッチは置かない・2026-09-15 ユーザー確定）。出した日から残業管理の「通常シフト」の欄はなくなり、新しいタブで取り込み・修正。マネージャー以上には管理者が権限管理でタブを開いたときから出る。出す前に取り込みと保存を取り消し版で確かめる |
| 先に登録してある版 | 🚨 **A 残す**（レビュー R2・2026-09-15 ユーザー確定）。新しい版は「先の版が始まる前日まで」。確認に「◯◯さんは10/1からのシフトがあるので9/30まで」と名前つきで出す。黙って消さない |
| 変更なしの人 | 🚨 **2026-09-15 見直し（レビュー R6）：前の版と同じ人には版を作らない**（「変更あり◯人／変更なし◯人（そのまま）」）。先日の「表に出ている全員に作る」は取り消し |
| 1マスの入力 | **C：名前を押すと、その人の月〜日の入力欄が表の中に開く**。変えたマスは赤字＋「前：」 |
| PDF | 出す人を選べる（**全員／変更があった人**など）。並びは**チームごと → 役職の高い順** |
| 業務部門 | 🚨 正社員は2部門で働く（大人とこども・管理部とこども など）。受理の宛先に使う「所属チーム」（`group_names`）とは**別に持つ**。**メインの部門は1人1つ**（2026-09-15 ユーザー確定）。PDF・絞り込みはメインの部門で並べる |

### 5-2. 表をつなげる全体の段取り（2026-09-15 ユーザー確定）
いただいた紙の表：勤務表（2026.8.16～）／毎日の掃除担当表（2026/9/16～）／こどもシフト表（2026年9月案）。
- **② 通常シフト**：一括編集に、日ごと・人ごとの**書き添え**（「洛西口18:45～20:20」「月1(木)下鴨夢…」など）と**表の呼び名**（恭子・貴子・晃平など）を足し、**勤務表と同じ形の PDF**
- **③ 勉強会**：1件ずつ登録（曜日・開始・長さ・校・参加者）→ 参加者全員の勤務表の欄とこどもシフト表に自動で出す。
  🚨 **決めるときに今のシフトを並べて、誰と誰の時間が合うかを見ながら決められる**こと（ユーザー要望）
- **④ 掃除担当表**：**掃除担当表を元にする**（勤務表の掃除欄は時刻だけで、場所・仕事を持たないため逆向きは作れない）。
  勤務表の掃除欄・「休み」「担当なし」は自動。休みの人・出勤前・別の校にいる人は印を付ける。PDF
- **⑤ こどもシフト表**：月ごとの案。曜日×校・階×時間帯に班・担当・P・打合せ＋マスの書き添え。「社員休み」は自動、出勤とのずれに印。PDF
- 通常シフトを直したら、掃除表・こどもシフト表の「その時間に出勤していない」マスに**印を付けるだけ**（黙って書き換えない）

| タブの名前 | **シフト管理**（2026-09-15 ユーザー確定）。中に［勤務表］［勉強会］［掃除担当表］［こどもシフト表］を並べる（② は勤務表だけ） |

### 5-4. ② の実装（2026-09-15・取り消し版の検算まで。本番DB・push はまだ）
- **段0（DB変更なし）**：週のシフトを読む3か所（`OvertimeAdminTab` の一覧／`OvertimeShiftImport`／`ShiftAdjustTab` の候補）に「その日に効いている行だけ」の条件を足した（R1）
- **DB（`supabase/migrations/20260915033000_shift_roster.sql`）**
  - `shift_work_areas`（6つ・読み全員・足す／直すは `can_manage_admin_tab('shift_patterns')`・消さずに隠す）
  - `staff_main_work_areas`（1人1つ・最初は所属チームと同じ名前の部門・書くのは保存の関数だけ）
  - `weekly_shift_patterns` に `segments`（時間・校・部門の区切り）・`note`・`saved_by`。既存308行は今までの列から `segments` を作った
  - `weekly_shift_person_notes`（人ごとの書き添え・版で切り替わる）
  - `shift_band_break` / `shift_pattern_fields`（`lib/breakCalc.ts` の休憩の表の写し。つながった区切りは1つの時間帯、校は「A→B」、時間帯は2つまで、本務は長いほう）
  - `shift_patterns_token`（開いた時点の目印）／`shift_patterns_save`（一括保存）
  - 読みの決まり `patterns_select_shift_admin_tab` を足した。🚨 書き込みの決まり `patterns_admin_all` は広げていない
- **取り消し版の検算 52項目一致**（本番の実データで実施し、取り消し後に表・列・関数が残っていないことを実測）
  - 🚨 既存308行：`segments` から作り直した時間・校・休憩・労働が保存値と**ずれ0件**
  - 1回目の検算で**休憩が2倍になる不具合**（2つ目の時間帯が無いとき `shift_band_break(null,null)` が60を返す）を見つけて直した
  - 変えた人だけ版／同じ内容は0行／同じ日付はその場で直す／stale で断る／今日より前は確認つき／先の版を残す（9/10〜9/15）／
    未登録の人に休みの行を作らない／途中で校が移る・テレワーク＋本務の計算／重なり・無い校・3つの時間帯・崩れた時刻・逆の時刻を断る／
    2人目で失敗したら1人目も残らない／書き添えの入れる・同じ・消す／メインの部門／タブを開いたマネージャーは保存できる・閉じると断る・リーダーは断る
- **画面側の計算（`lib/shiftRoster.ts`）**：既存308行すべてで保存値と一致（`tsx` で検算・ずれ0件）
- **画面**
  - 管理画面の「スタッフ」の段に **📑 シフト管理**（`components/admin/ShiftManagementTab.tsx`）。残業管理から「通常シフト」の欄と `OvertimeShiftImport.tsx` を外した
  - 表（月〜日・メインの部門ごと→役職順）／名前を押すと月〜日・祝・出・過去の履歴が開く／赤字＋「前：」／週合計／直した人だけ表示／
    保存の確認（変更あり・なし・表示していない人の変更・先のシフトを残す曜日・入力のまちがい）／stale のとき「読み込み直す」（直した内容は残す）
  - Excel取り込み（`ShiftExcelLoader.tsx`）：読み取った値を表に入れるだけ。部門はメインの部門
  - PDF（`lib/shiftRosterPrint.ts`）：別の窓に書き出して印刷。A（1人1ブロック・A4縦・8人）／B（週の一覧・A4横）・出す人（表示している全員／変更があった人）・赤字の有無
  - 部門の一覧（名前・短い名前・色・隠す／戻す・追加）
  - `lib/adminTabs.ts` に `shift_patterns`（権限管理でマネージャー以上に開ける）
- 社内FAQ：「通常シフト」を含む回答3件はスタッフの申請画面の話で、管理画面の置き場所には触れていない → 直すものなし
- tsc 0／vite build 成功／rules-of-hooks 0／新しいファイルの ESLint 0件／触った共通ファイルは HEAD と同じ件数

### 5-5. ③ 勉強会で決まったこと（2026-09-15〜 ユーザー確定・一問一答）
- 既存の仕組み：アプリ・DB・社内FAQとも「勉強会」は0件（新しく作る）
- 紙の形：「12:30(30)濱口・馬場」＝開始（長さ・分）＋参加者2〜3人。同じ1件が参加者全員の勤務表の欄と、こどもシフト表の校・階のマスに書かれている

| 項目 | 決定 |
|---|---|
| 繰り返し | **毎週の定例**（勤務表と同じく「いつから」で切り替え）。その日だけの中止・変更は後から足す |
| 1件に持つもの | **曜日・開始・長さ・参加者（2人以上）＋校・階（任意）・内容メモ（任意）**。階は⑤こどもシフト表で紙と同じ階の列に出すため |
| ずれたとき | **印を付けて保存できる**（2026-09-15 ユーザー確定）。入力のときは赤い注意、一覧と勤務表にも「⚠️ ◯◯さんはこの時間に勤務していません／この時間 四条本校」を出し続ける。あとで勤務表を直してずれたときも同じ印（勉強会は消さない・書き換えない） |
| 印の「確認した」 | **確認したで消せる**（レビュー S11・2026-09-15 ユーザー確定）。押すと薄くなり数に入らない（「⚠️ 5件（確認済み 3）」）。誰がいつ確認したかを残す。🚨 勤務表や勉強会が変わってずれ方が変わったら、また出す（確認は「勉強会の版×人×ずれの中身」に付ける） |
| PDF の ⚠️ 印 | **出すときに選べる・初期は刷らない**（レビュー S11・2026-09-15 ユーザー確定）。PDF の設定に「☐ 勉強会の⚠️印も刷る」。配る紙には既定で警告が載らない |
| 本人に見せる切り替え | **管理者だけが切り替える**（レビュー S9・2026-09-15 ユーザー確定）。シフト管理→勉強会の先頭。オンにする前に「正社員の参加者◯人に見えます／パートの◯人（名前）には見えません／内容のメモも見えます」を出す。マネージャー以上には今の状態だけ出す。app_settings の今の決まり（書き込みは管理者）のまま |
| 終わらせるとき | **先の変更も一緒に終わらせる**（レビュー S10・2026-09-15 ユーザー確定）。確認に「10/1からの変更（13:00）も取り消します」と一覧で出してから終わらせる。終わらせた勉強会があとから復活しない（🚨 勤務表の「先の版は残す」とは逆。勉強会の「終わらせる」だけの決まり） |
| 最初の登録（紙の食い違い） | **画面から手で1件ずつ登録**（レビュー S4・2026-09-15 ユーザー確定）。保存のとき、同じ曜日に同じ参加者が入った別の勉強会があれば「時刻だけ違う勉強会があります」と注意する（止めない）。紙の食い違いは登録する人がその場で正しい方を選ぶ。Excel からは取り込まない |
| 勤務表の欄の名前 | **名字だけ・同じ名字の人だけフルネーム**（「12:30(30)濱口・馬場」「15:05(25)太田 英次朗・谷北」・2026-09-15 ユーザー確定）。勉強会の一覧の画面はフルネーム。🚨 名字の判定は在籍者の名前の空白で区切った1つ目（同じ名字＝太田・山本・西村・森本 の4組・2026-09-15 実測） |
| 本人に見せるか | **まずは管理画面とPDFだけ。切り替えで本人のページ（残業・時間管理の通常シフトの表示のそば）にも出せる**（2026-09-15 ユーザー確定「まずは1でいいが、切り替えで2もできるといい」）。切り替えは最初オフ。🚨 パートの方は残業・時間管理のページに入れないので、オンにしても見えない |
| 入力のプレビュー（C） | **見本のとおり**（2026-09-15 ユーザー確定）：曜日と校を選ぶと、その日その校にいる人が時間の帯で並ぶ。名前を押すと参加者に入り、選んだ全員がそろう時間を青、入れた勉強会の時間を赤い枠で重ねる。勤務表の欄の書き方（「12:30(30)濱口・馬場」）もその場で出す |

### 5-6. ③ 勉強会のレビュー（2026-09-15・UI/UX とシニアエンジニアの2体・重要な主張は自分で裏取り）
| # | 重さ | 指摘 | 裏取り → 設計への反映 |
|---|---|---|---|
| S1 | 致命的 | 勉強会そのものと版を分けないと、参加者だけの変更の履歴・将来の「その日だけ中止」が結べない | 勤務表は人×曜日の EXCLUDE で1本の流れ ✅ → **study_sessions（ID）／study_session_versions（曜日・開始・長さ・校・階・メモ・valid_from/to・EXCLUDE）／study_session_members（版×人）** |
| S2 | 致命的 | 保存の関数で書き込みのあとに ok:false を返すと途中まで確定する | ② は書き込み前の2か所だけ ✅ → 確かめは書き込み前に全部、以降は raise |
| S3 | 致命的 | 勤務表の適用開始日と勉強会の「いつから」がずれると判定がずれる／先の日付でずれても印が出ない | `applyFrom` は画面の中だけ ✅・パート 9/16 版 ✅ → **判定は勉強会の版の期間の中で、両方の版の切り替え日ごとに見る**（「10/1から ⚠️」） |
| S4 | 致命的 | 紙の勤務表に食い違い：清水 水 18:35 と 阿部・馬場 14:50／片方の欄だけ（曽川・川井 木 13:30、濱口・東 金 19:45、安福・幾田 火 18:00）／「西村・成田」の西村が不明 | PDF で確認 ✅ → 最初の登録の扱いを**ユーザーに確認** |
| S5 | 重要 | プレビューからパートが抜ける（勤務表の対象の初期値が「正社員だけ」） | ✅ → プレビューは雇用形態で絞らない |
| S6 | 重要 | 退職・削除で参加者が黙って消える（profiles は auth.users に ON DELETE CASCADE） | 実測 ✅ → 外部キーは CASCADE のまま、「2人以上」は関数で見て、画面で「参加者1人」「退職」を ⚠️ |
| S7 | 重要 | 勤務表の画面は在籍者だけ読む → 在籍していない4人の名前が空 | `shiftRosterApi.ts:45` ✅・実測4人 ✅ → 参加者の名前は is_active で絞らない |
| S8 | 重要 | 印の判定：時間帯の中に収まるか・終了ちょうどに始まるのは ⚠️・時間帯をまたぐ ⚠️・校が全部同じなら OK・「A→B」の6行は「校を確かめられない」・行が無い人は「未登録」 | 実測6行 ✅ → `lib/shiftRoster.ts` に1つ。勤務表の保存確認にも「勉強会◯件が時間外になります」 |
| S9 | 重要 | 本人のページは RLS で書くと自己参照で無限再帰 → SECURITY DEFINER の関数で切り替えも中で確かめる／切り替えの app_settings は管理者しか書けない | app_settings の書き込み is_admin() ✅ → 誰が切り替えるかを**ユーザーに確認** |
| S10 | 重要 | 修正は版を足す・削除は「◯/◯で終わる」・先の版がある勉強会に札・今日より前は確認 | ② と同じ流儀 ✅ → 反映。中止の日より後に版があるときを**ユーザーに確認** |
| S11 | 重要 | ⚠️ を「確認した」で消せるべきか／PDF に刷るか | → **ユーザーに確認** |
| S12 | 重要 | 本人に見せると内容メモも見える／パートに見えないことを人数と名前で出す | `OvertimePage.tsx:3534`【正社員専用】・`:4310 MyPatternToggle` ✅ → 反映 |
| S13 | 改善 | 校名を短くする処理が2か所（`shiftRosterPrint.ts:110` と `shortSchool`） | ✅ → **直した**（shortSchool に寄せた） |
| S14 | 改善 | 名字だけの名前は `lib` に1つ（在籍者全体で同じ名字を判定）・階は校ごとの選択肢（master_options）・PDF A は紙と同じ「掃除」「勉強会」の2列を今決める・長さは 10/15/25/30/45 のボタン | → 反映 |
| — | 改善 | ② の `shift_patterns_save` は user_id が null のとき「同じ人が2回」と誤った理由を出す（count distinct が null を数えない） | ✅（画面からは null を送らないので実害なし）→ 次に触るときに直す |
| — | 判断 | 版は人が保存したときしか増えないので掃除の cron は要らない。将来の「その日だけ中止」の表は1年で消す cron と一緒に作る | — |

### 5-7. ③ 勉強会の実装（2026-09-15・取り消し版の検算まで。本番DB・push はまだ）
- **DB（`supabase/migrations/20260915142603_study_sessions.sql`）**
  - `study_sessions`（ID）／`study_session_versions`（曜日・開始・長さ5〜240・校・階・メモ200字・valid_from/to・EXCLUDE で同じ勉強会の期間は重ならない）／
    `study_session_members`（版×人・並び順）／`study_session_acks`（版×ずれの鍵・誰がいつ確認したか）
  - 読みはすべて `can_manage_admin_tab('shift_patterns')`。書き込みの許可は acks の insert（本人の名前で）だけ。版と参加者は関数だけが書く
  - 階の選択肢：`master_options` の `floor_四条本校`（3F/4F/5F/6F）。本人に見せる切り替え：`app_settings` の `study_sessions_show_self`（最初は false）
  - `study_sessions_token()`／`study_sessions_save(p_payload)`：確かめは書き込み前に全部（S2）→ 今日より前の確認 → ロック → stale。
    修正は同じ内容なら何もしない・同じ日付ならその場で直す・違えば今の版を前日で閉じて足す。
    終わらせる＝その日で閉じ、先の版を消す（`deleted_future` で返す）・版が0になれば勉強会ごと消す
  - `my_study_sessions(p_date)`：切り替えがオンで、自分が参加者のときだけ返す（名前つき）。`#variable_conflict use_column`
  - 3つの関数とも anon を revoke
- **取り消し版の検算 45項目一致**（1回目は 3件 NG だったが、検算の日付が今日より前で「今日より前の確認」が正しく働いていただけ。検算を直した）。取り消し後に表が0を実測
- **画面の計算（`lib/studySessions.ts`・`lib/staffName.ts`）**：`tsx` で 25項目一致（名字だけ／同じ名字／退職者は数えない／中に収まる／終了ちょうど／
  一部外れる／休み／未登録／校が違う／途中で移る／中抜け／「A→B」／10/1 から ⚠️／鍵に日付／退職と人数／ほかの曜日だけある人＝休み）
- **画面**
  - シフト管理に ［勤務表］［勉強会］。勉強会（`components/admin/StudySessionsPanel.tsx`）：表示する日／曜日ごとの一覧（「◯/◯から始まる」「◯/◯から変わる」の札）／
    ⚠️ と［確認した］／追加・修正（いつから・曜日・開始・長さボタン・校・階・参加者・メモ）＋プレビュー C（パートも・そろう時間の青・勉強会の赤い枠・勤務表の欄の文・人ごとの ⚠️・時刻だけ違う同じ参加者の注意）／
    終わらせる（先の変更を一覧で出してから）／本人に見せる切り替え（管理者だけ・オンの前に正社員◯人・パートの名前）
  - 勤務表の欄に勉強会の行（ずれは赤＋⚠️）／保存の確認に「勉強会◯件が時間外になります」（直す前は問題なく、直すと ⚠️ になるものだけ）／
    勤務表に未保存の変更があるまま勉強会に切り替えると「⚠️ とプレビューは保存済みのシフトで判定」と出す
  - PDF：A に「掃除」（④まで空欄）「勉強会」の列、B はマスの中に勉強会の行、「☐ 勉強会の⚠️印も刷る」（初期はオフ）
  - 残業・時間管理の「自分の通常シフトを確認」を開くと、切り替えがオンのときだけ自分の勉強会を出す（オフなら何も出ない）
- 社内FAQ：「勉強会」を含むトピック・回答は0件 → 直すものなし
- tsc 0／vite build 成功／rules-of-hooks 0／新しいファイルの ESLint 0件／触ったファイルは HEAD と同じ件数（ShiftManagementTab 0・shiftRosterPrint 0・OvertimePage 28）

### 5-8. ④ 掃除担当表で決まったこと（2026-09-15〜 ユーザー確定・一問一答・途中）
- 紙（掃除担当表20260916.pdf）：行＝校・階・掃除の仕事（「＊」＝掃除機の紙パック点検・行の下の注意書きあり）／列＝月〜日／
  マス＝呼び名＋開始時刻（1マスに2人・「(会)」「（授業前）」「エアコン」などの書き添え・斜線＝その日は無し・上桂の日曜は「西村」1人で全部）／
  表の下に「休み」「担当なし」／右上に「2026/9/16～」／赤字＝前の表から変わった所
- 🚨 紙の名前は呼び名（恭子＝太田 恭子・貴子＝長岡 貴子・晃平＝林 晃平・理恵子＝宮川 理恵子・美香＝西村 美香・友輝＝山本 友輝）。
  旧姓・別の書き方も残る（尾上＝森本 千佳子・渡辺＝渡邊 理子）

| 項目 | 決定 |
|---|---|
| 切り替え方 | **変えたマスだけ**（勤務表・勉強会と同じ）。「いつから」を入れて直したマスだけ切り替え、赤字は前の日と比べて変わったマス。先に登録してある変更は残す |
| 名前の書き方 | **人ごとに呼び名を登録**。未登録の人は名字、同じ名字の人がいればフルネーム。呼び名がほかの人とかぶると保存できない。🚨 勉強会の欄も同じ呼び名にそろえる（今の `lib/staffName.ts` を置き換える） |
| 休み・担当なし | **外す人を選べる**。週のシフトが登録されている人全員（正社員・パート）を対象にし、掃除をしない人だけ「掃除の対象外」。休み＝その曜日に勤務予定がない／担当なし＝出勤するのに掃除のマスに1つも入っていない |
| ずれの印 | **勉強会と同じ ⚠️**（休み・出勤より前・掃除の途中で退勤・その時間はほかの校）。出勤時刻ちょうどの開始は OK。掃除の長さは本校・西陣・洛西口・南草津 15分、上桂 30分。保存は止めず［確認した］、PDF に刷るかは選べる |
| 勤務表の「掃除」の欄 | **時刻と場所**（「9:30 4Fトイレ」「西陣 ゴミ出し」）。仕事ごとに短い名前を付けておく |
| マスの中身 | **人と開始時刻を何行でも＋マスの書き添え1つ**（「(会)」「授業前」「エアコン」）。時刻の無い人（「小川（授業前）」）は時刻を空にし、⚠️ の判定はしない。斜線＝「この日は無し」を押したマス。同じ人が1つの校のその曜日の全部の行に入っているとき（上桂の日曜「西村」）は、PDF で自動で1マスにまとめて描く（入力は各行に入れる） |
| 行の一覧 | **画面から直す・消さず隠す**（部門の一覧と同じ）。校・階・仕事の名前・勤務表に出す短い名前・＊印・行の下の注意書き・並び。名前を直すと「いつから」に関係なく全部の表で変わる。表の上の注意書き（「＊出勤後すぐの掃除は…」）もここで直す |
| 最初の登録 | **紙の PDF から私が入れる**（行の一覧・9/16 からのマス・呼び名を DB の変更と一緒に）。入れたあと画面と PDF で紙と見比べてもらう |
| 紙の名前の対応 | 恭子＝太田 恭子／貴子＝長岡 貴子／晃平＝林 晃平／理恵子＝宮川 理恵子／美香＝西村 美香／友輝＝山本 友輝／**尾上＝森本 千佳子（呼び名「尾上」のまま）**／森本＝森本 純矢／太田＝太田 英次朗／山本＝山本 香澄／西村＝西村 友彦／渡辺＝渡邊 理子（表は「渡邊」）／髙林＝高林 淳子（表は「高林」）。ほかは名字のとおり（2026-09-15 ユーザー確定） |
| 名前が同じかの判定 | 🚨 未登録の人の名字は、**ほかの人の表に出る名前（呼び名を含む）**とかぶるときだけフルネーム。森本 純矢は千佳子さんが「尾上」なので「森本」、太田 英次朗は「太田」 |
| PDF | **紙と同じ A4横1枚（全校）＋校を選んで1枚**（各校に貼る用）。校ごとの表の「休み」「担当なし」はその校で勤務する人だけ。赤字（変更）と ⚠️ を刷るかは出すときに選ぶ（⚠️ は初期は刷らない） |

#### 5-8-1. ④ の設計の案（レビュー前）
- **DB**
  - `staff_display_names`（user_id・呼び名・在籍者でかぶり禁止）… 掃除担当表と勉強会の欄が使う。書くのは関数だけ
  - `cleaning_rows`（校・階・仕事・短い名前・＊印・行の下の注意書き・掃除の長さ（本校・西陣・洛西口・南草津 15分／上桂 30分）・並び・隠す）
  - `cleaning_cells`（行×曜日×valid_from/to・「この日は無し」・書き添え・`entries` jsonb `[{user_id,start}]`・saved_by・EXCLUDE で期間が重ならない）
  - `cleaning_excluded_staff`（掃除の対象外）／`cleaning_acks`（マスの版×ずれの鍵）／表の上の注意書き（1行の設定）
  - `cleaning_token()`／`cleaning_save(p_payload)`：② の `shift_patterns_save` と同じ流儀（変えたマスだけ版・先の版は残す・今日より前は確認・stale で断る・確かめは書き込み前に全部）
  - 読み書きとも `can_manage_admin_tab('shift_patterns')`。anon を revoke
  - 最初の中身（行・9/16 からのマス・呼び名）は migration の seed
- **画面**（シフト管理に ［勤務表］［勉強会］［掃除担当表］）
  - いつから／表（行＝校・階・仕事、列＝月〜日）。マスを押すとその場で入力（人・時刻を足す／書き添え／この日は無し）。赤字＋「前：」／⚠️ と［確認した］
  - 表の下に「休み」「担当なし」（週のシフトから自動・対象外の人は出さない）
  - 保存の確認（変えたマス・先の版を残すマス・⚠️ になるマス）／stale のとき「読み込み直す」
  - 行の一覧／人の一覧（呼び名・掃除の対象外）／PDF（全校・校を選ぶ・赤字・⚠️）
  - 勤務表の欄と PDF A の「掃除」の列に「9:30 4Fトイレ」、勤務表の保存の確認に「掃除◯件が時間外になります」
  - 判定は `lib/studySessions.ts` の `dayIssue` を掃除にも使う（長さは行の値）。名前は `lib/staffName.ts` を呼び名つきに置き換え

#### 5-8-2. ④ のレビュー（2026-09-15・UI/UX とシニアエンジニアの2体・重要な主張は自分で裏取り）
| # | 重さ | 指摘 | 裏取り → 設計への反映 |
|---|---|---|---|
| C1 | 致命的 | 紙の PDF の文字は空のマス・斜線が残らず、そのまま読むと曜日の列がずれる | 3F 拭き掃除の文字は先頭が「阿部 13:00」だが画像では月が空・火が阿部 ✅ → **画像から行×曜日の一覧を作り、見比べてもらってから入れる**。入れたあと行ごとの文字に戻して照合 |
| C2 | 致命的 | 紙と週のシフトが食い違う → 入れた時点で ⚠️・休みが紙と違う | 実測 ✅：月 安福 本校4F 12:15／週のシフト 洛西口校 12:15〜。月 曽川 本校6F 12:30／洛西口校 10:15〜。月 馬場 週のシフト「四条本校→上桂校」。日 小川・栗木 紙は休み／週のシフトは本校 9:15〜（どちらも 7/16 版）→ **ユーザーに確認** |
| C3 | 致命的 | 人を jsonb に入れると、削除した人が宙に浮き保存が止まる | profiles は auth.users に CASCADE（③で実測済み）✅ → **`cleaning_cell_entries`（マス×人・CASCADE・開始・並び）の別表** |
| C4 | 致命的 | 表の上の注意書きを app_settings に置くとマネージャーが書けない | 書き込みは is_admin() ✅ → **専用の表（can_manage_admin_tab）・複数の注意書き** |
| C5 | 致命的 | 名前入りの初期データを migration に書くと Public のリポジトリで公開される | CLAUDE.md ✅ → **ユーザーに確認** |
| C6 | 誤り | 「久我・古家・大岡・中嶋・鴨井・奥村・村田・左近は登録が無いかも」 | 8人とも在籍者 ✅（指摘は誤り） |
| I1 | 重要 | 行の校は正式名（四条本校・西陣校・上桂校・洛西口校・南草津校）で持つ。表示で短くする | `studySessions.ts:89` は完全一致 ✅ → 反映 |
| I2 | 重要 | dayIssue は「出勤前」「途中で退勤」を分けられない | outside/partial だけ ✅ → 判定を `shiftRoster.ts` に移し種類を分ける（勉強会の文は今のまま） |
| I3 | 重要 | 読み取りの 1,000 行打ち切り | ✅ → valid_to が null か基準日以降で絞る |
| I4 | 重要 | 呼び名を直すとほかの人の表示も変わる／勉強会の欄が変わる | `staffName.ts` ✅ → 保存の前に「表示が変わる人」を並べる。`shortNameMap(staff, 呼び名)` を1か所 |
| I5 | 重要 | 同じ日付のその場の直しで「確認した」が古いまま残る | ③ の acks は版 ID ✅ → ずれの鍵に行・曜日・人・開始・種類・校を入れる |
| I6 | 重要 | 行の校・階を後から変えると過去の版まで移る | ✅ → マスがある行は校・階を変えられない（名前・注意書き・長さは直せる） |
| I7 | 重要 | 校×曜日まるごと無し（日曜の西陣・洛西口・南草津）を1マスずつ押すのは手間／PDF でまとめて描く決まりが無い | 紙 ✅ → 「この校のこの曜日は無し」を置き、PDF で1つの斜線に |
| I8 | 重要 | 同じ人・同じ時刻・同じ校が複数（西陣 火 馬場 13:15×2）→ 勤務表の欄が溢れる | 紙 ✅ → 勤務表の欄は「13:15 西陣 掃除機・拭き掃除」の1行にまとめる |
| I9 | 重要 | 空のマス（未入力）と斜線（無し）を区別。空に戻すときも版を作る | ✅ → 反映。保存の確認に「空のマス◯件」 |
| I10 | 重要 | 表が縦に長い（約31行）→ 曜日の見出しを固定 | `ShiftManagementTab.tsx` は左の列だけ固定 ✅ → 反映 |
| I11 | 重要 | 校ごと PDF の「休み」はその日のシフトが無いので校が決まらない | ✅ → **ユーザーに確認** |
| I12 | 重要 | 対象外の初期値（紙に無いパート） | 実測：今田・加藤・小菅は週のシフトあり・紙に無し ✅（渡邊は紙に「渡辺」で有り）→ **ユーザーに確認** |
| C2 決定 | — | 紙と週のシフトの食い違い | **紙のまま入れて ⚠️ で出す**（2026-09-15 ユーザー確定）。入れる前に全曜日ぶんの食い違いの一覧を見せる |
| C5 決定 | — | 名前入りの初期データ | **リポジトリに載せない**（2026-09-15 ユーザー確定）。migration は表の形と行の一覧だけ。マスの中身と呼び名は許可のあと本番DBに1回だけ流し、手元のファイルも消す |
| I12 決定 | — | 対象外の初期値 | **最初は誰も外さない**（2026-09-15 ユーザー確定）。今田・加藤・小菅さんも出勤する日は「担当なし」に出る。外すのは画面から（すぐ効く・いつからは持たない） |
| 決定 | — | 休み・担当なしにパートを出すか | **休みは正社員だけ・担当なしはパートも出す**（2026-09-15 ユーザー確定）。紙の休みは正社員＋三枝さんだけで、パートも出すと毎日10人前後増えるため。三枝さんは休みに出なくなる |
| I11 決定 | — | 校ごと PDF の休み・担当なし | **校ごとの表には出さない**（2026-09-15 ユーザー確定）。全校の表だけに出す |
| — | 判断 | 勤務表の掃除の欄：同じ人がその校のその曜日の全部の行に同じ時刻で入っているときは「9:00 上桂 全部」と1行 | — |
| — | 判断 | 書き添えはマスに1つのまま（5-8 で決定済み）。表の上の注意書きは複数持てる。PDF では地の色を足さず（新しい色を足さない決まり）、紙の赤字の注意は赤字で刷る | — |
| — | 判断 | 直せる人は勤務表と同じ `can_manage_admin_tab('shift_patterns')`（管理者＋タブを開いたマネージャー以上・パソコン） | — |
| — | 判断 | ⚠️ の判定を `lib/shiftRoster.ts` の `shiftTimeIssue` 1か所にまとめ、勉強会の `dayIssue` もこれを使う（勉強会の文は今までどおり：勤務時間に重ならなければ「勤務していません」、一部なら「一部の時間が勤務時間外」） | — |
| — | 判断 | 最初の版は比べる前の版が無いので赤字は出ない（紙の土曜の赤字は前の表からの変更）。次の変更から赤字 | — |
| — | 判断 | 受付・電話の行も ⚠️ の対象。長さは行ごとに持ち、初期値は 15分（上桂は 30分） | — |

#### 5-8-3. ④ の実装（2026-09-15・取り消し版の検算まで。本番DB・push はまだ）
- **DB（`supabase/migrations/20260915174738_cleaning_roster.sql`）**：`cleaning_rows`（31行を seed）／`cleaning_cells`（EXCLUDE で行×曜日の期間が重ならない）／
  `cleaning_cell_entries`（profiles に CASCADE）／`cleaning_acks`／`cleaning_excluded_staff`／`cleaning_notes`（見出し1・注意書き3を seed）／`staff_display_names`
  - 読みはすべて `can_manage_admin_tab('shift_patterns')`。行・注意書き・対象外は画面から直接（同じ判定）。マスは `cleaning_save`、呼び名は `staff_display_name_save` だけ
  - `cleaning_rows_guard`：校は校の一覧の値だけ／マスがある行の校・階は変えられない
  - `cleaning_save`：変えたマスだけ版・同じ内容は何もしない・同じ日付はその場で直す（確認したを消す）・先の版は残す・空に戻すときも版・効いている版が無い空のマスは作らない・
    今日より前は確認・stale で断る・確かめは書き込みの前に全部（「9:30」と「09:30」は同じ時刻として数える）
  - 3つの関数とも anon を revoke。🚨 名前入りの中身（マス・呼び名）はこのファイルに入れない
- **取り消し版の検算 55項目一致**（1回目は4件 NG：同じ時刻の書き方の違いを別と数える不備1件を直した。残り3件は検算の書き方の誤り）。取り消し後に表0を実測
- **最初の中身**：紙の画像から読んだ行×曜日（217マス）と呼び名7人を、名簿の名前で引いて入れる SQL（scratchpad だけ・リポジトリに入れない）。名前の対応 41人は全員が在籍者で1人ずつに当たる
- **画面の計算**（`lib/cleaningRoster.ts`・`lib/shiftRoster.ts` の `shiftTimeIssue`・`lib/staffName.ts` の呼び名つき `shortNameMap`）：`tsx` で検算 全部一致
  （呼び名と同じ名字の出し分け／上桂の日曜は西村でまとめる・西陣の日曜は1本の斜線／勤務表の欄「13:15 西陣 掃除機・拭き掃除」「上桂 全部」／マスの文字「森本・山田 10:00」「小川（授業前）」／
   出勤前・途中で退勤・ほかの校／勉強会の文は今までどおり）
- **画面**
  - シフト管理に ［掃除担当表］（`components/admin/CleaningRosterPanel.tsx`）：適用開始日／表（曜日の見出しと左の列を固定）／マスを押してその場で入力（人・時刻・書き添え・この日は無し・その校のその曜日を全部無し）／
    赤字＋未保存の枠／⚠️ と［確認した］・⚠️ の一覧／表の下の休み・担当なし／保存の確認（変えたマス・空に戻すマス・先の変更を残すマス・新しく ⚠️ になるマス・入力のまちがい）／stale のとき読み込み直す
  - 行・注意書きの一覧（名前・短い名前・＊・長さ・行の下の注意書き・並び・隠す・足す）／呼び名・対象外（保存の前に「表に出る名前が変わる人」を並べる）
  - PDF（`lib/cleaningRosterPrint.ts`）：全校 A4横（紙と同じ並び・斜線・まとめ・休み／担当なし）・校を選んで1枚・赤字・⚠️
  - 勤務表：マスに「掃除 9:30 4Fトイレ」／保存の確認に「掃除◯件が時間外になります」／PDF A の掃除の列・B のマス
  - 勉強会の欄も呼び名（`StudySessionsPanel` と勤務表）
- PDF は見本データで開発用の画面に出し、まとめ（西陣・洛西口・南草津の日曜は1本の斜線、上桂の日曜は西村）・注意書きの行・校ごとの表に休み／担当なしが出ないことを確かめた。狭い幅で5マスはみ出したので折り返すようにした
- tsc 0／vite build 成功／rules-of-hooks 0／新しいファイルの ESLint 0件／触ったファイルは HEAD と同じ件数

### 5-9. ⑤ こどもシフト表で決まったこと（2026-09-15〜 ユーザー確定・一問一答・途中）
- 紙（`D:\ダウンロード\2026年9月こどもシフト表.pdf`・「2026年9月～ こどもシフト表案（2026.9.9時点）」・A4横1枚）：
  曜日ごとの段（月〜日）×列（四条本校 3F／5F／6F・西陣校・上桂校・洛西口校・南草津校・園指導）。土日は列の組み方が違う（土は本校 3F・5F、日は本校1列＋上桂の選手）。
  校の見出しに「授業準備：◯◯」「フロント：◯◯（月3回）」。マスの中は時刻順の文字：班（「15:40～2班（太田・小出）」＋次の行に担当「森本・幾田・西村」）／
  P（「P16:00(30)晃平・貴子」）／打合せ（「打合せ15:20～15:30」）／勉強会（緑の字「14:00(30)鈴木・谷北」）／定員（「リトル14:40～ 定員5」）／
  6F は時間帯の行（「10～11」「13～14 栗木」）／園指導（「陵和園 10:25～11:50（月1回）馬場」）。段の下に「（社員休み）」。赤字＝変わった所。
  枠の外の書き添え（「こどもミーティング (水)(木)(金)で順番に行う」「今田・奥村ー誰か休みの時、各校に午前中出勤可能」）

| 項目 | 決定 |
|---|---|
| 切り替え方 | **「いつから」で切り替える**（勤務表・勉強会・掃除担当表と同じ・2026-09-15 ユーザー確定）。変えたマスだけ切り替え、赤字＋「前：」。「10月の表」のような月の単位は持たず、PDF は「◯/◯時点」の日付を選んで出す。月1回の授業はマスの書き添え（「（月1回）」）で書く |
| 案 | **決定済みの表とは別に、名前を付けた案をいくつでも持てる**（2026-09-15 ユーザー確定「案を3案ぐらい作ることがある」）。［＋ 新しい案］で決定済みの表かほかの案を写して始める。案の中で直しても決定済みの表は変わらない。⚠️ は案の中でも出す。2つの案を比べると違うマスに印。案ごとに PDF（題「こどもシフト表 案2」）。［この案で決定する］→ 適用開始日 → いつもの保存の確認 → その日から切り替え |
| 案の持ち方 | **案は「変えたマスだけ」を持つ**（2026-09-15 ユーザー確定・案A）。案で触っていないマスはいつも決定済みの表と同じ（あとで決定済みの表を直すと案にも入る）。同じマスを案でも決定済みの表でも変えていたら案の値のまま、決定するときに「⚠️ ◯曜 ◯◯校は、案を作ったあとに決定済みの表でも直されています」と出す（決定であとからの直しを黙って消さない）。🚨 「まるごとの写し」にはしない |
| 決定したときの残りの案 | **しまう**（2026-09-15 ユーザー確定・案A）。決定した案も使わなかった案も「しまった案 ▼」へ（「案2（10/1 に決定・林 9/15）」「案1（使わなかった）」）。見るだけで、［写して新しい案にする］で使い直せる。🚨 **決定から2年たつと自動で消える**（ユーザー「2年は残したい」）＝毎晩の cron で掃除。見積もり：掃除担当表 217マス・人177件で 184kB（2026-09-15 実測・DB 全体 43MB）。こどもシフト表をその3倍・月3案・案はまるごと写しと多めに見ても2年で約40MB＝無料枠 500MB に収まる |
| マスの中身 | **マス（曜日×校・本校は階）の中に「行」を何行でも。1行＝種類・開始・終了・人・書き添え**（2026-09-15 ユーザー確定・案A）。人と時刻を持つので ⚠️・社員休み・勤務表の欄への反映を自動にできる。勉強会の行は勉強会の画面から自動で出す（ここでは直せない）。🚨 時間帯の格子・自由な文章だけ、にはしない |
| 紙の書き方の意味（ユーザー回答） | 「15:40～2班（太田・小出）／次の行 鈴木・谷北・渡辺」＝**2班**はレッスンの班分けの数（人数とは別。「16:40～1班」に5人）／**次の行の人**がそのクラスのレッスンを担当する人／**（ ）の人**は勤務しているがこのクラスのレッスンを担当していない人。「授業準備：森本」＝事前の準備をする人が決まっているので書いている（校の見出しに欄を持つ）。「渡辺19まで」「16:45～小菅」＝その人だけ途中で抜ける・途中から入る（行の中で人ごとに開始・終了を持つ）。「リトル15:40～ 定員10」「養成17:30～18:30」「選手17:30～21:15」＝クラスの名前と定員。🚧 未回答：月曜 5F「17:40～3班（小出・鈴木）／太田・鈴木・谷北・渡辺」で鈴木さんが両方に入っている（最初の登録のときに確認）。🚨 PDF の文字の取り出しでは列が混ざる（位置で分け直すこと。月曜 5F の続きに西陣校の「森本・幾田・西村」を読み違えた） |
| 行の種類（最初の一覧） | **レッスン**（クラス名＝空ならふつうの班・班の数・定員・担当・勤務中（担当しない））／**P**（開始・長さ・担当・手入力）／**打合せ**（開始・終了・人なしも可）／**園指導**（園の名前・開始・終了・担当・書き添え）／**大人コース業務**（開始・終了・担当。6F の列はこの行だけを入れる場所）／**勉強会**（勉強会の画面から自動・ここでは直せない）／**その他**（開始・終了・担当・書き添え）。どの種類でも人ごとに「19まで」「16:45～」を持てる。管理者が画面から足す・名前を変える・隠す（2026-09-15 ユーザー確定「おｋ」） |
| 校の見出し | **曜日×校の見出しに「役割」を何行でも**（2026-09-15 ユーザー確定）。役割は一覧から選ぶ（最初は 授業準備／フロント／受付・授業・管理者が足せる）・人・**サポート（任意）**・書き添え（「月3回」）。「なし」を選べる（空欄と区別）。時刻を持たないので ⚠️ は「その曜日に出勤しているか」だけ。案・いつからの切り替えはマスと同じ。紙の「授業準備：東(幾田)」の（幾田）＝**新人の東さんに付く2年目のサポート役**（ユーザー回答）→ 書き添えではなくサポートの人として持ち、⚠️ の対象にする |
| 社員休み | **自動：メインの部門が「こども」の正社員で、その曜日に勤務予定がない人**（2026-09-15 ユーザー確定）。9/16 時点の週のシフトで紙と突き合わせ、月〜土は完全に一致。日曜は決まりで尾上（森本 千佳子）さんも出るが、紙の書き漏れ（ユーザー回答「もれてるだけ」） |
| ⚠️ の決まり | **勉強会・掃除担当表と同じ決まり。園指導だけ見る範囲をせまく**（2026-09-15 ユーザー確定「おｋ」）。レッスン・P・打合せ・大人コース業務・その他＝その曜日が休み／その時間より前に出勤／途中で退勤／その時間はほかの校（「四条本校→上桂校」のように移る時刻が分からない日は校を確かめない）。見出しの役割（サポート含む）＝その曜日が休み。**園指導＝その曜日が休みだけ（時刻・校は見ない）**：紙の3件（火 陵和園 馬場／木 太秦保育園 阿部・栗木／木 下鴨夢 小出）は月1回の朝で、週のシフト（西陣 13:15〜 など）には入っていないため、確かめると常に ⚠️ になる。勉強会は勉強会の画面と同じ ⚠️。人ごとの「19まで」「16:45～」があればその時間で見る。大人コース業務（6F）は四条本校にいるかで見る。保存は止めない・［確認した］・PDF/Excel に出すかは選ぶ。🚨 9/16 時点の週のシフトの `segments` は部門（area_id）が全員 null＝部門では確かめられない（校だけ） |
| 曜日ごとの列 | **中身（行か見出し）がある列だけをその曜日に出す**（2026-09-15 ユーザー確定・案A）。列の一覧（校・階・名前・並び・隠す）は管理者が直す。［＋ 列を足す］で空の列を出して入れ始める。PDF・Excel も中身のある列だけ。いつから・案で列を足すとその日から出る。紙の組み方：月〜金＝本校 3F・5F・6F／西陣／上桂／洛西口／南草津／園指導（金は見出しに「課外教室」）、土＝本校 3F・5F／西陣／上桂／洛西口／南草津、日＝本校 3F・5F／上桂。🚨 日曜の本校の「幼」「小」は**別の列＝3階と5階**（ユーザー回答）。本校は 3F・5F・6F の列で持ち、「本校（全体）」の列は作らない（「打合せ4階」は行の書き添え） |
| 枠の外の書き添え | **曜日ごとの書き添え＋表全体の書き添えの2種類**（2026-09-15 ユーザー確定・案A）。曜日の書き添え（「今田・奥村ー誰か休みの時、各校に午前中出勤可能」＝土曜）は段の下・社員休みの横に出し、いつから・案でマスと一緒に切り替わる。表全体の書き添え（「こどもミーティング (水)(木)(金)で順番に行う」）は題の横・何行でも（掃除担当表の `cleaning_notes` と同じ作り） |
| 作る順番と使い方（ユーザー説明） | **年度替わり**：こどものシフト（レッスンを運営できること）→ 40時間の勤務表 → 掃除 → こどもシフトの担当・（かっこ）を詰める。**ふだん**：個別に調整（40時間の前後にずらす程度）。🚨 いちばん大事なのは**班の数でスタッフの数が決まるので漏れがないこと**。人がいなければ「追加必要」として入れておき、最終的にレッスンが回ればよい（2026-09-15 ユーザー説明）。紙の「貴子・（　）」「栗木・阿部・今田・(　)」がその空き枠。**2段に分ける**（2026-09-15 ユーザー確定「おｋ」）。**段階1（今回）**：こどもシフト表（いつから・案・行・見出し・社員休み・書き添え・列）＋人数の漏れ（必要な人数・うちレッスンできる人・切り替え・追加必要・案ごとの件数）＋⚠️（案の中では最初は隠し切り替えで出す）＋勤務表の保存の確認に「⚠️ こどもシフト表◯件が時間外になります」＋PDF・Excel。段階1は大きいので、さらに2回に分けて出す（分け方はレビューのあとで相談）。勤務表のマス・PDF にはこどもシフト表の中身を出さない。**段階2（2027年4月の前）**：案から「勤務表のたたき台」（40時間との差）→ 勤務表に写す |
| 人数の漏れを防ぐ | **案A（切り替えつき）**（2026-09-15 ユーザー「A にもできるようにしておいて、切り替えできるといいかも」）。ユーザー説明：班の数でスタッフの数が決まる／**レッスンできる人は3班なら最低2名**／パートでも可能な場合がある。→ レッスンの行に「必要な人数」（初期値＝班の数・行ごとに直せる）と「うちレッスンできる人の最低人数」（初期値＝班の数ごとの表・管理者が持つ・行ごとに直せる）。人に「レッスンできる」の印（初期値：正社員 ☑・パート ☐）。足りない分は行に「あと◯人」、表の上に「追加必要 ◯件」、担当の枠に［追加必要］（紙の「（　）」・PDF/Excel は「（　）」）。「栗木or阿部」「未定」は書き添え。案ごとに追加必要の件数。勤務中（担当しない）は数えない。**切り替え**：表全体で1つ「レッスンできる人の確かめ ［する］［しない］」。しない＝人数だけ数える（印と行ごとの最低人数は残る）（ユーザー「あってる」）。**班の数ごとの人数（初期値・2026-09-16 ユーザー確定「あってる」）**：1班＝必要2人／うちレッスンできる人2人、2班＝2人／2人、**3班＝3人／2人（3人目はパートでも可）**、4班以上は今のところ無い。管理画面で直せる・行ごとにも直せる。
  **3F と 5F にまたがる担当（レビュー U4）＝案A：行を列ごとに分ける**（2026-09-16 ユーザー確定）。🚨 年度替わりの最初は誰がどちらの階かまで決まっていないので、**先に班の数だけ入れて人は後から**入れる使い方にする（担当が空でも「追加必要 ◯人」で人数の漏れが分かる）。**同じ時間に2つのコースがある所は、それぞれのコースでこの基準**＝レッスンの行を2つに分けて、行ごとに数える。紙の担当にはパートが多く入る（小菅・今田・奥村・大岡・久我・中嶋・古家・村田・渡邊・山本 友輝） |
| 名前の書き方 | **掃除担当表・勉強会と同じ呼び名の決まり**（`staff_display_names`＋`lib/staffName.ts` の `shortNameMap`・2026-09-15 ユーザー確定「おｋ」）。登録済み7人（友輝・尾上・恭子・晃平・理恵子・美香・貴子）。紙の太田＝太田 英次朗／森本＝森本 純矢／西村＝西村 友彦／渡辺＝渡邊。🚨 紙の「山本」は山本 香澄（正社員・大人）か山本 友輝（パート・呼び名「友輝」）か紙だけでは決まらない → 最初の登録で1つずつ確かめる |
| 同じ時間に2か所（レビュー U3） | **確かめる**（2026-09-16 ユーザー確定・案A）。こどもシフト表の中だけで、同じ人が同じ時間の別の行に入っていたら「⚠️ 重なり」を出す（年度替わりは勤務表がまだ無く ⚠️ が効かないため）。保存は止めず［確認した］で消せる。**同じ校の中の重なりも出す**（2026-09-16 ユーザー確定・案イ。本当の掛け持ちは［確認した］で消す） |
| 案の日付（レビュー U5・エンジニア1） | **案ごとに「予定の適用開始日」を持つ**（2026-09-16 ユーザー確定・案A）。案を開くとその日の決定済みの表に案の変更を重ねて出し、赤字・⚠️・追加必要もその日で判定。決定のときは適用開始日にその日が入った状態で開く（その場で変えられる）。＝見ていた表と決定後の表が同じになる |
| 案で元に戻したマス（レビュー U7） | **案から外す＝触っていない扱い**（2026-09-16 ユーザー確定・案A）。保存のとき決定済みと同じ内容のマスは案から外す。マスに「決定済みの表に戻す」も置く。＝あとから決定済みの表に入った直しをそのまま受け入れ、決定のときに聞かれない。🚨 赤字の見え方はどちらの案でも同じ（赤＝いまの中身が決定済みと違うマス）で、違いは決定のときの確認だけ |
| 決定のときの確認 | **ぶつかったマスはマスごとに［案の値］［決定済みを残す］を選ぶ**（2026-09-16 ユーザー確定・案A）。決定は2回に分け、1回目は何も書かずに一覧を出す |
| 作業中の案の上限 | **10個**（2026-09-16 ユーザー確定「おｋ」）。上限に達したら「使わない案をしまってから」と出す（しまった案は［写して新しい案にする］で戻せる）。しまった案は2年で cron が消す |
| 重ねる勉強会 | **校・階が合う勉強会を全部（案A）＋切り替え**（2026-09-16 ユーザー確定「A でいいが切り替えできるように」）。表の上に ［すべて］［こども部門の人がいるものだけ］・初期は「すべて」・切り替えは端末に覚える・PDF/Excel も同じ選択で出す（出すときに変えられる） |
| 既存の不具合の直す時期 | **①（タブの切り替えで未保存が消える）はこどもシフト表の1回目に含めて先に直す／②（勉強会の確認が日付で外れる）は確認の鍵を作る回（2回目）で直す**（2026-09-16 ユーザー確定・案A） |
| 見る人・直す人 | **勤務表・掃除担当表と同じ：`can_manage_admin_tab('shift_patterns')`（管理者＋権限管理でシフト管理を開いてもらったマネージャー以上・パソコン）だけが見て直す。スタッフには PDF・Excel で配る**（2026-09-15 ユーザー確定・案A）。スタッフの画面に出す切り替え・こどもシフト表だけ直せる権限は作らない。2026-09-15 時点でシフト管理をマネージャーに開いていない（`manager_admin_tabs` は空）・マネージャー以上7人・メインの部門がこどもの正社員18人 |
| 6F の列 | 「10～11 尾上」「大人19 渡辺」＝**大人コースを6階でやっているので、その業務に行く**（2026-09-15 ユーザー回答）。種類の名前は「6F の時間帯」ではなく大人コースの業務として持つ |
| P と場所予約（調べた結果・判断待ち） | ユーザー「プライベートは枠としてはここで管理しているので重ならないのでは」。🚨 本番の場所予約を読んだところ（2026-09-15）、紙の P と同じ枠が**すでに場所予約にも入っている**：これから4週間のプライベートは 募集中の枠 45件（ほぼ単発＝一括入力で入れたもの）・予約 68件、繰り返しの決まり 19件。紙と一致する例：月 3F 18:50 小出／月 西陣 18:40 栗木・幾田／火 5F 13:10 森本／火 3F 15:00 森本／土 5F 12:40 林。紙にあって場所予約に無い例：月 3F 18:40 晃平・月 西陣 18:40 森本（すでに埋まった・消えた可能性あり・未確認）。→ **決定：現段階では場所予約とつなげない**（2026-09-15 ユーザー確定「こどもシフト表とは別でいい」）。P はこどもシフト表で手入力の行として持ち、場所予約を読まない・書かない・照らす印も出さない。🚨 同じ枠が2か所にあり、片方だけ直すと食い違うことは説明済み。つなげるとき（照らす印・自動で出す・場所予約へ作る）は場所予約の担当と相談 |
| 出力 | **PDF と Excel**（2026-09-15 ユーザー追加「エクセルで排出も」）。Excel の形は表の作りが決まってから聞く |
| PDF | **紙と同じ A4横1枚（全校）＋校を選んで A4横1枚**（2026-09-15 ユーザー確定・案A・掃除担当表と同じ出し方）。段は「月・火」「水・木」「金・土・日」。書き方は紙と同じ（「15:40～2班（勤務中の人）」の次の行に担当・「P16:00(30)晃平・貴子」・「19まで」「16:45～」）。題「2026年10月～ こどもシフト表 案2（2026.9.15時点）」＋表全体の書き添えを題の横。出すときに選ぶ：全校／校・時点の日付・題に「案」・変わった所を赤字（前の版と比べて）・追加必要を「（　）」・⚠️ を刷る（初期は刷らない）。入りきらない段は文字を自動で小さく、読めない大きさになるときは画面で「2枚に分けて（校を選んで）ください」と出す |
| 最初の登録 | **紙を「案1」として入れる**（2026-09-15 ユーザー確定・案A）。紙の画像から行ごとの一覧を作り（PDF の文字は列がつながるため）見比べてもらってから、本番DBに1回だけ入れる（名前入りの作業ファイルはリポジトリに置かず消す）。画面・PDF・Excel で紙と見比べて直す（月 5F「17:40～3班（小出・鈴木）」の鈴木さん・「山本」の特定・日曜の社員休みの尾上さん など）→［この案で決定する］で決定済みの表に。決定済みの表は最初は空 |
| 4階の勉強会 | **4F の列は作らず、本校の見出しの下に1行で出す**（2026-09-16 ユーザー確定・案B。ほかに「近い列に（4F）と書き添え」「段の下」「勉強会の登録で出す列を選ぶ」「出さない」も比べた）。例「四条本校（授業準備：鈴木）　4F：勉強会 13:00(30)濱口・馬場」。**紙の「打合せ4階 9:16～9:23」も同じ1行に入れる**（2026-09-16 ユーザー確定）＝見出しの下の「4F」の行には、勉強会だけでなく打合せなどの行も足せる。**校を選んでいない勉強会もこどもシフト表に出す**（2026-09-16 ユーザー確定「いれたい」）。**校を選んでいない勉強会は曜日の段の下（社員休みの横）に出す**（2026-09-16 ユーザー確定・案A）。2026-09-16 時点で勉強会の登録は0件・階の選択肢は 3F/4F/5F/6F |
| Excel | **シート3枚**（2026-09-15 ユーザー確定・案A）。「表」＝紙に近い形（曜日×列・セルの中で改行して紙と同じ順・見出しの役割・社員休み・書き添えも）／「一覧」＝1行1件（曜日・校・階・種類・開始・終了・クラス・班・必要・うちレッスンできる人・担当・勤務中（担当しない）・追加必要・書き添え・⚠️）／「人ごと」＝1行1人（メインの部門・曜日ごとの「本校5F 14:00-19:00（5件）」・週の件数）。案から出すときはファイル名とシートの先頭に「案2」と時点の日付 |

#### 5-9-1. ⑤ の設計の案（レビュー前・段階1）
- **考え方**：1つの「マス」＝ 置き場所×曜日。置き場所は3種類 ── 列（本校3F・西陣校・園指導 …）／校の見出し（授業準備・フロント）／曜日の書き添え。
  マスの中身をまとめて「いつから」の版にする（掃除担当表の `cleaning_cells` と同じ流儀：変えたマスだけ・先の版は残す・今日より前は確認・stale で断る・確かめは書き込みの前に全部）
- **DB（新しい migration・名前は `kids_shift_*`）**
  - 一覧（画面から直す・消さず隠す・`can_manage_admin_tab('shift_patterns')`）：
    `kids_shift_columns`（校・階・表示名・並び・隠す。マスがある列は校・階を変えられない＝`cleaning_rows_guard` と同じ）／
    `kids_shift_row_kinds`（レッスン／P／打合せ／園指導／大人コース業務／その他。持つ欄の組み合わせ・⚠️ の見方〔全部／曜日の休みだけ〕・並び・隠す）／
    `kids_shift_role_kinds`（授業準備／フロント／受付・授業）／`kids_shift_notes`（表全体の書き添え）
  - 設定（1行）：`kids_shift_settings`（レッスンできる人の確かめ する/しない・班の数ごとの最低人数 `{1:2,2:2,3:2}`）。🚨 app_settings は管理者しか書けないので専用の表
  - 人：`kids_shift_staff_flags`（user_id・レッスンできる。行が無ければ 正社員＝できる／パート＝できない）
  - 版：`kids_shift_cells`（置き場所の鍵・曜日・valid_from/valid_to・saved_by・EXCLUDE で同じ鍵×曜日の期間が重ならない）
    ＋中身 `kids_shift_items`（マス×並び：種類・開始・終了・クラス名・班の数・必要な人数・うちレッスンできる人の最低人数・役割の種類（見出しのとき）・「なし」・書き添え）
    ＋人 `kids_shift_item_people`（行×人：担当／勤務中（担当しない）／サポート・人ごとの開始・終了・並び。profiles に CASCADE）
  - 案：`kids_shift_plans`（名前・作った人・保存した人と日時・状態〔作業中／しまった〕・決定した日・適用開始日・しまった理由〔決定／使わなかった〕）
    ＋`kids_shift_plan_cells`（案×置き場所×曜日・**写した元の決定済みの版の ID**＝決定のときに「決定済みの表でも直されています」を出すため）＋中身と人は版と同じ形の表（plan_cell_id で持つ）
  - 確認した：`kids_shift_acks`（版（または案のマス）×ずれの鍵：行・人・開始・種類・校）
  - 関数：`kids_shift_token()`／`kids_shift_save(p_payload)`（決定済みの表を直接直す）／`kids_shift_plan_save(p_payload)`（作る・写す・名前・マスを保存・しまう・写して新しい案）／
    `kids_shift_plan_decide(p_plan, p_valid_from, p_token, p_confirm)`（案のマスを決定済みの表に「変えたマスだけ」入れ、案をしまう。決定済みでも直されたマスがあれば確認を返す）。すべて anon を revoke
  - 掃除：cron `purge-kids-shift-plans-daily`（しまってから2年の案を消す・時刻は既存の掃除と重ならない所）
  - 🚨 名前入りの中身（紙の案1）は migration に入れない。一覧（列・種類・役割・設定）だけ seed
- **勉強会の行**：`study_sessions` の校・階から列を決めて、表示の時に重ねる（保存しない）。🚨 勉強会の階は `floor_四条本校`（3F/4F/5F/6F）で、こどもシフト表の列に 4F は無い → 4F の勉強会は本校の見出しの横に出す（要確認）
- **画面**（シフト管理に ［こどもシフト表］・`components/admin/KidsShiftPanel.tsx`）
  - 上：表示する日／［決定済みの表］［案1］［案2］［＋ 新しい案］／しまった案 ▼／追加必要 ◯件（押すとその行へ）／⚠️ ◯件（案の中では初期は隠す・切り替え）
  - 表：曜日の段（月・火／水・木／金・土・日）×中身のある列。見出しの役割・社員休み・曜日の書き添え。マスを押すと表の中で開いて行を足す・直す（種類ごとに欄が変わる）。赤字＋「前：」
  - 比べる：案を2つ選ぶと違うマスに印
  - 保存の確認（変えたマス・先の版を残すマス・新しく ⚠️ になる行・追加必要が増える行）／stale のとき読み込み直す
  - 設定：列／種類／役割／表全体の書き添え／レッスンできる人（人ごと）／確かめの切り替えと最低人数の表
  - PDF（`lib/kidsShiftPrint.ts`）・Excel（`lib/kidsShiftExcel.ts`・シート「表」「一覧」「人ごと」）
  - 勤務表の保存の確認に「⚠️ こどもシフト表◯件が時間外になります」
- **計算（`lib/kidsShift.ts`・supabase を読まない）**：案を決定済みの表に重ねる／追加必要（人数・レッスンできる人）／⚠️（`lib/shiftRoster.ts` の `shiftTimeIssue`。園指導と見出しは「曜日の休み」だけ）／社員休み（メインの部門がこども・正社員・勤務予定なし）／紙の書き方の文（PDF・Excel 共通）
- **出す順（段階1をさらに2回・2026-09-16 ユーザー確定「おすすめでいい」）**
  - **1回目**：DB は全部（表・関数・cron。🚨 確認したの表を後から足すと保存と決定の関数を作り直すことになるため）／
    画面は 一覧の設定・表の編集（赤字・保存の確認）・案（作る・保存・決定・しまう）・追加必要と重なり・社員休み・全校の PDF・①の不具合の修正。
    そのあと紙から案1 を入れて、画面と PDF で紙と見比べてもらい決定
  - **2回目**：⚠️ と［確認した］（＋②の不具合の修正）・案を2つ比べる・Excel 3シート・校ごとの PDF・勤務表の保存の確認
  - 理由：年度替わりは勤務表より先にこどもシフト表を作るので ⚠️ はまだ意味を持たない。人数の漏れ（追加必要・重なり）は勤務表が無くても効く

#### 5-9-2. ⑤ のレビュー（2026-09-16・UI/UX とシニアエンジニアの2体・重要な主張は自分で裏取り）
| # | 重さ | 指摘 | 裏取り → 設計への反映 |
|---|---|---|---|
| U1 | 致命的 | シフト管理のタブを切り替えると未保存の入力が消える（1マス10行なので痛い） | ✅ `ShiftManagementTab.tsx:496` はパネルごと外し、下書きはパネルの中（`CleaningRosterPanel.tsx:50`）。🚨 **今の掃除担当表にも同じ穴** → 未保存があるときはタブ・案・日付の切り替えの前に［保存］［捨てる］［やめる］ |
| U2 | 致命的 | 決定済みの表と案を切り替えると下書きが別の表に乗る | ✅ 掃除担当表は日付を変えても下書きを持ち越す（`CleaningRosterPanel.tsx:76`）→ 下書きを表ごと（決定済み／案ID）に分ける |
| K1 | 致命的 | 案の「写した元の版 ID」だけでは、同じ適用開始日のその場の直し（ID が変わらない）を検出できない | ✅ `cleaning_roster.sql:417-421`（同じ日付は update）→ 案のマスに元の版 ID＋**中身の署名**（SQL 関数1つ・保存の「同じなら何もしない」と共用）。決定のときは適用開始日に効いている版の署名と比べる |
| K2 | 致命的 | 置き場所の鍵を nullable にすると EXCLUDE が効かない（null どうしは一致しない） | PostgreSQL の決まり ✅ → **置き場所の表 `kids_shift_places`（種類＝列／校の見出し／曜日の書き添え）**を作り、版は place_id NOT NULL で EXCLUDE |
| K3 | 重要 | 中身と人を版用・案用の2組にすると確かめと書き込みが食い違う | → 中身の表・人の表は1つずつ（cell_id か plan_cell_id のどちらか1つ）。確かめは内部関数1つ |
| K4 | 重要 | 案で「触っていない」と「空に戻した」を区別する印 | → 案のマスの行がある＝上書き（中身0件なら空）／無い＝決定済みのまま |
| K5 | 重要 | 決定の最中の案の保存・2人同時の決定・決定済みの直接保存の重なり | ✅ 掃除は関数ごとのロック（`cleaning_roster.sql:390`）→ 3関数で同じロック・案に revision・決定は2回（1回目は書かずに「直されたマス・先の版・今日より前」を返す） |
| K6 | 重要 | 「確認した」が消える：掃除担当表は1マスの1行を直すとそのマスの確認が全部消える／🚨 **③ 勉強会は確認の鍵に基準日が入り、日が変わると確認が外れる（既存の不具合）** | ✅ `cleaning_roster.sql:420`／`studySessions.ts:107,113`（`since`＝max(版の開始, 基準日)・基準日の初期値は今日 `StudySessionsPanel.tsx:56`）。勉強会は本番0件なので実害はまだ無い → こどもの鍵は日付を入れず行の中身で作る。③ を直す時期はユーザーに確認 |
| K7 | 重要 | 1,000行で黙って欠ける（人の表だけで1枚約1,500件・推測） | → マス→中身→人を埋め込みで読み、版は valid_to で絞る。しまった案は開いたときに1案ずつ |
| K8 | 重要 | 退職者が表に残る／人の削除で中身と署名が変わる／saved_by に外部キーを付けると delete-user が止まる | 退職者4人 ✅ → 退職者は ⚠️「退職しています」・必要な人数に数えない。saved_by・created_by は外部キーなし |
| K9 | 重要 | 作業中の案は2年の掃除に入らず無制限に貯まる | CLAUDE.md ✅ → 作業中の案の数に上限（例10・要確認）。cron は既存の掃除（UTC 18:00〜18:55）と重ならない時刻 |
| U3 | 重要 | 同じ人が同じ時間に2か所に入っても分からず、追加必要が少なく出る。年度替わりは勤務表より先に作るので ⚠️ に頼れない | 月 5F 17:40 の鈴木さん ✅ → 週のシフトを使わない「重なり」を足すか**ユーザーに確認** |
| U4 | 重要 | 3F と 5F にまたがる担当を表せない | ✅ 月曜 本校 16:40：3F「1班」と 5F「3班」の真ん中に「太田・小出・鈴木・谷北・渡辺」が1行。土日の幼／小も同じ → **ユーザーに確認** |
| U5 | 重要 | 「表示する日」と「適用開始日」の2つで迷う・赤字の比べ先が表で変わる | → 案に「予定の適用開始日」を持たせ表示・⚠️・検出をそろえる（エンジニアも同じ提案）→ **ユーザーに確認** |
| U6 | 重要 | いま何を直しているか分かりにくい | → 表の真上に「案2を直しています（保存しても決定済みの表は変わりません）・最後の保存 林 9/15 18:20」。ボタンは「案2を保存」「決定済みの表に保存（◯/◯から）」。しまった案は見るだけの帯 |
| U7 | 重要 | 案で決定済みと同じ内容に戻したマスが「案で変えたマス」のまま残る／決定の確認が全体で1つだと決定済み側を残せない | → 同じ内容なら案から外す案と、決定の確認をマスごとに［案の値］［決定済みの値］→ **ユーザーに確認** |
| U8 | 重要 | 案を2人が同時に直す判定が無い | → K5 の revision で「読み込み直す」 |
| U9 | 重要 | 画面で「月・火」を横に並べると16列 | 掃除は7列で最小幅 900px（`CleaningRosterPanel.tsx:564`）→ 画面は1段に1曜日＋［月］〜［日］の飛び先・列の見出し固定。3曜日並びは PDF だけ |
| U10 | 重要 | 入力の手数 | → 行は開始時刻で自動の並び／上の行と同じ人／マスを別の曜日へ写す／人は打って絞る（こども・その校・レッスンできる人を先頭）／Enter・Esc／［＋（　）］で必要な人数+1 |
| U11 | 重要 | 紙の「（　）」の数が班の数と合わない | ✅ 木 西陣：15:40～2班「栗木・阿部・（　）」＝3人分、16:40～3班・17:40～2班は空き込み4人分 → 初期値＝班の数のままでよいか**ユーザーに確認** |
| U12 | 改善 | 1マスに赤字・（　）・⚠️・勉強会が重なる | → 赤＝変わった所だけ／追加必要＝既存の黄色の地／⚠️ はマスの先頭に1つ／曜日の見出しに件数 |
| U13 | 改善 | マス単位の比べ方では粗い | → 行に印・違いの一覧・決定済みとも比べられる・案ごとの追加必要の比較表 |
| U14 | 重要 | 紙と同じ密度の PDF にならない（固定の文字の大きさでは入らない） | → 列の幅を中身で決める・同じ日の列は時刻の帯で横線をそろえる・はみ出しは描いてから測る |
| U15 | 改善 | Excel のセル内の改行が1行につながって見える恐れ | `package.json:19` は SheetJS の無料版（書式を書けない）✅ → 実測してだめなら時刻の帯ごとに行を分ける |
| K10〜12 | 改善 | 勉強会と列の対応（(校・階)が一致すれば列・4F と階なしは見出しの横）／勉強会を種類の一覧に入れない・中身がある種類の欄は guard／人の一意は（行・人・役割） | → 反映 |
| — | 出す分け方 | 2体とも「1回目＝表の編集・案・決定・追加必要（DB は全部入れる：あとから確認の表を足すと保存と決定の関数を作り直すため）／2回目＝⚠️ と確認した・比べる・Excel・勤務表の保存の確認（画面だけ）」。UI/UX は1回目に全校 PDF（紙と見比べる用） | → ユーザーに相談 |

#### 5-9-3. ⑤ の実装（1回目・DB は 2026-09-16 に本番適用済み）
- **DB（`supabase/migrations/20260916092222_kids_shift.sql`）**：表12・RLS 12・決まり26・関数11・cron 1本
  - `kids_shift_places`（列／校の見出し／曜日の書き添えを1つの表・place_id は not null＝レビュー K2）／`kids_shift_row_kinds`／`kids_shift_role_kinds`／
    `kids_shift_cells`（EXCLUDE で置き場所×曜日の期間が重ならない）／`kids_shift_plans`（案・revision）／`kids_shift_plan_cells`（base_cell_id＋base_sig）／
    `kids_shift_items`・`kids_shift_item_people`（版と案で同じ表＝レビュー K3・人は profiles に CASCADE・役割 lead/onduty/support）／
    `kids_shift_acks`／`kids_shift_notes`／`kids_shift_settings`／`kids_shift_staff_flags`
  - 関数：`kids_shift_cell_sig`／`kids_shift_payload_sig`（🚨 同じ形で作る2つ・片方を直したら両方）／`kids_shift_token`／`kids_shift_check_items`／
    `kids_shift_put_items`／`kids_shift_open_version`／`kids_shift_save`／`kids_shift_plan_save`（create/update/cells/archive・写して作る）／
    `kids_shift_plan_decide`（1回目は書かずに一覧・マスごとの選択）／見張り2つ。3つの書き込みは同じロック（K5）
  - cron `purge-kids-shift-plans-daily`（`5 19 * * *`＝日本時間 朝4時5分・しまってから2年の案を消す）
- **取り消し版の検算 49項目すべて一致**（取り消し後に表0・関数0・cron 元の22本を実測）
  - 🚨 レビュー K1 の検算：決定済みの表を**同じ日付でその場で**直した（版の ID は変わらない）あと、決定の1回目が
    そのマスを「ぶつかり」として見つけられることを確かめた（中身の署名で見つけている）
  - ほか：おかしな時刻・古い目印・今日より前／同じ内容は何もしない・同じ日付はその場・先の日付で版が分かれる・先の版は残る／
    案（作る・入れる・古い revision は断る・同じ内容に戻すと外れる・写す・しまう）／決定（選んだマスは決定済みのまま・案はしまわれる・しまった案は決定できない）
- **適用後の実測（2026-09-16）**：表12・RLS 12・決まり26・関数11・anon はすべて false・ログイン済みは true・
  置き場所14／種類6／役割3／設定（確かめる=true・上限10）・中身0／案0／行0・cron 22→23・DB 44MB
- **画面（1回目・2026-09-16）**
  - `components/admin/KidsShiftPanel.tsx`（新規）：［決定済みの表］［案…］［＋ 新しい案］／しまった案 ▼（写して新しい案にする）／
    案の帯（「◯◯を直しています（保存しても決定済みの表は変わりません）」・予定の適用開始日・最後の保存・しまう）／
    曜日は1段に1曜日（月〜日のボタン・レビュー U9）／校の見出し（役割・サポート）／列は中身がある所だけ＋［＋ 列を足す］／
    マスを押すと下で入力（種類ごとに欄が変わる・人の役割は 担当／勤務中（担当しない）／サポート・人ごとの時間・上の行と同じ人・
    ＋（　）で必要な人数を1つ増やす・決定済みの表に戻す）／赤字と未保存の枠／曜日の書き添え／社員休み／
    追加必要と重なりの一覧（押すとそのマスへ）／保存の確認（変えたマス・空に戻すマス・先の変更を残すマス・今日より前）／
    stale のとき「読み込み直す」／決定（1回目の一覧 → ぶつかったマスごとに［案の値］［決定済みを残す］）／
    列・行の種類・表全体の書き添え・レッスンできる人・設定（確かめる/しない・班の数ごとの人数・案の上限）／全校の PDF
  - `lib/kidsShift.ts`（計算・supabase を読まない）／`lib/kidsShiftApi.ts`（読み書き）／`lib/kidsShiftPrint.ts`（PDF）
  - `ShiftManagementTab.tsx`：［こどもシフト表］を追加。🚨 **①の不具合を直した**＝いちど開いた画面は外さずに隠すだけにしたので、
    タブを切り替えても未保存の入力が消えない（開いていない画面は読み込みもしない）
- **画面の検算（`tsx` で 17項目すべて一致・実行後に削除）**：署名の形／追加必要（3班で担当1人→あと2人・正社員2＋パート1でそろう・
  パートだけ→レッスンできる人2人不足・行で必要な人数を直せる・確かめない設定）／重なり（見つける・続けて入るのは重なりでない・人ごとの時間）／
  紙の書き方（（ ）の勤務中・19:00まで・追加必要の「（　）」・見出しのサポート・フロント：なし）／社員休み（パート・大人の部門は出さない）
  - 🚨 **画面の署名と DB の署名が同じことを実測**：同じ中身で `md5(画面の文字)` ＝ `kids_shift_payload_sig` ＝ `d751c234…`
- tsc 0／vite build 成功／rules-of-hooks 0／役職名ルール 0／新しい4ファイルの ESLint 0件／ShiftManagementTab 0件
- 🚧 **実機未確認**（ログインが要る）。🚧 2回目：⚠️ と［確認した］・②の不具合・案を比べる・Excel・校ごとの PDF・勤務表の保存の確認

### 5-3. ②〜⑤ のレビュー（2026-09-15・UI/UX とシニアエンジニアの2体・重要な主張は自分で裏取り）
| # | 重さ | 指摘 | 裏取り |
|---|---|---|---|
| R1 | 致命的 | 週のシフトを読む3か所が件数・終わりの日で絞っていない。全員に版を作ると数回で1,000行を超え、黙って欠ける | `OvertimeAdminTab.tsx:656`／`OvertimeShiftImport.tsx:251`／`ShiftAdjustTab.tsx:490`（`lte valid_from` だけ）✅。本番308行 ✅ → **②の最初に「期間に効く行だけ返す読み取りRPC」へ置き換える** |
| R2 | 致命的 | 適用開始日以降に始まる版を削除する作りのため、先の版（パート9/16版126行）が黙って消える。赤字の比べ先（前日）にも出ない | `OvertimeAdminTab.tsx:730`／`OvertimeShiftImport.tsx:294` ✅・本番 126行 ✅ |
| R3 | 重要 | 古い画面のまま保存すると、別の人の変更を44人分上書き | 設計に決めごと無し ✅ → RPC にロック＋「開いた時点の目印」照合 |
| R4 | 重要 | 休憩・労働時間の計算が画面（`lib/breakCalc.ts`）だけ。一括保存で DB 側と2か所になる | ✅ → SQL の計算関数を作り、本番308行の保存値と一致を検算 |
| R5 | 重要 | 1日の途中の校・部門の移り（「本校大10～14:30→上桂」「(こども15:30～17:30)」「テレワーク6:30～7:15」）が文字でしか持てない。④⑤の印が不正確になる | 紙の勤務表 ✅・`location` 1列 ✅ |
| R6 | 重要 | 「対象」の切り替えが保存の範囲まで変える／変わっていない人にも版を作ると履歴が全員分並び、行の無い正社員2人が「未登録」→「全曜日休み」に変わる | 5-1 ✅・正社員28人中26人登録（(47)）✅ |
| R7 | 重要 | `patterns_admin_all` を広げると API からチェック無しで削除できる → 書き込みは RPC だけ。読みは `shift_pattern_directory` 頼みなので読み取り RPC にもタブ判定 | 本番 RLS ✅ |
| R8 | 重要 | 呼び名は手入力＋在籍者で重複禁止＋別名（渡辺→渡邊・尾上→森本 千佳子） | 紙 ✅・`overtime_name_aliases` に尾上のみ ✅・椿原さんは退職扱い ✅ |
| R9 | 重要 | 所属チーム（受理の宛先）と業務部門（2部門）の食い違い。業務部門は別に持ち、所属は触らない | 正社員26人とも所属1つ ✅・ユーザー「大人とこども、管理部とこども」 |
| R10 | 改善 | 取り込みと一括編集で差分の見せ方・保存の決まりが違う → 取り込みは「一括編集の表に値を入れるだけ」に | `OvertimeShiftImport.tsx:172` ✅ |
| — | 誤り | 「①は本番に入っていない」→ 入っている（設計書の見出しが古かった・直した）／「社員休みの小川・阿部・川井・東・尾上は大人所属」→ 5人とも所属はこども ✅ |

段取り（エンジニア案）：0 読み取りRPC（R1）→ 1 計算関数＋保存RPC（R2〜R4）→ 2 取り込み・1人ずつ編集を RPC へ → 3 一括編集の表と PDF → 4 マネージャーに開く → 5 ③④⑤。
**段取り1の前に決める**：保存の中身の形（時間帯ごとの校・部門）／呼び名／業務部門。

## 6. レビュー（CLAUDE.md の決まり：大きな設計は2体でレビュー・指摘は自分で裏取り）

2026-09-14 に「DB・権限」「画面・使い勝手」の2体でレビューし、**重要な主張は本番の定義とコードを読んで裏取りした**。

### 6-1. 裏取りして本当だった指摘（設計に反映する）
| # | 重さ | 指摘 | 裏取り |
|---|---|---|---|
| A | 致命的 | `announcement-notify` に送信済みの確認が無い。id を渡すたびに全員分のベル（notifications insert）とメールを作る | index.ts の取得→insert→send-email に既送の判定なし（✅） |
| B | 致命的 | `announcement-remind` の二重送信防止は `announcements.remind_last_sent_on` だけ。編集できる人が増えるとこれを null に戻して毎日送り直せる | index.ts:71-72, :125（✅） |
| C | 致命的 | お知らせの画面は保存・切り替え・削除の失敗を黙って捨て、通知の関数の結果も見ない（失敗しても「✓ 出しました」） | AnnouncementsTab.tsx:120-131, 161-176（✅） |
| D | 重要 | 所属（`profiles.group_names`）は受理依頼の宛先に効く。`resolve_role_recipients` が「同じ所属チームの上長」に絞っている | 本番の実定義 :39-51 `p.group_names && v_teams`（✅） |
| E | 重要 | リマインドの「対応状況」`scheduled_reminder_status` は本番でも管理者だけ → マネージャーは全行で失敗 | 本番の実定義 `role <> 'admin' → raise`（✅） |
| F | 重要 | リマインドの送り先チャンネル一覧は `board_channels` の RLS で「メンバー／作成者／管理者」だけ | 本番 RLS（✅） |
| G | 重要 | 安否の締め・取消は `is_manager_plus()` だけ → **タブをオフにしても DB では止まらない**（今までと同じ） | 本番 `cancel_safety_check`/`close_safety_check`（✅） |
| H | 重要 | 安否の削除ボタンは条件なしで出ている／発信者名は管理画面の `users` から引く（読み込みを止めると「—」） | SafetyChecksTab.tsx:592-595, :70, :290（✅） |
| I | 重要 | グループの名前変更・追加・削除のボタンがマネージャーにも出る／所属は `profiles` を直接更新 | GroupsTab.tsx:68, :116, :212, :43/:146/:168/:192（✅） |
| J | 重要 | 管理画面の枠が誰にでもストレージ・DB使用量の RPC、`useAdminSetupAlerts(true)`、修正依頼の件数を読む | AdminPanel.tsx の冒頭（✅） |
| K | 重要 | `?tab=` の初期値は `ADMIN_TABS` に含まれるかだけ・既定は `'approvals'` | AdminPanelContext.tsx:253（✅・4-2 で対策済みの方針） |
| L | 重要 | 設定のキーとタブの名前が合っていない（通常シフトは `overtime_admin` の一部、お知らせと安否は別タブ） | AdminPanelContext.tsx:16（✅） |
| M | 前例 | 設定を読む関数は `board_can_manage_groups()` と同じ形（`jsonb_typeof = 'array'` を確かめて読む） | 本番の実定義（✅） |

### 6-2. 設計に足すこと（① で必ずやる）
- **お知らせ**：`announcements.notified_at` を足し、`announcement-notify` の最初で「`notified_at is null` の行だけ更新できたら送る」。
  管理者以外は `remind_last_sent_on`／`notified_at`／`created_by` を変えられない（トリガー）。
  関数の中の権限確認は **利用者の JWT のまま** `rpc('can_manage_admin_tab')` を呼ぶ（service_role で呼ぶと auth.uid() が null で常に false）。
  画面は保存・切り替え・削除・通知の error を必ず出す（C）。**画面より先にこの直しを入れる**
- **グループ**：所属の RPC は1文の `array_append`／`array_remove`、0件は例外、相手が管理者・全社の役職・自分より上なら断る。
  所属チーム（`shift_report_group`）を触らせるかはユーザー判断（D）
- **リマインド**：`scheduled_reminder_status` の判定に `can_manage_admin_tab('scheduled_reminders')`（E）／
  チャンネルの id と名前だけを返す SECURITY DEFINER の関数（F）／insert の with check に `created_by = auth.uid()`
- **安否**：削除ボタンは管理者だけ（H）。発信者名のために安否タブでも `users` を読む。オフでも DB で止まらないことを明記（G）
- **グループの画面**：名前変更・追加・削除は管理者だけ。メンバーの「削除」は「外す」に（I）
- **管理画面の枠**：ストレージ・DB使用量・設定もれ・修正依頼の件数は `isAdmin` のときだけ（J）
- **判定を1か所に**：`useAdminAccess()` が `{ ready, canOpen, reason, visibleTabs, isTabAllowed }` を返し、
  NavBar・AdminPage・AdminPanelContext が同じものを使う。`TAB_GROUPS` と設定のキー一覧は lib の定数1つ（L）
- **読み込み中**：設定を読み終えるまで入口は「読み込んでいます...」（読み終える前にホームへ飛ばさない）
- **行き止まりにしない**：マネージャー以上がスマホで開いた／タブが0件 → 理由のカードと［← ホームへ］
- **オフにされたとき**：設定は `usePolling` で読み直し、開いているタブが外れたら案内して残りのタブへ
- **FAQ**：`can_edit_faq()` を広げると、タブがオンのマネージャーには下書きの回答と検索語のログも API で見える（画面は今までどおり）
- **設定の枠**：各タブで「できるようになること」「DB の書き込みも許可されるか」を出す。お知らせは「全員（約46名）に通知・メール」と二段階の確認
- **連絡文**：共通ファイルに `NotificationsTab.tsx`・`useAuth.ts` も足す

### 6-2b. レビューを受けてユーザーが決めたこと（2026-09-14）
| 項目 | 決定 | 設計への反映 |
|---|---|---|
| お知らせの編集・削除 | **他の人が作ったものも、マネージャー以上が直せる・消せる** | `announcements` の書き込みは `created_by` で絞らない。ただし `created_by`・`notified_at`・`remind_last_sent_on` は管理者以外は変えられない（トリガー）。二重送信の歯止め（`notified_at`）は必須のまま |
| 所属チーム（こども・大人・管理部） | **マネージャー以上も変えられる**（受理依頼の宛先が変わることは説明済み） | 所属の関数は `shift_report_group` の値も扱う。🚨 相手が管理者・全社の役職・自分より上の役職なら断る。画面に「所属チームを変えると、休暇・残業の受理依頼が届く上長が変わります」と出す |
| リマインド設定の「送るタイミング」 | **両方開く**（定期リマインドと、有給奨励日・連絡板未読の送る時刻） | `reminder_days_settings` の書き込みも `can_manage_admin_tab('scheduled_reminders')` |
| 安否・緊急 | **今までどおり**（タブのオン・オフは管理画面に出すかだけ） | 締め・取消・発信の関数は触らない。設計書 4-1 の「オフで DB も止まる」に安否は例外と明記 |

### 6-3. リハーサルに足す試験
anon は新しい関数を呼べない／リーダーは所属の関数で断られる／マネージャーは管理者・社長の所属を変えられない／
マネージャーは `remind_last_sent_on`・`created_by`・`notified_at` を変えられない／同じお知らせの通知は2回目に送られない／
`scheduled_reminder_status` がタブ ON のマネージャーで通り OFF で断られる／FAQ 編集専用アカウントが今までどおり使える／
設定が「行なし・null・文字列」のとき false。Edge Function は deploy 後に「タブ OFF のマネージャーで 403」「通知 OFF のお知らせで push:0,email:0」を実測

## 6-4. ① の実装（2026-09-15・✅ 本番DB適用・announcement-notify v6 deploy・push `b72b02b` まで完了。最初は全部オフ）

### DB（`supabase/migrations/20260915001444_manager_admin_tabs.sql`）
- `app_settings` 'manager_admin_tabs' ＝ `[]`（全部オフ）
- `can_manage_admin_tab(text)`：`is_admin()`、または `is_manager_plus()` かつ設定の配列にタブがある。配列でない値（null・文字列）・行なしは false
- お知らせ：`notified_at` を追加し、**いまある行は `created_at` で埋めた**（古いお知らせに作成時の通知を飛ばせないように）。
  トリガー `trg_announcements_guard_system_columns`：画面からの操作（`current_user = authenticated`）で管理者でなければ、
  作成者は自分・送信済みの印（`notified_at`／`remind_last_sent_on`）は変えられない。Edge Function（service_role）はそのまま通る。
  書き込みの決まり `announcements_admin_write` を `can_manage_admin_tab('announcements')` に（名前はそのまま）
- グループ：`set_profile_group(p_user_id, p_group, p_member)`（設計 4-3 の add/remove を1本にした）。
  `master_options` の group／shift_report_group にある値だけ。1文の `array_append`／`array_remove`。二度押しは今の値を返す。
  管理者以外は、相手が管理者（app_metadata）・全社の役職・自分より上（roles.sort_order）なら断る
- リマインド：`reminders_admin_write`／`管理者のみ編集可` を `can_manage_admin_tab('scheduled_reminders')` に（名前はそのまま）。
  作るときの作成者は自分（`reminders_insert_own_creator`・restrictive）。
  `scheduled_reminder_status` は本番の実定義から起こし、判定の1行だけ差し替え（機械で突合：違いは判定の行と見出しの大文字小文字だけ）。
  `reminder_channel_options()`（id と名前だけ）
- FAQ：`can_edit_faq()` に `or public.can_manage_admin_tab('faq')`（本番の実定義から起こした・欠けた行 0）
- 新しい関数3つとも anon の実行権限を外した

### 取り消し版の検算（65項目すべて一致）
anon は3つとも呼べない／設定が空・文字列・null・行なしで false／ON でマネージャー・社長は通りリーダー・パートは通らない／
Q&A管理専用は今までどおり／お知らせ：他の人の分を直せる・送信済みの印と作成者は変えられない・他の人の名前で作れない・
service_role の「送る印」は1回目1件・2回目0件・OFF で直せない作れない／リマインド：作れる・他の人の名前で作れない・
直せる・送るタイミングも直せる・対応状況とチャンネル26件が読める・リーダーと OFF は断る／
グループ：同じ役職の人に足す・二度押しで1つ・外す・他の所属と役職はそのまま・社長と管理者は断る・無いグループは断る・
リーダーと OFF は断る・管理者は社長も変えられる。取り消し後、本番に設定・関数・列が残っていないことを実測

### Edge Function `announcement-notify`
- 判定を `rpc('can_manage_admin_tab', { p_tab: 'announcements' })`（利用者の JWT のまま）に
- 送る前に `notified_at is null` の行だけ印を付け、付けられなければ 409「このお知らせの通知はすでに送っています」
- 送る相手を読めなかったときだけ印を戻す。push の失敗・メールの失敗数を返す（`push_failed`／`email_failed`）
- `deno check` は HEAD と同じ 0件

### 画面
- `lib/adminTabs.ts`（タブの鍵・説明・判定 `decideAdminAccess`）／`hooks/useAdminAccess.ts`（設定の共有と読み直し）
- `App.tsx`：`AdminPage` は設定を読み終えるまで「読み込んでいます...」、開けない理由はカード（`AdminAccessNotice`）。
  「⚙️ 管理」は `adminAccess.canOpen`
- `AdminPanel.tsx`／`AdminPanelContext.tsx`：開いているタブだけ・URL 直打ちも不可・閉じられたら残りのタブへ移して黄色で知らせる。
  使用量・設定もれ・修正依頼・承認待ちの読み込みは管理者だけ。安否タブでは発信者名のためにスタッフを読む
- グループ：追加・名前変更・削除は管理者だけ。所属は `set_profile_group`（管理者も同じ道）。「削除」→「外す」。所属チームには注意書き
- お知らせ：保存・停止・削除・通知の失敗を出す（`describeUpdate`／`describePartial`）。履歴を読めないときは「まだありません」と言わない
- リマインド：チャンネルは `reminder_channel_options`。読み込みの失敗を出す
- 安否：削除ボタンは管理者だけ／FAQ：`canManageEditors` は管理者だけ
- 権限管理の末尾に「マネージャー以上に開く管理画面のタブ」（`ManagerAdminTabsSection`）。お知らせを開くときだけ二段階
- tsc 0／vite build 成功／rules-of-hooks 0／判定の検算 16項目一致

### 出す順（許可をもらってから）
DB 適用 → 実測（関数・anon・列・決まり・設定 `[]`）→ `git pull` → `announcement-notify` deploy（🚨 新しい判定は DB の関数を使うので必ず DB の後）→ push。
全部オフで出るので、出した瞬間に誰の画面も変わらない。管理者が確かめてからタブを1つずつオンにする。

## 7. 場所予約の担当への連絡文（案）

> 管理画面をマネージャー以上（PC のみ）に開く作業を始めます。2人共通ファイルのうち
> `App.tsx`（/admin の入口と「⚙️ 管理」ボタンの判定）、`useAuth.ts`（判定の読み出し）、
> `AdminPanel.tsx`／`AdminPanelContext.tsx`（タブの出し分け）、`NotificationsTab.tsx`（リマインド設定の画面）、
> `FeaturePermissionsTab.tsx`（設定枠を1行足す）の動きが変わります。
> 最初は全部オフで、管理者がタブを選ぶまで誰の画面も変わりません。場所予約（/rooms）には触りません。
> DB は `app_settings` に設定を1つ、判定の関数、所属・リマインドのチャンネル一覧の関数、
> FAQ・リマインド・お知らせの書き込みの決まりを広げます。Edge Function は `announcement-notify` を変えます
> （2人共通の9本には入っていませんが、deploy 前に pull します）。
> 重なる作業があれば教えてください。
