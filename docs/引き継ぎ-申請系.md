# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-08-31・備品購入申請／バックアップ）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】本番反映済み。DB列2つ追加（適用・実測確認済み）
・本番DB：purchase_request_comments に file_path / file_label を追加
  （migration 20260831700000・適用済み。既存データ0件のため影響なし）
・Edge Function の変更なし
・未追跡は AGENTS.md のみ（触らない）
・🆕 場所予約が master にマージされ本番公開された（別担当者）。
  依頼していた「マネージャー以上に限定」も対応済み（a581f5c）
・⚠️ 今日の実装は**実機未確認**。備品購入申請はログインが要るため
  Claude 側では画面を開けない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日やったこと
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① NASバックアップが12:00に失敗していた（一過性）
   「ネットワーク パスが見つかりません」で5件とも失敗し、ログがローカルに逃げていた。
   NAS本体は正常（同じ12:30のDBバックアップは成功）＝その瞬間だけ繋がらなかった。
   手動で再実行して復旧済み（13:16・5件ともバイト数一致を実測）。
   🚨 確認は日付でなく **backup_log.txt の末尾（バイト数）** か Get-FileHash で見る

② 失敗ログを .gitignore に追加（scripts/backup_error_log.txt）

③ 🆕 承認後に確定見積書などを共有できるようにした（備品購入申請）
   承認確定後は相見積もりを書き換えられない（RLSが status で拒否＝承認の根拠を
   守る正しい作り）ため、あとから届く確定見積書・納品書を共有する手段が無かった。
   → 承認内容に影響しない**質問・回答（追記型）にファイルを持たせた**
   ・種類の名札（確定見積書／納品書／請求書／カタログ・仕様書／その他）既定＝確定見積書
   ・共有ファイルだけ先頭にまとめて表示。閉じていても「📎 共有ファイル N件」と出す
   ・本文なしでファイルだけの共有も可
   ・通知は**既存の comment_added に乗るだけ**＝申請者と承認者全員に届く（新規実装ゼロ）
   ・添付できる人は制限しない（経理が請求書を貼る等を塞がないため・ユーザー決定）

④ 履歴の「今月」に今月出した申請が出ない問題を直した
   絞り込みが購入日基準で、8月申請でも購入予定日が9月1日なら9月扱いになり
   「すべて」でしか見えなかった。**申請日／購入日を選べるようにし、既定は申請日**
   （休暇申請CSVで同じ問題を選択式で解決した前例に揃えた）

⑤ 商品ページのURLを貼る場所を案内した
   内訳・条件欄のURLは**既に自動でリンクになる**（2026-07-14実装）のに案内が無く
   貼る場所に迷っていた。説明文と記入例に明記

⑥ 見積書・レシートの添付画面の配色を灰色系に統一（紫系が取り残されていた）

⑦ 実機フィードバックで2件改善（同日）
   ・「💬 質問する」だけではファイル共有の入口と分からない（実際に迷われた）
     → ボタン・見出しを「💬 質問・ファイル共有」に変更
   ・ファイルを2件以上共有できるようにした（1回の送信で複数添付）。
     アップロードが終わるたび「送信待ち」一覧に積まれ、続けて次を添付できる。
     DBは1行=1ファイルのまま、行を分けて保存（本文は先頭の行だけ）。
     通知の2行目は「確定見積書 他1件が共有されました」の形

⑧ 引き継ぎの置き場所を再編（2人開発の対策・詳細は CLAUDE.md）
   ・🚨 旧 CLAUDE.md は約41万トークンで**コンテキスト上限の2倍**。毎セッション自動で
     読み込まれるため**サブエージェントが起動できない状態だった**（実測で確認）。
     分割で約3千トークンに。整理ではなく必要な修理だった
   ・このファイル＝最新1セッション分だけ。過去は 引き継ぎ-申請系-アーカイブ.md
   ・CLAUDE.md 末尾の @docs/... で最新分は自動読み込みされる
   ・マイグレーション番号は「担当ごとの帯」ではなく **date +%Y%m%d%H%M%S の時刻方式**に
     （相手は1日6本作る実績があり、帯では枠が足りなかった）
   ・docs/開発ルール.md の §5（番号）・§7（引き継ぎの書き方）も新方式に更新済み
   ・AGENTS.md（356KBの古いコピー・第3の情報源）は案内だけのファイルに差し替え（未追跡のまま）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 この周辺を触るときの注意
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **承認確定後は相見積もり（purchase_request_item_quotes）を書き換えられない**。
  これは承認の根拠を守るための正しい作り。あとから足したいものは
  「追記型」の質問・回答に載せる。RLSを緩めて例外を作らないこと
・🚨 **共有ファイルのパスは必ず `_quote.` を含める**。
  receipt-signed-url が「_quote. を含む＝見積書」で閲覧できる役職を切り替えている
  （リーダー以上＋本人）。命名を変えるとリーダーが開けなくなる
・🚨 **通知の1行目（tpl.template）は変えない**。App.tsx の判定が1行目の文言を見ており、
  「お知らせ」「リマインド」等が入ると連絡板の通知と誤判定されて /board に飛ぶ。
  ファイル添付の案内は**2行目（sub_message）**に入れてある
・🚨 **説明文を足すときは金額の出し分けの外に置く**。価格比較の説明は
  1万円未満のときだけ出る作りで、中に入れると1万円以上の申請では表示されない
  （今回まさに踏みかけた）
・質問・回答の部品（PurchaseCommentThread）は**履歴・承認・管理の3画面で共用**。
  1つ直せば3画面に反映される。逆に3画面に配線する形にしない（渡し忘れが起きる）
・ファイルを開く処理は props でなく直接 import している（配線漏れ防止）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 2人開発になって分かったこと
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **マイグレーションの番号が衝突する**。相手も同じ日に 20260831200000 を使い、
  同番号のファイルが2つできた（今回 700000 にリネームして解消）。
  **作る前に `ls supabase/migrations/ | grep ^2026<日付>` で空き番号を確認する**
・push が「behind」で弾かれたら、いきなり pull せず
  `git diff --name-only HEAD...origin/master` で**双方が触ったファイルを先に確認**する。
  今回は0件だったので競合なしでマージできた
・CLAUDE.md は相手が「読み方」の案内を冒頭に作り、引き継ぎを2本立てにしてくれている。
  **この構成を壊さない**（片方を消さない・自分の節だけ更新する）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・今日の分】備品購入申請
  ＜履歴＞期間ボタンの右に「申請日で絞る／購入日で絞る」が出るか。
    **既定の「申請日」＋「今月」で、9月1日購入予定の申請が出るか**（今まで出なかった）
  ＜共有ファイル＞ボタンが「💬 質問・ファイル共有」になっているか→
    種類（確定見積書が既定）を選んで添付→「送信待ち（1件目）」に積まれるか→
    **続けて2件目を添付できるか**→「ファイル2件を共有する」で送信→
    上部に「📎 共有ファイル（2件）」→閉じてもボタンに「📎 2件」が出るか→
    **承認した5名に通知が届くか**（2行目「確定見積書 他1件が共有されました」）→
    それぞれのファイルを開けるか→「✕ 外す」で送信前に取り消せるか
  ＜申請フォーム＞「商品ページのURLがあれば〜」が**1万円以上でも出るか**
  ＜ダークモード＞添付欄の色が他の画面と揃っているか
2.【実機確認・繰越】残業の事後報告の時刻制限（8810756・まだ誰も動かしていない）
3.【要送信】幹部への連絡（LINE・B案）は文面のみ作成済み・未送信。2を確認してから
4.【実機確認】お客様向けFAQ（上桂校・四条本校・スマホ）
5.【実機確認・依頼済み】残業の「実績を報告する」（3ead881）
6.【要送信】残業の実績報告についての連絡文（B案・C案）＝未送信
7.【繰越】テンキー第2段の見た目／Excel出力の差分／プッシュ受信時間帯ほか
8.【要確認・税理士】領収書の保存年数と紙廃棄の可否
9.【ユーザー操作】WordPressにFAQ設置（🚨 設置後にスマホの戻るボタンを確認）
10.【10月以降】山科教室：FAQ_SCHOOL_OPTIONS と FAQ_COURSE_SCHOOLS の両方に追加
11.【運用・年1回】バックアップの復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 保留（必要になったら）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・共有ファイルの**元のファイル名を保存していない**（表示は種類の名札＋投稿者＋日時）。
  アップロード部品（QuoteFileUploader）が元の名前を返さないため。
  申請フォームでも使う共通部品なので今回は触らなかった。
  必要なら列を1つ足し、onChange に第2引数（optional）を追加すれば対応できる
```

---

