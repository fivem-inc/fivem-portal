# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-09 続き・プッシュ文面を全面的に作り直した）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ すべて本番反映まで完了（master 93f5ef0／リモート一致／本番JSの中身で実測確認済み）
・本番DBの変更 **2本**（取り消しテスト → 適用 → 実測 まで完了）
    20260909172723 announcements.push_show_title（お知らせの件名を出すか・既定ON）
    20260909173741 修正依頼／取消依頼で event_key を分ける＋🚨 anon の実行権限を塞いだ
・Edge Function **9本** deploy：push-dispatch ／ attendance-notify ／ leave-approved-notify ／
  overtime-grant-request-notify ／ purchase-reimbursement-notify ／ safety-check-send ／
  safety-check-remind ／ shift-report-confirmed-notify ／ time-adjustment-notify
・tsc 通過／vite build 成功／rules-of-hooks 0件
・✅ **通知の仕組みを端から端まで通して確認した**（ベル→push_queue→cron→端末。sent を実測し、
  社長端末に届くところまで見た）。確認に使った行は削除済み（残0件を実測）
・🚨 **2人共通のファイルを触った**：App.tsx ／ push-dispatch ／ AnnouncementsTab ／
  Edge Function 8本。**相手（場所予約）へ連絡すること**（本日相手の push なし・重なり0件）
・未追跡は AGENTS.md のみ（触らない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨🚨 いちばん大事なこと：旧「NG語リスト」は無効だった
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
宿題だった「プッシュ文言の見直し」で全文言を洗い出し、社長端末（Android Chrome）へ
**20通を1通ずつ**送って実測した。結果、**1通も化けなかった**。
　・NG確定とされていた「確認」「依頼」は**単独でも**表示された
　・**文章形も表示された**（「未報告の残業が1件あります」）
　・実際のお知らせの件名 **35文字**も切れずに読めた
→ 2026-07-11 の判定は、いまの Chrome では成り立っていない。
🚨 当時NGに見えた原因は語ではない。同じ「依頼」が、今朝はNG・夕方はOKだった。
　 語では説明が付かないので、端末・Chrome 側の事情（更新など）と考えるのが自然。

🚨 **「送ったのに来ない」は、絞られたのではなく遅れて届いた**（この日1回起きて、
　 あとから届いたことを実機で確認した）。**プッシュは数分遅れることがある**。
　 来ないと思っても、すぐに送り直さず少し待つこと。
　 （送り直すと同じ tag で上書きされ、かえって切り分けが難しくなる）

🚨 **だから全面移行と同時に、戻す手段を必ず用意した**。
　 短い状態語（word）を EVENT_MAP から消していない。**PUSH_SENTENCE=0 を
　 Function の環境変数に入れるだけ**で旧方式（状態語＋件数）に戻る。
　 挙動が変わったということは、また戻る可能性があるということ。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 作ったもの
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① プッシュ本文を「状態語＋件数」→ **意味の通る文章**に（55イベント＋直送8本）
   例：「未承認 3件」→「休暇申請が届いています（3件）」
       「新着 1件」→「残業の実績報告がまだです」
   ・1件のときは件数を付けない（「（1件）」は読み手に意味が無い）
   ・🚨 **まとめる鍵を、いま表示に使う文言と揃えた**。word のままだと、同じ「新着」でも
     中身の違う通知（実績未報告／管理者の取消／締め後の許可）が1つに混ざり、
     先に来た1件の文章だけ出て残りが消える
   ・文言はユーザー承認済み（A案＝状態を言い切る・催促しない）。B案（〜してください）は
     **毎日飛ぶリマインドが催促になる**ため見送り

② 社内お知らせだけ**件名そのもの**を出せるようにした
   ・出すのは「①社内お知らせ ②まとめが1件 ③ONのとき」が揃ったときだけ
   ・引けなかったら黙って落とさず、ふつうの文章に戻す
   ・🚨 **連絡板には入れない**（ユーザー確定）。個人あてメッセージの用件がロック画面に出る
   ・お知らせ作成画面のチェックで切り替え（既定ON）。**入力中の件名が見本として出る**
   ・60文字を超える件名は末尾を「…」で切る
   ・✅ 実測：実際の件名「【ファイブMスタッフサイト アップデートのお知らせ】2026年8月6日」
     （**35文字**）が **切れずに最後まで読めた**（2026-09-09・社長端末）。
     🚨 60文字で切る設定そのものは未検証。もっと長い件名を出す日が来たら、
     そのとき実機で切れ方を見ること（この書き出しは毎回同じなので、
     切れると「どのお知らせか分からない」になる）

③ 修正依頼と取消依頼を分けた（event_key を6つに）
   ・correction_requests は2種類を扱うのに event_key が1つで、取消依頼でもプッシュは「修正」
     と出ていた（ベルには「取消依頼が届きました」と出るのに食い違い）
   ・new / resolved / declined × edit / cancel の6つ。アプリ名も「修正依頼」「取消依頼」
   ・🚨 **旧キーは消していない**。既存の通知が持っており、消すとベルの分類が壊れる。
     App.tsx は startsWith('correction:new') で新旧どちらも拾う

④ shift:admin_edited を EVENT_MAP に足した
   ・ベルには出るのに **スマホへ届いていなかった**（実データで4件）。EVENT_MAP に無かったため
   ・管理画面からOFFにできるよう notification_settings にも行を用意した

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 取り消しテストで防いだ事故（実測しなければ本番に出ていた）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨🚨 **submit_correction_request を anon（ログインしていない人）が呼べた**（2026-07-26 から）。
  このリポジトリは Public で anon キーは client/.env.production に入っている＝誰でも入手できる。
  しかも所有者チェック `if v_owner <> auth.uid()` は **auth.uid() が null だと null になり
  成立せず、insert まで進んでいた**。止まっていたのは requester_id の not-null 制約のおかげで、
  チェックが働いた結果ではない。**実際に anon になりすまして再現を確認した**。
  → ① null を先に弾く ② `<>` を `is distinct from` に ③ 実行権限を外す、の3段で塞いだ
・🚨🚨 **これまでの記録が逆だった**。「revoke ... from public では外れないので anon を
  名指しする」と書かれていたが、実測すると **anon だけ名指ししても外れない**
  （anon は PUBLIC の一員なので、PUBLIC に残っていれば呼べる）。
  **public と anon の両方から外し、authenticated へ付け直す**のが正しい。
  適用後の実測：anon=false ／ authenticated=true ／ service_role=true ／
  anon で実際に呼ぶと 42501 で入口から弾かれる
・🚨 is_admin() の判定を **coalesce で包んだ**（null が返ると `not null` = null で素通りする。
  今朝と同じ形の穴。2本とも直した）
・🚨 notification_settings の列名は **recipients ではなく recipient / subject / template**
  だった（1回目の取り消しテストで判明。想像で書くと通らない）
・🚨 EVENT_MAP に足す前に **push_queue の pending を必ず確認する**。
  溜まっていると、足した瞬間に古い分がいきなり飛ぶ（今回は0件だったので安全だった）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 この周辺を触るときの注意（今日ぶん）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **プッシュ文面のルールはアーカイブの「プッシュ文面のルール」を見る**。
  古い「検証済み語」「NG確定語」の一覧は 2026-09-09 に書き直した（無効になった）
・🚨 **文章にできても、個人名・金額・申請の中身は書かない**。ロック画面に出る。
  例外は社内お知らせの件名だけで、書いた人がON/OFFを選ぶ
・🚨 **まとめプッシュは「アプリ名＋本文」でまとめる**。文言を変えるときは、
  まとめ方（gKey）も一緒に見る。片方だけ直すと通知が混ざって消える
・🚨 **send-push の応答 `sent:1` は端末到達の保証ではない**（配信サービスが受け取っただけ）。
  必ず実機の画面を見てもらう。ただし **数分遅れて届くことがある**（今日1回起きて、
  あとから届いた）。来ないと思ってもすぐ送り直さず、少し待つこと
・🚨 プッシュのテストは **間を空けて1通ずつ**（遅延と混ざると切り分けられなくなるため）
・🚨 通知を1件試すなら **notifications に1行入れるのが確実**（トリガーで push_queue に載り、
  cron が1分以内に拾う）。ただし本番に行が残るので、確認後に id 指定で必ず消す
・🚨 `supabase db query --linked --file` は **最後の1文の結果しか返さない**。
  raise notice も見えない。複数の値を見たいときは1つの select にまとめるか、
  一時テーブルに入れて最後に select する
・🚨 RPCを create or replace するときは **本番の pg_get_functiondef から起こす**（今回も実施）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【要連絡】相手（場所予約）へ、共通ファイルを触ったことを伝える。
   本日ぶん：App.tsx ／ push-dispatch ／ AnnouncementsTab ／ Edge Function 8本。
   午前ぶん：useAuth.ts ／ FeaturePermissionsTab.tsx ／ NotificationsTab.tsx も含む
2.【実機確認】お知らせ作成画面の新しいチェック
   ・「件名をスマホの通知に出す」が既定ONで出るか／入力中の件名が見本に出るか／
     外すと説明が「社内お知らせが届いています」に変わるか
   ・実際にお知らせを1件出して、**件名がそのまま通知に出るか**（ここはまだ未確認）
   ・チェックを外して出すと「社内お知らせが届いています」になるか
3.【実機確認】修正依頼／取消依頼を実際に出して、プッシュのアプリ名が分かれるか
4.【実機確認・繰越】午前ぶんの10件（引き継ぎアーカイブの 2026-09-09 の節に一覧）
   ・**明日の朝から毎朝のお知らせが動く**（対象7件・JST 9:10）
5.【要判断・繰越】連絡板グループでのシフト調整（案A提示済み・決めること5つ）
6.【宿題・繰越】**FAQの戻るボタン（スマホ）**。FAQを開いて戻ると、開く前の作業ページまで
   戻ってしまう。「FAQの中で1段戻る」を期待している。着手時に実機で動きを追う
7.【要検討・今日の発見から】**他の関数にも anon の実行権限が残っていないか総点検する**。
   今日見つけた3本と同じ作られ方をしている関数が他にもあるはず。
   確認：select p.proname, has_function_privilege('anon', p.oid, 'execute') from pg_proc p
         join pg_namespace n on n.oid=p.pronamespace where n.nspname='public';
8.【繰越】ベルの closeOnTap とホームのバナー（備品精算）／FAQウィジェットの WordPress 設置／
   Vercel の商用利用の問い合わせ／Notion の貼り直し／幹部への LINE・Slack 2通／
   休暇申請の ✕（9/05・未確認）／テンキー第2段／税理士への領収書の質問／
   山科教室（10月）／復元リハーサル
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
