# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-08・連絡板の集中修正10件）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ すべて本番反映まで完了（master 9c5a4e1／リモート一致／本番JSの中身で実測確認済み）
・本番DBの変更 **2本**（適用・実測確認まで完了）
    20260908170308 board_can_manage_groups()（グループ編集の権限）＋ メンバー削除の RLS ＋ 山本さんをチャンネルへ
    20260908190343 board_messages.recipient_presets / recipient_extra_ids（宛先の選び方）
・tsc 通過／vite build 成功／rules-of-hooks 0件
・🚨 **App.tsx（2人共通）を2か所触った**：useBoardUnread の数え方／ProtectedLayout の state.from。
  相手（場所予約）は本日 push なし・重なり0件。**次に相手が作業するとき一声かける**
・未追跡は AGENTS.md のみ（触らない）
・9/07〜08 の備品精算・FAQ集計の分は **実機確認 OK**（ユーザー報告）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日直したこと（連絡板・10件・すべてユーザー確定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① サイドバーの ✕ を撤去。削除は 管理画面→連絡板 だけ（「メンバー◯人・投稿◯件」の確認つき）
   🚨 管理者以外が押すと RLS で本体だけ弾かれ「自分だけ抜けて投稿だけ消えてグループは残る」半端な状態だった
② メンバー編集を「グループを作れる人」に開放。判定は DB の board_can_manage_groups() 1か所（画面も RLS も）
③ 宛先ボタン「マネージャー・リーダー」を役職判定に（フロア責任者は含めない・ユーザー確定）。
   山本さんは名簿（ユーザーが管理画面で追加）とチャンネル（migration）の両方に入れた
④ 🚨 読了が消える競合：loadAll が confirmations を丸ごと置き換え、後に終わると受信トレイぶんが消えていた
   → 統合に変更。前面に戻ったときの読み直し（visibilitychange/focus/pageshow・5秒で1回・loadAll は silent）
⑤ 本文欄の拡大（PC 取っ手／スマホ 自動）／送信後に受信トレイも読み直す
⑥ 🚨 ベルから該当のお知らせが開かない（2体レビュー→実測→実装）
   原因：react-router 7 の setSearchParams(prev=>…) は **prev がレンダー時点のURL**で、2回続けて呼ぶと
   2回目が1回目を打ち消す。＋ 手元の古い一覧から探していた
   → 対象1件を DB から引いて正体を決め、URL は完全な文字列で書く（一覧に replace → 詳細を push）、
     既読を書いてから読み直す。グループ・DM の通知は channel_id を見てチャンネルへ
⑦ 2行タブ（状態／種類）＋数字バッジ。**「確認」の種類が絞り込みに無かった**（未対応5・内訳4）→ 追加
⑧ 上のバーと受信トレイの数字＝「未読 または 未対応」（重なりは1件・足し算しない）
⑨ ログイン後に元のページへ戻す（state.from）。濱口さんが「リンクから開けない」＝未ログインのブラウザ
   → ログイン後にホームへ固定で飛んでいた。Gmail の設定ではない
⑩ 送信トレイに「コピーして作成」。宛先の選び方（押したボタン＋個別）を保存し、コピー時に
   「いまの該当者」へ当て直す（昇格は入り、降格・退職は外れる）。過去分は当時のIDの在籍者だけ
＋ 本文欄を横いっぱい・送信ボタンを下に（Slack と同じ配置）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 この周辺を触るときの注意（今日ぶん）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **react-router 7 で setSearchParams(prev => …) を同じ処理内で2回呼ばない**。prev は前の結果を受け取らない。
  完全な URLSearchParams を作って渡す（BoardPage の openInboxId の effect が見本）
・🚨 patchBoardParams は常に openInboxId を外す（利用者の操作が勝つ）。新しい UI からは呼ばない
・🚨 confirmations は必ず「統合」（setConfirmations(prev => ({...prev, ...}))）。置き換えると受信トレイの読了が消える
・🚨 loadInbox は通し番号で最新だけ採用。この画面で既読にしたもの（localReadRef）は読み直し結果に必ず足す
・🚨 上のバー（App.tsx useBoardUnread）と受信トレイの行（BoardPage inboxAttention）は同じ数え方。片方だけ変えない
・🚨 宛先の一括ボタンの中身は recipientPresetIds 1か所（作成画面／送信時の記録／コピー時の当て直し）。
  **押したボタンを記録する**方式。宛先の中身から逆算すると、人数の少ないボタンが誤判定される（検算で発覚）
・🚨 上のバーの数字は連絡板を開いている間は出さない（仕様。サイドバーが同じ数字を出す）
・🚨 ベル・プッシュは自分には送らない（元からの作り）。「自分宛てが受信トレイに出ない」は読み直し不足だった
・🚨 グループ・DM の投稿欄（1行型）は触っていない。揃えるなら別件

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先／今日の分】
   ・スマホで読了 → 別画面 → 戻る／プッシュから開く → 「✓ 読了済み」が残る（④）
   ・ベル → 該当のお知らせが開く（連絡板を開いたままでも）。戻るで一覧（⑥）
   ・スマホのタブ2行・アーカイブが右端に見える・「確認」チップ・未対応の数と内訳が合う（⑦）
   ・上のバーの数字＝受信トレイの行の数字。開いただけでは減らず［完了］で減る（⑧）
   ・ログアウト状態でメールのリンク → ログイン → そのお知らせが開く（⑨）
   ・コピーして作成：黄色い案内／過去分は「選び方が残っていません」／今日以降の分は「選び方：…」／
     下書きがあると「置き換えますか」（⑩）
   ・本文欄が横いっぱい・送信が下／グループの「👥 メンバー」を作れる人が編集できる（②）
2.【要判断】アンケート（ベルで既読にするか）の結果。⑥で①の本来の動きになったので、②にするなら別途
3.【要連絡】相手（場所予約）に App.tsx の2か所を触ったことを伝える
4.【データ整理・ユーザー操作】6/15 のテストお知らせ4件（管理者→林）が未対応に残り、数字を4件上乗せしている
5.【前回からの繰越】ベルの closeOnTap とホームのバナー（備品精算）／FAQウィジェットの WordPress 設置／
   Vercel の商用利用の問い合わせ／Notion の貼り直し（27%→8%）／幹部への LINE・Slack 2通（文面なし）／
   休暇申請の ✕（9/05・未確認）／テンキー第2段／税理士への領収書の質問／山科教室（10月）／復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 作業のしかたで分かったこと
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 設計レビュー（2体）は今日も**同じ致命的な穴を独立に指摘**した（フラグを state で持つと同じ描画内で見えない）。
  さらにエンジニア側だけが「グループ・DM の通知も同じ入口」を見つけた。指摘は実物で裏取りしてから採用
・🚨 なりすまし検算で **ID を取り違えた**（aad2b6cf＝管理者を太田さんと思い込み「関数が false」と誤診）。
  ID は必ず名前に引き直してから使う
・🚨 検算は本番と同じ規則を写して動かす。宛先の逆算方式の穴は検算で初めて見えた（コードを読んだだけでは気づけない）
・🚨 デプロイ確認の grep 語は**コードの実際の文言**で。「反映されません」で探して 0 件と誤認した（実物は「反映されないので」）
・🚨 なりすまし中は一時テーブルに書けない → set_config で持ち回す（前回の教訓を今日も使った）
・🚨 データ変更 CTE を副問い合わせに入れられない。with d as (delete … returning 1) select … from d の形
・🚨 Edit ツールで .tsx を直すと CRLF が LF になることがある。commit 前に node で CRLF に戻す
  （App.tsx は元から LF・SignIn/BoardPage は CRLF）
```

---


---
