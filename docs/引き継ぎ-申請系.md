# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-09 夕方・プッシュ文面の作り直しと、失敗が消える不具合の是正）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ すべて本番反映まで完了（master 7dc58c9／リモート一致／本番JSの中身で実測確認済み）
・本番DBの変更 **2本**（取り消しテスト → 適用 → 実測 まで完了）
    20260909172723 announcements.push_show_title（お知らせの件名を出すか・既定ON）
    20260909173741 修正依頼／取消依頼で event_key を分ける＋🚨 anon の実行権限を塞いだ
・Edge Function **9本** deploy（push-dispatch ほか。下の「触った共通ファイル」参照）
・コミット6本：93f5ef0（プッシュ文面）／b9435ce・61cf9da（記録）／
  e331f08（段0）／9a84181（段1）／7dc58c9（段2）
・tsc 通過／vite build 成功／rules-of-hooks 0件
・✅ **通知の仕組みを端から端まで通して確認**（ベル→push_queue→cron→端末。確認用の行は削除済み）
・🚨 **2人共通のファイルを触った**：App.tsx ／ AdminPanelContext.tsx ／ AdminPanel.tsx ／
  AnnouncementsTab.tsx ／ push-dispatch ＋ Edge Function 8本。**相手へ連絡すること**
・未追跡は AGENTS.md のみ（触らない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ① プッシュ文面を「状態語＋件数」→ 意味の通る文章にした
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
「未承認 3件」→「休暇申請が届いています（3件）」。55イベント＋直送8本。

🚨🚨 **旧「NG語リスト」は無効だった**（社長端末＝Android Chrome へ20通を1通ずつ実測）。
　 NG確定とされていた「確認」「依頼」は**単独でも**表示され、**文章形も35文字の件名も**通った。
　 → 2026-07-11 の判定はいまの Chrome では成り立っていない。
　 🚨 **詳しいルールはアーカイブの「プッシュ文面のルール」に書き直した**（旧一覧は無効）。

🚨 **戻す手段を必ず用意した**。短い状態語（word）を EVENT_MAP から消していない。
　 Function の環境変数に **PUSH_SENTENCE=0** を入れるだけで旧方式に戻る。
　 挙動が変わったということは、また戻る可能性があるということ。

・社内お知らせだけ**件名そのもの**を出せる（①お知らせ ②まとめが1件 ③ONのとき の3つが揃ったときだけ）。
　🚨 連絡板には入れない（個人あてメッセージの用件がロック画面に出る）。
　お知らせ作成画面のチェックで切り替え（既定ON・入力中の件名が見本に出る）。
　✅ 実測：実際の件名35文字が**切れずに最後まで読めた**。60文字で切る設定自体は未検証。
・修正依頼／取消依頼で event_key を6つに分けた（旧キーは消していない。App.tsx は startsWith で新旧を拾う）
・shift:admin_edited を EVENT_MAP に足した（ベルには出るのにスマホへ届いていなかった・実データ4件）

■ 🚨 プッシュの扱いで分かったこと
・**`sent:1` は端末到達の保証ではない**。**数分遅れて届くことがある**（今日1回発生し、あとから届いた）。
　来ないと思ってもすぐ送り直さない（同じ tag で上書きされ、切り分けが難しくなる）。
・テストは**間を空けて1通ずつ**。新しい文面は必ず社長端末で1通試す。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ② 🚨🚨 失敗が静かに消える箇所を洗い出し、危ない順に直した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
機械的に数えたところ **136件**あった。
　・`.then(null, () => {})` … 54件
　　🚨 ただし **全部が無意味ではない**。受け手が supabase のビルダーなら死んでいるが、
　　　 素の async 関数（dispatchEmail 等）なら**正当な守り**。**一括置換すると壊れる**。
　・戻り値を受けていない書き込み … 82件

🚨 **いちばん大事な前提**：
　 **RLS（権限）で弾かれた update / delete は、error ではなく「0件成功」で返る。**
　 だから error を見るだけでは足りず、必ず `.select('id')` の件数を見る。
　 （「0件チェックは競合検出にならないから意味が薄い」は**誤り**。RLS拒否を検出する唯一の手段）

【段0】🚨 DBが変わっていないのに、外部が動いてしまう箇所（4件・最優先で直した）
　差し戻し／種別を変更して受理／受理の取り消し／**勤怠の取消**。
　いずれも更新・削除を検査せず進むため、**Googleカレンダーの予定は実際に消え、
　申請者や上長へ通知・メールまで飛んでいた**。画面には「できました」と出るので誰も気づけない。

【段1】管理画面で「失敗」が **✓ の緑カード**で出て、**3秒で勝手に消えていた**（66件を是正）
　「カレンダーを確認してください」という手で直す指示まで、読む前に消えていた。
　→ 成功=薄緑✓（3秒で消える）／失敗=薄赤!（消えない）／部分成功=薄黄!（消えない）の3つに分けた。
　🚨 **部分成功を赤で出さないこと**。本体は成立しているので、赤にすると
　　 「できなかった」と読まれ、今回の事故と**逆向きの誤解**になる。

【段2】差し戻しの取り消し／締め後申請の許可の取り消し
　🚨 `.eq('status','rejected')` を足したが**挙動は変わらない**。
　　 そのボタンは該当の状態の行にしか出ないため、人が押せる操作は減らない。
　　 減るのは「一覧が古いまま押した」「他人が先に処理した」場合だけ＝止めたい方。

■ 判定と文言は lib/statusUpdate.ts の1本に集約した（新規）
　🚨 **supabase を import しない側に置いた**。画面を開かずに検算できる（19項目を通し、実行後に削除）。
　・件数0の理由は3つあり、**利用者がすべきことが正反対**なので文言を分ける：
　　　権限がない／対象が消えた → 「（権限が不足しているか、すでに取消・削除されています）」
　　　他人が先に処理した       → 「他の操作により…一覧を更新してご確認ください」
　　　通信が不安定（一時的）   → 「少し待ってから、もう一度お試しください（◯◯は行われていません）」
　　🚨 この3つを混ぜないこと。混ぜると、直る失敗を諦めさせ、直らない失敗を延々と押させる。
　・一時的かどうかの判定は **lib/netFailure.ts の isTransientFailure** に任せる。
　　🚨 文字列で判定しない（iPhone Safari の失敗は "Load failed" で "fetch" も "network" も入らない）。
　・専用の「もう一度」ボタンは作っていない。モーダルも一覧も開いたままなので元のボタンを押せばよい。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 取り消しテスト・レビューで防いだ事故
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨🚨 **submit_correction_request を anon（ログインしていない人）が呼べた**（2026-07-26 から）。
　 このリポジトリは Public で anon キーは誰でも入手できる。しかも所有者チェック
　 `if v_owner <> auth.uid()` は auth.uid() が null だと **null になって成立せず**、insert まで進んでいた。
　 止まっていたのは requester_id の not-null 制約のおかげで、チェックが働いた結果ではない。
　 **実際に anon になりすまして再現を確認した**うえで、①null を先に弾く
　 ②`<>` を `is distinct from` に ③権限を外す、の3段で塞いだ。
・🚨🚨 **これまでの記録が逆だった**。「revoke ... from public では外れないので anon を名指しする」
　 と書かれていたが、実測すると **anon だけ名指ししても外れない**（anon は PUBLIC の一員のため）。
　 **public と anon の両方から外し、authenticated へ付け直す**のが正しい。
　 適用後の実測：anon=false／authenticated=true／service_role=true／
　 anon で実際に呼ぶと 42501 で入口から弾かれる。
・🚨 is_admin() の判定を **coalesce で包んだ**（null が返ると `not null` = null で素通りする）。
・🚨 notification_settings の列名は recipients ではなく **recipient / subject / template**。
・🚨 EVENT_MAP に足す前に **push_queue の pending を確認する**（溜まっていると足した瞬間に古い分が飛ぶ）。
・🚨 エラーバナーが**禁止色 #4a1515** を使っていた（2026-07-24 に全廃されたはずのもの）。
　 標準の固定色に直した。**残り10箇所ある**（下の「次にやること」）。
・🚨 JSXを1回壊した（コメントを `&&` の内側に入れた）。**tsc は通ったが vite build で落ちた**。
　 両方を必ず通すこと。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【要連絡】相手（場所予約）へ、本日触った共通ファイルを伝える（文面は作成済み）。
　 App.tsx／AdminPanelContext.tsx／AdminPanel.tsx／AnnouncementsTab.tsx／
　 useAuth.ts／FeaturePermissionsTab.tsx／NotificationsTab.tsx／Edge Function 9本。
　 🚨 **deploy は関数を丸ごと置き換えるので、出す前に git pull が要る**ことも伝える。
　 🚨 あわせて「room_bookings に同じ形が9件ある（そちらの担当なので触っていない）」も伝える。
2.【実機確認】
　・社内お知らせを1件出して、**件名がそのまま通知に出るか**（チェックON／OFFの両方）※未確認
　・修正依頼／取消依頼を出して、プッシュのアプリ名が分かれるか
　・管理画面で失敗させて、**赤いカードが消えずに残るか**
　・午前ぶんの10件（アーカイブの 2026-09-09 の節に一覧）
　・**明日の朝 9:10 の毎朝のお知らせ**（対象7件）
3.【段3・未着手】失敗が静かに消える箇所の残り
　・権限・役職（profiles / roles）… UsersTab:574 は失敗すると画面が黙って元の役職に戻る
　・🚨 **FeaturePermissionsTab:268-269 は「2文で1つの意味」**（役職名の改名）。
　　 片方だけ成功すると roles と profiles.role_title が**食い違ったまま残る**。
　　 件数チェックを足すだけでは終わらない。**RPCで1トランザクションにするか、
　　 片方だけ成功したときの扱いを決めるか**を先に判断すること（同型：AdminPanelContext:427-428/437-438）。
　・leave_requests の delete 2件（有給奨励日の対象から外す処理）
　・設定・マスタ（NotificationsTab／weekly_shift_patterns／company_calendar／email_templates／faq）
　・🚨 **room_bookings の9件は相手の担当なので触らない**
4.【要検討】**再発防止の ESLint ルール**（レビュー提案・未着手）
　 型情報が要らない no-restricted-syntax で2本：`.then(null, …)` の禁止／
　 戻り値を受けない update・delete の禁止。
　 🚨 **files: でディレクトリを絞って段階的に広げる**こと。一気に入れると
　 　 「lint は常に0件」という不変条件を82件の違反で壊す。
　 🚨 eslint.config.js にルールを足すと**相手のコードもエラーになる**（RoomBookingPage）。
　 　 ディレクトリを絞れば連絡は不要。
5.【残り】禁止色 #4a1515 / #5a1a1a が**10箇所**残っている
　 （OvertimePage 2／ReportsTab 2／LeaveRequestsTab 2／ShiftReportPage 1／
　 　ShiftReportsTab 1／LeaveRequest 1）。エラーバナーに isDark の出し分けを書かない。
6.【要判断】管理者が「失敗が起きている」ことを知る手段は**まだ無い**。
　 いま入れたのは「その場の人に分かる」ところまで。記録の仕組みを作るなら
　 🚨 **掃除（毎晩の cron ＋ 1日の上限）とセット**にすること
　 （cron記録が130,817件・115MBでDB全体の87%を占めた事故がある）。
　 現在のDB使用量は **40MB / 500MB**（実測）。
7.【要判断・繰越】連絡板グループでのシフト調整（案A提示済み・決めること5つ）
8.【宿題・繰越】**FAQの戻るボタン（スマホ）**。FAQを開いて戻ると開く前の作業ページまで戻る
9.【繰越】ベルの closeOnTap ／ホームのバナー（備品精算）／FAQウィジェットの WordPress 設置／
　 Vercel の商用利用／Notion の貼り直し／幹部への LINE・Slack 2通／休暇申請の ✕（9/05未確認）／
　 テンキー第2段／税理士への領収書／山科教室（10月）／復元リハーサル
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
