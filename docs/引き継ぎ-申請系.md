# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-05・休暇申請の完了バナーの ✕）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ 本番反映まで完了（master 7013834／リモート一致／本番JSの中身で実測確認済み）
・**DBの変更なし。Edge Function の変更もなし**
・触ったのは `client/src/components/LeaveRequest.tsx` の**1ファイルだけ**。
  相手（場所予約）とは重なり0件
・tsc 通過／vite build 成功／rules-of-hooks 0件
・⚠️ **実機未確認**（ログインが要るため Claude 側では画面を開けない）
・未追跡は AGENTS.md のみ（触らない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 開始時にやらかしたこと（次に触る人へ・いちばん大事）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
セッション最初の `git pull` が "Already up to date" だったので同期済みと判断したが、
そのあと `git fetch` したら**13コミット遅れていた**（相手が 9/04 21:38〜22:09 に push）。
ユーザーから「相手の状況を確認しなくていいのか」と指摘されるまで気づいていなかった。
→ 🚨 **pull の結果だけを信じない。** 必ずここまで実測する：
     git fetch origin
     git rev-list --left-right --count HEAD...origin/master   # 左=自分 右=相手
     git diff --name-only HEAD...origin/master                # 相手が触ったもの
     git diff --name-only origin/master...HEAD                # 自分が触ったもの
   重なりの確認（CLAUDE.md ⑤）と同じコマンドで、開始時の同期確認も兼ねられる。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日やったこと（コミット1本・7013834）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
実機報告：iPhone で、休暇申請の完了バナー「申請しました」の **✕ を押すと画面が真っ黒**になる。
　　　　　**「確認しました」では起きない**。**再読み込みで戻る**。1回だけ発生・再現性は不明。

対応：`note`（読んでほしい案内）があるときは **✕ を出さない**ようにした（BannerSuccess）。
　・変えたのは表示条件 `{!note && ...}` の**1か所だけ**。押したときの処理・文言・配色・並びは変更なし
　・`note` が無いバナー（調整休の「登録しました」など）は**3秒で自動的に消える**ので ✕ は残した
　　（早く消したい人のための、実害の無い便利機能）
　・他4ファイル（BusinessTripReport / ExpenseForm / PurchaseRequestForm / ReimbursementForm）の
　　同名部品は `note` に対応していないため**波及しない**（実測確認済み）
　・本番JSで `!t&&jsx("button",…children:"✕")` を実測（`t` が `note`）

理由（ユーザー提案＋合意）：
　・✕ と「確認しました」は**まったく同じ処理**を呼んでおり、出口が2つある必然性が無い
　・✕ は「**読まずに閉じる**」ための導線。3秒の自動クローズをわざわざ外してまで
　　読んでもらう案内なのに、右上に読まずに消すボタンを置くのは目的と逆向き

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨🚨 これは修理ではない。「真っ黒」の原因は未特定のまま
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・「再読み込みで戻る」＝ **React が落ちている**。
  🚨 **アプリ全体にエラーバウンダリが1つも無い**（grep で0件）ため、落ちると
  `index.css` の地色 `#242424` だけが残り「真っ黒」に見える。
  同じ症状の記録が LeaveRequest.tsx の中にもある（2026-07-25〜・原因は Hook の位置だった）
・調べて**シロ**だったもの（同じ所を二度調べないこと）：
   - ✕ と「確認しました」は**同じ `onClose` を呼ぶ**。コード上は差が無い
   - LeaveRequestForm も ProtectedLayout も、**Hook はすべて早期returnより前**にある
   - `setSubmitted(true)` は Slack・サイト通知・メールを**すべて待ち終えたあと**に呼ばれる
     （LeaveRequest.tsx:650）ので、「押すのが早すぎて裏の処理と競合した」筋も消えた
・つまり今回の変更は**報告された唯一の経路をふさいだだけ**。
  同じ処理を呼ぶ「確認しました」は残っているので、**原因が生きていれば別の形でまた出る**
・🚨 原因を捕まえるには**エラーバウンダリ**が要る（未導入）。落ちても真っ黒にせず
  エラーの文字を画面に出せるので、次に起きたとき**推測せずに直せる**。
  🚨 **App.tsx か main.tsx を触るので、相手への一声が要る**（ユーザー承認も未取得）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先／今日の分】休暇申請の完了画面（iPhone）
   ・✕ が出なくなっているか（「確認しました」だけになっているか）
   ・「確認しました」でホームに戻るか（**真っ黒にならないか**）
   ・調整休の「登録しました」は**従来どおり ✕ が出て3秒で消える**か
   ・🚨 もし「確認しました」でも真っ黒が出たら、**そのときの状況を必ず記録する**
     （毎回か1回きりか／端末とブラウザ／直前に何をしたか）
2.【要判断】エラーバウンダリを入れるか（上記）。入れるなら**相手への連絡が先**
3.【実機確認・繰越】9/04 の休暇申請ぶん
   （詳細はアーカイブの「▶ 前セッション（2026-09-04・休暇申請の表示まわり）」）
   ・🚨 スマホのダークで、黄色い帯と「自己登録（申請不要）」の※2行が読めるか
   ・申請先：選ぶ前は黄色い枠 → 選んだあと**グレーに戻る**か
   ・🚨 **管理者の画面で、自分宛でない申請に「あなたが最初の受理者です」が出ていないか**
   ・管理画面の申請先に `└ ② ◯◯` が出るか。表が横に詰まっていないか
4.【実機確認・繰越】備品購入申請（2026-09-01 の分）
   ・ベルの通知を押して申請に着地するか（期間が自動で「すべて」になる）
   ・ファイル2件／本文なしで送信できるか
   ・ナビ「📦 備品精算」の数字（承認待ち＋未確認）と「📋 履歴」タブの数字
   ・ホームの黄色バナーが水増しされていないか
5.【実機確認・繰越最優先】残業の事後報告の時刻制限（8810756・まだ誰も動かしていない）
6.【要送信】幹部へのLINE（B案）🚨 5を確認してから。**文面は残っていない。作り直し**
7.【要送信】Slack 2通（開発担当者向け＋Claude向け）も**文面が残っていない**
8.【実機確認】お客様向けFAQ（上桂校・四条本校・スマホ）
9.【実機確認・依頼済み】残業の「実績を報告する」（3ead881）＋連絡文B/C案未送信
10.【繰越】テンキー第2段／Excel出力差分／プッシュ受信時間帯／WordPress設置
11.【要確認・税理士】領収書の保存年数と紙廃棄の可否（確定見積書等も積み上がる）
12.【10月以降】山科教室：FAQ_SCHOOL_OPTIONS と FAQ_COURSE_SCHOOLS 両方に追加
13.【運用・年1回】バックアップの復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 未回答の質問（2026-09-05・ユーザーから）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
「以前、**無料アカウントでは2つしか作れない**から どうする、という会話をしたが何のことか」
→ 🚨 **リポジトリの記録には見つからなかった**（docs / CLAUDE.md / アーカイブを grep 済み）。
   もっとも可能性が高いのは **Supabase の無料プランの「1組織あたり、動かせるプロジェクトは2つまで」**。
   ・CLAUDE.md の「本番のデータベースは1つだけ・ブランチで分けられない／
     試す場所（ステージング）はありません」と整合する
   ・DISASTER-RECOVERY.md が復元リハーサルで「終わったら**使い捨てプロジェクトを削除する**」と
     書いているのも、枠が空かないためと読める
   🚨 **確証は無い（推測）**。確かめるなら Supabase の管理画面で
     組織のプロジェクト数と上限を見るのが早い。分かったらここに事実として書き直すこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 保留（必要になったら）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**1,000件の壁**（備品購入申請）。実測で申請19／明細14／相見積20／質問共有1。
  年間およそ120件で到達は8年ほど先なので手を入れていない。
  🚨 ただし4つのクエリとも**件数指定なし**（.range も .limit も0件）。
  Supabase は指定がないと**1,000行で黙って打ち切る**。
  いちばん先に届くのは**相見積**。直すときは1,000件ずつ分割して読む
・ファイルの種類の名札がコード内の固定リスト（備品購入申請）
・共有ファイルの元のファイル名を保存していない（備品購入申請）
```

---
