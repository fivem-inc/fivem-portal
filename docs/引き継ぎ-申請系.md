# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-21〜22・連絡板の返信／退職チェック表の作り込み／MaxMind／Edge Function の門／FAQのレビューと数え直し）

```
【状態】master `9e1bcbcc` まで push 済み（リモート一致・2026-09-22）
  作業ツリーは `AGENTS.md` だけ（管理外）
・本番DBに migration 5本適用：
  `20260921162235`（お知らせへの返信）／`20260921172202`（チェック表の「対象外」）／
  `20260921181410`（項目を直しても消しても記録が変わらない）／`20260922042003`（手続きのメモ）／
  `20260922061906`（FAQ の都道府県を夜間に引く cron）
・Edge Function を9本 deploy：record-signup-ip／**faq-geo-fill（新規）**／
  overtime-grant-request-notify／shift-report-confirmed-notify／send-overtime-slack／
  overtime-approve／gcal-sync／send-email／slack-notify
・push 13回。tsc 0／vite build 成功／rules-of-hooks 0／触ったファイルの ESLint は HEAD と同数
・🚨 **利用者から見える変化**：お知らせに「返信を受け付ける」／退職チェック表（対象外・メモ・項目の削除と修正）／
  交通費の却下に退職者の警告／役職プレビューに「退職者」／FAQ集計の都道府県と出典
・🚧 **実機未確認**：下の「次にやること 1」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (101) 連絡板：お知らせに返信を付けた（migration `20260921162235`✅・push `3246978e`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：「らくらくの誕生日メッセージのように、連絡板でも返信できるようにしたい。パートからも」
   ・🚨 **実測で分かった前提**：お知らせは返信不可（リプライはグループ・DMだけ）。
     DM は会話できるが **`dm_default_send_permissions` がマネージャー・管理者・社長だけ**なので
     **パートは受け取っても返信できない**（画面だけの制限。DB は止めていない）。
     パートが入っている DM は **0件**（14件すべて正社員同士・最終6月）
   ・✅ **ユーザー確定（蒸し返さない）**：
     送信時のチェックで返信を受け付ける（**既定OFF**）／返信は**お知らせの中で往復**（🚨 **DM は作らない**）／
     読めるのは当事者2人だけ／**30日で自動終了**／送信者はいつでも終了でき、**終了後は送信者だけが再開**／
     再開するとその日からまた30日／文言は「**↩ 送信者に返信する**」／
     🚨 **案B（DM の「始める」と「返す」を分ける）は取りやめ**（返信が DM を通らなくなったため不要）
   ・DB：`board_messages` に `allow_reply` / `reply_until` / `reply_closed_at`。
     🚨 **日数（30）を持つのは `board_reply_days()` だけ**。期限は送信時にトリガーが入れる。
     判定は `board_reply_open()`（画面は `lib/boardReply.ts`）。書き込みのポリシーも締めた（画面だけでは不十分）
   ・🚨 **途中で見つけて直した穴2つ**
     - 写しで受け取る代表者が送信トレイで開くと、押しても必ず弾かれる返信ボタンが出ていた → 枠ごと出さない
     - **`status` の既定値が `'sent'`** なので、返信がナビの赤バッジに数えられていた
       （一覧は `parent_id is null` で絞るので**出てこない**＝「バッジは1なのに開いても何も無い」）
       → `App.tsx` に `.is('parent_id', null)` を1行追加
   ・🚨 通知は既存の `board:dm_message` を使い回した（新しい種類を作ると設定の行が無く全員に飛ぶため）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (102) 退職のチェック表を作り込んだ（migration 3本✅・push `fce43a29` `d04e99cd` `46cd2c83`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・**対象外**（`20260921172202`）… 未済の行の右端に小さく［対象外］→ その場で確認。印は「—」、取消し線は付けない
   ・**項目の文字を直せるように**…［修正］→［保存］の2段（onBlur では保存しない）
   ・🚨🚨 **ユーザー指摘で設計を作り直した**（`20260921181410`）：
     「そもそも修正しても過去のを変更したらだめでは」「削除で消えるのもないよね」
     → **記録するときの項目名を写す**（`item_label`）＋**項目を消しても記録が残る**（cascade → set null）。
       **どの項目にも［削除］**。消した項目の記録は「削除された項目の記録」として当時の文字で残る。
     🚨 **記録が0件のうちに直せたのが幸い**。1件でも溜まると「当時の文字が分からない記録」が残った
   ・**メモ**（`20260922042003`）… 項目ごと＋その方ぜんたい。呼び名は「メモ」（案1）
     🚨 **別の表（`retire_notes`）にした**。既存の記録に混ぜると「**メモを書いただけで必須の残りから消える**」
       （数え方が「行があれば片付いた」のため）。掃除の cron は付けていない（行数が頭打ち・理由は migration に明記）
     🚨 更新のとき `updated_by` を自分で入れる（既定値が効くのは insert だけ。入れないと RLS で0件）
     🚨 入力欄に「パスワードなど秘密の情報は書かないでください」
   ・🚨 **残り件数の数え方は3回とも1文字も触っていない**（画面の `retireRemaining` と朝9時の通知の両方が乗っているため）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (103) 新規登録の地域判定を MaxMind GeoLite2 に差し替えた（push `c38a87ab`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・🚨 `ip-api.com` の無料枠は「**非商用のみ**」で、商用の当システムでは使えなかった（(98) の宿題）
   ・MaxMind の無料アカウントをユーザーが取得。**Account ID 1414730**。
     鍵は Supabase の Secrets（`MAXMIND_ACCOUNT_ID` / `MAXMIND_LICENSE_KEY`）。🚨 鍵の値はどこにも書かない
   ・窓口 `https://geolite.info/geoip/v2.1/city/{IP}`・基本認証・**1日1,000件**
   ・応答に `geo: 'ok'/'unknown'` を足した（🚨 **鍵が違っても IP の記録は成功する**ので、確かめる手がかりが要る）
   ・実測：存在しないユーザーIDで1回呼び `{"success":true,"geo":"ok"}`／誰のデータも変わっていない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (104) FAQ の都道府県と出典表示（migration `20260922061906`✅・push `b668876c`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・Edge Function **`faq-geo-fill`（新規）** を毎晩 **4:20 JST** の cron で呼ぶ
   ・🚨 **同じIPは1回だけ引く**（実測：22件に対してIPは11種類）。1回に引くIPは200件まで
   ・🚨 引けなかったら「**不明**」を入れて二度と引かない（空だと毎晩引き直して枠を無駄にする）。
     集計では「未調査＝まだ／不明＝調べたが分からなかった」を見分けられる
   ・🚨 鍵が無いときは何も書かずに終わる（'不明' で埋めると鍵を入れ直しても引き直せない）
   ・🚨 **出す順番をいつもと逆にした**（Edge Function → DB → push）。cron が呼ぶ相手が先に無いと空振りする
   ・実測：`{"ips":11,"filled":18,"unknown":4,"failed":0}`／東京都6・京都府4・兵庫県3・大阪府2・千葉県2・ヘッセン州1
   ・✅ **ユーザー確定**：出典は**管理画面の利用状況の下に1行**（案1）。文言は日本語で短く（案い）
     `地域の判定：GeoLite2 データ（MaxMind） https://www.maxmind.com`
     🚨 条件は「使っていると述べる広告・説明資料に出すこと」。都道府県は社内の集計でしか使わないので、
        公開ページに出す義務はそもそも無い。念のための保険として置いている

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (105) 🚨 Edge Function 7本の入口に「呼ぶ人が在籍者か」の門（push `92471784` `34b5d206` `f07f8838`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・設計書 §9-5 の3。**`supabase/functions/_shared/callerGate.ts`（新規）** に集約
   ・🚨🚨 **調べて分かったこと：7本のうち5本は誰が呼んでいるかを一度も確かめていなかった**
     （Bearer が付いているかを見るだけ）。たとえば `slack-notify` は本文の名前をそのまま Slack に流すので、
     **ログインできる人なら誰でも好きな名前で通知を流せた**。退職者対策と同じ作業で両方が直る
   ・通す相手：**service_role（cron・他の関数）は素通り**／staff／retiree_grace。それ以外は 401・403
     🚨 立場を読めなかったときは**通さない**。判定は DB の `my_access_state()` を呼ぶだけ（条件を書き直さない）
   ・3段に分けて出した：①grant-request / shift-report-confirmed ②send-overtime-slack / overtime-approve / gcal-sync
     ③slack-notify / send-email（🚨 受理・差し戻しの連絡がすべて通る道なので最後）
   ・🚨 **`send-email` は出す前に呼び出し元17か所を1つずつ確かめた**：Edge Function 12本すべて service_role。
     `announcement-notify` だけ anon と service_role の両方の接続を作っていたが、メールは service_role のほう
   ・🚨 `gcal-sync` は自前の判定を門に寄せた。**返す形（`success:false / unauthorized`）は変えていない**（画面が見ている）
   ・実測：ログインしていない鍵で7本とも 401。**在籍者が通ることは実機で確認済み**
     （勤務変更の報告を1件出すと通知が6件作られた）
   ・🚨 共用9本のうち slack-notify / send-email / gcal-sync / record-signup-ip を触った（deploy 前に git pull 済み）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (106) 退職者の仕上げ（§9-5）が全部終わった（push `e422d111` `3d450627`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・**交通費の却下にも差し戻しの警告**。判定は `retireeReturnNote` 1本（残業・勤務変更報告と同じ）。
     `useExpenses` の管理者側の select に `is_active` / `retiree_access_until` を足した（問い合わせは増えていない）。
     🚨 一般ユーザー側の select は触っていない（email を混ぜると退職者で 42501）
   ・**役職プレビューに「退職者」**。🚨 プレビュー中は `realIsAdmin` が false になり選び直す欄が消えるので、
     **バナーの［× 終了］が唯一の出口**。退職者のときも必ずバナーを出す（入れ忘れると戻れなくなる）。
     🚨 バナーに「※ 見た目だけの下見です」と明記（DB 側の権限は真似できない）
   ・✅ **ユーザー確定：社外の連絡先は載せない**（在職していた方は連絡先をご存じのため）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (107) 🚨 FAQ集計のレビュー2体と、見つかった不具合の修正・記録のリセット（push `ec35a77f` `9e1bcbcc`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：ユーザー「内訳が見にくくないかな」→ UI/UX とシニアエンジニアの2体でレビュー（重い指摘は本番で裏取り）
   ・**直した①：会社のIPが黙って消える道**（`ec35a77f`）
     🚨 読み込みの `error` を受け取っておらず、読めないと入力欄が**空**になっていた。
       そのまま保存すると `{"ips": []}` で上書きされ「保存しました（0件）」と緑で出る。
       社内/社外は集計のたびに判定し直すので**過去2年ぶんが全部「社外」に変わり、元の値は残らない**
     → error を見て入力欄を空にしない／空で保存するときは確認／打ちかけを読み直しで消さない
   ・**直した②：問い合わせ率が150%**（`9e1bcbcc`）
     🚨 原因は**分母と分子の単位違い**。回答を読んだ＝`once` 付き（人数）、問い合わせ＝`once` 無し（回数）。
       この画面の並びは「問い合わせが多い順＝直す優先順」なので、**連打した質問が最上位に来ていた**
     ✅ **ユーザー確定（案A）**：「読んだ人のうち何割が問い合わせに回ったか」＝**人数で揃える**。
       `goContact` に `once: contact:<topicId>:<reason>` を足した。100%超は率を出さないガードも入れた。
       同じ式が表とCSVの2か所にあったので `contactRate()` 1本に
   ・**記録のリセット**（✅ ユーザー確定・書き出しは不要と明言）
     🚨 実測：72件のうち49件は**計測を始める前**（9/20 02:22 より前・端末も流入元も空）。
       9/20以降の23件は**問い合わせ0件・回答を読んだ1件だけ**（12人はほぼ「開いて閉じた」だけ）
     🚨 **9/17 16時に FAQ を公開**していたため、16時以降には**本物のお客様が混ざっている**
       （「休会」「翌月の予約の枠が表示されません」を間をあけて読んで問い合わせに進んだ方がいる。
        ＝**この2つの回答が不十分かもしれない**という気づきは残しておくこと）
     → `delete from faq_public_event`（72→0）。🚨 `faq_query_log`（98件）・質問97・回答114・会社のIPは**すべて無事**
   ・🚨 **確認のしかたで学んだこと**：お客様向けウィジェットは **`/faq-widget.html` という別の入口**。
     `main-*.js` からは辿れない。本番反映は `assets/faq-widget-*.js` を見る（バイト数で突合）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない
   ▼ **連絡板の返信**：作成画面に「↩ 返信を受け付ける」が出る／**既定でOFF**／
     チェックして送る→相手に［↩ 送信者に返信する］と「あと30日」／返信でベル・プッシュが届く／
     送信トレイに「◯◯さんとのやり取り」／🚨 **他の宛先には何も見えないこと**／
     ［このやり取りを終了する］→両方書けなくなる→［再開する］で戻る／
     🚨 **チェックを入れずに送ったお知らせには返信の枠が出ないこと**
   ▼ **退職チェック表**：右端の［対象外］と確認／「— （対象外）」／必須の残りが1つ減る／
     項目の［修正］［削除］／🚨 **済みにした項目の文字を変えても、その行の文字が変わらないこと**／
     🚨 項目を削除しても「削除された項目の記録」に当時の文字で残ること／
     メモ（項目ごと・その方ぜんたい・未済でも書ける・🚨 **書いても必須の残りが減らないこと**）
   ▼ **交通費の却下**：退職して申請期間中の方を却下しようとすると警告が出る（在籍者には出ない）
   ▼ **役職プレビュー →「退職者」**：退職者の見え方になる／🚨 **［× 終了］で戻れること**
   ▼ **メール**：受理か差し戻しを1件して、いつもどおりメールが届くか（🚨 (105) の門の最終確認）
   ▼ 9/20 以前の分（(93)〜(100)）の未確認項目も残っている
2.【FAQ の続き】レビューの残りの指摘（重い順）
   ・**期間を変えたとき、前の期間の内訳・滞在時間が残る**（`load` の途中 `return`。エラーも後勝ちで上書きされる）
   ・**ヘッダーと表の合計が合わない**（`topic_id` が無い問い合わせは表に出ない）／
     **当期0件の行が消える**（直した効果が見えない）／**「件」と「人」の単位が混在**
     ← 🚨 **これが「見にくい」の正体**。表示の作り直しはここから設計する
   ・検索ログの `.limit(500)` が黙って打ち切る（`order` も無い）→ RPC に寄せる
   ・`faq_is_internal_ip` を全行に1行ずつ呼ぶ（いまは実害なし・放置すると当たる）
   ・**見せ方そのもの**：✅ UI/UX のおすすめは**案B＝軸ごとの小さな横棒**（🚨 新しい色は足さない・ライブラリ追加なし）。
     チップは**幅が文字数で決まる**ので量を逆に読ませる。案A（並べ方と数字の出し方だけ）→案B は積み増しでできる
   🚨 **記録が0件になったので、数日ぶん溜まってから表示の作り直しに入るほうが確実**
3.【要ユーザー作業】他校のIPを登録（各校のPCで `https://api.ipify.org` を開く）。
   四条本校 150.9.88.61 は登録済み。🚨 192.168.x.x は建物の中だけの番号なので一致しない
4.【相談】電車遅延（太田さん）… ✅ **いったんアプリは変更なし**（2026-09-22 ユーザー判断）
5.【連絡】場所予約の担当へ（下にまとめ）
6.【繰越】FAQの続き（(2-2)）／こどもシフト表の2回目／シフト調整の手順9／
   休暇の穴の第3段／表示速度／有給の管理（(A)）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 場所予約の担当への連絡（次に連絡するときにまとめて）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**共用の Edge Function を4本 deploy した**（2026-09-21〜22・いずれも deploy 前に `git pull` 済み）
  - `record-signup-ip` … 地域判定を ip-api.com → MaxMind に差し替え（🚨 非商用枠を商用で使っていたため）
  - `slack-notify` / `send-email` / `gcal-sync` … 入口に「呼ぶ人が在籍者か」の門を足した。
    🚨 **service_role（cron・他の関数）は素通りする**ので、そちらの呼び出しには影響しない。
    🚨 新しく `supabase/functions/_shared/callerGate.ts` を置いた（**このリポジトリ初の `_shared`**）。
      deploy すると各関数にこのファイルも一緒に上がる
・**2人共通ファイル**（足しただけ・値は変えていない）
  - `App.tsx` … 返信をナビの赤バッジに数えない1行／役職プレビューに「退職者」
  - `types/index.ts` … `Profile` に `is_active` / `retiree_access_until`（どちらも任意）
・`supabase/config.toml` は**触っていない**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**お知らせの返信は「DM を作らない・30日で終了・再開は送信者だけ」**（2026-09-21）。
  DM の送信権限を広げる案は**取りやめ**
・**退職チェック表の項目は修正・削除とも自由**。過去の記録は当時の文字のまま残る（2026-09-21）
・**退職者の案内に社外の連絡先は載せない**（2026-09-22。在職していた方は連絡先をご存じのため）
・**FAQ の問い合わせ率は人数で揃える**（2026-09-22・案A）。過去の記録は**全消しして今日から数え直す**
・**GeoLite2 の出典は管理画面に日本語で1行**（2026-09-22・案1＋案い）
・**電車遅延はいったんアプリを変更しない**（2026-09-22）
・（以前からの決定はアーカイブの「ユーザーの決定」を参照）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 今日わかった落とし穴（次も踏みやすい）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**`board_messages.status` の既定値は `'sent'`**。親を持つ行を足すと、一覧に出ないのにバッジだけ増える
・**`supabase-js` の `auth.getUser()` は、グローバルの Authorization ヘッダーで動く**
  （Edge Function で使える。心配して調べたが問題なかった）
・**新しい関数は `authenticated` への grant を明示的に書く**（2026-09-19 に PUBLIC の実行許可を外したため）
・**`unique (a, b)` は b が null の行を縛らない**。1人1件にしたいなら部分一意索引で補う
・**列の既定値が効くのは insert だけ**。update で `updated_by` を入れ忘れると RLS で0件になる
・**お客様向けウィジェットは `/faq-widget.html` の別入口**。本番反映は `assets/faq-widget-*.js` を見る
・**文言が増えない変更はバイト数で突合する**（手元の `dist/assets/*.js` と本番を比べる）
・**`git show HEAD:<path> > 一時ファイル` で ESLint を HEAD と比較できる**（実行後に必ず消す）
・**このリポジトリの .tsx は CRLF**。node で複数行を置換すると当たらない。1行ずつに分けるか `\r\n` を使う
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
