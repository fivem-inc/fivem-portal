# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-10 続き2・申請依頼を「押せば分かる」形に／「対応しない」の理由／連絡板のベル自動既読）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ **本番反映まで完了**（master `af9b5b7`／`git ls-remote` で突合済み・作業ツリーは
       docs のこの更新ぶんのみ）
・**DBの変更なし**（migration 0本）。🚨 **Edge Function を1本 deploy した**
  （push-dispatch・**version 33**。下の (7)）
・commit 4本：`53facb1`（申請依頼を押せば分かる形に・移動＋ハイライト／メモ／予定時間）／
  `9dbb30a`（移動先を入力欄の先頭に・有給管理の計画を記録）／
  `516962a`（「対応しない」に理由・ハイライトを共通フックへ）／
  `af9b5b7`（連絡板のベル自動既読）
・tsc 通過／vite build 成功／rules-of-hooks **0件**／新ルール **0件**
・✅ **本番JSの中身で実測済み**（デプロイ後に curl 1回。手元の build と**バイト数まで一致**）
    main-CKe8mMpY.js（213,530バイト）…
      `fivem:bell-refresh` 1件／「ベルの既読化に失敗」2件／
      `board:group_message` 1件／`board:dm_message` 1件／
      **`board:confirm_request` 0件**（＝対応リマインドを既読にしない、が入っている証拠）
  🚨 **今回の語は `main-*.js` に載る**。`lib/notifications.ts` は連絡板とベルの**共有部品**なので、
     遅延読み込みの `BoardPage-*.js` を見ても見つからない。**チャンクを間違えると
     「載っていない」と誤判定する**
  🚨 `grep -c` は「**一致した行数**」なので、1行に固めた本番JSでは何個あっても1になる。
     語ごとに `grep -o … | wc -l` で数えること
  🚨 ファイル名のハッシュは再ビルドで変わるので、名前では判定できない。**バイト数と中身**で見る
・🚨 **今回の分はすべて実機未確認**（画面を開けないため）。確認項目は下の「次にやること」1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (7) 申請依頼のベルが「残業ページに行くだけ」だった（🚨 Edge Function の変更あり）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 実機指摘「押すと残業ページに行くだけで何が必要なのか分からない」）
   ・ベルの行き先を `/overtime?tab=history&focus=<依頼のID>` にした。
     🚨 **勤怠カレンダーと同じ `?focus=` の作法**（新しい仕組みを作っていない）
   ・**プッシュ通知も同じ場所へ飛ぶ**ようにした。`push-dispatch` の
     `application_request:received` と `overtime_proposal:received` に **`bell: true`** を足した
     （＝押すとベル一覧を開いて該当行を光らせる既存の経路に乗る）
   🚨 **Edge Function を deploy する前に必ず `git pull`**。deploy は関数を**丸ごと置き換える**ので、
      取り込まずに出すと相手（場所予約）の修正が無言で消える。今回は pull 済みで出した
   🚨 push-dispatch は **2人共通の9本**のうちの1本。次に触る人も同じ手順を踏むこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (8) 🚨🚨 ハイライトを自作したことが不具合の原因だった（共通フックに寄せて解決）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   症状（実機）：入力欄へ移ったあと、**数秒すると履歴タブへ引き戻される**。
   原因：自作したハイライト処理が、6秒後に **URL から `focus` を外していた**。
   URL が変わると「履歴タブへ切り替える」effect が**再実行**され、タブが戻されていた。
   🚨 **自分が入れた後片付けが、自分の別の処理を蹴っていた**。URL を状態の置き場所にしている
      画面では、**URL を書き換える後片付けが他の effect の引き金になる**。
   → 休暇の承認画面が使っている共通フック **`useFocusHighlight`** に差し替えた。
     このフックは **URL を書き換えない**ので、二重実装と不具合が同時に消えた。
   🚨 **呼ぶ位置に決まりがある**：`appRequests` の宣言より**後**（前だと変数が未定義で落ちる）・
      早期 return（`isConfirmView`）より**前**（後ろだと Hook の規則違反で画面が真っ白になる）。
      この2つに挟まれた場所しかない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (9)「この内容で申請する」の移動先（3回直して最終形）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ページ先頭 → 説明枠 → **入力欄の先頭**（`inputsTopRef`）。ユーザー指摘2回で最終形になった。
   🚨 **移動はフォーム側が持つ**（依頼カード側でスクロールしない）。
      2か所に書くと、移動先を直すときに片方だけ直す事故になる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (10) 予定の勤務時間が空欄だった（`draftHasTimes`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   原因：下書きに**時刻が入っていないのに下書きの日付を使って**シフトを引いていたので、
   シフトからの自動補完が効かなかった。**時刻を持っている下書きのときだけ**日付を使うようにした

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (11) 上司のメモを「理由」の欄に入れるのをやめた（案1・ユーザー確定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   きっかけ：ユーザー「理由が既に入っていると**そのまま出さないかな**」。
   ・メモは **理由の欄の上にカードで表示**。**「理由に入れる」ボタンは置かない**
     （ユーザー確定。置くと結局そのまま送る）
   ・`FormDraft` に `requestMemo` / `requestFrom` を足し、**自動保存にも通した**
     （通さないと下書きから戻ったときメモが消える）
   ・メモの**入力例を種類ごとに**出し分けた。1つの文を共通で使っていたため、
     休暇の依頼でも残業の例文が出ていた。
     残業＝「例：お客様対応が延びたため、18:00まで勤務」／休暇＝「例：私用のため有給を取得」
     🚨 **休暇の例は「私用のため」までにとどめる**。有給は理由を問わないのが原則で、
        例文は真似されるため

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (12)「対応しない」に確認と理由を付けた（案3・ユーザー確定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   きっかけ：押した瞬間に一覧から消えていた（ユーザー「一瞬で消えた」）。
   ・**選択式4つ**：すでに申請した／**日付や内容が異なる**／申請が不要になった／その他
   ・🚨 **「その他」を選んだときだけ入力欄を出し、そのときだけ必須**。
     **自由記入を必須にしない**：上長からの依頼を断るのに理由を必ず書かせると断りにくい圧になり、
     押せないまま依頼が「未対応」で溜まる（＝上長にも状況が伝わらない）。とくに
     **有給は理由を問わないのが原則**なので、そこと逆向きの運用にしない
   ・🚨 **言葉を「対応しない」で統一**（カードのボタン／確認の見出し／実行ボタン／上長の画面）。
     一度「消す」にしたが、押した言葉と確認の言葉が違って迷うため戻した。
     「送信」も採らない（この画面には本物の「送信」があり紛れる）
   ・🚨 `window.confirm()` は使わない決まりなので、**同じカードの中のインラインパネル**で聞く
   ・🚨 理由は**既存の未使用列** `application_requests.recipient_note` に入れた（**DB変更不要**）。
     残業調整の提案が同名の列を「相手のひとこと」に使っている前例がある
   ・🚨 **上長の依頼一覧（`LeaveApprovals`）にその理由を出すところまでやった**。
     出さないと理由を入力させても行き先が無く、**なぜ申請が来ないのか分からない**まま
     （そのために足した機能なので、片方だけでは意味がない）
   ・依頼カードの日付に**曜日**を出した（「9/10（火）」）。🚨 既存の `dowLabel` を使う
   ・🚨 **調整依頼（overtime_proposal）も同じ扱いに揃えた**（ユーザー指示）。
     片方だけ直すと「同じ操作なのに画面によって聞かれ方が違う」になる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (13) 連絡板：受信トレイで読んだら、そのベル通知も既読にする
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 ユーザー依頼。「ベル → 絞り込まれた受信トレイ」の**逆**を作った）
   ・効く場所は**4つ**：受信トレイの一覧から開く（＝依頼の本体）／ベルから開く（従来どおり）／
     グループ・DM を開く／お気に入りから開く
   ・突き合わせは `notifications.reference_id`（＝連絡板のメッセージID）。
     🚨 判定と更新は **`lib/notifications.ts` の `markBellReadForMessages` 1か所**に置き、
        4か所はそれを呼ぶだけにした（**同じ条件を書き写さない**）
   ・🚨 既読にするのは「**届きました**」系の3つだけ
     （`board:notice` / `board:group_message` / `board:dm_message`）。
     **`board:confirm_request`（対応がまだ完了していません）は既読にしない**。
     読んだだけでは対応が終わっていないので、消すと**対応漏れに気づけなくなる**
   ・🚨 ベルは**30秒ごとの自動更新**なので、それだけだと「読んだのに数字が減らない＝効いていない」
     に見える。既読にできたときだけ合図 **`BELL_REFRESH_EVENT`（`fivem:bell-refresh`）** を投げ、
     `App.tsx` の `BellIcon` がその場で読み直す。
     🚨 合図は**行が変わったときだけ**投げる（毎回投げると読むたびに無駄な問い合わせが出る）
   ・🚨 `.update()` は件数も見る（このリポジトリの決まり）。ただし**0件は正常**として扱う
     （自分が送ったメッセージ・すでに既読・そもそも通知が無い場合がある）
   ・🚨 読む操作を遅くしないため `void` で投げっぱなしに呼ぶので、**失敗は関数の中で必ず受け止める**。
     外に出すと通信断のときに「未処理のエラー」になる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **「依頼する相手」の一覧が残業だと短く、休暇だと全員出る**のは**コードのせいではない**。
  `feature_permissions` の設定で、**残業が パート／フロア責任者／一般 で OFF**、
  **休暇は パート だけ OFF** になっているため。
  → **管理画面（役職・機能権限）から変えられる**。コードを触る話ではない
・**申請依頼とは何か**（ユーザーに聞かれたので整理）：上長が部下に
  「この日の残業／休暇を申請してください」と促す仕組み。部下はベルか残業ページの依頼カードから、
  **日付や時刻が入った状態のフォーム**を開いて出すだけでよい
・**「対応しない」は記録が残り、一覧から消えるだけ**（status が変わる）。物理削除ではない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）

   ▼ 今回の分（(7)〜(13)・**すべて未確認**）
   ・**(13) ベルの自動既読**：ベルに連絡板の通知が来ている状態で、**受信トレイから**その
     お知らせを開く → **ベルの数字がその場で減る**（30秒待たない）／グループ・DM でも減るか／
     🚨 **「対応がまだ完了していません」の通知は減らないまま**であること（これが正しい）
   ・**(7)** ベルを押すと残業ページの履歴タブが開き、**該当の依頼が光る**か／
     **プッシュ通知からも同じ場所へ飛ぶ**か
   ・**(8)** 🚨 **数秒待っても履歴タブへ引き戻されないこと**（今回いちばん確かめたい）
   ・**(9)** 「この内容で申請する」を押すと**入力欄の先頭**へ移るか（説明枠でもページ先頭でもない）
   ・**(10)** 予定の勤務時間が**入っている**か
   ・**(11)** 上司のメモが**理由の欄の上にカードで**出て、**理由の欄は空**か／
     休暇の依頼で残業の例文が出ていないか
   ・**(12)** 「対応しない」を押すと確認が出るか／**その他だけ入力欄が出て必須**か／
     他の3つは入力欄が出ないか／**上長の一覧に理由が出る**か／日付に曜日が付いたか／
     🚨 **調整依頼でも同じように出るか**

   ▼ 前回からの残り（🚨 詳しい確認項目はアーカイブの
     「▶ 前セッション（2026-09-10 続き・実機確認／アーカイブの削除を印方式に…）」を見ること）
   ・🚨🚨 **いちばん大事**：送信者側の「**対応状況 ◯/◯人**」「**未対応◯人**」「送信トレイの◯人」が、
     **受信者が削除しても変わらないこと**（＝行を消さず `hidden` の印にした理由。設計の要）
   ・受信トレイ／送信トレイの詳細で☆の隣に 🗃 が出るか／
     🚨 チャンネル（グループ・DM）の投稿には**出ていないこと**
   ・PCで「💬 連絡板　📥 受信トレイ」が左に並び、右端の「🔍 ＋お知らせ送信 💡 通知設定」が
     全部読めるか／スマホの連絡板TOPに今までどおり4つ出ているか
   ・検索：打っている途中で切り替わらないか／**日本語の変換確定の Enter では検索が走らないこと**／
     2文字未満で赤字／場所タブの数字／「古い順」で本当に古いものが出るか／「もっと見る」
   ・A-7 お知らせの削除／B 管理画面の8項目／C ダークモード

2.【未着手】**「相談した日」を依頼カードに出す**（ユーザー要望
   「9/9（月）に相談した 9/10（火）の…」）。
   🚨 **いまのDBに相談した日を持つ列が無い**ので、**列を1つ足す migration が必要**。
   手順は 取り消し版（begin/rollback）→ ユーザー許可 → 適用 → 実測 の順（CLAUDE.md の決まり）

3.【任意・未承認】「対応しない」にしたことを**上長に知らせるか**（いまは一覧を見れば分かるだけ）。
   🚨 通知を新しく足すと「設定が無い＝全員に送る」作りなので、**スタッフ46人に飛ぶ**。
   足すなら通知設定の一覧（`NotificationsTab.tsx`＝2人共通ファイル）まで含めて設計する

4.【既知の矛盾・直していない】`channelListPanel` に `width: 280` と `flex: 1` の**両方**が
   指定されており、flex が勝って実際の幅は画面の約半分になっている。
   右の見出しは `left: 280` から始まるので食い違う。
   **見出しまわりを触る人はまずここを疑うこと**（直すとサイドバーの見た目が大きく変わる）

5.【繰越】`docs/残作業-失敗が消える箇所.md` の残りは **13件**（すべて
   `pages/RoomBookingPage.tsx` ＝**場所予約担当の領域なので触らない**）

6.【繰越】`.then(null, () => {})` **54件**は未着手。
   🚨 一括置換しないこと。受け手が supabase のビルダーなら直す対象、
   素の async 関数（dispatchEmail 等）なら**正当な守り**で、外すと未処理エラーになる

7.【要連絡・繰越】相手（場所予約）へ … 役職の属性化の連絡文はアーカイブにそのまま置いてある。
   eslint の脆弱性（下記8）も一緒に伝えるとよい

8.【繰越】依存パッケージの脆弱性（**対応不要と判断済み**・やるなら要相談）。
   js-yaml(high) / @humanfs/node(moderate) はどちらも eslint 経由の**開発用**で、
   利用者のブラウザに配られるファイルには入らない。`npm audit fix` は42パッケージ追加で
   影響が大きく、`package-lock.json` は2人共通ファイル。直すなら **eslint だけを上げる**

9.【繰越】役職の属性化の後片付け … Edge Function 3本（attendance-notify /
   leave-approved-notify / shift-report-confirmed-notify）に旧処理のグループ計算が
   未使用のまま各5か所残っている／画面が Slack に役職名の文字を渡したままで、
   改名すると引けずに既定値へ落ちる穴がある

10.【繰越】前々セッションからの宿題 … 連絡板グループのシフト調整（案A提示済み・決めること5つ）／
   FAQの戻る（スマホ）／ベルの closeOnTap ／ホームのバナー（備品精算）／
   管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ユーザーの要望：
     ・管理者が**個人ごとの有給の回数**を確認したい
     ・**付与**と**繰越**の機能。管理者設定で管理する
     ・**本人もどこかで見られる**ようにしたい
     ・出す／出さないを **ON/OFF** で切り替えられるようにする。
       ON にしたら「いまの持ち分・利用回数」が見える形にしたい

   🚨 **調べた結果（2026-09-10 本番DBで実測。次の人は調べ直さなくてよい）**
     ・**残数・付与・繰越の表は「無い」**。関連する表は
       `leave_requests` / `leave_approvals` / `leave_request_history` /
       `leave_shift_alert_settings` /
       `paid_leave_encouragement_days` / `_responses` / `_targets` だけ。
       🚨 `overtime_submission_grant*` は**残業の提出許可**で、有給とは無関係（名前で混同しないこと）
     ・`paid_leave_encouragement_*` は「**有給奨励日**」の仕組み（この日に取ってください、の案内と回答）。
       残数の管理ではない
     ・**取得（使った分）は数えられる**。`leave_requests` に
       `leave_type` / `leave_dates`（カンマ区切りの日付）/ `status` があるため。
       実測した値：
         leave_type … その他 / **有給休暇** / 調整休
         status … pending / manager_approved / approved / rejected
         有給休暇 … approved 31件=40日分／manager_approved 6件=7日／pending 1／rejected 1
     ・つまり「**付与がゼロから作る必要のある部分**」で、取得側は既にある値から出せる

   🚨 **作る前に決めること（ユーザーに確認）**
     1. 数える単位は「**日数**」か「**回数**」か（`leave_dates` は日付の配列なので日数が自然）。
        半日有給を扱うなら、いまのデータでは表せない（列が無い）
     2. **どの status を「取得済み」と数えるか**（`approved` だけか、`manager_approved` も含むか）
     3. 付与のやり方 … 入社日からの自動計算にするか、**管理者が手で入れる**か
        （🚨 自動計算は法令（6か月後10日・以後1年ごと…）に踏み込むので、まずは手入力が安全）
     4. 繰越の上限（労基法は2年で消滅）と、年度の区切り
     5. ON/OFF は**誰の単位か**（会社全体で1つ／役職ごと／本人ごと）

   🚨 **作るときの注意**
     ・ON/OFF は既存の「役職・機能権限」（`feature_permissions`）に乗せられる。
       新しい仕組みを作らずに済むか先に検討する
     ・付与・繰越は**記録を貯める仕組み**になるので、CLAUDE.md の決まりどおり
       **掃除の仕組み（または上限）を一緒に設計する**
     ・残数の計算は**1か所（lib）に置く**。管理画面と本人画面が同じ関数を呼ぶこと
       （2か所に書くと必ず食い違う。このリポジトリで何度も起きている）

(B)【新規】権限管理の画面が見にくい（並べ替え・区切り線）
   ユーザーの要望：項目が増えて見にくいので、**項目ごとに並べ直し**、
   **項目ごとに横の区切り線**を入れて見やすくしたい。
   ・対象は管理画面の「役職・機能権限」（`components/admin/FeaturePermissionsTab.tsx`）
   ・🚨 **2人共通のファイル**（CLAUDE.md の一覧にある）。触る前に一声かける
   ・🚨 並び順を変えるだけでなく「**どの項目をどのまとまりに入れるか**」を決める必要がある
     （申請系／勤怠系／連絡板／場所予約／管理 など）。案を出してから実装する
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
