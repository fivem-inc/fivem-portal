# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-01・質問／共有の区別・確認した）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ 本番反映まで完了（master 4203197／リモート一致）
・本番DB：migration 2本とも適用・実測確認済み
  - 20260901060755 … kind列／reads表／制約2本／RLS3本／anon権限0件
  - 20260901072938 … purchase_unconfirmed_count()／INVOKER／anon権限0件
・Edge Function の変更なし。未追跡は AGENTS.md のみ（触らない）
・⚠️ **実機で不具合が2件出て、同日中に直して出し直している**（下の⑥）
・⚠️ **⑦のバッジは実機未確認**。いま未確認は林さんの共有1件だけなので、
  **承認者5名の画面に「1」が付いているはず**。押すまで消えない
・相手（場所予約）の変更は0件。競合なし

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日やったこと
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 🚨 実機で判明：共有しただけの投稿に赤い「回答待ち」が出ていた
   判定が「最後の発言が自分以外か」だけで質問と共有を区別しておらず、
   確定見積書を共有しただけの投稿に、申請者＋承認者5名の**6人全員**へ
   「あなたが答える番」と読める赤が出た。返事は要らないので誰も答えず、
   赤は永久に消えない状態だった。

② 投稿に種別（kind = question / share）を持たせた
   ・赤い「回答待ち」は**質問にだけ**出す
   ・送信欄の2択は**既定を自動で寄せる**（添付あり→共有／本文だけ→質問）ので
     ふだんは何も操作しなくてよい
   ・🚨 **説明文は足さない**（「返事がほしい」等）。「質問」の語だけで通じる
     ＝画面の文字を増やさない（ユーザー決定）
   ・🚨 「最後の投稿」ではなく「**最後の質問**」を見る。
     質問 →（誰も答えない）→ 経理が請求書を共有、でも赤が消えない

③ 赤の出口として「✓ 確認した」を置いた（案A・ユーザー決定）
   ・**質問にも共有にも同じボタン**を出す。押すと赤が消える
   ・🚨 これが無いと、答えをもらった側に赤が移って**往復し続け**、
     最後は誰かが放置して終わる（昨日までの動きがこれ）
   ・赤の意味＝「まだ目を通していない、自分宛の書き込み」
   ・🚨 「未確認 N名」は**出さない**。誰が見ていないかを並べると催促になり、
     共有のたびに関係者全員が急かされる（今回の赤と同型の失敗）
   ・「💬 質問する」を併置。共有を見てその場で聞ける（種別が質問に倒れる）

④ 🚨 昨日の積み残しバグを修正：ファイルだけ／複数ファイルの共有が送れなかった
   purchase_request_comments の CHECK (btrim(body) <> '') が残っており、
   2件目以降の行は body='' で入るため **insert 全体が弾かれていた**。
   「1件＋本文」のときだけ通るので、昨日の実機確認をすり抜けた。
   → 本文が空でもファイルがあれば通るよう制約を緩めた。
   🚨 引き継ぎに「複数ファイル対応」と書いたが、**昨日まで本番では動いていなかった**

⑤ 1,000件の壁を実測した（→ **今日は手を入れないと判断**）
   申請19／明細14／相見積20／質問共有1。稼働 2026-07-04 から年間およそ120件。
   1,000件到達は**8年ほど先**。「2年後に壊れる」は私の見立て違いだった。
   🚨 ただし `purchase_requests` `purchase_request_items`
      `purchase_request_item_quotes` `purchase_request_comments` の4つとも
      **件数指定なしで読んでいる**（.range も .limit も0件）。
      Supabase は指定がないと**1,000行で黙って打ち切る**。
      いちばん先に届くのは**相見積**（1申請に商品3つ×3社なら年1,000行超）。
      症状が「申請は出るのに内訳・共有ファイルだけ静かに欠ける」なので気づけない。
      直すときは場所予約と同じく1,000件ずつ分割して読む

⑥ 🚨🚨 実機で不具合が出たので同日中に直して出し直した（コミット2本目）
   ユーザーが実機で「通知を押しても『この条件に当てはまる記録はありません』」と報告。
   専門エージェント2体（UI/UX・シニアエンジニア）のレビューでも、両者が独立に
   これを最優先で指摘していた。直したのは4件：

   (1) 🚨 **通知から飛んでも申請が出ない**（いちばん重い）
       履歴の期間の既定は「今月」（PurchaseRequestPage.tsx）。確定見積書・納品書・
       請求書は**承認が終わったあとに届く**ので、申請した月より後に共有が起きるのが普通。
       8/31 の申請に 9/1 共有 → 「今月（9月）・申請日」に入らずカードごと描画されず、
       `?focus=` の強調もスクロールも**無音で不発**になっていた。
       → **?focus= が付いていたら期間を自動で「すべて」にする**。
       🚨 初期値ではなく effect で見ること。同じページを開いたまま通知をタップしても
          画面は作り直されないため、1回きりの読み取りでは変化に気づけない
          （useFocusHighlight が同じ理由で effect にしている）。
       🚨 **これは 8/31 に既定を「申請日」に変えたことで悪化させた**。
          それまでの購入日基準なら、購入予定が9月のこの申請は「今月」に入って見えていた。
          **絞り込みの既定を変えるときは、通知の着地に影響しないかを必ず見ること**

   (2) 赤い「回答待ち」が**消せなくなる行き止まり**があった
       Aさんの質問に**ファイルを付けて返信**すると種別が自動で「共有」になり、
       最新の投稿が自分＝ボタンが消える一方、最後の"質問"はAさんのままなので
       赤だけが残り、消す手段が画面から無くなっていた。
       原因は「ボタンを出す判定」と「赤を出す判定」を**別々の式で書いた**こと。
       → 未読の判定を**1か所に統一**（未読＝自分以外の投稿で、自分の last_seen_at より新しい）。
         ボタン＝未読がある／赤＝未読の中に質問がある。
       🚨 CLAUDE.md「同じ意味の判定を2か所に書かない」を踏んだ実例

   (3) 答えた直後に**自分の画面に赤が残った** → **投稿したら確認済みにする**
       （投稿＝目を通したということ。でないと答えた人に催促が出る形になる）

   (4) 通知が「💬 **さんが**〜書き込みました」と名前なしになり得た
       履歴側だけ namesToFetch に自分を入れていなかった（承認画面は入れている）

⑦ 🆕 ナビ「📦 備品精算」のバッジに**未確認件数を足した**（ユーザー発案・同日実装）
   ナビの数字＝「✅ 承認」タブの件数 ＋「📋 履歴」タブの未確認件数。
   開けばタブ側に内訳が出るので、どちらの用事か分かる。
   ・集計は **RPC `purchase_unconfirmed_count()`**（migration 20260901072938・適用済み）
     🚨 **SECURITY INVOKER**。絞っているのはRLSではなく where 句なので、
        INVOKER ならRLSが二重の網になり、将来ポリシーが増えても追従する
     🚨 **申請の数**で数える（投稿の数ではない）。1回の共有でファイル3つなら3行入るため
     🚨 対象は**関わった申請だけ**（申請者・リーダー・依頼された承認者・共有先・
        全員承認の対象者・**過去にそのやりとりに書いた人**）。マネージャー以上全員は含めない
   ・🚨 **usePurchasePendingCount には足していない**。あれはホームの黄色バナー
     「備品購入申請の確認依頼が N件 あります」と承認タブでも共用しており、足すと
     承認0件でもバナーが出て、飛び先の承認タブは空のまま**消せなくなる**。
     → **usePurchaseUnconfirmedCount を新設**し、合算は App.tsx のナビの描画箇所だけ
   ・🚨 履歴タブは **canPurchaseRequest** で判定する（canApprovePurchase ではない）。
     承認の権限で絞ると、共有が届いた**本人（一般社員・パート）の数字だけ常に0**になる
   ・確認・投稿の直後に `purchase-unconfirmed-changed` を発火（30秒待たせない）
   ・バッジの色は**赤のまま**（案①・ユーザー確定）。履歴だけ青にする案は見送り。
     このアプリに青いバッジの前例がなく、共通部品に色を足すと他4ページに波及するため
   ・検算：利用者になりすまして 投稿者本人0／承認者1／無関係0／確認後0 を確認済み
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **赤いバッジは「対応が要る」と読まれる**。返事の要らないものに出すと、
  関係者全員が自分の番だと思い、結局誰も動かず永久に残る。
  出すなら必ず「押せば終わる出口」を同時に置くこと
・🚨 **「確認した」は props で渡さない**。PurchaseCommentThread が自分で読む。
  履歴・承認・管理の3画面に配線すると、必ずどこかで渡し忘れて
  「その画面だけ確認できない」になる（ファイルを開く処理と同じ方針）。
  開いたときだけ読むので通信は増えない
・🚨 **確認の記録は「どこまで見たか」を1人1申請につき1行**。
  投稿1行ごとに持つと、ファイル2件の共有でボタンが2つ並んで押し忘れる
・🚨 **通知は増やしていない**。「確認した」で通知を飛ばすと46人に届き、
  押すこと自体が嫌がられる
・🚨 質問にファイルが付いていても、通知2行目に「共有されました」と書かない。
  返事が要る投稿を共有と読み違えると、そのまま放置される
・承認確定後の相見積もりは書き換え不可（正しい作り）。追記は質問・共有側に載せる
・共有ファイルのパスは必ず `_quote.` を含める（閲覧できる役職の判定に使われている）
・通知の1行目（tpl.template）は変えない（App.tsx が文言で分岐して /board に飛ぶ）
・🚨 **一覧の絞り込みの既定を変えるときは、通知の着地に影響しないか必ず見る**。
  カードが絞り込みで消えると `?focus=` の強調もスクロールも**無音で不発**になり、
  画面には「この条件に当てはまる記録はありません」しか出ない。エラーは出ない
・🚨 **`created_at` を `.slice(0,7)` で月として使わない**。timestamptz はUTC表記で返るので
  日本時間 0:00〜8:59 の申請が前月扱いになる。`toJstDateStr(new Date(...))` を通す。
  購入日（purchased_at / requested_purchase_date）は date 型なので変換不要

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・今日の分／最優先】備品購入申請の質問・ファイル共有
   ・🚨 **ベルの通知を押して、その申請にちゃんと着地するか**（濱口さんで失敗した箇所）。
     期間ボタンが自動で「すべて」になり、カードが黄色く光るのが正しい
   ・林さんの共有投稿から**赤い「回答待ち」が消えているか**
   ・**ファイルを2件添付して送信できるか**（8/31 の実装では失敗していた箇所）
   ・**ファイルだけ（本文なし）で送信できるか**（同上）
   ・「✓ 確認した」を押すと赤が消え、「✓ 確認：お名前」が出るか
   ・**質問に答えたら、自分の画面の赤がその場で消えるか**（押さなくても消える）
   ・**質問にファイルを付けて返信しても「✓ 確認した」が消えないか**（行き止まりの再発確認）
   ・「質問」「共有」の2択が出て、添付すると自動で「共有」に寄るか
   ・「💬 質問する」で本文欄に飛び、種別が「質問」になるか
   ・🆕 **ナビ「📦 備品精算」に数字が出るか**（承認待ち＋未確認の合計）。
     承認者5名には未確認1件ぶんが付いているはず
   ・🆕 **「📋 履歴」タブに未確認の数字が出るか**。「✓ 確認した」を押すと
     **その場で**ナビと履歴タブの数字が減るか（30秒待たされないか）
   ・🆕 ホームの黄色バナー「備品購入申請の確認依頼が N件」が**水増しされていないか**
     （承認待ちの件数のままであること。ここが増えていたら実装ミス）
   ・ダークモードの配色
2.【保留・提案済み】**履歴の検索欄**。案A（常時表示・打つと期間が自動で「すべて」に）
   を提示して**未着手**。対象は品名／内訳の品名／購入先／用途／使用先／指示者／
   申請理由／備考＋申請者名＋金額。全件がすでにブラウザ側にあるのでDB変更は不要。
   🚨 期間が「今月」のまま検索すると0件になり「壊れている」と誤解される
3.【実機確認・繰越最優先】残業の事後報告の時刻制限（8810756・まだ誰も動かしていない）
4.【要送信】幹部へのLINE（B案）🚨 3を確認してから。
   🚨 **文面はセッションが切れて残っていない。送るなら作り直し**
5.【要送信】Slack 2通（開発担当者向け＋Claude向け）も**文面が残っていない**
6.【実機確認】お客様向けFAQ（上桂校・四条本校・スマホ）
7.【実機確認・依頼済み】残業の「実績を報告する」（3ead881）＋連絡文B/C案未送信
8.【繰越】テンキー第2段／Excel出力差分／プッシュ受信時間帯／WordPress設置
9.【要確認・税理士】領収書の保存年数と紙廃棄の可否
   🚨 今回から確定見積書・納品書・請求書も積み上がる。**何年で消してよいかが
      決まらないと消せない**。無料枠1GB・8割（819MB）で警告が飛ぶ
      （supabase/functions/storage-usage-check/index.ts:6）
10.【10月以降】山科教室：FAQ_SCHOOL_OPTIONS と FAQ_COURSE_SCHOOLS 両方に追加
11.【運用・年1回】バックアップの復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 保留（必要になったら）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**ファイルの種類の名札がコード内の固定リスト**（確定見積書／納品書／請求書／
  カタログ・仕様書／その他）。「検収書」を足すだけでデプロイが要る。
  場所予約は「後から設定で足せるように」としたので、揃えるなら設定に出す
・共有ファイルの**元のファイル名を保存していない**（表示は種類の名札＋投稿者＋日時）。
  QuoteFileUploader が元の名前を返さないため。列を1つ足し、
  onChange に第2引数（optional）を足せば対応できる
・種別を3つ（質問／回答／共有）にする案は**見送り**。ボタンが増え、
  答える人が選び間違えると赤が残るため（2026-09-01 案A採用）

・🚨 **バッジまわりを触るときに踏む罠（実装済みだが、次に触る人が必ず読むこと）**
  1. 🚨 **usePurchasePendingCount は3か所で共用**（App.tsx のナビ／
     App.tsx の PurchaseApprovalBanner／PurchaseRequestPage の承認タブ）。
     ここに何かを足すと**ホームの黄色バナーが「確認依頼が N件」と水増しした数を名乗り**、
     飛び先の承認タブは空。**このバナーは閉じる手段が無い**ので永久に残る。
     未確認は usePurchaseUnconfirmedCount に分けてあるので、混ぜ戻さないこと
  2. 🚨 **承認タブは canApprovePurchase（リーダー以上）でしか数えない**。
     未確認を同じ配線に乗せると、**共有を受け取った一般社員・パート本人の数字が
     常に0**になる（いちばん見せたい人に出ない）
  3. 🚨 **対象者は「関わった申請」だけ**。RLSは親委譲でマネージャー以上は
     全社の申請が見えるため、絞りを外すと無関係な共有が全マネージャーのバッジに
     積み上がる。ベル通知は流れるが**バッジは押すまで消えない**
  4. 🚨 **未読の判定は画面（PurchaseCommentThread の isUnseen）とSQL（RPC）の
     2か所にある**。片方だけ直すと「バッジは1件と言うのに、開いても赤いものが無い」
     が起きる。どちらかを触るときは必ず両方を見ること

・🚨 **未確認のバッジを「見た＝自動で確認済み」にする案は採っていない**。
  履歴ではやりとりが1件でもあればスレッドは最初から開いて描画されるので、
  自動にすると「見た人」の記録になり、確定見積書の確認記録としては弱くなる。
  押し忘れが実害になるようなら、そのとき改めて判断する
```

---
