# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---


## ▶▶ 次セッション ここから開始（2026-09-09・申請系の大きな追加10件）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ すべて本番反映まで完了（master a296f56／リモート一致／本番JSの中身で実測確認済み）
・本番DBの変更 **8本**（すべて 取り消しテスト → 適用 → 実測 まで完了）
    20260909071455 ②の列／⑧シフト調整の列＋RPC／⑨の印／宛先解決の関数／通知設定
    20260909073400 上長の操作3つを権限化＋RPCを権限方式に作り直し
    20260909074539 シフト未調整の毎朝の cron
    20260909084555 application_requests（申請の依頼）
    20260909084714 leave_shift_alert_settings（お知らせの時刻・時期を設定に）
    20260909104709 overtime_plan_items（残業の調整案）
    20260909132334 調整案に休憩の列
    20260909142331 社内FAQ（46件 → 51件）
・Edge Function：remind-leave-shift-adjust（新規）／push-dispatch（共用9本）を deploy
・tsc 通過／vite build 成功／rules-of-hooks 0件
・🚨 **2人共通のファイルを5本触った**：App.tsx／useAuth.ts／FeaturePermissionsTab.tsx／
  NotificationsTab.tsx／push-dispatch。**相手（場所予約）へ連絡すること**（本日 push なし・重なり0件）
・未追跡は AGENTS.md のみ（触らない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日作ったもの（すべてユーザー確定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① Googleカレンダーに載る1行を、チェック直下と送信前に見本として出す
   🚨 組み立ては lib/overtimeTypes の buildGcalSummary。gcal-sync と**対で管理**（片方だけ直さない）
   🚨 見本には【申請中】を付けない（ユーザー確定。受理後の形＝みんなが見る形を見せる）
② 「内容を修正する（取り消して再申請）」を履歴カードの表に出した
   ・勤務日のあとは出さない（実績報告に一本化）。取消の確認文もその日で切り替える
   ・🚨 押した瞬間に取消が走るので確認を1段はさむ（無いと誤操作で受理済み申請が消える）
   ・overtime_reports.modified_from_id を足し、受理者に「🔁 修正」と修正前の内容を出す
③④ 申請できる期間の上限：残業＝今期から3期先の期末／休暇・勤務変更＝1年先の前月末
   判定は lib/breakCalc の advanceRequestMaxDate / leaveRequestMaxDate に集約
⑤ 履歴と合計時間数カードを「前期〜3期先」に拡張。履歴に期の見出し（確定／見込み）
   🚨 カードと見出しでデータ元を1つにまとめた（別々に数えると必ず食い違う）
⑥ 残業の調整案（自分用）… 合計時間数カードの下に開閉で置いた
   ・申請前に見込みを組む。**本人だけが見る**（RLSも本人のみ・管理者の閲覧も入れていない）
   ・日付を選ぶとその日の勤務（時間・勤務地・休憩・労働）が出る
   ・残業は休憩を手で直せる（イベントは休憩の取り方が違うため。申請フォームと同じ形）
   ・行ごとに「申請する」→ 内容が入った申請フォームが開く（理由・申請先は本人が入れる）
   ・締め（支給月17日）を過ぎたら自動削除。先の期間の案はその期間の締めまで残る
⑦ 申請の依頼（上長 → スタッフ）
   ・出す場所は**休暇の受理ページ1か所**（残業・休暇とも）。パートへの送信枠の**上**に置いた
   ・受け取った側は残業ページの「履歴・実績報告」タブの先頭に出る
   ・🚨 申請が取り消されたら依頼が自動で「未申請」に戻る（DBトリガー）
⑧ 休暇のシフト調整（未／調整済／確認済（変更なし））
   ・勤怠カレンダーの一覧の**2行目**にチップ（状態の列はスマホで42pxしかなく入らない）
   ・押すとその場で3択。絞り込み「シフト未調整だけ」
   ・対象は**マネージャー受理以降**（有給は経理・社長の受理が残るが、シフトを組む人は動ける）
⑨ シフト未調整の毎朝のお知らせ（3ヶ月前・1ヶ月前）
   ・時刻・時期・送るかどうかを管理画面から設定（通知タブの「🔁 シフト未調整のお知らせ」）
   ・宛先（役職・同じチームに絞るか）は通知一覧から選べる（リーダーを含めるかも選べる）
   ・cron は15分おき。設定した時刻を過ぎた最初の回で送る
⑩ 社内FAQ 46件 → 51件（直す2件・足す5件）＋画面の注意事項3か所
＋ 上長の操作3つを管理画面の権限に：🔁シフト調整の記録／📩申請の依頼／📨パートへ送信
＋ 通知の履歴の日付「9月/8日 17:11」→「2026年9月8日 17:11」／見出しに「既読は30日で自動削除」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 取り消しテストで防いだ事故（実測しなければ本番に出ていた）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨🚨 **RPCの役職チェックが素通りしていた**。`(auth.jwt()->'app_metadata'->>'role')='admin'` は
  app_metadata を持たない人（管理者以外はほぼ全員）で **false ではなく NULL** になり、
  `NULL or false` は NULL、`if not NULL` は成立しない。リーダー・一般・パート全員が
  シフト調整を変更できる状態だった。**coalesce を必ず付ける**
・🚨 毎朝のお知らせが**過去分59件を含めて一斉送信**するところだった
  （未調整67件のうち59件が過ぎた休暇）。「過ぎた休暇日は対象外」を入れて7件に収まった
・🚨 **faq_answer_targets は（回答×役職）に一意制約が無い**（主キーは独自の id）。
  `on conflict do nothing` が何も防がず、2回流すと役職が二重に入る（26→52）。
  **消してから入れる**形にした。この表を触るときは必ず再実行テストをすること
・🚨 プッシュの文言に **NG確定の語**を使っていた（「依頼」）。検証済み語の一覧を見ずに書いた

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 この周辺を触るときの注意（今日ぶん）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **プッシュの新しい語は必ず1通ずつ社長端末に送って試す**。同時に2つ変えると
  どちらが原因か分からない。今日の収穫：**「依頼」単独はNGだが「申請依頼」は表示された**
  ＝語の一部にNG語が入っていても使えることがある（検証済み語の一覧に追記済み）
・🚨 **push-dispatch の EVENT_MAP に無い event_key はプッシュされない**（ベルだけ出て静かに欠ける）。
  新しい通知を作ったら必ず足す。共用9本なので deploy 前に git pull
・🚨 **NotificationsTab の ROLE_GROUP_BROADCAST_EVENTS / PUSH_ROLE_SELECT_EVENTS に登録しないと、
  役職のチェック欄が画面に出ない**（宛先が変えられない）
・🚨 **ベル（App.tsx classifyNotif）とプッシュ（EVENT_MAP）は別管理**。飛び先を変えるときは両方
・🚨 **通知の飛び先で迷わせない**（ユーザー指示）。まとめた通知は1日だけ光らせても残りが
  見つからないので、**絞り込んだ状態で着地**させる（/calendar?shift=pending&view=fyi）。
  絞っているときは必ず画面にそう書く（黙って件数が減ると「無くなった」と誤解される）
・🚨 **同じ判定を2か所に書かない**は今日も何度も効いた：
  期間の上限＝breakCalc／差分＝overtimeShift／宛先解決＝resolve_role_recipients（SQL）／
  カレンダーのタイトル＝buildGcalSummary。**新しい画面はこれらを呼ぶだけにする**
・🚨 **計算できないときに 0 を返さない**。「9:3」のような入力途中で 0:00 と出すと
  「計算した結果が0分」と読める。null を返して「—」にし、理由も出し分ける
・🚨 **JSXで日本語の太字の前後に半角スペースを入れない**（そのまま空白として出る）
・🚨 **ブロックの順番をスクリプトで入れ替えるとJSXが壊れる**（今日1回壊した）。
  隣り合う2つを入れ替えるだけなら、囲って flexDirection: 'column-reverse' にする
・🚨 シェル経由の node -e で `${...}` を含むテンプレート文字列を書くと bash に食われる（今日2回）。
  複雑な置換は Edit ツールを使うか、スクリプトをファイルに書く

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【要連絡】相手（場所予約）へ、共通ファイル5本を触ったことを伝える
2.【実機確認・最優先／今日の分】
   ・修正の再申請 → 受理者に「🔁 修正」と修正前の内容が出るか
   ・カレンダー表示の見本が正しいか（種別ごとに時刻の出方が違う）
   ・履歴の ‹ › が3期先まで進むか／期の見出しの数字がカードと一致するか
   ・調整案：日付を選ぶとシフトが出るか／休憩を直せるか／「申請する」でフォームが開くか
   ・申請の依頼：出す→受け取る→申請する→依頼が「申請済み」になるか／取消で「未申請」に戻るか
   ・シフト調整のチップ／「シフト未調整だけ」の絞り込み／権限のOFFで押せなくなるか
   ・管理画面の権限3つ／通知の設定（時刻・時期・宛先）
   ・**明日の朝から毎朝のお知らせが動く**（対象7件）。飛び方がおかしければ通知設定でOFFにできる
3.【要判断・案を提示済み】**連絡板グループでのシフト調整**（案A）
   休暇の受理で自動投稿 → そこで相談 → 調整済/変更なしをその場でチェック。
   いま LINE でやり取りしている運用の置き換え。決めることは「いつ投稿・どのグループ・
   押せる人・取消時の扱い・毎朝の通知との関係」の5つ
4.【宿題】プッシュ文言の見直し（現在の全文言の一覧を出してまとめてテスト）
5.【宿題】**FAQの戻るボタン（スマホ）**。FAQを開いて戻ると、FAQを開く前の作業ページまで
   戻ってしまう。「FAQの中で1段戻る」を期待している。着手時に実機で動きを追って整理する
6.【前回からの繰越】ベルの closeOnTap とホームのバナー（備品精算）／FAQウィジェットの
   WordPress 設置／Vercel の商用利用の問い合わせ／Notion の貼り直し／幹部への LINE・Slack 2通／
   休暇申請の ✕（9/05・未確認）／テンキー第2段／税理士への領収書の質問／山科教室（10月）／復元リハーサル
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
