# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-11・🚨休暇のSlackが止まっていた／相談した日／対応しないの知らせ／期限リマインド／権限管理の並べ替え）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ **本番反映まで完了**（master `8412b81`／`git ls-remote` で突合済み・作業ツリーは
       docs のこの更新ぶんのみ）
・**DB変更あり**（migration 2本・どちらも取り消し版→許可→適用→実測まで完了）
    `20260911004905_application_request_consulted_on`（相談した日の列）
    `20260911071358_remind_application_request_cron`（期限リマインドの cron ＋ 通知設定3行）
・**Edge Function 6本 deploy**
    send-leave-slack v20 ／ attendance-notify v15 ／ leave-approved-notify v8 ／
    shift-report-confirmed-notify v14 ／ **remind-application-request v1（新規）** ／
    push-dispatch v34
・commit 3本：
    `f3a4e09` 🚨 休暇のSlackが1通も出ていなかったのを修正／宛先を立場コードに一本化／旧処理の削除
    `93feb6f` 相談した日／「対応しない」を上長に知らせる／権限管理を分野ごとに並べ替え
    `8412b81` 申請依頼の期限リマインド
・tsc 通過／vite build 成功／rules-of-hooks **0件**／役職名ルール **0件**／
  🚨 **`deno check` を Edge Function 5本に通した**（下の (14) のとおり、これで不具合が出た）
・✅ **本番JSの中身で実測済み**（毎回、手元の build とバイト数まで一致することを確認）
    main …「fivem:bell-refresh」1／`application_request:due` 1
    OvertimePage（203,117バイト＝一致）…「に相談した」1／「対応しない」と回答がありました」1
    LeaveApprovals …「相談した日」1
    AdminPanel …「上長として行う操作」1／「申請依頼 期限リマインド」1
  🚨 `grep -c` は**一致した行数**なので、1行に固めた本番JSでは何個あっても1になる。
     語ごとに `grep -o … | wc -l` で数えること
  🚨 **どのチャンクに載るかを間違えると「無い」と誤判定する**。
     `lib/notifications.ts` は連絡板とベルの共有部品なので **main-*.js**。
     /overtime は OvertimePage-*.js、管理画面は AdminPanel-*.js（遅延読み込み）
・🚨 相手（場所予約）の変更との**重なりは毎回0件**（push 直前に毎回突合した）
・🚨 **実機未確認**。確認項目は下の「次にやること」1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (14) 🚨🚨🚨 休暇の Slack が **2026-09-10 00:41 から1通も出ていなかった**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   見つけ方：A3（下の (15)）を直すために **`npx deno check` を通したら出た**。
   HEAD でも同じエラーが出る＝**前セッションの段4（`7f46256`）で自分が入れたもの**。

     TS2304 Cannot find name 'supabase'.   send-leave-slack/index.ts:137

   原因：DBへの接続 `supabase` は **`fetchExtraChannels` の中だけ**で作られていたのに、
   宛先を決めるところ（`serve` の中）からも同じ名前を参照していた。そこには届いていないので
   実行すると **ReferenceError** で落ちる。
   🚨 落ちると外側の catch が 500 を返すが、**呼び出し側（画面）は `functions.invoke` の
      結果を見ていない**（`lib/leaveSlack.ts` が捨てている）ので、
      **Slack が1通も出ないまま画面上は成功に見えていた**。
   🚨 役職名を渡すと必ずこの経路を通るため、**休暇の Slack は全イベントが止まっていた**
      （申請・受理・差し戻し・取消）。ベル通知とメールは別経路なので影響なし。

   直し：接続を作る **`adminClient()` を1か所**に置き、2か所がそれを呼ぶ。
   あわせて同ファイルの `catch (error)` が `error.message` を直接読んでいた型エラーも直した
   （catch で受けたものは Error とは限らない）。

   🚨🚨 **気づけなかった理由と、次にやること**
     CLAUDE.md の「直したあと」の手順は `tsc -b` / `vite build` / `npm run lint` で、
     **Edge Function を型チェックする手順が入っていない**。
     → **`npx deno check supabase/functions/<名前>/index.ts` を手順に足すべき**。
        Edge Function を触ったら必ず通すこと。deploy は型チェックをしないので、
        **書き間違いがそのまま本番に出る**。
   🚨 デプロイ済みの版が壊れているかは `npx supabase functions list` の
      `version` と `updated_at` で当たりを付け、`git log -- <ファイル>` と突き合わせれば分かる。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (15) Slack の宛先を「役職名の引き当て」から「立場コード」に一本化した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   これまで画面は役職名（'マネージャー' 等）を渡し、Edge 側が `roles` を**名前で引き当て直して**
   いた。🚨 同じ「マネージャー宛か、リーダー宛か」を
   **ベル通知は `acts_as`・Slack は名前**の2通りで判定していた＝食い違う余地がある。
   ・画面が `approverActsAs`（'leader' / 'manager'）を渡す。宛先が変わるのは **new_request だけ**
   ・`LeaveRequest` は既に同じ値を `apprKey` として持っていたので、**それを1回だけ出して
     Slack・サイト通知・メールの3つが同じ値を使う**形にした
   ・`OvertimeProposalResponse` は `roles(acts_as)` を読むようにした
     （🚨 `!inner` にしない。役職が引けない人でも提案は開けるように）
   ・Edge 側の名前引き当ては**古い画面が開いたままのブラウザ用**に残すが、
     🚨 失敗すると既定の leader チャンネルへ落ちる＝**誤配なのに誰も気づけない**ので log に出す
   🚨 **訂正**：着手時に「役職を改名すると既定値へ落ちる」と説明したが**それは誤り**。
      `rename_role` は `profiles.role_title` も書き換えるので、その経路では落ちない。
      実際に壊れていたのは (14) の方だった。**思い込みで原因を語らないこと**。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (16) 使われていない旧処理を Edge Function 3本から削除した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   attendance-notify / leave-approved-notify / shift-report-confirmed-notify。
   段4 で宛先の解決を `resolve_role_recipients` に移したあと、所属チームを組み立てる処理が
   **計算した結果をどこからも使っていなかった**（`profiles` と `master_options` を
   読むだけ読んで捨てていた＝毎回2回の無駄な問い合わせ）。
   🚨 消す前に**本番の実定義**（`pg_get_functiondef`）を読み、同じ絞り込みが RPC の中にあること・
      「マスタが空なら全グループで判定する」逃げ道の条件まで**同一**であることを確認した。
      ＝「同じ判定が2か所」だったので、画面側を消して RPC 1か所に寄せた。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (17) コメントが実際のコードと食い違っていたのを直した（BoardPage.tsx）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   「アーカイブは開けないので `archived` は常に false」と書いてあったが、
   **同じ日にアーカイブも探すよう直したので、その説明は嘘になっていた**。
   🚨 場所予約の㉜（コメントは正しいのにコードが抜けていた）と**逆向きの同じ事故**。
      直したときは、その処理を説明しているコメントも必ず読み直すこと。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (18) 🚨 eslint の脆弱性は**やらないことにした**（着手して止めた）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   「eslint だけ上げれば済む」と見込んで着手したが、実測すると違った：
   ・`js-yaml` の修正版は **5.x**。`@eslint/eslintrc@3.x` が js-yaml **4系**を固定しているので、
     外すには **eslint 10（メジャー更新）** が必要（eslint 10 は eslintrc を依存から外している）
   ・**9.39.5 が 9系の最後**＝9系のままでは直せない
   ・プラグイン4つ（typescript-eslint / @eslint/js / react-hooks / react-refresh）は
     いずれも eslint 10 に対応している（peerDependencies を実測）
   → それでも止めた理由：**`package-lock.json` は2人共通ファイル**で、しかも
     頼りにしている `rules-of-hooks` と役職名禁止ルールが**黙って効かなくなっても気づけない**。
     やるなら「**ルールが本当に発火することを確かめる**」工程込みで、相手への連絡もセット。
   🚨 どちらも **eslint 経由の開発用**で、利用者のブラウザに配られるファイルには入らない。
      急ぐ話ではない。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (19) 依頼カードに「相談した日」を出した（migration 20260911004905・適用済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 ユーザー要望「9/9 に相談した 9/10 の…」／案①で確定）
   ・`application_requests.consulted_on`（date・任意・null可）
   ・🚨 **`created_at`（依頼を作った日）では代用しない**。口頭で話した日と画面から依頼を
     作る日は数日ずれることがあり、ずれた日を「相談した日」として出すと**画面が嘘をつく**。
     ユーザーにも案②（created_at で代用）を出したうえで①を選んでもらった。
   ・🚨 **任意**。口頭の相談を経ずに依頼することもある。必須にすると存在しない日付を
     入れさせることになる。空のときはカードに何も出さない。
   ・🚨 **時刻は持たない（date）**。「何日に話したか」で足りる。時刻を持つと UTC で切って
     前日になる事故の的が1つ増える。
   ・🚨 **新しい許可は足していない**。既存の `appreq_insert`
     （`requester_id = auth.uid()` かつ「申請の依頼」の機能権限）だけで書ける。
   ・入力欄は依頼シートの「メモ（相談で聞いた内容）」の**すぐ上**（相談まわりをまとめる）
   ・表示は「◯◯さんから、**9/9（月）に相談した** 9/10（火）の残業について…」

   【取り消し版のリハーサル（6項目すべて一致）】
   ・型 date／空を許す／既定値なし／既存3件はすべて空／許可は4のまま／書いた値 2026-09-09
   ・🚨 **なりすましを2通り試した**：依頼した本人＝**1件書けた**／無関係の人＝**0件**。
     本人だけで試すと「RLS がそもそも効いていないから通った」場合を見逃す。
   ・🚨 列を足すだけで終わらせず**実際に書き込むところまで**通し、
     取り消し後に列が残っていないことも実測した。
   【適用後の実測】型 date／空を許す／既定値なし／依頼3件すべて空／許可4のまま／列の説明あり

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (20) 「対応しない」を依頼した上長に知らせるようにした（ベルのみ・DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   きっかけ：ユーザー「コメント入れたのを返すだけでは（足りないのでは）」。
   理由を入れてもらっても、**上長が依頼一覧を開かなければ気づけない**ため届けることにした。

   🚨🚨 **`notifications` に `event_key` を入れると、DBのトリガー
      （`enqueue_push_notification`）が自動で `push_queue` に積む＝スマホが鳴る。**
      「ベルだけ」にしたいときは **`event_key` を付けない**、が正しい書き方。
      （既存の leave-approved-notify のコメント「event_key は付けない」がこれと同じ意味だった）
   ・宛先は依頼した本人1人だけ。🚨 `dispatchSiteNotification` は使わない
     （あちらは宛先の設定が無いと**全員＝46人**に飛ぶ作り）
   ・押すと休暇承認ページの依頼一覧へ行き、その行が光る（🚨 色は既存と同じ `#fff9c4`）
   ・🚨 `LeaveApprovals` で `useFocusHighlight` を**2つ**使うことになるが噛み合わない：
     どちらも同じ `?focus=` を読み、**合致する行があるほうだけ**が focusRef を受け取る。
     ID は別の表（leave_requests / application_requests）の UUID なので取り違えも起きない。
   ・ベルの行は読んだら消える（`closeOnTap: true`）。ここでやることは無いお知らせ。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (21) 権限管理を分野ごとに並べ替え、区切り線と小見出しを入れた（案3・ユーザー確定）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   項目が20個の1列で探せなくなっていた（前セッションからの繰越）。
   ・分野：**休暇／勤怠・時間／申請・精算／連絡・情報／その他**
   ・各分野の中で「**上長として行う操作**」を下に寄せ、小見出しを付けた
     🚨 言葉は自分で作らず、**既にこのファイルのコメントにあった「上長が部下に対して行う操作」**
        に合わせた。最初「他人のぶんを見る・触る」と案を出して**違和感があると指摘された**。
        → 教訓：**文言は既存のコメント・ラベル・FAQ を grep して、社内の言い方に合わせる**。
   ・🚨 **並べ替えと見出しだけ。権限の値・DB・キーは1つも変えていない**
     （HEAD と機械で突合：キー23件・ラベルとも変化なし＝順序と `group`/`bySuperior` の追加だけ）
   ・🚨 見出しは**前の項目と比べて変わり目にだけ**差し込む。分野ごとに別の配列を作って回すと、
     片方に足し忘れて項目が画面から消える（同じ一覧を2か所に書かない）
   ・🚨 `colSpan` は **4 ＋ 役職の数**（機能・全公開・リーダー以上・社長のみ＝4）。役職が増えても合う
   ・🚨 `FeaturePermissionsTab.tsx` は**2人共通ファイル**。
     **ユーザー判断で「先に変えて後から連絡」にした**（値を変えないため）。
     push 直前に突合し**重なり0件**を確認。連絡文は用意してユーザーに渡した。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (22) 申請依頼の「期限」リマインドを作った（migration 20260911071358・適用済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   きっかけ：ユーザー「これ 期限までに入れなければ通知がいくやつかな？？」
   → 調べたら **無かった**。`due_date` は**カードに文字で出すだけで誰も見ていない**。
     リマインドの仕組みは9本あるのに、申請依頼のものだけ無かった（実測）。
     🚨 ユーザーと整理した結論：「対応しない」は**部下が意思表示した＝止まっていない**状態で、
        本当に困るのは**何も返ってこないまま期限が過ぎる**方。だからこちらを先に作った。

   【送るとき（ユーザー確定）】新関数 **remind-application-request**／cron `0 0 * * *`
     ＝ UTC 0時 ＝ **JST 朝9時**（他の日次リマインドと同じ時刻に揃えた）
     ・期限の**前日** → 「📩 明日までに申請が必要な依頼が◯件あります」
     ・期限の**翌日** → 「📩 申請の期限を過ぎた依頼が◯件あります」
     ・両方 → 「📩 申請がまだの依頼が◯件あります（期限を過ぎたもの ◯件）」
     ・相手は**その依頼を受けている本人だけ**。ベル＋プッシュ（メールは既定OFF）
     ・🚨 **毎日は送らない**（毎日届くものは読まれなくなる・ユーザー確定）
     ・🚨 **期限を入れていない依頼は対象外**（決めていない＝急いでいない）
     ・🚨 `status='open'` だけ。applied / dismissed / withdrawn に催促を送ると
       「済んだことを蒸し返す」ことになる

   【🚨 設計で効かせたこと】
   ・**「送った記録」を持たない**。日付を比べるだけで前日／翌日が決まるので記録が不要。
     ＝貯まるものが無いので、CLAUDE.md の「記録を貯めるなら掃除の cron と上限をセットで」に
     **そもそも該当しない**。今回いちばんの判断はこれ。
   ・**通知設定の行を必ず入れた（site/push/email の3行）**。
     🚨 管理画面（NotificationsTab）は `notification_settings` に**行があるチャンネルだけ**を
        出す作りなので、行が無いと「**通知は飛ぶのに管理画面から止められない**」＝画面が嘘をつく。
     🚨 宛先は本人固定なので `recipient` は使わない（null）＝「設定が無い＝全員」の事故は起きない。
        管理画面では宛先を選ばせない側の一覧（`overtime:unreported` 等）に加えた。
   ・**鍵は vault から取る**（`vault.decrypted_secrets`）。SQL にも cron の履歴にも
     鍵の文字を残さない。既存の remind-* と同じ形。
   ・🚨 **プッシュの文面に個人名・依頼の中身を書かない**（ロック画面に出る）。
     🚨 本文に「リマインド」「お知らせ」「メッセージが届き」を入れない。
        **App.tsx が本文の文字で連絡板の通知かを判定している**ので、入れると連絡板扱いになる。
   ・🚨 **依頼を読めなかったら何もせず終わる**。この関数は「未読の前回ぶんを消してから
     付け直す」作りなので、読めないまま進むと**消しただけ**になる（未対応の催促が消える）。
   ・`reference_id` は依頼が1件のときだけ依頼ID、複数なら今日の日付。
     1件なら押したときに該当カードまで光る。複数はどれを光らせるか決められないので一覧に着地。
     🚨 日付を入れるのは `push_queue` の重複判定（user・event_key・reference_id）を
        **日ごとに別ものとして扱わせる**意味もある。App.tsx 側では日付形式なら focus に渡さない。

   【取り消し版のリハーサル（7項目すべて一致・何も残していない）】
     設定3行（site=true / push=true / email=false）／cron 1件／時刻 `0 0 * * *`／active=true／
     鍵は vault から取っている／同名の重複なし。
     取り消し後：設定0行・cron 0件・**既存の cron は18本のまま**。
   【適用後の実測】設定3行・cron 1件・cron 全体 **18→19本**

   🚨🚨 **cron を入れる＝いつ誰に飛ぶかが決まる。入れる前に対象を数えて伝えること。**
     今回、適用前に実データを数えたら **期限 9/10 の未対応の依頼が2件**あり、
     **受け取るのは本人以外の2名**（うち1名はプッシュ購読あり）だった。
     ＝適用した瞬間、その日の朝9時に本物のスタッフへ飛ぶ状態。
     ユーザーに件数と人数を伝え、「**テストなので飛んで大丈夫**」と回答を得てから適用した。
     🚨 実名は出さずに「自分あてか・何人か」だけを数えた（このリポジトリは Public）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **`notifications` の `event_key` はプッシュのスイッチ**。トリガー
  `enqueue_push_notification` が「event_key が入っていて購読がある」ときだけ
  `push_queue` に積む。ベルだけにしたいなら付けない。
・🚨 **push-dispatch は `notification_settings` の push 行が無いと「送る」**
  （`pushEnabled.get(key) === false` のときだけ止める）。行が無い＝止められない。
・🚨 **管理画面の通知設定は「行があるチャンネルだけ」出す**。新しい通知を作ったら
  必ず `notification_settings` の行も入れる。入れないと管理画面から止められない。
・**`application_requests` にはすでに掃除の cron がある**
  （`purge-application-requests-daily`／済みは90日、未対応は期限から60日で削除）。
  新しい掃除は要らない。
・**日次リマインドはすべて朝9時（JST）**＝cron `0 0 * * *`。新しく作るならここに揃える。
・**cron の鍵は vault から取る形が既定**（`decrypted_secrets` の `service_role_key`）。
  直書きすると本番DBに鍵が残る。
・**`resolve_role_recipients`（本番の実定義を確認）は所属チームの絞り込みを自分でやる**。
  Edge Function 側で同じ計算をする必要はない（(16) で消した）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**連絡板の見出しの矛盾は直さない**（2026-09-11 ユーザー確定「今問題ないので このままでいい」）。
  `channelListPanel` に `width: 280` と `flex: 1` の**両方**が指定されており、
  `flex: 1` の `flex-basis: 0%` が `width` を上書きするため、**PCのサイドバーは280pxではなく
  画面の約半分**ある。右の見出しは `left: 280` から始まるので食い違っている。
  🚨 **見出しまわりを触る人はまずここを疑うこと。** 直すとサイドバーが細くなり見た目が変わる。
・**2人共通ファイルの「値を変えない変更」は、先に実装して後から連絡でよい**
  （2026-09-11 ユーザー判断）。ただし **push 直前に必ず突合**すること
  （`git diff --name-only HEAD...origin/master`）。動きや値が変わる変更・DB変更は今までどおり事前相談。
・**eslint の脆弱性は対応しない**（(18) の理由）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）

   ▼ 🚨 **今朝9時に初回が動く**（2026-09-11 8:03 時点で、9時に**2件・2名**へ飛ぶ見込み）
   ・その2名のベルに通知が来ているか／プッシュが鳴ったか（1名は購読あり）
   ・押すと残業ページの履歴タブが開き、**該当の依頼カードが光るか**（1件なので光る想定）
   ・🚨 **同じ通知が2つ並んでいないこと**（未読の前回ぶんを消してから付け直す作り）
   ・🚨 **翌日にもう一度飛んでいないこと**（前日と翌日の2回だけ。毎日ではない）
   ・管理画面 → 通知設定 → ⏰ に「申請依頼 期限リマインド（本人・前日と翌日の朝）」が出るか

   ▼ (14) **休暇の Slack が復活しているか**（いちばん確かめたい）
   ・次の休暇の申請・受理で Slack に流れるか。流れなければ Supabase の Logs に理由が出る
     （今回、引き当てに失敗したら log に出すようにした）
   ・🚨 **new_request がマネージャー宛のときに manager チャンネルへ行くか**
     （立場コードを渡すようにした所）

   ▼ (19)(20)(21)
   ・依頼シートに「相談した日（任意）」が出て、入れると本人のカードに
     「9/9（月）に相談した 9/10（火）の…」と出るか／**空のときは何も出ないか**
   ・「対応しない」を押すと**上長のベルに届くか**／🚨 **スマホが鳴らないこと**（ベルだけ）／
     押すと休暇承認ページの依頼一覧の該当行が光るか／読んだらベルの行が消えるか
   ・管理画面「役職・機能権限」が**分野ごとに区切られて読みやすくなったか**／
     🚨 **20項目すべて残っているか**／「上長として行う操作」の小見出しが出るか／
     🚨 **権限の値が変わっていないこと**（チェックの入り方が前と同じか）／ダークモード

   ▼ 前々回からの残り（🚨 詳細はアーカイブの
     「▶ 前セッション（2026-09-10 続き2…）」と「（2026-09-10 続き…）」を見ること）
   ・🚨🚨 **いちばん大事**：送信者側の「**対応状況 ◯/◯人**」「**未対応◯人**」「送信トレイの◯人」が、
     **受信者が削除しても変わらないこと**（＝行を消さず `hidden` の印にした理由。設計の要）
   ・申請依頼のベル → 数秒待っても履歴タブへ引き戻されないこと（前回直した不具合）
   ・連絡板：受信トレイで読むとベルの数字がその場で減るか／
     🚨「対応がまだ完了していません」は減らないままであること
   ・連絡板の検索（変換確定の Enter で走らないこと・場所タブ・古い順・もっと見る）
   ・A-7 お知らせの削除／B 管理画面の8項目／C ダークモード

2.【✅ 2026-09-11 に実施】**Edge Function の型チェックを手順に入れた**（(14) の教訓）。
   CLAUDE.md の「直したあと」に `npx deno check supabase/functions/<名前>/index.ts` を足した。
   ・あわせて **全40本を通して基準を取った**：**TS2304（名前が見つからない＝本物の不具合）は 0件**。
     型定義のズレ等が6本に計12件（gcal-sync 1／push-dispatch 1／receipt-bulk-zip 2／
     send-purchase-slack 1／send-push 6／slack-notify 1）。CLAUDE.md に一覧を書いてある。
     🚨 **自分が触った関数で件数が増えたら、それは自分が入れたもの**、と分かる形にした。
   ・🚨 **CLAUDE.md は2人共通の決まりを置く場所なので、相手に伝えること**（下の 5 に連絡事項）。
   ・🚧 **残っている小さな宿題**：TS18046（`catch (e)` の値をそのまま `.message` で読んでいる）が
     gcal-sync / send-purchase-slack / slack-notify の**3本に1件ずつ**ある。動きはするが、
     Error 以外が投げられると落ちる。直すと deploy が要るので**今回は手を付けていない**
     （🚨 3本とも**2人共通の Edge Function**）。

3.【繰越】`docs/残作業-失敗が消える箇所.md` の残りは **13件**（すべて
   `pages/RoomBookingPage.tsx` ＝**場所予約担当の領域なので触らない**）

4.【繰越】`.then(null, () => {})` **54件**は未着手。
   🚨 一括置換しないこと。受け手が supabase のビルダーなら直す対象、
   素の async 関数（dispatchEmail 等）なら**正当な守り**で、外すと未処理エラーになる

5.【要連絡・繰越】相手（場所予約）へ …
   ・🚨 **CLAUDE.md に「Edge Function を触ったら `npx deno check` を通す」を足した**（上の 2）。
     2人共通の決まりなので必ず伝える。**きっかけは、型チェックをしていなかったせいで
     休暇の Slack が約1日1通も送られていなかったこと**（(14)）
   ・**権限管理の並べ替え**（(21)・もう出してある。連絡文はユーザーが持っている）
   ・役職の属性化の連絡文はアーカイブにそのまま置いてある
   ・eslint を上げない判断（(18)）も伝えるとよい

6.【繰越】役職の属性化の後片付け … 画面が Slack に役職名の文字を渡している所は (15) で直した。
   残りは **Edge Function 3本（attendance-notify / leave-approved-notify /
   shift-report-confirmed-notify）の旧処理**＝(16) で削除済み。
   🚨 **この項目はこれで終わり**。次は無し。

7.【繰越】前々セッションからの宿題 … 連絡板グループのシフト調整（案A提示済み・決めること5つ）／
   FAQの戻る（スマホ）／ベルの closeOnTap ／ホームのバナー（備品精算）／
   管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ユーザーの要望：
     ・管理者が**個人ごとの有給の回数**を確認したい
     ・**付与**と**繰越**の機能。管理者設定で管理する
     ・**本人もどこかで見られる**ようにしたい
     ・出す／出さないを **ON/OFF** で切り替えられるようにする。
       ON にしたら「いまの持ち分・利用回数」が見える形にしたい

   🚨 **調べた結果（2026-09-10 本番DBで実測。次の人は調べ直さなくてよい）**
     ・**残数・付与・繰越の表は「無い」**。関連する表は
       `leave_requests` / `leave_approvals` / `leave_request_history` /
       `leave_shift_alert_settings` /
       `paid_leave_encouragement_days` / `_responses` / `_targets` だけ。
       🚨 `overtime_submission_grant*` は**残業の提出許可**で、有給とは無関係（名前で混同しないこと）
     ・`paid_leave_encouragement_*` は「**有給奨励日**」の仕組み（この日に取ってください、の案内と回答）。
       残数の管理ではない
     ・**取得（使った分）は数えられる**。`leave_requests` に
       `leave_type` / `leave_dates`（カンマ区切りの日付）/ `status` があるため。
       実測した値：
         leave_type … その他 / **有給休暇** / 調整休
         status … pending / manager_approved / approved / rejected
         有給休暇 … approved 31件=40日分／manager_approved 6件=7日／pending 1／rejected 1
     ・つまり「**付与がゼロから作る必要のある部分**」で、取得側は既にある値から出せる

   🚨 **作る前に決めること（ユーザーに確認）**
     1. 数える単位は「**日数**」か「**回数**」か（`leave_dates` は日付の配列なので日数が自然）。
        半日有給を扱うなら、いまのデータでは表せない（列が無い）
     2. **どの status を「取得済み」と数えるか**（`approved` だけか、`manager_approved` も含むか）
     3. 付与のやり方 … 入社日からの自動計算にするか、**管理者が手で入れる**か
        （🚨 自動計算は法令（6か月後10日・以後1年ごと…）に踏み込むので、まずは手入力が安全）
     4. 繰越の上限（労基法は2年で消滅）と、年度の区切り
     5. ON/OFF は**誰の単位か**（会社全体で1つ／役職ごと／本人ごと）

   🚨 **作るときの注意**
     ・ON/OFF は既存の「役職・機能権限」（`feature_permissions`）に乗せられる。
       新しい仕組みを作らずに済むか先に検討する
     ・付与・繰越は**記録を貯める仕組み**になるので、CLAUDE.md の決まりどおり
       **掃除の仕組み（または上限）を一緒に設計する**
     ・残数の計算は**1か所（lib）に置く**。管理画面と本人画面が同じ関数を呼ぶこと
       （2か所に書くと必ず食い違う。このリポジトリで何度も起きている）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
