# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-16〜17・連絡板を全体で使い始めた／見えていなかった3件を直した／お客様向けFAQをホームページに設置）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】master `8001dcfb` まで push 済み（リモート一致）＋🚨 **未push 1本＝この docs だけ**（次にコードを出すときに一緒に出す）。
  作業ツリーは `AGENTS.md` だけ（管理外）
・本番DBに 3本適用：`20260917161147_faq_public_contact`（FAQの受付時間の設定）／`20260916201950_board_notice_cc_select`（写しを読めるように）／
  `20260916222808_board_recipients_sender_cc`（宛先を読めるように）
・tsc 0／vite build 成功／rules-of-hooks 0／
  BoardPage.tsx の ESLint 48件（HEADと同数）／BoardSettingsTab.tsx 0件
・🚨 **利用者から見える変化**：連絡板を全スタッフに開いた（権限の変更はユーザーが実施）。
  9/16 18:12 に全体お知らせを送信済み（読了確認 9/30 まで・宛先46→45人）。
  代表者（林・濱口・太田恭）の送信トレイに**過去の写しが一気に出る**（太田28・濱口28・林10件）
・🚧 **実機未確認**：宛先の折りたたみ／送信トレイの送信者名／既読の一覧／
  「宛先の候補に出さない人」の設定（Q&A管理専用を選んで保存するところまで）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (72) 連絡板を全体で使い始めた（らくらく連絡網プラスと 10/15 まで併用）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・お知らせの文面は**2通**（連絡板用・らくらく用）を作成。違いはリンクの有無だけ
   ・9/16 18:12 に「【重要】社内連絡ツール変更のお知らせ」を送信済み
   ・🚨 **10/16（金）から連絡板のみ**。それまでは送る人が両方に流す
   ・決めたこと：当面は「**お知らせの受信だけ**」に使う（グループ・DMは現状では使わない）。
     受け取る人は「📥 受信トレイ」を見る／送る人は［＋お知らせ送信］から

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (73) 🚨 濱口さんの「不正な疑いのある通知」＝ Android Chrome の機能（こちらの不具合ではない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・スクリーンショットで確定。Chrome が**サイト単位**で不審と判定し、通知を Chrome 名義の
     グループにまとめて中身を伏せる機能（セーフブラウジング）。**通知自体は届いている**
   ・送信側は正常：9/16 18:12 に `board:notice` を26人へ1通ずつ・全員 sent・失敗0・連投なし。
     文面は「連絡板に新しい投稿があります」。濱口さんの端末は Android（FCM）
   ・直し方（本人の端末で）：警告通知を**開いて**［通知を表示］を選ぶ。
     🚨［ブロック］を押すと二度と届かなくなるので、案内するときは必ず添える
   ・出る人と出ない人がいるのは Chrome のバージョンとセーフブラウジングの設定による（端末固有）
   ・🚧 効かなかったときの根本策は**独自ドメインへの移行**（`vercel.app` は他所と評判を共有する）。未着手
   ・🚨 **`push_queue` の記録は 9/9 が最古**（全社121件）。それ以前は残っていない。
     「この人には今まで届いていない」と判断してはいけない。**直送8本はキューを通らない**し、
     記録も消えている。濱口さんのベル通知は 7/4 から86件あった（＝ずっと使っている）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (74) 🚨 お知らせの「写し（CC）」が代表者の送信トレイに1件も出ていなかった
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （migration `20260916201950`・✅適用・push `872d51fb`）
   症状：送信画面の「他の代表者の送信履歴に加える」（既定ON）を入れても、
   代表者の送信トレイに何も出ない。**原因が2つ重なっていた**。
   ① 画面：写しの対象から**宛先に入っている人を外していた**。全員あてだと代表者も宛先に
      含まれるので、写しが0人になっていた
   ② 権限：`board_messages` の読み取りが「送信者／宛先／管理者」しか見ておらず、
      `cc_user_ids` を見ていなかった。代表者4人のうち `app_metadata.role='admin'` は
      **「管理者」アカウントだけ**（林＝社長・濱口＝マネージャー・太田恭＝マネージャーは違う）
      → **この機能は実質「管理者」にしか効いていなかった**
   ・直し方：①は宛先の人も写しに入れる。②は
     「**写しに入っている かつ いまの代表者設定に入っている**」ときだけ読めるようにした
     （写しに入れれば誰でも読める形にはしない）。判定は `board_is_notice_cc_rep()` の1か所
   ・🚨 **`cc_user_ids` は uuid[] ではなく text[]**。`auth.uid()::text` で比べる
   ・🚨 更新（送信トレイのアーカイブ）は今までどおり送信者と管理者だけ。写しの人に許すと、
     代表者が片付けたときに**送信者本人の送信トレイからも消える**（1行を共有しているため）
   ・取り消し版の検算9項目・適用後の実測7項目とも一致

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (75) 🚨 送信トレイの宛先・既読・読了が**全部0人**と出ていた（同じ形の穴が宛先側にもあった）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （migration `20260916222808`・✅適用・push `a3e21bc7`）
   症状：(74) を直したあと、送信トレイに「宛先0人／既読0 未読0」と出た。**また原因が2つ**。
   ① 権限：`board_message_recipients`（誰あてに送ったかの表）を読めるのは
      「管理者／自分あての行／同じお知らせを受け取った人」だけ。
      🚨 **送った本人が、自分の送り先を確認できない**（自分が宛先に入っていない場合）
   ② 画面：送信トレイが宛先・既読・読了を取りにいく対象が「自分が送ったもの」だけで、
      **写しで入ってきた分とアーカイブ分が抜けていた**
   ・裏取り（林さんの画面で実測）：自分が送信者かつ宛先の 9/8 のお知らせ**だけ**6人と出て、
     ほかは全部0人だった。数字が完全に説明できた
   ・🚨 **ポリシーから `board_messages` を直接参照しない**。`board_messages` 側のポリシーが
     `board_message_recipients` を参照しているため、**RLS が相互参照して無限再帰**になる。
     既存の `get_my_recipient_message_ids()` と同じく **security definer の関数**にして避けた
     （`board_can_see_recipients(uuid)`）
   ・実測：無関係の人に見える宛先の行数は **46 → 46 で変化なし**（適用前後で確認）

   🚨🚨 **(74)(75) に共通する教訓**：**本文が読めることと、付帯情報が読めることは別**。
      お知らせは「本文・宛先・既読・読了」の4つの表に分かれており、権限もバラバラ。
      片方だけ開けると、画面は**エラーを出さずに「0人」と嘘をつく**。
      連絡板まわりで「なぜか0件」を見たら、**まず RLS を4つとも当たること**。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (76) 送信トレイの見た目と「宛先の候補に出さない人」（push `a3e21bc7` / `c7bceeca`・DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・**宛先はふだん閉じる**（「宛先 45人　▼ 表示する」）。46人あてだと名前が本文の前を埋めるため。
     🚨 **受信トレイと送信トレイで見た目が食い違っていた**（受信＝全員展開・送信＝10人まで表示）。
     `RecipientTags` という部品に切り出し、**両方が同じものを使う**ようにした。
     開いたかどうかは部品の中で持ち、呼ぶ側が `key` にお知らせの id を渡してリセットしている
     （使わなくなった `showAllOutboxRecipients` は7か所の参照ごと削除）
   ・**送信トレイの一覧に送信者名**を出す（自分のものは「自分」）。写しで他人のお知らせも並ぶため
   ・🆕 **管理画面 → 連絡板 →「👤 宛先の候補に出さない人」**（`app_settings` の
     `board_recipient_exclude_user_ids`）。選んだ人は送信画面の候補・「全員」・区分ボタンの
     **すべてから消える**。🚨 除外は `activeOthersForCompose` の**1か所**で効く（2か所に書かない）
   ・きっかけ：**FAQ専用の「Q&A管理専用」が全員あての宛先に入っていた**。
     既読0件・プッシュ0台で、FAQ の更新記録にも0件。人が使わないアカウント。
     🚨 放置すると**未読が永久に1人分残り、読了確認も完了にならない**
   ・今日のお知らせからは**本番DBで1行削除して外した**（46→45人・既読12・確認済み6は不変）
   ・🚨 人を選ぶ一覧（90行）を `ProfileMultiPicker` に切り出した。
     「代表者CC」「グループ作成できる人」に**同じものが2回書き写されていた**ため。
     **新しい設定だけがこの部品を使う**（既存2つは動いているので触っていない）。次も部品を使うこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (77) 電車遅延の扱い（太田恭子さんの依頼・**設計の相談資料まで。実装していない**）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   依頼：電車遅延で遅刻すると記録が「−2時間」になり、残業で埋めようとする人が出そう。
   ・いまの計算は `buildWorkDiff`（`lib/overtimeShift.ts:269`）の
     「実際の労働 − 通常シフトの労働」1か所に集約されている
   ・🚨 **当初案（差分の式そのものを変える）は取り下げた**。差分は経理が Excel で受け取る数字で、
     そこに「実際には働いていない時間」を混ぜると**残業（割増の対象）と区別できなくなる**。
     → 式は据え置き、**免除分を別の項目で持つ**形に変えた
   ・法令（調べた結果・出典は相談資料に）：
     遅延分を**払う義務はない**（ノーワーク・ノーペイ／民法536条1項）／**払うのも適法**
     （就業規則への追記が必要）／上限の決まりは**ない**／
     🚨 **実際の遅刻時間を超える控除は労基法91条の制裁**になる（いまの作りは問題なし）
   ・🚨 **いちばんの論点は「正社員のみ」にすること**（パート有期法8条）。近い判例（精皆勤手当）は
     最高裁2件とも**不合理**。「正社員だから」は理由になりにくい。
     **2026-10-01 に同一労働同一賃金の指針の改正が施行**される
   ・ユーザーの回答：対象は**正社員のみ**／(1) 勤務扱いにするか・(3) 対象の事由は**保留**／
     証明書は3案から選び直し（案ア＝交通手段ごとに証明の方法を分ける、を推奨）
   ・🚧 **次**：太田さんと相談 → 決まれば実装。相談資料は会話に残してある（Googleドキュメント用の書式）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (78) 実機確認リストを Artifact にした／濱口さんへの案内を訂正した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・「ポータル実機確認表」 https://claude.ai/artifact/SGdCHrY8HF4FtiehHDAvi9
     （A 連絡板／B こどもシフト表／C 9/15分／D シフト調整／E 自動ログアウト・49項目・リンク付き・
      OK／要修正の印とメモがページに保存される。🚨 **Claude のログインが無いと開けない**）
   ・別の確認担当に渡すときは、【読むだけ】【書き込む】【通知が飛ぶ】を付けたテキスト版を渡した
     🚨 C-15・C-16（打刻忘れの送信）と D（シフト調整の決定）は**実際のスタッフに通知が届く**
   ・🚨 濱口さんへの「不正な疑いのある通知」の案内は**誤りだった**：通知をタップすると普通に開くだけ。
     正しくは**本文ではなく通知右の「∨」で広げる**とボタンが出る（名前は機種・Chrome の版で違う）。
     出ない場合は Chrome →設定→サイトの設定→通知で fivem-portal.vercel.app が「許可」か確認

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (79) お客様向けFAQ：答えが見つからないときの案内をフォーム優先に（migration `20260917161147`・✅適用）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （push `430b5517` / `c8fa4250` / `8001dcfb`）
   ・案内（部品 `ContactLinks`・3画面共通）：**青い全幅「お問い合わせフォームへ／24時間受付中」が主役**、
     その下に「お電話でも承ります」＋075-255-4401（四条本校 総合受付）＋受付時間（文字のリンク）
   ・文言：「申し訳ございません、こちらではお答えできませんでした。お手数ですが、お問い合わせフォームからご連絡ください。」
   ・フォームのリンク先は `https://www.five-m.com/inquiry/#form`、**target=_top**（同じページのままフォームへ）。
     🚨 以前は _blank ＋ページ先頭で「同じページの一番上が別タブで開く」になっていた（実機指摘）
   ・受付時間は**管理画面 → FAQ →「🕐 お客様向けFAQに出す電話の受付時間」**（管理者のみ・見本つき・空は保存不可）
     ＝ `app_settings.faq_contact_phone_hours`（文字の配列）。本番の値：
       月〜金 9:30〜12:15／13:15〜20:00（水は19:45まで）
       土 9:30〜12:40／13:40〜18:00
     🚨 **日曜は出さない**（日曜の受付は会員様向け・新規の問い合わせは月〜土）
   ・未ログインのウィジェットは `faq_public_contact()` で**この鍵だけ**読む（security definer・
     🚨 意図して anon に実行を許可。faq_public_data() と同じ扱い）。読めなければ lib の初期値を出す
   ・番号・呼び名・鍵・初期値は `lib/faq.ts` の1か所（`FAQ_CONTACT_PHONE` ほか）。以前は3か所に直書きだった
   ・検算：未ログインで受付時間が読める／app_settings を直接読めるのは0行／一般スタッフの書き換え0行
   ・公開Q&A「電話番号・受付時間を知りたい」の日曜に「（会員様のみ）」を追記
     （faq_answers `a0000000-…-2701`・更新者「林　晃平（Claudeが代行）」）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (80) ホームページ（five-m.com）に FAQ を設置した（晃平さんが WordPress で対応）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・貼るコードは1行：`<script src="https://fivem-portal.vercel.app/faq-embed.js" async></script>`
     （表示されるのは公開中・今日有効な社外向けQ&Aだけ＝46問）
   ・`/faq.html`：タイトルと画像の直下に案内文＋FAQ。**古い15問は残している**
     案内文：「よくお寄せいただくご質問をまとめています。キーワードで検索するか、下の一覧からお探しください。」
   ・`/inquiry/`：「よくあるご質問」の見出しの下に FAQ／ページ上部に「フォームからすぐにお問い合わせする ▼」／
     見出し「フォームからお問い合わせができます」に **`id="form"`**（本番で確認済み）／
     各コースの画像リンクと見出しの「プログラム詳細」は**非表示**にした（問題があれば戻す）
     案内文：「よくお寄せいただくご質問をまとめています。解決しない場合は、フォームからお気軽にお問い合わせください。」
   ・案内文は他社12件を調べて決めた：**結果を約束する言い方（見つかります／見つかる場合があります）はどこも使っていない**。
     「紹介＋お気軽に」の型（ルネサンス・スクールIE）にした
   ・スタッフへの共有文は晃平さんに渡した（送るのは晃平さん）
   ・🚧 ホームページ側に残っていること（晃平さんの判断）：
     ① `/inquiry/` の電話の画像（月〜金 10:00〜20:00／土 9:30〜18:00）とフッターの時間が、FAQの時間と違う
     ② `/inquiry/` の「下記プログラム一覧より、…」の文（一覧を非表示にしたので言い回しの見直し）
     ③ `/program/jr.html` の「よくあるご質問」の中身が**ウェルネス体操のコピー**になっている

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）
   ▼ 連絡板（今日の分）… 宛先の折りたたみ／送信トレイの送信者名／既読の一覧（誰がいつ読んだか）／
     読了を押した人／管理画面の「宛先の候補に出さない人」で Q&A管理専用を選んで保存
     🚨 送信者名は `allProfiles`（在籍者）から引くので、退職した人が送ったものは「不明」と出る
   ▼ こどもシフト表（⑤）… **昨日作ったまま誰も触っていない**。確認項目はアーカイブの (70)
   ▼ (62)〜(68) の未確認項目もアーカイブに残っている
   ▼ 🔗 すべてを1枚にしたチェック表：(78) の Artifact
   ▼ お客様向けFAQ（(79)）… 見つからない言葉で検索 → 青いフォームのボタン → フォームの位置へ移動／受付時間2行／スマホ
2.【相談】電車遅延（(77)）… 太田さんと。決まれば実装
2-2.【FAQの続き】① `/faq.html` の古い15問のうち、公開Q&Aに無いもの（駐車場・スキーツアーの引率・ケガの対応など）を
   ポータルへ移してから、古い一覧を外す ② プログラムページ用に**コースで絞った表示**（`data-course` 指定・ユーザー「まだ」）
   ③ 公開Q&Aの 075-255-4401 は18件の回答に書かれている（番号を変えるときは全部）
3.【次の実装】⑤ の2回目（⚠️・確認した・勉強会の既存不具合②の修正・案を比べる・Excel・校ごとPDF・
   勤務表の保存の確認に「こどもシフト表◯件が時間外になります」）→ 紙の案1を入れる
4.【継続調査】毎朝の約3秒（アーカイブ (53)）。朝いちの「起動の内訳」の印3を見てから直す
5.【次の実装】シフト調整の**手順9 公開**（`docs/計画-シフト調整.md`。🚨 3つを同時にやらない）
6.【要連絡】場所予約の担当へ … 9/15〜16 に 2人共通ファイル（`App.tsx`／`useAuth.ts`／
   `FeaturePermissionsTab.tsx`／`NotificationsTab.tsx`／`AdminPanel.tsx`／`AdminPanelContext.tsx`／
   `ShiftManagementTab.tsx`／`push-dispatch` v34→v36）を触った。値は変えていない。
   🚨 今日触った `BoardPage.tsx`／`BoardSettingsTab.tsx` は**共通ファイルではない**ので連絡不要
7.【繰越】休暇の穴の第3段（アーカイブ (39)）：`insert_own` を `status='pending'` に締め、
   `update_admin` に WITH CHECK
8.【繰越】表示速度の残り：`useFeaturePublished` の共有
9.【繰越】`docs/残作業-失敗が消える箇所.md` の残り13件（すべて `RoomBookingPage.tsx` ＝場所予約なので触らない）
10.【要判断・繰越】修正依頼の Slack 文面・記録の日付を揃えるか
11.【未着手・繰越】残業申請と勤怠カレンダーの二重登録に気づく仕組み（案だけ）
12.【繰越】管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**お客様向けFAQの電話の案内は「四条本校 075-255-4401・月〜土の2行」**（2026-09-17 確定。日曜は会員様向けなので出さない）。
  フォームが主役・電話は控えめ。案内文は「紹介＋お気軽に」の型（「見つかります」系は使わない）
・**コースで絞ったFAQ表示（プログラムページ用）は「まだ」**（2026-09-17）
・**連絡板は当面「お知らせの受信」だけに使う**（グループ・DMは使わない）。らくらくとの併用は 10/15 まで
・**個人あてのお知らせも、代表者の送信トレイから読めてよい**（2026-09-16 確定）。
  1人あてでも写しが付く作りはそのまま。🚨 気になったときはこの決定を思い出すこと
・**残業の Excel に「事前申請の予定時間帯」は出さない**（2026-09-15 確定「不要」）
・**こどもシフト表の決定は設計書 `docs/計画-管理画面の開放.md` 5-9 の表がすべて**
・**連絡板の見出しの矛盾は直さない**（2026-09-11 確定）。`channelListPanel` の `width:280` と `flex:1`
・**2人共通ファイルの「値を変えない変更」は先に実装して後から連絡でよい**（push 直前に必ず突合）
・**eslint の脆弱性は対応しない**／**事前の遅刻・早退は全員の既定で載せる**
・**休暇の日数は実際に休む日の数。飛び飛びは日付を並べる。月・日の頭にゼロを付けない**
・シフト調整で決まったことは `docs/計画-シフト調整.md`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **連絡板は「本文・宛先・既読・読了」の4つの表に分かれ、権限もバラバラ**。
  片方だけ開けると画面は**エラーを出さずに「0人」と嘘をつく**（今日2回踏んだ）
・🚨 **`board_reads` と `board_confirmations` の select は `true`**（ログインしていれば誰でも読める）。
  引き継ぎの「ログイン済みなら誰でも読み書き」ポリシー是正の対象（繰越の項目）
・🚨 **`notifications` の `event_key` はプッシュのスイッチ**。付けなければベルだけになる
・🚨 **push-dispatch は `notification_settings` の push 行が無いと「送る」**。行が無い＝管理画面から止められない
・🚨 **管理画面の通知設定は「行があるチャンネルだけ」出す**。新しい通知を作ったら必ず行も入れる
・🚨 **push_queue は 9/9 より前の記録が残っていない**＋**直送8本はキューを通らない**。
  「届いていない」の判断に使わない
・**`application_requests` にはすでに掃除の cron がある**（済み90日・未対応は期限から60日）
・**日次リマインドはすべて朝9時（JST）＝cron `0 0 * * *`**
・**cron の鍵は vault から取る形が既定**（直書きすると本番DBに鍵が残る）
・**`resolve_role_recipients` は所属チームの絞り込みを自分でやる**
・🚨 **本番反映の確認に「バイト数の一致」を使わない**。**文言の有無で数える**。
  🚨 ただし**文言が1つも増えない変更**（今日の(74)(75)など）は文字では確かめられない。
  その場合は実機で動作を見てもらうこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ・管理者が個人ごとの残数を見る／付与と繰越を管理画面で入れる／本人も見られる／出す出さないを ON/OFF
   🚨 調べた結果（2026-09-10 実測・調べ直さなくてよい）：**残数・付与・繰越の表は無い**。
     取得は `leave_requests` から数えられる（有給休暇：approved 31件＝40日分／manager_approved 6件＝7日）。
     🚨 `overtime_submission_grant*` は残業の提出許可で無関係
   🚨 作る前に決めること：数える単位／どの status を取得済みと数えるか／付与は手入力か自動計算か／
     繰越の上限と年度の区切り／ON/OFF は誰の単位か
   🚨 作るときの注意：ON/OFF は既存の `feature_permissions` に乗せられないか先に検討／
     記録を貯めるので掃除か上限も一緒に設計／残数の計算は lib の1か所
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
