# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-10 続き・実機確認／アーカイブの削除を印方式に／見出しと検索の作り直し）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ **本番反映まで完了**（master `0c8f369`／`git ls-remote` で突合済み）
・**DB変更あり**（migration 20260910133240・適用と実測確認まで完了）。Edge Function の変更なし
・commit 6本：`4407c49`（受信トレイの詳細にアーカイブ・アイコン統一）／`ea67610`（hidden の印）／
  `8434faa`（送信トレイも☆の隣に統一・戻す処理を1か所に集約）／
  `b806541`（隠れていた通知設定を右へ・スマホに連絡板TOPボタン）／
  `5b3dede`（見出しの並びを最終形に・検索に送信トレイ）／
  `0c8f369`（検索を Enter 実行に・結果の絞り込み・もっと見る）
  ＋合流 `3f2d433`（相手の場所予約とマージ・**重なり0件**）
・tsc 通過／vite build 成功／rules-of-hooks **0件**／新ルール **0件**
・✅ **本番JSの中身で実測済み**（毎回、手元のビルドと数が一致することを確認）
    「自分の受信トレイから完全に消します」1件／
    「あなたがアーカイブから削除したため開けません」1件／
    「送信トレイに戻す」3件／「連絡板TOP」1件／
    「2文字以上入れてください」1件／「この場所には見つかりませんでした」1件
  🚨 `grep -c` は「**一致した行数**」なので、1行に固めた本番JSでは何個あっても1になる。
     語ごとに `grep -o … | wc -l` で数えること
  🚨 本番JSの**ファイル名のハッシュは再ビルドで変わる**ので、名前では判定できない。
     /board は遅延読み込みなので **BoardPage-*.js** を見る（main-*.js には入っていない）
・✅ **実機で確認できたもの**（2026-09-10 ユーザー実施）
    ① **検索結果を押すと中身が開く**（受信・送信・グループとも）＝(6)(a)(d) の直しが効いている
    ② **アーカイブの［削除する］で本当に消え、開き直しても戻ってこない**＝(2) の hidden 方式が効いている
・🚨 **上の2つ以外は実機未確認**（画面を開けないため）。残りは下の「次にやること」1
・🚨 相手（場所予約）も同日 **本番DBに migration 2本を適用済み**
  （20260910105837_room_can_see_contacts_null / 20260910105920_room_basic_settings_role_ids）。
  こちらの領域への影響なし。触ったファイルは RoomBookingPage.tsx / roomBooking.ts /
  eslint.config.js で、こちらは BoardPage.tsx のみ＝**重なり0件**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 実機確認の結果（2026-09-10 ユーザー実施・前回の28か所ぶん）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ A-1 お知らせの送信 … 正常（🚨 いちばん心配していた「宛先の保存で誤って止まる」は起きなかった）
   本番DBで実測：宛先の行 **1件**（自分だけ）／CC **3名**（名簿4名から自分を除いた数＝正解）
✅ A-2 CC ／ ✅ A-3 修正（「(編集済み)」が付く）／ ✅ A-4 ★お気に入り（二度押しで赤が出ない）
✅ A-5 アーカイブ／戻す
⚠️ A-6 アーカイブのまとめて削除 … **「◯件を削除できませんでした」と赤**。
   → 原因を特定して**この日に直した**（下の (2)）
🚨 **未実施**：A-7（お知らせの削除）／B（管理画面の8項目）／C（ダークモード）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (1) 受信トレイで**開いた中**からもアーカイブできるようにし、戻すアイコンを統一した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 実機指摘・ユーザー承認＝案A・DB変更なし）
   きっかけ：一覧の行には 🗃 があるのに、開いた中には無く、いちいち一覧に戻る必要があった。
   ・置き場所は **☆の隣**（一覧の行とまったく同じ位置・同じ形）。
     🚨 下のボタン列（修正する・取消・削除）に混ぜない。あれは**送信者と管理者にしか出ない**ので、
        混ぜると人によってボタンの数が変わり、アーカイブの位置まで動く
   ・🚨 `renderMsg` は **チャンネルの投稿と送信トレイの詳細でも使い回している**。
     第4引数 `inboxArchived`（既定 **null＝出さない**）で明示的に出し分ける。
     条件を付けずに足すと、アーカイブの無い画面にまでボタンが出る
   ・🚨 出し分けは「いま開いているタブ」ではなく **`archivedMessages` に居るか**で見る。
     タブで見ると、中で「受信トレイに戻す」を押したあともボタンが「戻す」のまま残る
   ・**📤 を廃止**。受信トレイのアーカイブ済みの行に既にあった「箱＋上向き矢印」を
     **`UnarchiveIcon` として部品化**し、**3か所が同じこれを呼ぶ**
     （受信の一覧／受信の詳細／送信のアーカイブ）。絵は書き写していない
     🚨 📤 は「文字化けしないことを確かめた絵文字」の一覧に**入っていない**。
        送信トレイの**タブ名**の 📤 は表記として一貫しているので触っていない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (2) 🚨🚨 アーカイブの「削除する」は**作られてから一度も動いていなかった**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （migration 20260910133240・**適用・実測確認済み**）
   原因：`board_message_recipients` は RLS が有効なのに、許可が
   **INSERT / SELECT / UPDATE の3つしか無く、DELETE の許可が1つも無かった**。
   🚨 RLS で弾かれた delete は **エラーにならず0件で返る**ので、
      これまでは画面から消えたように見えて、開き直すと戻ってきていた。
      前回（2eacb0e）で件数を見るようにして、初めて表に出た。

   🚨🚨 **DELETE の許可を足す案は採らなかった。ここが今日いちばん大事な判断。**
   この表は**2つの意味を兼ねている**：
     ① 送信者の記録「誰に送ったか」  ② 受信者の受信箱（archived / hidden）
   本番で実測したところ、**送信トレイの「◯人」・「対応状況 ◯/◯人 完了」・
   「未対応 ◯人」の催促リストは、すべてこの表を数えている**
   （`loadOutbox` の recipients map／受信トレイ詳細の useEffect）。
   → 行を消せるようにすると、**受信者がアーカイブを整理しただけで送信者の記録が書き換わり、
     未対応のまま消えた人を追えなくなる**。回答・提出・承認・確認では実害になる。
   → **「誰に送ったか」は送信者の記録なので、受信者に消させない。**

   直し方：**行を消さず `hidden` の印を立てる**。
   🚨 送信トレイのアーカイブが `board_messages.outbox_hidden` で同じことをしており、
      **この考え方はすでにこのシステムの中に前例がある**（新しい発明をしていない）。
   ・`board_message_recipients.hidden`（boolean・NOT NULL・既定 false）
   ・🚨 **新しい許可は足していない**。既存の `board_recipients_update_own`
     （`user_id = auth.uid()`）だけで書ける
   ・確認パネルの文言を「**◯件を自分の受信トレイから完全に消します。元に戻せません。**」に
     （ユーザー確定・案1。「完全に削除」だけでは**全員から消えた**と読める）
   ・ベルから開いたときに hidden だったら**理由を分けて出す**
     （既存の「送信者が取り消した可能性があります」を出すと**嘘になる**）

   🚨🚨 **`hidden` を除いて読む場所を間違えると穴が開く。**
     除く　　… 受信トレイの一覧 / アーカイブの一覧 / 連絡板の検索（＝受信者向けの3か所）
     除かない … 送信トレイの「◯人」/ 対応状況・未対応リスト / 既読の詳細（＝送信者の記録）

   【本番での検算（取り消し版・全項目一致）】
   ・列を足すだけでなく**実際に印を書き込むところまで**通した
   ・🚨 **スタッフになりすまして**試した：自分の行 **1件**／他人の行 **0件**／
     なりすまし成立 **true**。なりすましが効いていないと「管理者権限で通っただけ」になる
   ・既存120行はすべて false（＝**誰の受信箱も勝手に消えない**）
   ・取り消し後に「列が残っていないこと」も実測
   【適用後の実測】boolean・NOT NULL・既定 false・120行すべて false・
     許可は**5つのまま**（増えていない）・列の説明文あり

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (3) 送信トレイの詳細も「☆の隣」に統一した（2026-09-10 実機指摘・DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   きっかけ：(1) で受信トレイを☆の隣にしたところ、送信トレイの詳細は下に
   ［コピーして作成］［修正する］［🗃 アーカイブ］［完全削除］と**4つ並んで窮屈**で、
   しかも受信トレイと位置が違っていた（ユーザー指摘「これも そろえて」）。
   ・下のオレンジの「🗃 アーカイブ」を**廃止**し、☆の隣のアイコンに統一（ボタンは3つに）
     🚨 廃止した場所にコメントを残した。**ここに戻すと同じ操作が2か所になる**
   ・renderMsg の第4引数を boolean から **`archiveBtn { archived, onToggle }`** に変えた。
     受信は `archiveMessage`、送信は `archiveOutboxMsg` / `unarchiveOutboxMsg` を呼ぶだけで、
     **renderMsg 側に処理を持たせない**
   ・🚨 「送信トレイに戻す」の処理が**一覧の JSX の中に直接書かれていた**ので
     **`unarchiveOutboxMsg` として関数に出し、一覧の行と開いた中の2か所が同じこれを呼ぶ**
   ・状態は JSX の外で組む（`inboxDetailArchiveBtn` / `outboxDetailArchiveBtn`）
   🚨 **既存の限界（今回は直していない）**：送信トレイのアーカイブにあるメッセージは
     **押しても開けない**（`outboxDetail` が `outboxMessages`＝アーカイブを除いた一覧から
     しか探していない）。そのため `outboxDetailArchived` は現状つねに false になるが、
     **状態で分けて書いてある**（開けるようにしたときに「戻せないボタン」になるのを防ぐため）。
     戻すのは一覧のアイコンからできるので業務は止まらない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (4) 🚨 PCで「通知設定」が**見えていなかった**／見出しの並びを作り直した（DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 実機指摘・何度かやり取りして最終形になった。**以下が最終形**）
   原因：ボタンは存在していた。**サイドバーの見出しは幅280px固定**で、そこに
   「💬 連絡板 / 🔍 / ＋お知らせ送信 / 💡 / 通知設定」の**5つ**が入っており、
   通知設定が280pxを越えてはみ出して、**右の見出し（受信トレイ）の背景に
   塗りつぶされていた**（どちらも `zIndex: 50` で、**後にDOMに出てくる右側が勝つ**）。
   🚨 「消えている」ではなく「**下に隠れている**」。`position: fixed` の見出しが2枚並ぶ
      画面で再発しうる。

   【最終形（ユーザー確定）】
     PC　　 … 左に「💬 連絡板　📥 受信トレイ」、右端に「🔍 ＋お知らせ送信 💡 通知設定」
     スマホ … 連絡板TOPの見出しに4つ（現状維持）／トレイの中は「← 💬 TOP 📥 受信トレイ　🔍」
   ・`boardHeaderActions`（ボタン4つ）と `boardViewTitle`（いまの場所の名前）を
     **それぞれ1回だけ定義**し、**置き場所だけ**を画面幅で切り替える
   ・🚨 **題名は「💬 連絡板」と同じ箱（サイドバーの見出し）に入れる**。右の見出しに置くと
     **リストの上に浮いて見える**。理由：右の見出しは `left: 280` から始まるのに、
     サイドバーの箱は `width: 280` と `flex: 1` の**両方**が指定されており、
     **flex が勝って実際の幅は画面の約半分**になっている（channelListPanel）。
     🚨 **この矛盾そのものは残してある**（直すとサイドバーが細くなり見た目が大きく変わる）。
        次に見出しまわりを触る人は、まずここを疑うこと
   ・🚨 題名は `flex: 1` で伸ばさない。伸ばすと幅いっぱいの中で題名が右へ離れて見える。
     右端へ寄せるのは**ボタン群だけ**（`marginLeft: 'auto'`）。役割を分けたので、
     ボタンが増えても題名の位置は動かない
   ・🚨 サイドバーの見出しから `justifyContent: 'space-between'` を外した
     （付けたままだと題名が280pxの右端へ飛んで「連絡板」と離れる）
   ・🚨 **スマホは2つの見出しが同時に出ない**（`showSidebar` で排他）。
     ボタンを右へ移すと**連絡板TOPから「＋お知らせ送信」が消える**。
     **この排他を知らずに「PCと同じにしよう」とすると事故る**
   ・「💬 連絡板」を押すと連絡板TOPへ（🚨 既存の `resetToTop()` を呼ぶだけ）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (5) スマホで「←」から連絡板TOPへ戻れなかった（DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 実機指摘・ユーザー選択＝案B）
   原因：← は **`navigate(-1)`＝ブラウザの戻る**。履歴が1つ戻るだけなので、
   **そこが連絡板TOPとは限らない**（ベルから来た／詳細を開閉した／受信トレイを2回押した
   などで履歴が深くなると、連絡板の外へ出たり同じ一覧に戻ったりする）。
   ・← は**変えない**（詳細→一覧の1段戻りは今までどおり使える）
   ・隣に「**💬 TOP**」を足した（スマホのみ。PCはサイドバーが常に見えている）
   ・🚨 処理は**既存の `resetToTop()` を呼ぶだけ**（連絡板ボタンの再タップと同じ動き）。
     同じ行き先を2通りに書くと、片方だけ直す事故になる。
     `resetToTop` は `bv/bsb/bch/bin/bout/bth` を **replace で**一括クリアする
     （＝1段戻るのではなく根本へジャンプ）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (6) 検索を作り直した（Enter実行・結果の絞り込み・もっと見る）（DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   （2026-09-10 実機指摘3件＋ユーザー承認の案）
   (a) 🚨🚨 **検索結果を押しても開けなかった**（「お知らせはありません」に見えた）
       原因：押したときに `setSearchText('')` をしており、検索の effect にある
       「文字が空になったら検索画面から出る」処理（`navigate(-1)`）が走って、
       **いま開いた画面から引き戻されていた**。
       → 押したときに文字を消さない。effect からも `navigate(-1)` を外した
         （検索画面から出るのは 🔍 の再タップ／← ／💬 TOP）。
       🚨 **同じ画面の中で「押す → 状態を戻す」を混ぜると、遷移が打ち消される**。
   (b) 🚨 **打っている途中で検索が始まって困る**（日本語の変換中に画面が切り替わる）
       → 打っている文字（`searchText`）と実際に検索した言葉（`searchQuery`）を分け、
         **Enter か［検索］ボタン**で実行する（ユーザー確定）。
       🚨 **IME の変換確定の Enter では走らせない**（`e.nativeEvent.isComposing` を見る）。
          見ないと、変換を確定した瞬間に検索が走る。
       🚨 このリポジトリの「打つたびに保存しない・Enter と離れたときだけ」に合わせた形。
   (c) **スマホでトレイの中に 🔍 と検索欄を出した**（ユーザー承認＝案A）
       🚨 出すのは**検索だけ**（4つ出すと375pxに収まらない）。他はTOPに戻れば使える。
       🚨 検索ボタンと検索欄は**それぞれ1回だけ定義**し、置き場所を切り替える。
       🚨 検索欄を開くと見出しが縦に伸びるので、**中身の上余白（`searchPad`）を足した**。
          足さないと一覧の先頭が隠れる。**数値は1か所**に置き、各画面はそれを足すだけ
          （8か所の `paddingTop` に `+ searchPad` を付けた）。
   (d) **検索の対象に送信トレイを追加**。それまで「自分がメンバーのグループ・DM」と
       「受信トレイ」だけだった。アーカイブ済みの送信も対象。
       🚨 結果を押したときの行き先も直した（channel_id が無い結果を**すべて受信トレイとして
          開く**作りで、自分が送ったものは開けなかった）。結果ごとに `searchSrc` を持たせ、
          送信トレイのものは送信トレイで開き、ラベルも「📤 送信トレイ」と出す。
       🚨 `outboxDetail` がアーカイブ済みを探しておらず開けなかったのも直した
          （受信トレイの `inboxDetail` と同じ形にした）。
   (e) **結果の絞り込みと「もっと見る」**（ユーザー承認＝案2 下線タブ）
       それまで各対象30件・合わせて50件で打ち切り、**残りは黙って消えていた**。
       ・場所（すべて／受信トレイ／送信トレイ／グループ／DM／送信メール）の下線タブ。
         🚨 **結果にある場所だけ**出す（0件のボタンを作らない）。数字は表示している中の数。
         🚨 判定は `searchSrcOf` **1か所**。絞り込みも `shownSearch` **1か所**で行い、
            件数と一覧の両方がそれを使う（片方だけ絞ると食い違う）。
       ・並び順（新しい順が既定／古い順）。🚨 **DBの並びに渡す**。画面の中だけで並べ替えると
         「読み込んだ分の中で古い順」になり、**いちばん古いものが出ない＝嘘になる**。
       ・「もっと見る」で **50件ずつ**（`SEARCH_PAGE`）。まだ先がありそうなときは
         「（この先にもあります）」と出す。
         🚨 各対象からこの数ずつ取って合わせてこの数に切る。「全体の新しい順◯件」は
            「各対象の新しい順◯件」の中に必ず入るので、この取り方で正しい。
         🚨 読み込み中に一覧を「検索中...」へ差し替えない（消えて先頭に戻ってしまう）。
       ・絞った結果が0件のときは「この場所には見つかりませんでした」＋［すべてに戻す］。
       ・配色は既存の「すべて／未読／未対応」と**同じ #007bff**（新しい色は足していない）。
       🚨 **抜けているもの**：自分が抜けたグループの過去の投稿は引けない。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（次に触る人へ・画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **「押しても効かない」を疑ったら、まず `pg_policies` で cmd を数える。**
    select tablename, cmd, policyname, qual from pg_policies
    where schemaname='public' and tablename='<表>';
  **RLS が有効なのに、その操作の許可が無い＝いつでも0件**。エラーは出ない。
  実際、今回は DELETE の行が1つも無いことで原因が確定した（画面を開かずに分かった）
・🚨 **`.select('列')` を書く前に、列と読み取り権限の両方を実測する。**
  `board_reads` / `board_confirmations` / `board_message_recipients` に **`id` 列は無い**
  （鍵は message_id と user_id の組）。読み取りは board_reads / board_confirmations が
  「全員読める（using true）」、board_message_recipients と board_favorites が「自分の行だけ」
・✅ **お知らせの削除（deleteNoticeRows）は正しく動く。**関連3表は `error` だけを見る作りで、
  そこは変えなくてよい。理由は2つ：
  ① `board_confirmations` に **DELETE の許可が無く**、`board_reads` は**自分の分だけ**なので
     他人の行は消せない（0件が正常）
  ② 🚨 3表とも `message_id` に **ON DELETE CASCADE** が付いており、
     **本体を消せば自動で消える**（2026-09-10 に本番で実測）。取り残しは出ない
・🚨 **CC の仕組み**（実機で聞かれたので調べた）
  ・相手は毎回選ぶのではなく、**管理画面の固定名簿** `app_settings.board_notice_cc_user_ids`
    （2026-09-10 時点で**4名**。🚨 このリポジトリは Public なので実名は書かない。
     中身は管理画面 → 連絡板の設定で見えるか、`app_settings` の
     `key = 'board_notice_cc_user_ids'` を profiles と突き合わせれば分かる）
  ・CCされた人の**受信トレイには入らない。通知もメールも飛ばない**。
    その人の**送信トレイに並ぶ**（`board_messages.cc_user_ids` を `contains` で読んでいる）
  ・記録されるのは「名簿 − 自分 − 宛先に入っている人」
  ・🚨 チェック「他の代表者の送信履歴に加える」が**出るのは名簿に入っている人だけ**だが、
    `composeIncludeCC` の既定が true なので **名簿外の人が送ってもCCは記録される**。
    ＝「一般の人の送信は常に代表者へ共有／代表者どうしはチェックで切れる」という作り
・🚨 **実機テストの安全策**：お知らせの送信テストは**宛先を自分だけ**にする
  （46名にプッシュとメールが飛ぶ）。CCを入れると代表者3名の**送信トレイ**に1件並ぶが、
  そのお知らせを削除すれば3名の画面からも消える（同じ1件を見ているため）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）
   ・**今日の(2)**：✅ **削除できること・開き直しても戻らないことは確認済み**（ユーザー実施）。
     🚨 **残り**：送信者側で「対応状況 ◯/◯人」「未対応◯人」「送信トレイの◯人」が
     **受信者が削除しても変わらないこと**（＝今回の設計の要。行を消さず hidden の印にした理由）／
     確認パネルの文言が「自分の受信トレイから完全に消します」になっているか
   ・**今日の(1)(3)**：受信トレイでお知らせを開くと☆の隣に 🗃 が出るか／押すと一覧から消えるか／
     アーカイブで開くと「箱＋上向き矢印」に変わるか／
     **送信トレイで開いた中も☆の隣に 🗃 が出て、下のボタンが3つ（コピーして作成・修正する・
     完全削除）になっているか**／送信トレイのアーカイブの「戻す」が白黒の線画になったか／
     🚨 **チャンネル（グループ・DM）の投稿には出ていないこと**（renderMsg を使い回しているため）
   ・**今日の(4)(5)**：🚨 **PCで「💬 連絡板　📥 受信トレイ」が左に並び、右端に
     「🔍 ＋お知らせ送信 💡 通知設定」が全部読めるか**（通知設定が切れていないこと）／
     「💬 連絡板」を押すと連絡板TOPへ戻るか／
     **スマホの連絡板TOPには今までどおり4つ出ているか**（＋お知らせ送信が消えていないこと）／
     スマホで受信トレイに入ると「← 💬 TOP」が出て、TOPを押すと必ず連絡板TOPへ戻るか
   ・**今日の(6)**：✅ **検索結果を押すと中身が開くことは確認済み**（受信・送信・グループとも。
     ユーザー実施）。
     🚨 **残り**：打っている途中で画面が切り替わらないか／
     **日本語を変換して Enter を押しても、確定だけで検索が走らないか**／
     2文字未満で Enter → 赤字で「2文字以上入れてください」／
     スマホでトレイの中に 🔍 が出て、検索欄を開いても**一覧の先頭が隠れないか**／
     場所のタブで絞れるか（数字が合っているか）／「古い順」で**本当に古いもの**が出るか／
     「もっと見る」で増えるか（一覧が消えて先頭に戻らないこと）／ダークモード
   ・**前回ぶんの残り**：A-7 お知らせの削除（1件ずつ／まとめて）／
     B 管理画面（メール定型文・定期リマインド・送信タイミング・会社カレンダー・
     通常シフト・グループの改名/削除・チャンネル権限・既読詳細）／C ダークモード
2.【要連絡・繰越】相手（場所予約）へ。役職の属性化の連絡文はアーカイブの
   「▶ 前セッション（2026-09-09 深夜〜09-10…）」の中にそのまま置いてある。
   eslint の脆弱性（下記6）も一緒に伝えるとよい
3.【繰越】`docs/残作業-失敗が消える箇所.md` の残りは **13件**（すべて
   `pages/RoomBookingPage.tsx` ＝**場所予約担当の領域なので触らない**）
4.【繰越】`.then(null, () => {})` **54件**は未着手。
   🚨 一括置換しないこと。受け手が supabase のビルダーなら直す対象、
   素の async 関数（dispatchEmail 等）なら**正当な守り**で、外すと未処理エラーになる
5.【繰越】役職の属性化の後片付け … Edge Function 3本（attendance-notify /
   leave-approved-notify / shift-report-confirmed-notify）に旧処理のグループ計算が
   未使用のまま各5か所残っている／画面が Slack に役職名の文字を渡したままで、
   改名すると引けずに既定値へ落ちる穴がある
6.【繰越】依存パッケージの脆弱性（**対応不要と判断済み**・やるなら要相談）。
   js-yaml(high) / @humanfs/node(moderate) はどちらも eslint 経由の**開発用**で、
   利用者のブラウザに配られるファイルには入らない。`npm audit fix` は42パッケージ追加で
   影響が大きく、`package-lock.json` は2人共通ファイル。直すなら **eslint だけを上げる**
7.【繰越】前セッションからの宿題 … 連絡板グループのシフト調整（案A提示済み・決めること5つ）／
   FAQの戻る（スマホ）／ベルの closeOnTap ／ホームのバナー（備品精算）／
   管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ユーザーの要望：
     ・管理者が**個人ごとの有給の回数**を確認したい
     ・**付与**と**繰越**の機能。管理者設定で管理する
     ・**本人もどこかで見られる**ようにしたい
     ・出す／出さないを **ON/OFF** で切り替えられるようにする。
       ON にしたら「いまの持ち分・利用回数」が見える形にしたい

   🚨 **調べた結果（2026-09-10 本番DBで実測。次の人は調べ直さなくてよい）**
     ・**残数・付与・繰越の表は「無い」**。関連する表は
       `leave_requests` / `leave_approvals` / `leave_request_history` /
       `leave_shift_alert_settings` /
       `paid_leave_encouragement_days` / `_responses` / `_targets` だけ。
       🚨 `overtime_submission_grant*` は**残業の提出許可**で、有給とは無関係（名前で混同しないこと）
     ・`paid_leave_encouragement_*` は「**有給奨励日**」の仕組み（この日に取ってください、の案内と回答）。
       残数の管理ではない
     ・**取得（使った分）は数えられる**。`leave_requests` に
       `leave_type` / `leave_dates`（カンマ区切りの日付）/ `status` があるため。
       実測した値：
         leave_type … その他 / **有給休暇** / 調整休
         status … pending / manager_approved / approved / rejected
         有給休暇 … approved 31件=40日分／manager_approved 6件=7日／pending 1／rejected 1
     ・つまり「**付与がゼロから作る必要のある部分**」で、取得側は既にある値から出せる

   🚨 **作る前に決めること（ユーザーに確認）**
     1. 数える単位は「**日数**」か「**回数**」か（`leave_dates` は日付の配列なので日数が自然）。
        半日有給を扱うなら、いまのデータでは表せない（列が無い）
     2. **どの status を「取得済み」と数えるか**（`approved` だけか、`manager_approved` も含むか）
     3. 付与のやり方 … 入社日からの自動計算にするか、**管理者が手で入れる**か
        （🚨 自動計算は法令（6か月後10日・以後1年ごと…）に踏み込むので、まずは手入力が安全）
     4. 繰越の上限（労基法は2年で消滅）と、年度の区切り
     5. ON/OFF は**誰の単位か**（会社全体で1つ／役職ごと／本人ごと）

   🚨 **作るときの注意**
     ・ON/OFF は既存の「役職・機能権限」（`feature_permissions`）に乗せられる。
       新しい仕組みを作らずに済むか先に検討する
     ・付与・繰越は**記録を貯める仕組み**になるので、CLAUDE.md の決まりどおり
       **掃除の仕組み（または上限）を一緒に設計する**
     ・残数の計算は**1か所（lib）に置く**。管理画面と本人画面が同じ関数を呼ぶこと
       （2か所に書くと必ず食い違う。このリポジトリで何度も起きている）

(B)【新規】権限管理の画面が見にくい（並べ替え・区切り線）
   ユーザーの要望：項目が増えて見にくいので、**項目ごとに並べ直し**、
   **項目ごとに横の区切り線**を入れて見やすくしたい。
   ・対象は管理画面の「役職・機能権限」（`components/admin/FeaturePermissionsTab.tsx`）
   ・🚨 **2人共通のファイル**（CLAUDE.md の一覧にある）。触る前に一声かける
   ・🚨 並び順を変えるだけでなく「**どの項目をどのまとまりに入れるか**」を決める必要がある
     （申請系／勤怠系／連絡板／場所予約／管理 など）。案を出してから実装する
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
