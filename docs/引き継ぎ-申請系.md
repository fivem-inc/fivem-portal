# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-04・休暇申請の表示まわり）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ 本番反映まで完了（master 56a1ca9／リモート一致）
・**DBの変更なし**。Edge Function の変更もなし
・触ったのは休暇申請の3ファイルだけ。相手（場所予約）とは重なりなし
・⚠️ **実機未確認**（ログインが要るため Claude 側では画面を開けない）
・未追跡は AGENTS.md のみ（触らない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 今日やったこと（休暇申請・コミット1本）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
きっかけ：マネージャーから「誰が最初に受理しているのか、自分が一番なのかが
分からない」。あわせて申請先の選び方と「申請後に直接相談」が伝わっていなかった。

■ 前提：休暇申請は**2人が順に受理する**
　　申請 → ①申請先（リーダー or マネージャー）が受理
　　　　　　↓ そのとき「次のマネージャー」を選ぶ
　　　　 ②マネージャーが受理 → 経理 → 最終受理
　　`approver_id`＝①／`approver2_id`＝②（①が選んだ人）

① 受理画面に「受理の進み具合」を1行出した（LeaveApprovals.tsx）
   　自分が1人目 …「あなたが最初の受理者です」
   　2人目      …「最初の受理者：山本 香澄 ✓」
   🚨 原因は、**受理画面が申請者の名前しか取得していなかった**こと。
      approver_id / approver2_id は持っていたのに名前を引いていないので
      出しようがなかった。受理者の名前も引くようにした（fetchRequests の userIds）

② 申請先の案内をセレクトの「**上**」に出した（LeaveRequest.tsx）
   　「申請先は、原則、休暇を取る日の勤務校リーダーを選んでください」
   🚨 **セレクトの中（option）には書かない**。ブラウザとOSが描くので色も書式も
      指定どおりにならず、iPhone では別のピッカーになる。
      「グレーの部分に色を付ける」は実現できない
   🚨 **「マネージャーでも構いません」とは書かない**（ユーザー確定）。
      書くとそちらが増えて、いま困っている「マネージャーへ直接来る」が増える。
      選択肢にマネージャーが並んでいるので、選べること自体は伝わる。
      「**原則**」の3文字で例外があることだけを示す

③ 完了画面に「申請先へ直接相談」を出した
   　申請しました
   　⚠️ このあと 申請先「山本 香澄さん」へ直接相談してください。
   　　　　　　　　　　　　　　　　　　　[ 確認しました ]
   🚨 **完了バナーは3秒で自動的に閉じてホームへ移動する作りだった**。
      必ず読んでほしい案内を3秒で消すと意味がないので、**案内があるときは
      自動で閉じず「確認しました」を押してもらう**形にした（ユーザー了承）。
      案内が無い他の完了バナー（調整休の登録など）は今までどおり3秒で消える
   🚨 名前を出すため、送信時の申請先を `submittedApproverLabel` に控えている
      （送信後に選択状態がリセットされても消えないように別で持つ）

④ 注意事項の文言を2か所直した
   　「申請先は、**原則**、休暇を取得する日の勤務校リーダーを選択してください。」
   　「申請後、**申請先へ**直接相談してください。」
   🚨 「リーダーへ」ではなく「**申請先へ**」。マネージャーを選ぶこともあるため。
      完了画面の案内とも同じ言い方に揃えている

⑤ 管理画面に②の受理者を出した（admin/LeaveRequestsTab.tsx）
   　申請先
   　山本 香澄
   　└ ② 濱口 美由紀
   ②の名前は**もともと取得済み**で、CSVには出していたのに表には出していなかった。
   列幅を 65 → 85 に広げている

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 この周辺を触るときの注意
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨🚨 **「申請だけでは決まりません」と書かない**（ユーザー指摘で撤回した文言）。
  有給休暇は申請した時点で原則成立する労働者の権利で、上司の許可は条件ではない。
  「相談しないと休暇が決まらない」と読める文章は**法的に問題**になりうる。
  相談してほしい理由（シフト・引き継ぎの調整）で書くか、単に
  「直接相談してください」に留めること
・🚨 **`<option>` の見た目は変えられない**。ブラウザとOSが描くため、色も書式も
  指定どおりにならず、iPhone では別のピッカーになる。
  選択肢に案内を入れたいときは**セレクトの外に出す**
・🚨 **受理者の名前は「引いていない」ことがある**。休暇の受理画面は
  申請者しか引いていなかった。データがあるのに出ないときは、まず取得側を見る
・🚨 完了バナー（BannerSuccess）は **LeaveRequest.tsx の中で独自に定義**されている
  （BusinessTripReport.tsx / ExpenseForm.tsx にも別々の同名部品がある）。
  直しても他の画面には波及しない代わりに、**揃えたいときは各ファイルを見る**
・休暇の受理は2人。①だけ見て「受理済み」と判断しないこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・今日の分】休暇申請
   ・**完了画面が「確認しました」を押すまで消えないか**（3秒で消えたら実装ミス）
   ・受理画面に「あなたが最初の受理者です」／「最初の受理者：◯◯ ✓」が出るか
   ・申請先セレクトの上に案内が出るか
   ・管理画面の申請先に `└ ② ◯◯` が出るか。**表が横に詰まっていないか**
   ・ダークモードの配色
2.【実機確認・繰越】備品購入申請（2026-09-01 の分）
   ・ベルの通知を押して申請に着地するか（期間が自動で「すべて」になる）
   ・ファイル2件／本文なしで送信できるか
   ・ナビ「📦 備品精算」の数字（承認待ち＋未確認）と「📋 履歴」タブの数字
   ・ホームの黄色バナーが水増しされていないか
3.【実機確認・繰越最優先】残業の事後報告の時刻制限（8810756・まだ誰も動かしていない）
4.【要送信】幹部へのLINE（B案）🚨 3を確認してから。**文面は残っていない。作り直し**
5.【要送信】Slack 2通（開発担当者向け＋Claude向け）も**文面が残っていない**
6.【実機確認】お客様向けFAQ（上桂校・四条本校・スマホ）
7.【実機確認・依頼済み】残業の「実績を報告する」（3ead881）＋連絡文B/C案未送信
8.【繰越】テンキー第2段／Excel出力差分／プッシュ受信時間帯／WordPress設置
9.【要確認・税理士】領収書の保存年数と紙廃棄の可否（確定見積書等も積み上がる）
10.【10月以降】山科教室：FAQ_SCHOOL_OPTIONS と FAQ_COURSE_SCHOOLS 両方に追加
11.【運用・年1回】バックアップの復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 秘密情報チェック（2026-09-01 に両担当で合意・完了済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・`client/.env.production` は**ファイル名では止めず、追加行の中身で判定する**
　（SERVICE_ROLE / VAPID_PRIVATE / _PRIVATE_KEY / hooks.slack.com /
　 RESEND / PASSWORD / SECRET）。VITE_ の追加など正当な更新は通る
・理由：`--no-verify` は**そのコミットのフック全体を無効にする**ので、
　名前で止めて毎回 --no-verify を強いるほうがかえって危ない
・**両PCとも差し替え済み・5パターンで確認済み**。控えは
　`C:/Users/kohei/.git-hooks/pre-commit.bak-20260901`
・設定手順とスクリプトは **docs/開発ルール.md** に載せた（新しいPC・新しい人向け）
・🚨 確認するときは `.git/hooks/` ではなく **core.hooksPath の指す先**を見る

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 保留（必要になったら）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**1,000件の壁**（備品購入申請）。実測で申請19／明細14／相見積20／質問共有1。
  年間およそ120件で到達は8年ほど先なので今日は手を入れていない。
  🚨 ただし4つのクエリとも**件数指定なし**（.range も .limit も0件）。
  Supabase は指定がないと**1,000行で黙って打ち切る**。
  いちばん先に届くのは**相見積**。直すときは1,000件ずつ分割して読む
・ファイルの種類の名札がコード内の固定リスト（備品購入申請）
・共有ファイルの元のファイル名を保存していない（備品購入申請）
```

---
