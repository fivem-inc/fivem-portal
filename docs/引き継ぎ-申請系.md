# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知に使ってよい文言 | `検証済み語` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-07〜08・備品精算の「未確認」／FAQ集計の期間と比較）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ すべて本番反映まで完了（master b3355a2／リモート一致／本番JSの中身で実測確認済み）
・本番DBの変更 **3本**（すべて適用・実測確認まで完了）
    20260905165636 社外FAQの利用記録（表・RPC・集計関数・掃除cron・1日の上限）
    20260907154813 その保存期間を 13か月 → **24か月** ＋ **20万行の上限**
    20260907205436 **purchase_unconfirmed_ids()** を新設し、件数関数はそれを数えるだけに
・cron は **15本**（9/05 に1本増やして以降は増えていない。中身の入れ替えのみ）
・tsc 通過／vite build 成功／rules-of-hooks 0件
・⚠️ **実機未確認**（ログインが要るため Claude 側では画面を開けない）
・未追跡は AGENTS.md のみ（触らない）
・相手（場所予約）は 9/05 夜が最後の push。**重なり0件**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨🚨 いちばん重い件：備品精算の「未確認」が2週間気づかれなかった
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
実機報告：「履歴に 1 と出ているが見られない」「ベルから飛ばなかった」「たどり着けず」
　　　　　「当月がないから見れない」「見たけど何をするのか分からない」「2週間気づかなかった」

【原因（本番DBで特定・推測ではない）】
🚨 **数えている範囲と、見せている範囲が違っていた。**
　・バッジ … `purchase_unconfirmed_count()` に**日付条件が無い**（いつの申請でも数える）
　・一覧　 … 既定が**「今月・申請日で絞る」**
　対象は品名「ビジネスフォン（中古）」で **申請日 2026-07-27**。共有の投稿は 8/31 と 9/06。
　7月の申請なので8月も9月も一覧に出ず、**バッジだけが「1」と言い続けていた**。

【直したこと】
① 未確認があれば履歴を**全期間で開く**
　 🚨 `useRef` で**1回だけ**。依存を件数にすると、2件のうち1件を確認した瞬間（2→1）に
　　 再発火して、利用者が選び直した「今月」を勝手に戻す
　　 （カウントは**30秒ごと＋押した直後**にも更新される）。期間ボタンを押したら以後は自動で変えない
　 🚨 これは**応急処置**。絞り込みは scope（自分の分）と kind（精算だけ）でも効く
② 履歴の**先頭に帯**「⚠️ 確認をお願いします（◯件）」。絞り込みを素通りする＝**恒久対策**
　 ・誰が・何を・**何日前か**を出す。🚨「1件あります」では人は動かないが「8日前」なら動く。
　　 放置の長さは、これまで社内のどの画面にも出ていなかった
　 ・［内容を見る］は**期間・種別・対象を3つとも戻して**から運ぶ（1つでも残ると無音で不発）
　 ・動きは `useFocusHighlight` とそろえた（**400ms後にスクロール・6秒でフェード**）。
　　 同じ帯を2回押しても効くよう、いったん null を挟んでから設定する
　 ・🚨 **「まとめて確認する」は置かない**。中身を読まずに全部消せて、機能の意味が無くなる
③ 通知（共有のみ）に「**開いて［✓ 確認した］を押してください**」を足した
＋ `load()` が **error を受けていなかった**のを修正（通信断で「記録はまだありません」と出て、
　 今回とまったく同じ見え方になる経路だった）

【この周辺を触るときの注意】
・🚨 **未確認の判定は `purchase_unconfirmed_ids()` の1か所だけ**。画面で書き直さない。
　 履歴は purchase_requests を**無条件に読み**、RLS でマネージャー以上は**全社の申請が見える**。
　 「関わった人だけ」の条件を画面側で書き忘れると、**無関係な申請にまで帯が出る**
　 （今回の逆＝「帯は3件・バッジは1」。どちらも間違っていないので原因にたどり着けない）
・🚨 帯は **`purchase-unconfirmed-changed` を聞く**こと。聞かないと「✓ 確認した」を押しても
　 バッジは0になるのに**帯だけ残る**＝「押しても消えない帯」になる
・🚨 ボタンの実物は **「✓ 確認した」**。「確認しました」は備品の画面に**存在しない**
　 （休暇申請の完了バナーには「確認しました」が実在する。**同じ意味のボタンが2つの名前**で
　　あるのは CLAUDE.md 違反だが、今回は触っていない。統一するなら別件で）
・🚨 帯の配色は同じ画面の `warnBg/warnBorder/warnText` を再利用した。
　 `#fff3cd` を直に書くと金額差の警告（`#fff8e1`）と並んで**黄色が2種類**になる

【🚨 まだ残っている穴（今回は手を付けていない・次回検討とユーザー確定）】
「そもそも気づかない」側が未解決。
・**プッシュの着地URLに focus が付かない**（`push-dispatch/index.ts:136`）→ ①で救われる
・**ホームのバナーに一度も出ていない**（`BANNER_ONLY_SOURCE_TYPES` に purchase_request が無い）
・**ベルでは「結果報告のみ（対応不要）」に分類**され、`closeOnTap: true`。
　 🚨 タップすると `read=true, banner_dismissed=true` になり、**ベルの一覧から消える**
　 （「薄くなる」ではなく消える）。同じ備品でも承認待ちは `closeOnTap: false` で残る。
　 投稿の通知が既存の source_type を使い回した副作用で、分類が実態と食い違っている
→ 直すなら `event_key === 'purchase_request:comment_added'` で見分ける（`correction:new` に前例）。
　 🚨 ただし **`App.tsx`（2人共通ファイル）を触るので相手への連絡が要る**。
　 🚨 さらに単に false にするだけでは**「消えない通知」を新しく作る**。
　　 「✓ 確認した を押したら、その申請の通知も既読にする」とセットで設計すること。
　 🚨 **今日の①②の効果を実機で見てから判断する**（同時に入れるとどちらが効いたか分からない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 社外FAQの集計に「年ごと・期間を指定」と比較を足した（2026-09-07）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・期間：**月ごと／年ごと／期間を指定**（既定は今までどおり月ごと・今月）
・比較：**比較しない／前月（年なら前年・指定なら直前の同じ日数）／前年同期**
　 件数は「120 回（2026-08 95 回 ／ +25）」の形。質問ごとの表にも「前期間」「増減」の列
・🚨 **増減に色は付けない**。問い合わせは「減ったほうが良い」ので、赤字＝悪いという
　 一般的な感覚と逆になる
・🚨 年ごとのときは「前年」と「前年同期」が同じになるので、後者のボタンを出さない
・保存期間を **24か月**に延ばし、**20万行の上限**（古いものから削除）を足した。
　 🚨 期間を延ばすと最悪時の容量が増える。「1日2万件」だけだと24か月で理論上4GB（無料枠500MB）。
　 「上限を決めたつもりで決まっていない」状態は残さない。20万行 ≒ 60MB
・🚨 日付の境目は **`client/src/lib/faqPeriod.ts`** に切り出した（supabase を読まない側）。
　 境目は間違えても画面上は「それらしい数字」が出るので、機械で確かめられる形にした。
　 **検算22項目すべて一致**（月末・年またぎ・終了日を含むこと・月初の直前が前月末・
　 うるう年 2/1〜2/29 の直前29日間・前年同期・増減の符号）。検算ファイルは実行後に削除済み

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 備品精算の履歴の絞り込みを1行1グループにした（2026-09-07・実機指摘）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
指摘：「今月／先月／すべて がどこで選んでいるのか分かりにくい。似た3種があるので見えにくい」
原因：3グループを1行に詰めており、区切りが `<span width:8>` だけ。
　　　ボタン同士の隙間が `gap:6` なので**差は2pxしかなく**、目には見えなかった。
　　　🚨 さらにスマホで折り返すと**別のグループが同じ行に並んで混ざる**。
　　　🚨 **「すべて」が2つあった**（期間と種別）。同じ文字・同じ見た目
直した：**1行＝1グループ**＋左に見出し（対象／期間／種別・幅34pxで左端を揃える）。
　　　　期間の「すべて」→**「全期間」**。「申請日で絞る」を期間の行の中へ。
　　　🚨 **ボタンの配色は1つも変えていない**（択一トグルの青は 🎨🔒 固定）。
　　　　見えにくさは**並べ方と言葉**で直した

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 設計レビュー（2件とも UI/UXデザイナー＋シニアエンジニアの2体で実施）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 **2体が独立して同じ致命的な穴を指摘する**ことが2回とも起きた。
　 FAQ集計 … `View` 型が contact に質問を持たず「解決しなかった質問の一覧」が作れなかった
　 備品精算 … 帯の判定に「関わった人だけ」が無く、無関係な人に帯が出る
🚨 **指摘は鵜呑みにせず、すべて実測で裏取りしてから採用した。**
　 訂正した例：「StrictMode で2回送られる」は**開発中だけ**（本番ビルドでは起きない）／
　 DB使用量は 27% ではなく **39MB＝約8%**（自分が書いた資料の数字が古かった）
🚨 当初案のままなら、備品精算では「無関係な人に帯が出る」「人の選択を上書きする」
　 「**存在しないボタンを案内する**」の3つを本番に入れていた。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先／今日の分】備品精算の履歴
   ・⚠️ 確認をお願いします（1件）の帯が出るか
   ・［内容を見る］でその申請へスクロールし、**黄色く光って6秒で消える**か
   ・🚨 **［✓ 確認した］を押すと、帯とバッジが両方消える**か
   ・帯が消えたあと「今月」に戻せるか（戻したあと**勝手に全期間に戻らない**か）
   ・絞り込みが3行（対象／期間／種別）に分かれ、スマホで混ざらないか／ダークモード
2.【実機確認】FAQ集計（管理画面 → FAQ管理 → 社外向け → 📋 利用状況の集計を見る）
   ・月ごと／年ごと／期間を指定 が切り替わるか（◀▶ が年のときは年を動かすか）
   ・「前月」を押すと比較の列が増え「◯◯ と ◯◯ を比べています」と出るか
   ・年ごとのときに「前年同期」ボタンが消えるか
   ・🚨 いまは全て0件なので、**表示の形だけ**の確認になる
3.【実機確認・繰越】休暇申請の完了バナーの ✕（9/05 の分・**まだ未確認**）
   ・✕ が出なくなっているか／「確認しました」で真っ黒にならないか
   ・調整休の「登録しました」は従来どおり ✕ が出て3秒で消えるか
   ・🚨 **「真っ黒」の原因は未特定のまま**。再発したら状況を必ず記録する
4.【要判断・次回】ベルの `closeOnTap` とホームのバナー（上の「まだ残っている穴」）
   🚨 App.tsx を触るので相手への連絡が要る。**1の実機確認のあとで判断する**
5.【未実施】🚨 **FAQウィジェットを WordPress に設置**（設置するまで集計は0件のまま）
6.【要判断・最優先級】🚨 **Vercel の商用利用**（`docs/外部サービスの構成と制限.md` §10-①）
   まずサポートに問い合わせる（$0）。放置するとサイト停止＝46名に影響
7.【要対応】**Notion のドキュメントを貼り直す**（27% → 8% の訂正が入っている）
8.【実機確認・繰越】備品購入申請（9/01 の分）・残業の事後報告の時刻制限（8810756）
9.【要送信】幹部へのLINE／Slack 2通（🚨 **文面は残っていない。作り直し**）
10.【実機確認】お客様向けFAQ（上桂校・四条本校・スマホ）
11.【繰越】テンキー第2段／Excel出力差分／プッシュ受信時間帯
12.【要確認・税理士】領収書の保存年数と紙廃棄の可否
13.【10月以降】山科教室：FAQ_SCHOOL_OPTIONS と FAQ_COURSE_SCHOOLS 両方に追加
14.【運用・年1回】バックアップの復元リハーサル

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 作業のしかたで分かったこと
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **`npx tsc` は必ず `client` に降りてから**。リポジトリ直下で打つと
　 「This is not the tsc command you are looking for」と出て**別物を取りに行く**（今回踏んだ）
・🚨 なりすましの検算は `set_config('request.jwt.claims', …, true)` ＋ `set local role authenticated`。
　 今回は**実在の未確認データで**「件数とIDが一致するか」まで確かめた
・🚨 デプロイ確認は**間隔をあけて**取り直す（curl を短い間隔で繰り返さない）。
　 1回目が古くても慌てない。バックグラウンドで `sleep 240` してから取ると1往復で済む
・🚨 `printf` に日本語の「％」を含めるとフォーマット指定と解釈されて落ちる。
　 コミットメッセージは Write でファイルに書くこと（既存ルールどおり）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 保留（必要になったら）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**1,000件の壁**（備品購入申請）。4つのクエリとも件数指定なし。
　 🚨 レビューの指摘で、いちばん先に届くのは**相見積ではなく `purchase_request_comment_reads`**
　　（申請数 × 確認した人数で増える）と分かった。46名なら申請100件で到達しうる。
　　今回の帯はDBの関数で数えているので影響しないが、`fetchPurchaseCommentReads` は要注意
・ファイルの種類の名札がコード内の固定リスト（備品購入申請）
・共有ファイルの元のファイル名を保存していない（備品購入申請）
```

---
