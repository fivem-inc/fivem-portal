# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-10・失敗が静かに消える箇所 28か所を是正）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ **本番反映まで完了**（master `2eacb0e`／`git ls-remote` で突合済み・作業ツリーは
       docs のこの更新ぶんのみ）。**DBの変更なし・Edge Function の変更なし**
・commit 1本（7ファイル・328行追加）。連絡板・設定/マスタの **28か所**
・tsc 通過／vite build 成功／rules-of-hooks **0件**／新ルール **0件**
・✅ **本番JSの中身で実測済み**（デプロイ後に curl 1回）
    BoardPage-DF0orTXS.js … 「宛先を保存できなかったため」1件／
      「CC（写しを送る相手）を記録できませんでした」1件／「お気に入りの解除に失敗しました」2件
    AdminPanel-fHI57NPW.js … 「前のシフトの締め」1件／「グループ名は変えていません」1件／
      「前の設定も消えています」1件
  🚨 **`grep -c` は「一致した行数」**なので、1行に固めた本番JSでは何個あっても1にしかならない。
     語ごとに `grep -o … | wc -l` で数えること（今回これで一度誤読しかけた）
・🚨 **実機未確認**（連絡板も管理画面もログインが要るので、こちらでは画面を開けない）
・🚨 **2人共通のファイルは触っていない**（App.tsx / useAuth.ts / 型 / notificationDispatch とも無変更）。
  相手（場所予約）への連絡は不要。ただし前回ぶんの連絡はまだ残っている（下の「次にやること」2）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 何をしたか：`docs/残作業-失敗が消える箇所.md` の残り43件のうち、**自分の領域を全部**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
supabase の `.update()` / `.delete()` は RLS で弾かれても error にならず「0件成功」で返る。
戻り値を見ていない箇所が残っており、**消せていないのに一覧から消える／保存できていないのに
「保存しました」と出る**状態だった（2026-09-09 に申請まわり8か所を直した続き）。

🚨 **件数は「30件」ではなく 28か所**だった。引き継ぎの数え方と、ESLint が機械で拾う数が違う。
   **目視で数えず、ルールを一時的に効かせて機械で出す**のが速くて正確
   （`eslint.config.js` を sed で複製し、対象ファイルを足した `eslint.measure.config.js` を作って
    `npx.cmd eslint --config eslint.measure.config.js <ファイル>`。測り終えたら消す）。

【連絡板（16か所）】
 ・🚨 **お知らせの削除4連を `deleteNoticeRows()` に集約**（BoardPage の module 直下）。
   「関連3表（board_confirmations / board_reads / board_message_recipients）→ 本体」の順序と
   件数の見方がここ1か所になった。**1件ずつ削除する所（deleteNotice）と、アーカイブで
   まとめて削除する所の2か所が同じこれを呼ぶ**。書き写さないこと
 ・まとめて削除は **消せなかったものを選んだまま残す**（もう一度押せる）＋「◯件は削除しました」
 ・お知らせの修正（saveNoticeEdit）は `error` だけを見て「保存しました」を出していた → 件数まで見る
 ・アーカイブ／受信トレイに戻す／送信トレイの隠す・戻す／お気に入り … **失敗したら見た目を変えない**
   （変えると、画面では消えているのに開き直すと戻ってきて「勝手に付く」ように見える）
 ・お気に入りの**解除の0件は正常**（すでに外れている＝二度押し）。error だけを見る
 ・🚨 **今回いちばん重い1件**：お知らせ送信で `board_message_recipients` の insert が失敗しても
   結果を見ておらず、**誰にも届かないのに送信済みとして扱っていた**。止めて通知・メールも送らず、
   送信トレイを読み直す（読み直さないと、その1件が画面に出ず「消してください」と言えない）
 ・CC の記録の失敗は 🚨 **赤にしない**（お知らせ自体は送れている）。黄色の部分成功にした。
   そのため BoardPage に `partialMsg` を新設（色は OvertimeProposalResponse の部分成功と同じもの）

【設定・マスタ（12か所）】
 ・BoardSettingsTab … チャンネルの権限保存／DMの権限保存（upsert）／既読詳細の ON・OFF
   （消してから入れ直す作りなので、失敗したらボタンも元に戻す。insert で失敗したときは
    「前の設定も消えています」と断る＝黙って元に戻ったように見せない）
 ・NotificationsTab … メール定型文の追加・更新・削除／予約リマインドの保存・ON/OFF・削除／
   リマインド日数の保存。エラー表示が無かったので、3つの部品それぞれに `errorMsg` を新設
 ・OvertimeAdminTab … 通常シフトの締め・削除、会社カレンダーの削除
 ・GroupsTab … グループの改名・削除

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 次に触る人へ（今回の教訓）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨🚨 **件数を見るための `.select('id')` を、列を確かめずに書かない。**
  `board_reads` / `board_confirmations` / `board_message_recipients` には **`id` 列が無い**
  （鍵は message_id と user_id の組・2026-09-10 に本番の information_schema で実測）。
  `id` と書くと「列が無い」エラーになり、**いつも失敗する削除**になっていた。
  → `.select('message_id')`。同様に `company_calendar` は `date`、`reminder_days_settings` は `event_key`。
  **書く前に1回だけ information_schema.columns を引けば全部分かる**
・🚨 **`.select()` は「消した行を SELECT できる権限」も要る**。読めない行を消すと、
  消せていても0件で返って「失敗」に見える。件数まで見るのは
  **その人が確実に読める行だけ**にすること（今回はすべてそう）
・🚨 **順序を「失敗しても、もう一度押せば直る」向きにする。**
  GroupsTab のグループ改名・削除は**順序を入れ替えた**：人の所属を先に、
  一覧のもとになる `master_options` を最後に。先にグループを消す／改名すると、
  直せなかった人が残ったときに**一覧から対象が消えて、探す入口ごと無くなる**
・🚨 **0件で止める作りにしたら、止めたあと「手元の一覧を読み直す」までがセット。**
  OvertimeAdminTab の通常シフトは、失敗したあと `fetchPatterns` を呼ぶようにした。
  読み直さないと、もう一度「保存」を押したときに**すでに消した行**をもう一度消しに行き、
  0件＝失敗と判定されて**何度押しても進めなくなる**
・🚨 **件数0を一律に失敗としない**（前回からの続き）。今回「0件が正常」としたのは
  ①お気に入りの解除（二度押し）②お知らせに読了・既読・宛先が1件も無い場合
  ③既読詳細の設定をまだ一度も保存していない場合

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 依存パッケージの脆弱性（2026-09-10 に調査・**対応不要と判断**）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
push のとき GitHub が moderate 1件を通知する。`npm audit` では**2件**出る：
  high     js-yaml 4.3.1        細工されたYAMLでCPUを食い潰される
  moderate @humanfs/node 0.16.6 再帰コピーがシンボリックリンクをたどる
🚨 **どちらも `eslint` だけがぶら下げている開発用**（client@0.0.0 → eslint@9.39.5 →
   @eslint/eslintrc → js-yaml ／ eslint → @humanfs/node）。
   **利用者のブラウザに配られるファイルには入らない**。動くのは開発PCで lint を実行したときだけ。
🚨 `npm audit fix` は dry-run の実測で **42パッケージ追加・1削除・3変更**（vite 系の
   ビルド道具まで入れ替わる）。しかも `client/package-lock.json` は
   **2人が触るファイル（CLAUDE.md で要相談）**。利用者に影響しない問題のために
   片方の判断でビルド環境を入れ替えない。
→ 直すなら **`eslint` だけを新しくする**（`npm i -D eslint@latest`）。相手に一声かけてから、
  そのあと `npx tsc -b` `npx vite build` `npm run lint` が通ることを確認する。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・今回ぶん】🚨 こちらでは画面を開けないので、動かして確かめてほしい
   ・連絡板：お知らせの削除（1件ずつ／アーカイブでまとめて）→ 消えるか。
     一覧から消えたものが**開き直しても戻ってこない**か
   ・連絡板：お知らせの修正 → 保存できるか（失敗したときだけ編集が閉じないこと）
   ・連絡板：アーカイブ／戻す／★お気に入り → 今までどおり動くか
   ・連絡板：お知らせの送信 → 今までどおり届くか（🚨 いちばん大事。宛先の保存で
     止める処理を足したので、正常時に誤って止まっていないことを必ず確認）
   ・管理画面：メール定型文の追加・修正・削除／定期リマインドの保存・ON/OFF・削除／
     リマインド送信タイミングの保存／会社カレンダーの削除／通常シフトの保存／
     グループの改名・削除／連絡板のチャンネル権限・既読詳細の ON・OFF
   ・ダークモード（黄色の部分成功トーストは暗色の値も入れてある）
2.【要連絡・前回からの繰越】相手（場所予約）へ。役職の属性化の連絡文は
   アーカイブの「▶ 前セッション（2026-09-09 深夜〜09-10…）」の中にそのまま置いてある。
   今回の脆弱性（eslint だけ上げる話）も一緒に伝えるとよい
3.【繰越】`docs/残作業-失敗が消える箇所.md` の残りは **13件だけ**（すべて
   `pages/RoomBookingPage.tsx` ＝**場所予約担当の領域なので触らない**）。
   別枠の `.then(null, () => {})` **54件**は未着手。
   🚨 一括置換しないこと。受け手が supabase のビルダーなら直す対象、
   素の async 関数（dispatchEmail 等）なら**正当な守り**で、外すと未処理エラーになる
4.【繰越】役職の属性化の後片付け … Edge Function 3本（attendance-notify /
   leave-approved-notify / shift-report-confirmed-notify）に旧処理のグループ計算が
   未使用のまま各5か所残っている／画面が Slack に役職名の文字を渡したままで、
   改名すると引けずに既定値へ落ちる穴がある
5.【繰越】前セッションからの宿題 … 連絡板グループのシフト調整（案A提示済み・決めること5つ）／
   FAQの戻る（スマホ）／ベルの closeOnTap ／ホームのバナー（備品精算）／
   管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---


