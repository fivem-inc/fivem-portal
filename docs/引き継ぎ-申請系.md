# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-15〜16・⑤こどもシフト表の設計と1回目／打刻ズレが保存できなかった件）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】master `d4fca4f8`（🚨 **未push が1本**＝docs だけ。次にコードを出すときに一緒に出す）
       push 済みは `5a321f4e` まで。作業ツリーは `AGENTS.md` だけ（管理外）
・本番DBは 2本適用済み：`20260916092222_kids_shift`（⑤の土台）／`20260916100055_overtime_clock_only_type`（打刻ズレの直し）
・tsc 0／vite build 成功／rules-of-hooks 0／新しい4ファイルの ESLint 0件
・🚨 **利用者から見える変化**：管理者のシフト管理に ［こどもシフト表］ が1つ増えた（中身は空）。
  マネージャーには権限管理で「シフト管理」を開いたときだけ。ほかの45人の画面は変わらない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (69) ⑤ こどもシフト表の設計（一問一答で30問以上・2体レビュー・裏取り済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   決まったことは **`docs/計画-管理画面の開放.md` の 5-9**、設計の案は 5-9-1、レビューは 5-9-2、実装は 5-9-3。
   🚨 次に触る人は、この引き継ぎではなく **設計書 5-9 を読む**（決定が全部そこに表で入っている）。要点：
   ・「いつから」で切り替え（変えたマスだけ）＋名前を付けた**案をいくつでも**（作業中は10個まで・しまった案は2年で cron が消す）
   ・マス＝置き場所（列／校の見出し／曜日の書き添え）×曜日。中身は行（レッスン／P／打合せ／園指導／大人コース業務／勉強会＝自動／その他）
   ・紙の読み方（ユーザー回答）：「15:40〜2班（太田・小出）／次の行 鈴木・谷北・渡辺」＝班分けの数／
     **（ ）はその時間に勤務しているがこのクラスを担当しない人**／次の行が担当。「19まで」「16:45〜」は人ごとの時間
   ・🚨 いちばん大事なのは**人数の漏れ**：必要な人数（1班2・2班2・3班3）＋うちレッスンできる人（どれも2人）＋
     人ごとの「レッスンできる」印（正社員☑・パート☐）→ 足りない分は「追加必要（　）」
   ・重なり（同じ人が同じ時間に2か所・同じ校の中も出す）／社員休み＝メインの部門こども・正社員・勤務予定なし（紙と月〜土一致）
   ・P は場所予約とつなげない（同じ枠が場所予約にも募集中45件ある＝片方だけ直すと食い違うことは説明済み）
   ・年度替わりは「こども → 40時間の勤務表 → 掃除 → 担当を詰める」の順。勤務表のたたき台は**段階2（2027年4月の前）**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (70) ⑤ の1回目を作った（migration `20260916092222_kids_shift.sql`・✅ 本番DB適用済み・push `82ffc662`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・DB：表12・RLS 12・決まり26・関数11・cron 1本（`purge-kids-shift-plans-daily`・日本時間 朝4時5分）。**名前入りの中身は0件**
   ・🚨 置き場所を1つの表に（nullable の鍵では EXCLUDE が効かない）／案のマスは「元の版」＋**中身の署名**
     （同じ日付のその場の直しは版のIDが変わらないので、署名でないと「決定済みでも直された」に気づけない）
   ・取り消し版の検算 **49項目一致** → 適用 → 実測（anon すべて false・中身0・cron 22→23・DB 44MB）
   ・画面：`KidsShiftPanel.tsx`＋`lib/kidsShift.ts`／`kidsShiftApi.ts`／`kidsShiftPrint.ts`。シフト管理に ［こどもシフト表］
   ・🚨 レビューで見つかった**既存の不具合①を直した**：シフト管理のタブを切り替えると未保存の入力が消えていた
     → いちど開いた画面は外さずに隠すだけにした（`ShiftManagementTab.tsx`）
   ・画面の計算の検算 17項目一致。🚨 **画面の署名と DB の署名が同じことを実測**（md5 が一致）
   🚨 **実機未確認**：表が出るか／［＋ 列を足す］→マスに行と人を入れて保存（赤字→確認→保存）／
     案を作る→直す→保存→決定（ぶつかったマスの選択）→しまった案に入るか／追加必要と重なりの数／社員休み／全校の PDF／
     勤務表↔こどもシフト表を切り替えても未保存の直しが消えないか
   🚧 **まだ無いもの（2回目）**：⚠️（出勤していない）と［確認した］／🚨 **勉強会の「確認した」が日付で外れる既存の不具合②の修正**
     （`studySessions.ts:113`・本番の勉強会は0件なので実害はまだ無い）／案を2つ比べる／Excel 3シート／校ごとの PDF／
     勤務表の保存の確認に「こどもシフト表◯件が時間外になります」
   🚧 そのあと：紙（`D:\ダウンロード\2026年9月こどもシフト表.pdf`）を「案1」として入れ、画面と PDF で見比べてもらってから決定

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (71) 🚨 残業の「打刻ズレ」が、作った日から一度も保存できていなかった（migration `20260916100055`・✅ 適用・push `5a321f4e`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・スタッフ（濱口さん）から「9/1（火）の退勤打刻忘れを報告したら弾かれた」。画面の文は
     `violates check constraint "overtime_reports_application_types_check"`
   ・原因：種別の検査は 2026-07-28 に作られ、打刻ズレ（`clock_only`）は 2026-08-05 に**画面へ足しただけ**で、
     検査の一覧に足し忘れていた。画面は `['clock_only']` で保存するので必ず弾かれる
   ・本番の実測：clock_only の記録は **0件**（残業報告は92件）＝8/5 から6週間、誰も記録できていない
   ・直したのは種別の一覧に1つ足すだけ（終日の種別が単独、などほかの検査は触っていない）。**画面の変更なし**
   ・取り消し版の検算 8項目一致＋**画面が送る中身そのままを本人の権限（RLS つき）で入れて通ることを確認**
     🚨 `submitted_by` と `entry_type` は画面が insert の引数で足している（record の中には無い）。検算で落として RLS に弾かれた
   ・🚨 **その付近の確認**：残業の種別 12↔12／勤務変更の種別 8↔8／残業の状態 6↔6／
     勤怠の種類は画面の6つが DB の8つに含まれる／勤務変更の `application_types` に検査は無い（今までどおり）→ **ずれはこの1件だけ**
   🚨 **教訓：新しい種別を画面に足したら、DB の検査の一覧も同じ回に足す。**
     足し忘れても画面は作れてしまい、**使う人が保存しようとした瞬間に初めて分かる**
   ✅ 濱口さんへの返信は**ユーザーが送信済み**（2026-09-16）。🚧 9/1（火）の分が登録し直されたかは未確認
     （確かめ方：`select count(*) from overtime_reports where application_types @> array['clock_only']::text[];`）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）
   ▼ こどもシフト表（(70) の「実機未確認」）
   ▼ (62) 管理画面をマネージャーに開く／(63) 勤務表／(64) 勉強会／(65) 打刻忘れ／(66) 掃除担当表／
     (67) 出張報告の Excel／(68) 残業 Excel の R列 … 確認項目は**アーカイブの (62)〜(68)**
   ▼ シフト調整（(49)〜(61)）・共有PCの自動ログアウト（(54)）… 同じくアーカイブ
2.【次の実装】⑤ の2回目（⚠️・確認した・②の修正・比べる・Excel・校ごとPDF・勤務表の保存の確認）→ 紙の案1を入れる
3.【継続調査】毎朝の約3秒（アーカイブ (53)）。朝いちの「起動の内訳」の印3の種類を見てから直す。🚨 数字を見るまで直しにいかない
4.【次の実装】シフト調整の**手順9 公開**（設計は `docs/計画-シフト調整.md`。🚨 3つを同時にやらない）
   (a) 今後の休みに場をまとめて作る (b) 既存のお知らせの整理（いまは古い通知と新しいまとめの両方が届く）
   (c) 権限をリーダー・マネージャーへ (d) 2週間後に古い仕組みを撤去
5.【要連絡】場所予約の担当へ … 2人共通ファイル（`App.tsx`／`useAuth.ts`／`FeaturePermissionsTab.tsx`／`NotificationsTab.tsx`／
   `push-dispatch` v34→v36／`ShiftManagementTab.tsx`）を触った。値は変えていない。push 直前の突合は毎回 重なり0件
6.【繰越】休暇の穴の第3段（アーカイブ (39)）：`insert_own` を `status='pending'` に締め、`update_admin` に WITH CHECK
7.【繰越】表示速度の残り：`useFeaturePublished` の共有（開く速さへの効き目は小さい・画面の切り替えには効く）
8.【繰越】`docs/残作業-失敗が消える箇所.md` の残り13件（すべて `RoomBookingPage.tsx` ＝場所予約の領域なので触らない）
9.【要判断・繰越】修正依頼の Slack 文面・記録の日付を揃えるか
10.【未着手・繰越】残業申請と勤怠カレンダーの二重登録に気づく仕組み（案だけ）
11.【繰越】管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**残業の Excel に「事前申請の予定時間帯」は出さない**（2026-09-15 確定「不要」）
・**こどもシフト表の決定は設計書 5-9 の表がすべて**（案の持ち方・人数の決まり・PDF/Excel の形・見る人・最初の登録 ほか）
・**連絡板の見出しの矛盾は直さない**（2026-09-11 確定）。`channelListPanel` の `width:280` と `flex:1` の食い違い。
  🚨 見出しまわりを触る人はまずここを疑う
・**2人共通ファイルの「値を変えない変更」は先に実装して後から連絡でよい**（push 直前に必ず突合）
・**eslint の脆弱性は対応しない**／**事前の遅刻・早退は全員の既定で載せる**
・**休暇の日数は実際に休む日の数。飛び飛びは日付を並べる。月・日の頭にゼロを付けない**
・シフト調整で決まったことは `docs/計画-シフト調整.md`（見る権限・決定する権限は新設）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **`notifications` の `event_key` はプッシュのスイッチ**。付けなければベルだけになる
・🚨 **push-dispatch は `notification_settings` の push 行が無いと「送る」**。行が無い＝管理画面から止められない
・🚨 **管理画面の通知設定は「行があるチャンネルだけ」出す**。新しい通知を作ったら必ず行も入れる
・**`application_requests` にはすでに掃除の cron がある**（済み90日・未対応は期限から60日）
・**日次リマインドはすべて朝9時（JST）＝cron `0 0 * * *`**。掃除の cron は UTC 18:00〜19:05 に並んでいる
・**cron の鍵は vault から取る形が既定**（直書きすると本番DBに鍵が残る）
・**`resolve_role_recipients` は所属チームの絞り込みを自分でやる**
・🚨 **本番反映の確認に「バイト数の一致」を使わない**（同じコードでも手元と本番でハッシュが変わる）。**文言の有無で数える**
  （詳しくはアーカイブの「(48) 本番反映の確認に」）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ・管理者が個人ごとの残数を見る／付与と繰越を管理画面で入れる／本人も見られる／出す出さないを ON/OFF
   🚨 調べた結果（2026-09-10 実測・調べ直さなくてよい）：**残数・付与・繰越の表は無い**。取得は `leave_requests` から数えられる
     （有給休暇：approved 31件＝40日分／manager_approved 6件＝7日）。🚨 `overtime_submission_grant*` は残業の提出許可で無関係
   🚨 作る前に決めること：数える単位（日数か回数・半日は今のデータで表せない）／どの status を取得済みと数えるか／
     付与は手入力か自動計算か（法令に踏み込むので手入力が安全）／繰越の上限と年度の区切り／ON/OFF は誰の単位か
   🚨 作るときの注意：ON/OFF は既存の `feature_permissions` に乗せられないか先に検討／記録を貯めるので掃除か上限も一緒に設計／
     残数の計算は lib の1か所（管理画面と本人画面が同じ関数を呼ぶ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
