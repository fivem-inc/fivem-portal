# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-19〜20・退職者の申請期間 1段目と2段目を全部／長岡さんの指摘3件／障害1件）

```
【状態】master `ddd7ba53` まで push 済み（リモート一致）＋🚨 **未push 14本＝DB の migration ファイルと docs だけ**
  （本番DBには全部適用済み・次にコードを出すときに一緒に push する）。作業ツリーは `AGENTS.md` だけ（管理外）
・✅ **退職者の申請期間は2段目まで完了**（Hook の有効化まで済み・(92)）。次は3段目（画面の限定モード）
・本番DBに 2026-09-19〜20 で 11本適用：
  `20260918223959`（退職の予約＋チェック表）／`20260919004431`（シフト調整の「調整不要」）／
  `20260919025525`（🚨 障害の修正・外部キーを外す）／`20260919034714`（レビューの指摘の修正）／
  `20260919060900`（anon から関数16本）／`20260919082601`（関数の PUBLIC 付け直し）／
  `20260919083329`（役割 retiree）／`20260919143724`（22表のポリシーを to authenticated）／
  **`20260920084248`（retiree に表・関数を許す）／`20260920084249`（退職の確定時に権限を外す・期限切れで ban）／
  `20260920084250`（Custom Access Token Hook）**
・push-dispatch を deploy（有給奨励日の行き先）／社内FAQ 2件を更新
・tsc 0／vite build 成功／rules-of-hooks 0／触ったファイルの ESLint は HEAD と同数
・🚨 **利用者から見える変化**：残業のメモの見た目（申請欄を隠す・［申請に戻る］・種類の選択欄）／
  申請の依頼の文言と「すでに申請があります」／勤怠カレンダーの「シフト 調整不要」／有給奨励日のベルの行き先／
  管理画面に「📋 退職の手続き」・ユーザー管理の［退職］／ホームの案内（マネージャー以上）
・🚧 **実機未確認**：下の「次にやること」0-2・1 と (85)(86)(88) の 🚧

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (85) 退職者の申請期間：設計を確定 → 1段目を本番へ（2026-09-19）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・設計と決定はすべて `docs/計画-退職者の申請期間.md`（§4 決めること7つ・§6 チェック表・§7 レビュー結果と追加の決定）
   ・レビュー2体の指摘は本番で裏取り済み。🚨 **§5-1「role_*(p_uid) に在籍判定を入れる」は取り下げ**
     （overtime_role_rank_target が「在籍しない＝1位」になり、退職者の残業が上長の集計から消える）
   ・**3段に分けて出す**：①退職の予約＋チェック表＋復活の直し（済）→ ②穴を塞ぐ → ③申請期間を開く。🚨 ②が終わるまで③を出さない
   ・**1段目**（migration `20260918223959_retire_schedule_and_checklist`✅適用・取り消し版26項目＋適用後の実測済み）
     - profiles：retire_date／retiree_access_until／retired_at／retired_role_id／retired_employment_type
     - RPC（管理者だけ）：retire_schedule（過去の日は即時切り替え）／retire_cancel（チェックも消す）／retire_restore（退職日も空に）
     - 内部：retire_apply（退職に切り替え＋確認者のまま残った残業・勤務変更報告・休暇を「管理者」に付け替え・retire_reassignments に記録）
       🚨 **備品購入は自動で付け替えない**（承認者が複数・承認済みが混ざる）→ チェック表に件数を出して［承認者を変更］で直す
     - cron：retire-daily（15:05 UTC＝0:05 JST）／retire-checklist-notify-daily（0:00 UTC＝9:00 JST・退職日当日に必須が残ればマネージャー以上＋管理者にベル。event_key なし＝プッシュなし）
     - 画面：ユーザー管理の［退職］＝その行の下に入力欄（道は1本）／状態の札／［退職日を取り消す］／［復活］は RPC／申請期間中は［削除］を出さない
       チェック表 `components/RetireChecklistPanel.tsx`（管理画面「📋 退職の手続き」＝管理者／`/retire`＝マネージャー以上・スマホ可）
       ホームの案内 `components/RetireChecklistBanner.tsx`。判定・呼び名・残りの数え方は `lib/retire.ts` の1か所
     - 🚨 期限の初期値は DB の retire_access_default() だけが持つ（画面は RPC で取る）
   ・🚨 2人共通ファイル（足しただけ）：App.tsx（/retire・案内・isManagerPlus）／AdminPanel.tsx（タブ1つ）／AdminPanelContext.tsx（AdminTab に 'retire'・復活の関数）／types/index.ts（2列）
   ・🚧 **実機未確認**：［退職］の入力欄と期限の初期値／予約→取り消し／チェックの済み・戻す／/retire をスマホで／ホームの案内
   ・🚧 1段目の残り：チェック表に「ポータル内の名指しの設定」を自動で並べる
   ・🚧 2段目の前に決めること：誰でも読める（true）ポリシーをどこまで締めるか（room_* は場所予約の担当と調整）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (86) 長岡さんの指摘3件（2026-09-19・migration `20260919004431`✅・push-dispatch deploy✅・FAQ 2件✅）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・**申請の依頼の「取り下げ」で有給が消えたと誤解**：ボタンは依頼（application_requests）を取り下げるだけ。
     文言を「依頼を取り下げる」「依頼を取り下げ済み」に（案1）。
     依頼を作るとき、その日にすでに申請があれば黄色で「すでに申請があります」（止めない・読めなければ出さない）。
     🚨 依頼が「申請済み」になるのは**依頼のあとに**出した申請だけ（先に入っている有給では「未申請」のまま残る）
   ・**有給奨励日の休暇が一律「シフト 未」**→ 状態に「**調整不要（not_needed）**」を足した（案A）。
     日が**すべて**奨励日（company_calendar.kind='work_on_closed_encouraged'）なら DB が自動で入れる
     （BEFORE トリガー `trg_leave_shift_adjust_not_needed`・判定は `leave_all_encouraged()` の1か所・**日付で判定**するので
     ふつうの申請で出した分も拾う）。既存61件を移行（ふつうの休暇の「未」11件は不変）。カレンダーに［調整不要］ボタン。
     作業場（shift_adjust_slots）では「変更なし」扱い。🚨 同期で「確認済（変更なし）」に戻さない（sync を本番定義から修正）
     🚨 9/19 朝に 10/31 の9件ぶん「シフト調整が未です」の通知が出ていた（今後は出ない）
   ・**有給奨励日のベル・プッシュが /leave（休暇申請）に飛んでいた** → 曽川さんがふつうの休暇申請から出した原因。
     ホームのバナーへ（`/?enc=1`・バナーまでスクロールして光る）。🚨 **push-dispatch（2人共用）の url を1つ変えた** → 場所予約の担当へ要連絡
   ・FAQ `b0…914`（シフト調整の記録とは）に調整不要の行／`b0…915`（シフト調整のお知らせ）の文を更新（更新者「林　晃平（Claudeが代行）」）
   ・🚧 実機未確認：依頼の文言と「すでに申請があります」／カレンダーの「シフト 調整不要」／ベルからバナーへ移動

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (87) 🚨🚨 障害：休暇申請で「受理者が登録されていません」（2026-09-19・当方の(85)が原因・同日修正）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・尾上先生から報告。原因：(85) で profiles.retired_role_id に **roles への外部キー**を付けた → profiles→roles が2本になり、
     `roles!inner(...)` などの埋め込みが **PGRST201** で全部失敗（休暇の申請先・勤務変更の報告先など・適用した 9/19 朝から）
   ・直し方：外部キーだけ外した（migration `20260919025525`✅・列と値は残す）。anon で埋め込みがエラーにならないことを実測。
     1段目の migration ファイルも外部キー無しに直した
   ・🚨🚨 **教訓：profiles（や埋め込みで読まれる表）に、同じ相手への外部キーを2本目として足さない**。
     足すなら全呼び出しを `roles!profiles_role_id_fkey(...)` に直す必要がある。取り消し版の検算は SQL だけで、
     **画面の読み方（PostgREST の埋め込み）までは通っていなかった**。DB の列を足したら、埋め込みを curl で1回叩いて確かめる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (88) 退職の管理のレビュー（UI/UX・シニアエンジニア）と直し（2026-09-19・migration `20260919034714`✅・push `ddd7ba53`・本番の文言で確認済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・DB：retire_daily を1人ずつ例外で受ける／休暇の1人目の付け替えは pending だけ／調整不要→ふつうの日で「未」に戻す／
     retire_purchase_pending（security definer・leader_id 含む）。関数は本番定義から起こした
   ・画面：過去日の警告と「今すぐ退職にする」／札は「在籍中（◯/◯ 退職予定）」橙・「退職済み（◯/◯ 退職）」・「在籍中（切り替えに失敗）」／
     「ログインできる期限」／［復活］の確認文と緑カード／チェック表は行全体44px・戻す前に確認・JST の日付・任意の印／
     ホームの案内を button に／`retire:checklist` のベル→/retire。🚨 1段目の間は「申請期間」と言わない（3段目で lib/retire.ts の1か所を変える）
   ・残業のメモの［申請に戻る］は**濃い灰の塗り**（ダークは明るい灰・案F・スタッフ像5人の聞き取りで F 3票）。▲▼ は廃止
   ・🚧 **2段目の前にやること**（シニアエンジニア）：誰でも呼べる security definer の関数120本を分類／
     管理者の判定は57か所を直さず「退職の確定時に app_metadata の role を外す＋セッション削除＋期限切れで ban」／
     profiles の SELECT は「呼ぶ人が在籍」で書く（「行が在籍」だと過去の申請の名前が消える＝(87)と同型）／
     締める前に pg_policies を丸ごと保存して戻し版を用意／1回に出すのは1つの表のまとまり
   ・🚧 **3段目までに決めること**（UI/UX）：期限直前・期限後の差し戻しの扱い／機能ごとの期限を入れた案内文（3案あり・設計書に追記予定）／
     退職者のホームが空にならない作り／Slack パスワードの項目の記録方法
   ・🚨 **検証手順に足す**：列・外部キー・ポリシーを変えたら、画面が使う埋め込み（profiles／roles）を anon で GET して PGRST2xx が出ないことを確かめる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (89) 🚨 ログインしていない人に全員の残業時間が読めていた（2026-09-19・以前からの状態・migration `20260919060900`✅）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・2段目の下調べで発見：公開 anon キーだけで `overtime_planned_totals` を呼ぶと、全スタッフの user_id と残業の分数が返った（本番で実測）
   ・security definer で anon が実行でき、中で本人を確かめない関数16本から anon の実行権限を外した（authenticated・service_role は付け直し）。
     🚨 `faq_public_*` 4本は意図して anon に開けているので残した
   ・実測：anon で planned_totals・get_database_usage_mb は 42501／faq_public_contact は今までどおり／profiles→roles の埋め込みも正常。
     取り消し版で authenticated の残業の読み込み（112件）も確認
   ・🚧 残り：anon が実行できる関数は全部で263本（definer 66本）。表のポリシー（roles=public）38か所が関数を使っているため一律には外していない。
     2段目でポリシーを `to authenticated` に寄せるときに一緒に片付ける

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (90) 退職者の申請期間・2段目：案B（退職者だけ別の DB の役割 retiree）に決定 → 第1歩を本番へ（2026-09-19・migration `20260919082601`✅）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・計画は設計書 §8（手順 §8-5 の1〜6）。案A（44表・121関数・管理者判定88か所に1つずつ条件を足す）は漏れやすいので採らない
   ・第1歩：public の関数から PUBLIC の実行許可を外し、anon・authenticated・service_role に今と同じものを付け直した。
     `alter default privileges for role postgres revoke execute on functions from public` も入れた
   ・実測：適用の前後で379本すべて anon・authenticated・service_role の実行可否が**1本も変わらない**（スナップショット比較）。
     faq_public_contact（anon）・profiles→roles の埋め込みも正常
   ・🚨 PUBLIC のまま残る188本は**すべて拡張 btree_gist**（supabase_admin 所有・索引の部品・データを読まない）
   ・🚧 次：手順2 retiree の役割を作る（表・関数の権限は何も付けない）→ 手順3 許すもの → 4 退職の確定時の処理 → 5 Hook（管理画面で有効化）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (91) 2段目の手順2・3-1を本番へ（2026-09-19〜20・migration `20260919083329` `20260919143724`✅）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・手順2：役割 `retiree` を作成（nologin・authenticator に grant・スキーマの usage だけ）。**表0・関数0**（btree_gist を除く）＝誰も使わない
   ・手順3-1：退職者に開ける22表の既存ポリシー38個を **`to public` → `to authenticated`** に付け替え（条件は1文字も変えない）
     🚨 理由：`to public` のポリシーは retiree にも効く → ①retiree が実行できない関数を呼んで表ごと読めなくなる ②広すぎる条件が効く
   ・**影響の実測**：8つの立場（パート・フロア責任者・マネージャー・リーダー・一般・社長・管理者・anon）× 22表 ＝ 176通りの
     「読める行数」を適用の前後で比較。**ログインしている7立場は変化0**。anon は残業3表が「権限エラー → 0件」になっただけ
   ・(89) の影響も確認：外した16本を直接呼ぶのは管理画面・場所予約・通知設定（ログイン後）と Edge 5本。
     Edge は3本が service_role、receipt-bulk-zip・receipt-signed-url は**ログインした本人の JWT** → 影響なし
   ・✅ 手順3-2・4・5は 2026-09-20 に適用（(92)）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (92) 2段目の手順3-2・4・5を本番へ（2026-09-20・migration `20260920084248` `20260920084249` `20260920084250`✅）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・詳しい記録と決めた細部は設計書 `docs/計画-退職者の申請期間.md` **§8-8**
   ・**手順3-2**：retiree に22表・34ポリシー・関数5本を許した。profiles は**12列だけ**（メール・IP・最終ログインは列ごと不許可）、
     行は「本人」と「在籍の承認者」だけ。app_settings は**6つの鍵だけ**（連絡板・DM の宛先設定は見せない）
     🚨 通知（ベル）の宛先は「本人が見られる在籍の承認者」まで広げた。設計書の「その申請の確認者だけ」では
       **交通費の通知（役職あて・行に紐づかない）が出せなくなる**ため
   ・**手順4**：`retire_apply` に「app_metadata の role を外す＋セッション削除」、`retire_daily` に「期限切れで ban＋
     プッシュの登録を消す」、`retire_restore` に「ban 解除＋権限を戻す」。外した権限の控えは **`profiles.retired_app_role`**（ただの text）。
     🚨 外部キーは付けていない（(87) と同型を避ける）
   ・**手順5**：`public.custom_access_token_hook`。🚨 **中で必ず例外を受け止めて、失敗したら何もせず返す**（ログインを止めない）
   ・**実測**：8つの立場 × 22表 ＝ **176通りの読める行数が前後で1つも変わらない**／
     retiree は自分の残業28件・他人0件・profiles 16行・email/休暇/連絡板/備品購入は 42501／
     他人の名前での insert は 42501／退職の確定でセッション 5件→0件／管理者で試すと role が外れ**［復活］で戻る**／
     期限切れで ban✓・プッシュ 1件→0件／Hook は 在籍者=authenticated・退職者=retiree・壊れた入力でもそのまま返す／
     anon の埋め込み（profiles→roles ほか4本）はすべて **HTTP 200・PGRST2xx なし**
   ・🚨🚨 **実測でわかったこと：`grant usage on schema auth to retiree` は書いても効かない**。
     auth スキーマの持ち主は supabase_admin で、postgres は usage を**もらっているだけ**（配る権限が無い）。
     PostgreSQL は権限の足りない grant を**エラーではなく警告**で素通りさせるので、**黙って何も起きない**。
     → それでも retiree のポリシーは動く（ポリシーの中の auth.uid() は作った時点で関数が決まっていて、
       読むときにスキーマの usage を確かめ直さない）。migration にもこの説明を残した
   ・✅ **Hook の有効化まで完了（2026-09-20 ユーザーが管理画面で実施）**。
     Authentication → Hooks → Customize Access Token (JWT) Claims → `public.custom_access_token_hook`（ENABLED）。
     実測：Hook を呼べるのは supabase_auth_admin だけ（anon・authenticated・retiree は不可）／
     在籍者・管理者を通しても `authenticated` のまま／**ユーザーがログインし直して 10:07 にログイン・10:08 に
     `touch_last_sign_in` が通った**（＝ログイン済みの鍵でしか呼べない処理なので、鍵が今までどおりである証拠）／
     ログインを止められている人0人
   ・🚨 **これで2段目は終わり**。次は3段目（画面の限定モード）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0.【(81) の続き・2026-09-18】
   ✅ C-6 … migration `20260918100023_reminder_channel_options_group_only` 適用済み。
     実測：候補 26→9件（グループの数と一致）・名前が空 0件・同名の関数1つ・anon 実行不可。既存リマインド1件の送り先は生きている
   ✅ A-7 … **もともと作っていなかった**（「未対応」の名前だけ出していた）。「押した人 ◯人 ▼」で名前＋日時を出すようにした。
     見えるのは 送信者／管理者／**写しで受け取る代表者**（写しに入っている かつ いまの代表者設定に入っている）。
     リマインドは送信者と管理者だけ。枠は `ConfirmStatusBox` 1つにまとめ、受信トレイと送信トレイで共用
     （代表者は自分あてでないお知らせを送信トレイで見るため、送信トレイにも出した）。
     「まだ押していない人」の数え方もこの部品1か所（使わなくなった `inboxDetailUnconfirmed` は削除）
   ✅ A-2 … 普通の Chrome でも再現（ユーザー実測）。**原因：宛先の枠（RecipientTags）と本文（renderMsg）が同じ並びで同じ key（お知らせのID）を持っていた** → React が前の宛先の枠を消し損ねた。key を `recipients-${id}` に変えて解決（受信・送信の2か所・push `13ad8b32`）。**2026-09-18 ユーザーが実機で直ったことを確認**。
     （調べた経過）データは正常（同じ人が2行の重複は全体で0件／9/16＝45人・7/2＝2人で表示と一致）。
     名前を出すのはコード内で1か所だけ。**再現も原因の特定もできていない**。
     → **普通の Chrome で同じ手順を試してもらう**（ユーザーに依頼済み）。再現すればこちらの不具合として直す
0-2.【実機確認】**残業申請のメモ**（(82)・push したあと）… マネージャー以上のアカウントで
   ▼ ［メモ（0）］が「クリア」の左に出る／押すとすぐ書く画面が開く（0件のとき）
   ▼ 遅刻→いま→文例→保存 で薄緑カードが出て、一覧の「今日」に並ぶ
   ▼ ［申請］で日付・時刻・理由が入り、**通常シフトの時刻に上書きされない**／過去の日は事後報告になる
   ▼ 書きかけがあるときに［申請］→「いまの入力を消して入れますか？」が出る
   ▼ 送信すると、そのメモが灰色の「申請済み」になる／同じ日に申請がある日は最初から「申請済み」
   ▼ ✕ → その場の確認 →［削除］で消える／「クリア」でメモの案内が消える
   ▼ 一般・リーダーのアカウント（役職プレビュー）ではボタンが出ない
   ▼ 20件まで入れたときの案内（急がなくてよい）／ダークモード／スマホ幅で1行に収まるか
0-3.【退職者の申請期間・つづき】設計書 `docs/計画-退職者の申請期間.md`（§8 が2段目の計画・**§8-8 が 9/20 の結果と残り**）
   ✅ **2段目は完了**（手順3-2・4・5＋管理画面での Hook の有効化まで 2026-09-20 に実施・(92)）
   ▼ 3段目（画面の限定モード）。🚨 ナビ・ホームは権限と関係なく連絡板・安否確認などを読むので、退職者では読まない作りにする
     🚨 通知の宛先を決める処理（`notificationDispatch.ts` / `overtimeNotify.ts`）が**上長のメールアドレスを読む**。
       retiree は email を読めないので、退職者のときはサーバー側に寄せる
     🚨 AuthContext：TOKEN_REFRESHED・画面に戻ったときに状態を読み直す（ログイン中に退職になった人を止める）
   ▼ Edge Function 7本の入口で「在籍か期限内の退職者か」を確かめる（設計書 §8-6）
   ▼ 1段目の残り：チェック表に「ポータル内の名指しの設定」を自動で並べる（設計書 §7-2 に候補の表の一覧）
   ▼ 3段目までに決めること（UI/UX レビュー）：期限直前・期限後の差し戻しの扱い／機能ごとの期限を入れた案内文（3案あり）／
     退職者のホームが空にならない作り／Slack のパスワードの項目の記録方法
1.【実機確認・最優先】🚨 こちらでは画面を開けない（ログインが要る）
   ▼ **今日の分**：残業のメモ（申請欄が隠れる・［申請に戻る］が濃い灰・種類の選択欄）／申請の依頼の文言と「すでに申請があります」／
     勤怠カレンダーの「シフト 調整不要」／有給奨励日のベル→ホームのバナー／ユーザー管理の［退職］（🚨 **過去の日で確定しない**）／
     管理画面「📋 退職の手続き」と /retire（スマホ）／ホームの案内
   ▼ 連絡板（今日の分）… 宛先の折りたたみ／送信トレイの送信者名／既読の一覧（誰がいつ読んだか）／
     読了を押した人／管理画面の「宛先の候補に出さない人」で Q&A管理専用を選んで保存
     🚨 送信者名は `allProfiles`（在籍者）から引くので、退職した人が送ったものは「不明」と出る
   ▼ こどもシフト表（⑤）… **昨日作ったまま誰も触っていない**。確認項目はアーカイブの (70)
   ▼ (62)〜(68) の未確認項目もアーカイブに残っている
   ▼ 🔗 すべてを1枚にしたチェック表：(78) の Artifact
   ▼ お客様向けFAQ（(79)）… 見つからない言葉で検索 → 青いフォームのボタン → フォームの位置へ移動／受付時間2行／スマホ
2.【相談】電車遅延（(77)）… 太田さんと。決まれば実装
2-2.【FAQの続き】① `/faq.html` の古い15問のうち、公開Q&Aに無いもの（駐車場・スキーツアーの引率・ケガの対応など）を
   ポータルへ移してから、古い一覧を外す ② プログラムページ用に**コースで絞った表示**（`data-course` 指定・ユーザー「まだ」）
   ③ 公開Q&Aの 075-255-4401 は18件の回答に書かれている（番号を変えるときは全部）
3.【次の実装】⑤ の2回目（⚠️・確認した・勉強会の既存不具合②の修正・案を比べる・Excel・校ごとPDF・
   勤務表の保存の確認に「こどもシフト表◯件が時間外になります」）→ 紙の案1を入れる
4.【継続調査】毎朝の約3秒（アーカイブ (53)）。朝いちの「起動の内訳」の印3を見てから直す
5.【次の実装】シフト調整の**手順9 公開**（`docs/計画-シフト調整.md`。🚨 3つを同時にやらない）
6.【要連絡】場所予約の担当へ … 9/15〜16 に 2人共通ファイル（`App.tsx`／`useAuth.ts`／
   `FeaturePermissionsTab.tsx`／`NotificationsTab.tsx`／`AdminPanel.tsx`／`AdminPanelContext.tsx`／
   `ShiftManagementTab.tsx`／`push-dispatch` v34→v36）を触った。値は変えていない。
   🚨 今日触った `BoardPage.tsx`／`BoardSettingsTab.tsx` は**共通ファイルではない**ので連絡不要
   🚨 **2026-09-18 追加（(82) 残業のメモ・push 済み `2847f285`）**：`App.tsx`（権限を1つ渡す2行）／
     `useAuth.ts`（`canOvertimeMemo` を追加）／`FeaturePermissionsTab.tsx`（一覧に1行）／
     `draftStorage.ts`（`overtimeMemoUnsent` のキーを1行）。**いずれも値は変えていない**（足しただけ）
   🚨 **2026-09-19 追加（ユーザー指示：次に連絡するときにまとめて伝える）**：
     ・**push-dispatch を deploy した**（`a0aa1fbf`）。変えたのは `reminder:encouragement` の url だけ（`/leave` → `/?enc=1`）。
       **相手が push-dispatch を deploy する前に必ず `git pull`**（しないとこの変更が消える）
     ・(85) 退職の1段目（`c7108dd2`）：`App.tsx`（/retire のルート・ホームの案内・Dashboard に isManagerPlus）／
       `AdminPanel.tsx`（タブ「退職の手続き」1つ）／`AdminPanelContext.tsx`（AdminTab に 'retire'・handleToggleActive → handleRestoreUser）／
       `types/index.ts`（AdminUserProfile に2列）。🚨 handleToggleActive は**廃止**（is_active を画面から直接書かない）
     ・(86)（`a0aa1fbf`）：`App.tsx`（有給奨励日のベルの行き先・バナーに移動）
     ・(89) `room_check_conflict` から anon の実行権限を外した（ログインしている人は今までどおり）
     ・2段目で `room_is_staff` と room_* の「誰でも読める」ポリシーを「在籍者だけ」に締める → ユーザー判断で**事後報告でよい**（2026-09-19）
   🚨 **2026-09-20 追加（(92)・共通ファイルのコードは1行も触っていない・DB だけ）**：
     ・退職者専用の役割 `retiree` に、申請系の22表だけを許した。**room_* は1つも開けていない**（退職者からは閉じたまま）
     ・`profiles` に列を1つ足した（`retired_app_role`・ただの text・外部キーなし）
     ・ログインの鍵を作る Hook（`custom_access_token_hook`）を用意した。**有効化はまだ**。
       有効にすると、期限内の退職者だけ DB の役割が `retiree` になる（在籍者は今までどおり `authenticated`）
7.【繰越】休暇の穴の第3段（アーカイブ (39)）：`insert_own` を `status='pending'` に締め、
   `update_admin` に WITH CHECK
8.【繰越】表示速度の残り：`useFeaturePublished` の共有
9.【繰越】`docs/残作業-失敗が消える箇所.md` の残り13件（すべて `RoomBookingPage.tsx` ＝場所予約なので触らない）
10.【要判断・繰越】修正依頼の Slack 文面・記録の日付を揃えるか
11.【未着手・繰越】残業申請と勤怠カレンダーの二重登録に気づく仕組み（案だけ）
12.【繰越】管理者が「失敗が起きている」ことを知る手段（🚨 作るなら掃除の cron ＋1日の上限とセット）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**お客様向けFAQの電話の案内は「四条本校 075-255-4401・月〜土の2行」**（2026-09-17 確定。日曜は会員様向けなので出さない）。
  フォームが主役・電話は控えめ。案内文は「紹介＋お気軽に」の型（「見つかります」系は使わない）
・**コースで絞ったFAQ表示（プログラムページ用）は「まだ」**（2026-09-17）
・**連絡板は当面「お知らせの受信」だけに使う**（グループ・DMは使わない）。らくらくとの併用は 10/15 まで
・**お知らせの対応状況（押した人の名前・日時）と回答の中身は、送信者・管理者・写しで受け取る代表者が見てよい**（2026-09-18 確定）。
  受け取った側は人数と自分の回答だけ。🚨 回答の中身も代表者に見える（送信トレイで全員分）のは**承知のうえ**でこのまま
・**個人あてのお知らせも、代表者の送信トレイから読めてよい**（2026-09-16 確定）。
  1人あてでも写しが付く作りはそのまま。🚨 気になったときはこの決定を思い出すこと
・**残業の Excel に「事前申請の予定時間帯」は出さない**（2026-09-15 確定「不要」）
・**こどもシフト表の決定は設計書 `docs/計画-管理画面の開放.md` 5-9 の表がすべて**
・**連絡板の見出しの矛盾は直さない**（2026-09-11 確定）。`channelListPanel` の `width:280` と `flex:1`
・**2人共通ファイルの「値を変えない変更」は先に実装して後から連絡でよい**（push 直前に必ず突合）
・**eslint の脆弱性は対応しない**／**事前の遅刻・早退は全員の既定で載せる**
・**休暇の日数は実際に休む日の数。飛び飛びは日付を並べる。月・日の頭にゼロを付けない**
・シフト調整で決まったことは `docs/計画-シフト調整.md`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 調べて分かったこと（画面を開かずに確かめられる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **連絡板は「本文・宛先・既読・読了」の4つの表に分かれ、権限もバラバラ**。
  片方だけ開けると画面は**エラーを出さずに「0人」と嘘をつく**（今日2回踏んだ）
・🚨 **`board_reads` と `board_confirmations` の select は `true`**（ログインしていれば誰でも読める）。
  引き継ぎの「ログイン済みなら誰でも読み書き」ポリシー是正の対象（繰越の項目）
・🚨 **`notifications` の `event_key` はプッシュのスイッチ**。付けなければベルだけになる
・🚨 **push-dispatch は `notification_settings` の push 行が無いと「送る」**。行が無い＝管理画面から止められない
・🚨 **管理画面の通知設定は「行があるチャンネルだけ」出す**。新しい通知を作ったら必ず行も入れる
・🚨 **push_queue は 9/9 より前の記録が残っていない**＋**直送8本はキューを通らない**。
  「届いていない」の判断に使わない
・**`application_requests` にはすでに掃除の cron がある**（済み90日・未対応は期限から60日）
・**日次リマインドはすべて朝9時（JST）＝cron `0 0 * * *`**
・**cron の鍵は vault から取る形が既定**（直書きすると本番DBに鍵が残る）
・**`resolve_role_recipients` は所属チームの絞り込みを自分でやる**
・🚨 **本番反映の確認に「バイト数の一致」を使わない**。**文言の有無で数える**。
  🚨 ただし**文言が1つも増えない変更**（今日の(74)(75)など）は文字では確かめられない。
  その場合は実機で動作を見てもらうこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🆕 あとでやる実装計画（2026-09-10 ユーザー依頼・まだ着手していない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
(A)【新規】有給の管理（付与・繰越・残数）
   ・管理者が個人ごとの残数を見る／付与と繰越を管理画面で入れる／本人も見られる／出す出さないを ON/OFF
   🚨 調べた結果（2026-09-10 実測・調べ直さなくてよい）：**残数・付与・繰越の表は無い**。
     取得は `leave_requests` から数えられる（有給休暇：approved 31件＝40日分／manager_approved 6件＝7日）。
     🚨 `overtime_submission_grant*` は残業の提出許可で無関係
   🚨 作る前に決めること：数える単位／どの status を取得済みと数えるか／付与は手入力か自動計算か／
     繰越の上限と年度の区切り／ON/OFF は誰の単位か
   🚨 作るときの注意：ON/OFF は既存の `feature_permissions` に乗せられないか先に検討／
     記録を貯めるので掃除か上限も一緒に設計／残数の計算は lib の1か所
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
