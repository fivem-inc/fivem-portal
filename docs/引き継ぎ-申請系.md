# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-24〜25・FAQ集計／連絡板の返信／残業の表入力を完成・自己受理の穴を DB で閉じた／シフト調整の「案」1〜4段目）

```
【状態】master に push 済み（リモート一致・最新 `8ee607cd`・2026-09-25）。作業ツリーは `AGENTS.md` だけ（管理外）
・2026-09-25 に本番へ出したもの（どれも DB 適用・Edge Function deploy・push・本番の反映まで確認済み）：
  DB migration：20260925140348（FAQ 表入力）／144144（自己受理をマネージャー以上に DB で締めた）／170256・205249（FAQ シフト調整）／
    171111（決定の取り消しで picked を戻す）／185551（シフト調整の案）／205420（意見の期限の見回り cron）
  Edge Function：send-overtime-slack（まとめて1通）／🚨 共通の門 _shared/callerGate.ts で7本（gcal-sync・send-email・slack-notify ほか）／
    push-dispatch（shift_adjust:cancelled・:ot・plan_created・plan_due）
・🚨 **9/25 の画面の変更は多くが実機未確認**（管理者ログインが要るため）。確かめた範囲は各項目の「✅」を見る
・2026-09-25 夜：(139) 残業の再提出で種別を変えられるように（DB変更なし・push 済み・✅ 実機で 10/17 を自己受理で再提出できた）
・2026-09-26：(140) 送信トレイのアーカイブを人ごとに（migration 20260926004919 適用済み・push 済み・🚧 実機未確認）／
  (141) FAQ集計の検索語を RPC で数える（migration 20260926073634 適用済み・push 済み・🚧 実機未確認）／
  場所予約の担当への事後報告を `docs/引き継ぎ-場所予約.md` の冒頭に書いた（push `3d9efdd4`）／
  ✅ (130) 写しの表示と外す・(140) 送信トレイのアーカイブ は同日ユーザーが実機で確認した／
  (142) お知らせのテンプレート ①DB（migration 20260926100016 適用済み）②権限の項目・lib まで push。**次は ③ 送信画面（見本HTML → 実装）**
・9/24 の記録（(111)〜(125)）はこの下にそのまま残してある。次に記録を書くときは、この ▶▶ を「▶ 前セッション」にしてアーカイブへ移すこと

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (111) 🚨🚨 「見にくい」の正体は**画面が嘘をついていた**こと（単位の混在）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ヘッダーの5行は全部「◯ 回」と書いてあったが、**本当に「回」なのは1行だけ**だった。
     ウィジェットは `once` という鍵で「同じ方は1回だけ」に間引いている。実測した鍵：

       ページを開いた        once='page'                  → 1人1回＝**人**
       回答を読んだ          once='topic:<質問>'          → 質問ごとに1人1回＝**のべ人**
       問い合わせに進んだ    once='contact:<質問>:<理由>' → 質問・理由ごとに1人1回＝**のべ人**
       「はい」              once='solved:<質問>'         → **のべ人**
       電話・フォームを押した once **なし**                → 押すたび＝**回**

   ・🚨 2026-09-22 に問い合わせ率を「人で揃える」と直したのは**中身だけ**で、
     **画面の文字は「回」のまま取り残されていた**。数え方を直すときは文字も一緒に見ること
   ・✅ **ユーザー確定（案い）**：行ごとに単位を書く（`人` ／ `のべ 人` ／ `回`）。
     🚨 唯一「回」の行はいちばん下に置く（人の行をまとめて読ませるため）。並べ替えないこと
   ・主表の列名も `閲覧`→**`読んだ人`**、`問い合わせに進んだ`→**`進んだ人`**。
     🚨 **数え方の式は1行も触っていない**（9/22 に人数へそろえたまま）。CSVの見出しも同じ言葉にそろえた
   ・「つまずいた理由」の表の見出しも `件数`→**`人数`**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (112) チップ（枠付きの札）を横棒にした（案B）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・🚨 やめた理由：**チップは幅が文字数で決まる**ので量を逆に読ませる。
     実データで「未調査 1人」と「大阪府 3人」がほぼ同じ幅になっていた
   ・部品は **`Bars` 1つ**。量を表す4か所すべてがこれを呼ぶ（来た方の内訳／検索して
     見つからなかった言葉／校・コース／社内FAQ）。🚨 2か所目を作らないこと
   ・🚨 **色は択一トグルの2色だけ**（棒 `#1976d2`／地 `#e3f2fd`・🎨🔒）。新しい色もライブラリも足していない。
     棒の上に文字を載せない＝**押せるボタンに見せない**ため
   ・長い一覧は**上位10件**＋「残り◯件を見る」。🚨 **並べ替えてから切る**（切ってから並べると上位が上位でなくなる）
   ・✅ **ユーザー確定**：時間帯・曜日は**時刻順・曜日順**にし、**記録が0のぶんも並べる**
     （多い順だと 17時→10時→03時… と並び「いつ来るか」の形が読めない）。
     🚨 順番に意味がある軸は**折りたたまない**（上位10件で切ると軸が途中で切れる）。
     🚨 その代わり、**全部0の期間は軸ごと出さない**（空の棒が24本並ぶため）
   ・🚨 想定していない値が来ても**落とさず末尾に足す**（DB側が増えたとき黙って消えないように）
   ・✅ **ユーザー確定**：来た方の内訳は**人数だけ**にした。
     🚨 これまで併記していた「◯件」は**記録の行数**で、`once` の間引き後の数。
        回とも人とも違い、業務上の意味を説明できなかったので出すのをやめた

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (113) 見出しを言い換えた（ユーザーの質問がそのまま不具合の報告だった）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザー「答えられなかった言葉　これは入力された言葉ってことでいいのかな？？」
     → **見出しだけでは、お客様が打った言葉なのかどうかが読み取れていなかった**
   ・実装を追って確かめたこと（`FaqWidget.tsx` の `doSearch`）：
     **Enter か🔍ボタンを押したときだけ**記録する（打っている途中は記録しない）／
     **候補が0件だったものだけ**／間引きが無いので**同じ方が3回検索すれば3回**（＝単位は「回」）
   ・✅ **ユーザー確定（案A）**：`答えられなかった言葉（新しい質問を作る材料）`
     → **`検索して見つからなかった言葉`**。条件は見出しに詰め込まず**下の説明行**に逃がした
   ・🚨 CSVの見出しとコード中の呼び名も同じ言葉にそろえた（片方だけ残すと別物に見える）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (114) 🚨 「その回答を書き直す」と書いてあるのに、どれを書き直すか出ていなかった
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザー指摘「回答を読んだが解決しない 1 → これがどれなのか分かるのか？？」
     → **分からなかった。ただし記録（topic_id）は最初から持っていた**。出していなかっただけ
   ・呼び出し元を1つずつ当たって、**6つの理由のうち質問が付くのは3つだけ**と確定：
     付く … `unsolved`（回答の画面から押す）／`noanswer`／`unknown`
     付かない … `search_nomatch` `search_nohit`（**検索結果の画面から押すので質問が決まっていない**）／`load_error`
   ・✅ **ユーザー確定（案1）**：理由の行の下に質問名を並べる（多い順・**上位3件＋「ほか◯件の質問」**）
   ・🚨 質問が付かない3つは**空欄にしない**。「どの質問かは記録できません。検索の結果から
     押すため、質問が決まっていません」と書く（空欄だと不具合で消えているのか区別が付かない）
   ・🚨 記号は **`・`** を使った（`└` は検証済みの一覧に無いため）
   ・実測：9月ぶんは `回答を読んだが解決しない 1人 ／ ・休会したいのですが 1人`。
     🚨 9/22 に書き残した「**この回答が不十分かもしれない**」の「休会」がそのまま出てくる

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (115) 🚨 連絡板の返信：ベルが飛ばない／検索で開かない を直し、バッジと印を付けた（push `f9d08685`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：ユーザーが実機で返信を確かめて「バッジつかないの？」「ベルの飛び先は？」
     「検索は件名も？返信も対象？」「名前でも探したい」
   ・🚨 **ベルを押しても何も起きなかった**（(101) の作り損ね）。飛び先の判定（`classifyNotif`）は
     通知の**文言**で連絡板かどうかを見ていて（`お知らせ`／`メッセージが届き`／`リマインド`）、
     「↩ ◯◯から返信が届きました」がどれにも当たらず **path:null** に落ちていた。
     → **`event_key` が `board:` で始まるか**で見るようにした。🚨 文言に語を足す直し方はしない
       （次に文面を変えた人がまた同じ穴を開ける）。reference_id には元のお知らせのIDが入っていたので、判定だけで直った
   ・🚨 **検索で返信を押しても開かなかった**。受け取った返信は受信トレイ経由で検索に出るのに、
     一覧は `parent_id is null` で読むので詳細が見つからない。→ **元のお知らせを開く**。
     結果には「↩ お知らせへの返信」。見つからないときは黙らず理由を出す。
     あわせて**自分が書いた返信**も検索に出るようにした（どの問い合わせにも入っていなかった）
   ・検索は**本文＋件名**（`subject`）の両方が対象だった（実測：お知らせ70件すべて subject あり）
   ・✅ **ユーザー確定（モックで確認）**：返信は**ナビの数字**／**連絡板の左の数字**／
     **元のお知らせの行に「↩ 返信 ◯」**。開けば既読になって消える。🚨 返信が一覧に1行として並ぶことはしない
   ・🚨🚨 **作っている途中で見つけた穴**：本番の最初の返信は「**管理者が送ったお知らせに林さんが返信**」で、
     管理者から見ると元のお知らせは**送信トレイ**にあった。**返信を受け取るのは多くの場合送った側**。
     受信トレイにだけ印を出すと、送った人は**ナビの数字だけ増えてどこにも印が無い**＝
     9/21 に避けた「開いても何も無い」をそのまま作り直すところだった。
     → 送信トレイ（とそのアーカイブ）にも印、サイドバーの送信トレイにも数字（それまで常に0）
   ・印は `ReplyUnreadMark` 1つを3か所（受信・送信・送信のアーカイブ）で呼ぶ。色は既存の `#4a90d9`
   ・9/21 に入れた「返信をナビの数字に数えない1行」（App.tsx）は**意味が逆になった**。
     いまは**数える**（行き先の印ができたため）。コメントに経緯を残した
   ・🚨 既読の書き込みは**返信では今回が初めて**。本番の権限で**取り消し前提の検算**をした：
     管理者 未読の返信 1 → 既読にして 0／元のお知らせは送信トレイ側＝true／林さん 未読 1／
     林さん 自分の返信を読める 1件。🚨 取り消し後に**何も残っていないことも実測**
   ・プッシュは来る（`board:dm_message` は push-dispatch にある）。**飛び先は連絡板のトップ**のまま。
     🚨 変えるには共用の `push-dispatch` を触る必要がある（場所予約の担当と相談）。
     トップに着けばサイドバーの数字から辿れるので、今回は触っていない
     → ✅ **同日 (117) で直した**（ユーザー「事後報告でいいからやって」）

   ・🚨 **同日の追加指摘「開かないと見えない」**：未読のときだけ印を出していたので、
     **読み終わると一覧からやり取りがあったことが一切分からなくなっていた**。
     ✅ **ユーザー確定（カラーの見本で比べて案B）**：**件数をいつも出す**。
       未読あり … 青いベタ「↩ 返信 2件（未読1）」／全部読んだ … 薄い青の札「↩ 返信 2件」。
       部品は `ReplyCountMark` 1つ（受信・送信・送信のアーカイブの3か所）。薄い札は返信の枠と同じ色（新しい色なし）。
       総数は「自分が読める返信」（RLS が絞る）＝開いたときに見える数。
       実測：管理者・林さんとも **2件**（開いたときの2件と一致）。
       🚨 一覧は自動更新のたびに作り直されるので、対象のIDと未読の数が変わったときだけ数え直す
     （見送った案：A＝最新の返信を薄い青の枠で本文まで／C＝「誰から・いつ」を1行）
   ・同日：返信の枠の中が**中央寄せ**になっていた（外側の詳細の枠から受け継いでいた）→ **左寄せ**に（ユーザー確定）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (116) 名前で検索／送信予約の札（push `f9d08685`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・✅ **ユーザー確定**：打った言葉が**送信者名**にも当たる（本文・件名・送信者名のどれか）。
     名前は読み込み済みの在籍者一覧（`allProfiles`）から id に直して or 条件に足す＝**問い合わせは増えない**。
     🚨 在籍者だけなので、**退職した方が送ったものは名前では引けない**（本文・件名では引ける）。
     🚨 読み込みが検索より後に終わる場合に備えて、検索の effect の deps に `allProfiles` を足した。
     実測：林さんで「管理者」→ 本文・件名だけなら0件、名前込みで50件
   ・✅ **ユーザー確定（案B）**：送信プレビューの「送信予約」を**薄紫の札**（背景 `#f3e8ff`・枠 `#c084fc`・
     文字 `#6b21a8`・ライトダーク共通の固定色）。🚨 以前は文字色 `#6f42c1` だけで、ダークで沈んでいた。
     `#6f42c1` はアプリで8か所使っているが**文字色に使っていたのはこの2か所だけ**（他は背景＋白文字）。
     2か所を `ScheduledChip` 1つにまとめた

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (117) 🚨🚨 ベルと連動するプッシュが 8/26 から**一度も連動していなかった**／返信のプッシュの飛び先
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：返信のプッシュの飛び先を直そうとして、push-dispatch の bell: true の仕組みを読んだ
   ・🚨 **本番の enqueue_push_notification（notifications の後ろで push_queue に積むトリガー）が
     notification_ids を積んでいなかった**。20260818 で足した処理を、20260826（受信時間帯・緊急）が
     **古い版から起こして上書き**していた（CLAUDE.md の「RPCは本番の実定義から起こす」と同型の事故）
   ・実測：push_queue に残っている **133件すべて notification_ids が空**（7日で消えるので9/17以降しか見えない）。
     → bell: true の全イベント（受理・差し戻し・出張報告・残業提案など）で、押しても**ベルが開かず光らず**、
       着地画面に着くだけだった。エラーは出ないので誰も気づけない
   ・直した：`20260924203627`＝本番の実定義（8/26 版）に「IDを積む」を足し戻した。緊急の格上げはそのまま
     ・取り消し版で検算：同じ宛先×同じ対象の2件目は合流して ids=2・緊急に格上げ／別対象は ids=1。
       取り消し後に検算の行が0件、本番の定義が旧版のままであることも実測してから適用
     ・適用後：定義に push_urgent／array_append／array[NEW.id] が揃う・トリガー有効 を実測
     🚧 **本物の通知でIDが積まれたかはまだ見ていない**（適用が夜で、新しい通知が来ていなかった）。
        次に `select event_key, cardinality(notification_ids) from push_queue order by created_at desc limit 10;`
   ・返信のプッシュ：event_key を **`board:dm_message:reply`**（下位キー）に分けた（BoardPage の sendNoticeReply）
     ・🚨 設定（ON/OFF）は `baseEventKey`＝先頭2つ＝`board:dm_message` を見るので**新しい設定の行は要らない**
       （＝「設定が無い＝全員」の穴は開かない。宛先は返信の相手1人のまま）
     ・push-dispatch の EVENT_MAP に `bell: true`。✅ **本文はユーザー確定（案A）「お知らせに返信が届いています」**
       （見送り：B「連絡板に返信が届いています」／C「連絡板のお知らせに返信があります」）。
       🚨 DMと同じ文面にしないこと。まとめる鍵（宛先×アプリ×文面×URL）が同じになり、DMと1通に混ざる
     ・押すと /board に着いてベルが開き、その返信が光る → 押すと元のお知らせが開く（(115) の classifyNotif）
     ・`lib/notifications.ts` の「メッセージを読んだらベルも既読」の対象にも足した
     ・🚨 **新しい文面は社長端末で1通試す決まり**（push-dispatch 冒頭）。まだ試していない
     ・古いキー（`board:dm_message`）で作られた過去の返信の通知は、今までどおり /board に着く

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (118) 残業のメモの時刻欄を、他の欄と同じ入力（TimeInput）にした
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザー指示「メモの時間入力、他と合わせてほしい」
   ・メモ（`OvertimeMemoSection.tsx`）だけ `<input type="time">` のまま＝**iPhoneでドラム**になっていた。
     残業の他の欄は 2026-08-26 から `TimeInput`（時・分の2枠・テンキー）
   ・開始と終了の両方がある種類は、開始の分を打ち終わると終了の時へ進む（advance）。「いま」ボタンはそのまま
   ・🚨 スタッフが触る画面で `type="time"` が残っているのは**これで0**。管理画面側
     （KidsShiftPanel・ShiftEditModal・OvertimeEditModal など）はまだ `type="time"`（触っていない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (119) 🚨 休暇の穴 第3段を締めた（アーカイブ (39) の続き・migration 20260924205429）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・`insert_own`：**本人の分・status=pending・承認者（approver_id／approver2_id）に自分を入れない**の3つ
   ・`update_admin`：WITH CHECK を新設。承認者が書き換えられるのは**他人の申請だけ**。管理者・社長は今までどおり
   ・締める前に入口を全部当たった：本人の申請（LeaveRequest）・調整休の提案受諾（OvertimeProposalResponse）は
     どちらも pending／取り消し・編集・有給奨励日は SECURITY DEFINER の RPC／承認は他人の申請だけ／
     自分で自分を承認した行は本番で0件
   ・🚨 **管理画面が他人の分を受理済みで作る2か所**（有給奨励日の修正・差し戻し時の種別変更）は、
     別の許可 `admin_insert_leave_requests`（`is_org_wide()`＝社長・管理者の2名）で通る。触っていない。
     🚨 **途中で「この2か所は今も失敗している」とユーザーに誤報した**。原因は pg_policies の結果を
        `tail -40` で切って読み、この許可を見落としたこと。**許可の一覧は必ず全部読む**（なりすましの検算で気づいた）
   ・なりすまし（取り消し版→適用後にもう一度）で確認：一般＝pending 通る／approved・承認者に自分・他人の分 弾く／
     他人の申請を承認 通る／自分の申請を承認 弾く。管理者＝他人の分を approved で作る 通る／更新 通る
   ・🚨 **画面も1か所直した**：休暇の申請先の候補に**自分が出ていた**（リーダー・マネージャーが申請するとき）。
     選ぶと DB に弾かれて申請できなくなるので `leaveApprovers` から自分を外した。
     同じファイルの「了承者」（調整遅出・早退）は休暇の承認ではないので触っていない

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (120) シフト調整の手順9（公開）は**まだやらない**（2026-09-24 ユーザー確定：先に実機確認）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・本番の実測（2026-09-24）：場 38件（未調整18・決定11・変更なし4・過去で閉じた5）／
     **決定で入る人の登録 0件／パートへの依頼 0件**／相談 1件
     → 決定済み11件は古いカレンダーのボタンの書き写し。**新しい画面の「決定」「依頼」は一度も使われていない**
   ・場が無い受理済みの今後の休み **13日分**（手順9(a) で作る対象）
   ・お知らせ（直近7日）：古い `leave:shift_adjust_due` 6通／新しい `shift_adjust:digest` 2通。
     🚨 新しいまとめは**社長・管理者だけ**（権限がテスト中のため）。「上長に毎日2種類」は言い過ぎだった（ユーザーに訂正済み）
   ・権限：新しい5つは社長・管理者だけ ON／古い `leave_shift_adjust` はリーダー・マネージャー・社長・管理者
   ・▼ **社長・管理者に試してもらう項目**（「次にやること」1 にも書いた）
     ① 案を作る（`shift_adjust_plan`）→ 別の人が「確認した」を押せるか
     ② 候補のパートに「出勤のお願い」を送る → パート本人の /shift-request に出る → 返事が送った人に届く
     ③ **先の日付**の場で「決定」→ 勤怠に登録される／正社員には通知が飛ぶ（🚨 本物の登録。自分が対象の場か先の日付で）
     ④ 「決定を取り消す」→ 勤怠が消え、依頼が取り下げになる
     ⑤ 毎朝のまとめを押すとシフト調整のタブで開く
   ・済んだら手順9を (a)→(b)→(c) で**日を分けて**。(c) で宛先が増える（宛先＝権限×役職）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (121) 残業の「表でまとめて入力」（PCだけ）を設計した（未実装・🚨 docs は未push）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・西村先生の相談（スプレッドシートで1か月ぶんを1行1日で書いている）。計画は **`docs/計画-残業の表入力.md`**（§10 が正）
   ・✅ ユーザー確定：通知はベル1件ずつ・メール/Slack はまとめる／行は「その日の状態」で事前申請・事後報告・実績報告・再提出が切り替わる
     （見本HTMLで確認）／申請先は表の上で1人＋行ごとに変更／**機能権限 overtime_grid・最初はマネージャー（acts_as=manager）だけ**
   ・2体レビューを2回。裏取りで分かったこと：本番の残業の**メール・Slack は OFF**（ベルとプッシュだけ）／
     時間帯だけ失敗したとき取り消す案は許可上できない（本人は cancelled に書き換えられない）→ 入れ直し＋要確認に変更
   ・🚨 **実績報告・再提出の行は最初は送らない**（何もしないと送信になる形は危ない）。第4段の前に見本を直して確認し直す
   ・出す順：①判定の切り出し（画面変化なし）②送信処理の切り出し ③表を読み取り専用 ④入力 ⑤送信（新規→実績報告・再提出→まとめ）。9〜12回ぶん
   ・別件：`overtime_update_reviewer` に WITH CHECK が無い／2本シフトの種別判定の疑い（該当5人・9件）／欠勤の自己受理の順位条件の疑い

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (122) 残業の表入力：第1〜3段を実装（ユーザー「実装もやるから push して進めて」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・🚨 **前提の不具合を先に直した**（push `d3d62cc8`）：種別の自動判定が**1本目の帯だけ**と比べていた。
     2本シフトの5人で誤判定（「9:15〜12:30＋14:00〜16:15」は午後を働くだけで残業／「9:30〜17:30＋朝6:30〜7:15」は朝の帯で早出）。
     `lib/overtimeShift` の `normalShiftWindow`（いちばん早い始まり〜いちばん遅い終わり）と比べるようにした。
     1本シフト・深夜またぎ・休みは結果が変わらないことを検算。🚨 朝の帯を夜へ移した日は「開始が遅い理由（調整／遅刻）」の2択が出るようになる（実態に合う）
   ・**第1段**（push `407b48df`・画面の変化なし）：判定を `lib/overtimeSubmit.ts` へ
     （toWorkSegments／segmentIssuesOf／detectOvertimeTypes／composeApplicationTypes／effectiveLocationOf／validateOvertime／overtimePhase／canReportOvertime）。
     検算＝**HEAD の画面内のコードを機械で抜き出して「正解」にし**、無作為24万回＋狙った16通りで比較・不一致0。わざと壊すと検出できることも確認
   ・**第2段**（push `aee1b319`・画面の変化なし）：buildOvertimeRecord（保存する1行）／friendlyOvertimeDbError を lib へ、
     書き込みは `lib/overtimeSubmitApi.ts`（saveOvertimeReport／syncOvertimeGcal）。組み立ては無作為5万回・不一致0。
     🚨 動きの変化は1つだけ：**修正の記録（overtime_report_history）を書き換えが成功した後に残す**（先に受理されていた時に記録だけ残っていた）
   ・**第3段**（この記録と同じ push）：権限 `overtime_grid`（migration `20260924224740`・**適用済み・マネージャーだけ ON**を実測）。
     残業ページの「事前申請・事後報告」タブに［📋 表でまとめて入力（試験中・パソコンだけ）］。
     **いまは見るだけ**（`components/OvertimeGrid.tsx`・判定は `lib/overtimeGrid.ts`・状態の色は `lib/overtimeStatus.ts` に移した）。
     シフト・会社カレンダー・申請は給与期間の範囲で自分で読み、**1つでも読めなければ赤で出して表を出さない**。
     判定は23通りで検算（期間の日付・年またぎ・うるう年・各状態の振り分け）
   ・🚧 **実機未確認**（ログインが要る）。見てほしいこと：マネージャー（または管理者）で PC から残業ページ → ボタンが出る／
     スマホ・タブレットでは出ない／表が1か月ぶん並ぶ／札（事後報告・事前申請・実績報告・再提出・済み）が実際の状態と合う／◀▶／ダーク
   ・次は**第4段の前に見本を直して確認し直す**（計画 §10-14）。そのあと入力（まだ送れない）
   ・🚨 社内FAQ・画面の注意事項は、試験が終わって広げるときに直す（いまはマネージャーだけの試験中）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (123) 連絡板：ベル・プッシュから自分が送ったお知らせを開けなかった（push `bca2bad6`）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザーが実機で発見：返信のベルを押すと「このお知らせは開けませんでした。送信者が取り消した（削除した）可能性があります」
   ・原因：ベルから開く処理（BoardPage の openInboxId）が「自分が**宛先**に入っているか」だけを見ていた。
     返信は多くの場合**送った側**に届くので、送信者には宛先の行が無く誤ったメッセージになっていた。
     🚨 (115) で「受け取るのは送った側」と書いておきながら、ベルから開く経路を見落としていた
   ・直した：自分が送った（または写しを受けた）お知らせなら**送信トレイの詳細**（bv=outbox&bout=）で開く。
     送信トレイの詳細は送信済み・アーカイブの両方から探すので、アーカイブ済みでも開ける
   ・🚧 実機で「同じベルを押して返信のやり取りが開くか」を確かめてもらう

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (124) 残業の表入力：第4段（入力と行ごとのチェック。**まだ送れない**）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザーが見本2を確認（「ok」）→ 実装。見本2は scratchpad の overtime-grid-mock2.html（生成は gen-mock2.cjs）
   ・行の計算は `lib/overtimeGrid.ts` の `computeGridRow`（中身は1件フォームと同じ lib/overtimeSubmit）。行の状態は
     view／empty／idle（実績報告・再提出の触る前＝送らない）／nochange／editing（入力中は赤くしない）／error／warn／ok
   ・🚨 **「通常シフトと同じ」の判定を `isSameAsNormalShift` に出した**：送信前チェックでは理由のチェックが先に来るため、
     表で普段どおりの日が「理由を入力してください」と赤くなっていた（検算で発見）。送信前チェックも同じ関数を使う＝動きは不変（24万回比較で確認し直した）
   ・実績報告の行：予定が最初から入る／［予定どおり］［残業なし］［送らない］／変えたら「予定から変わった理由」欄。申請先は元の申請のまま
   ・新しい行：初めて触ると通常シフトが入る（2本シフトの入れ忘れ防止）／行ごとに申請先（空＝表の上）／自己受理はマネージャー以上だけ
   ・［予定どおりの日を送る対象に入れる］＋［元に戻す］／［エラーの行だけ表示］／締め後は表の上のバナー1つ（新しい行だけ止める）
   ・下書き：`DRAFT_KEYS.overtimeGrid` ＋「:利用者ID:給与期間」。状態が変わった日の下書きは捨てて一言知らせる
   ・送信ボタンは押せない（「準備中」）。🚧 次は**第5段（送信）**：新規だけ → 実績報告・再提出 → まとめ
   ・検算：行の計算 23通り（残業・休日出勤・事前/事後/今日・自己受理・2択・締め・実績報告の予定どおり/変更あり/残業なし・再提出）
   ・🚧 実機未確認：時刻を打つ／Enter で下の行へ／［予定どおり］［残業なし］／エラーの出方／下書きが残るか（画面を閉じて開き直す）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (125) 残業の表入力：入口の不具合2つ／第5段の1つ目（新しく出す日だけ送れる）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・🚨 **管理者でもボタンが出なかった**（push `ed989dcd`）：「画面幅1024px以上」の条件のせい。Windows のノートPCは表示の拡大
     （125%・150%）でブラウザの幅が900px前後になる。→ 幅では絞らず「マウス操作の端末か」だけにした
   ・**ボタンの位置**（push `14f12d51`）：白い入力の枠の中で枠が二重になっていた（ユーザー「見にくい・位置が変」）。
     ✅ ユーザー確定（案A）：タブの下・「確認ページへ」と同じ形の青いボタン
   ・**第5段の1つ目**（この記録と同じ push）：新しく出す日（事前申請・事後報告）だけ送れる。実績報告・再提出は次に足す
     ・［◯件を確認して送信］→ 確認（申請先ごと・自己受理は「送った時点で確定」）→［◯件を送信する］
     ・送る直前にその行をいまの時刻でもう一度チェック。確認のときと種類が変わっていたら送らない（今日の行は時刻で事前／事後が変わる）
     ・1行ずつ順に `saveOvertimeReport`（時間帯は2回まで入れ直す）。途中の失敗で他は止めない。二度押しは ref で止める／送信中は離脱警告・表は触れない
     ・🚨 23505（同じ日が既にある）：差分と理由が同じなら「送信済み」、違えば「要確認」。どちらも通知は送らない
     ・🚨 時間帯だけ保存できなかった行は「要確認（履歴から修正）」で再送させない（取消済みに戻すことは許可上できない）
     ・通知：ベルは1件ずつ（`notifyOvertimeNewRequestBell` を新設）／メールは申請先ごとに1通（`notifyOvertimeNewRequestEmail`・いまは OFF）／
       Slack は1件ずつ（いま OFF。🚨 ON にする前にまとめる作りが要る＝send-overtime-slack に report_ids[]）。1件フォームの `notifyOvertimeNewRequest` は中で同じ2つを呼ぶだけ＝動きは不変
     ・カレンダー同期は全部入れ終えてから1件ずつ。失敗したら結果カードに［カレンダーに反映し直す］
   ・🚧 **実機未確認**。最初は**先の日付の1件を自己受理で**送って、履歴に出るか・カレンダーに載るか → 1件フォームから取り消す、で確かめてもらう

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (126) 🚨 役職プレビュー中、上のボタンを押しても交通費から移れなかった（2026-09-25・hooks/useAuth.ts）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザーが実機で発見：マネージャーのプレビューで「残業・時間」などを押しても交通費のまま。
     勤務変更・カレンダーだけ移れた（この2つはページの門で権限を見ていない）
   ・原因：useAuth はページごとに作り直され、プレビュー役職の権限を**毎回空から読み直していた**。
     届く前の最初の描画で canOvertime などが false になり、ページの門が `<Navigate to="/" />` で戻していた。
     🚨 今回の表入力で入れたものではなく、前からあった穴（出張報告・備品精算・場所予約なども同じ）
   ・直した：読んだ役職の権限を画面の中で覚えておく（previewPermsCache）＋まだ届いていない間は loading を true にする
     （門は「読み込んでいます」を出して待つ）。読めなかったときは空の権限で読み込みを終える（以前と同じ見え方）
   ・🚨 **2人共通ファイル**（useAuth.ts）。値は変えていない＝場所予約の担当には事後に連絡

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (127) 残業の表入力：第5段の2つ目（実績報告・再提出も表から送れる）（2026-09-25）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・✅ 第5段の1つ目の実機確認：ユーザーが 9/24 を自己受理の事後報告で送信（本番で実測：confirmed・時間帯1本・
     ベル0（自分宛のため）・カレンダー0（事後報告は載せない仕様）＝1件フォームと同じ）。🚧 試しの1件（理由「test」）は取り消してもらう
   ・送る直前に**その申請を id で読み直し**、表を読み込んだときと中身が違えば送らず「要確認」（計画 §10-5）。
     比べるのは行の計算に使う項目だけ（`lib/overtimeGrid.ts` の `sameGridReport`。状態・種別・勤務地・休憩・理由・申請先・シフトの控え・カレンダー・時間帯）
   ・書き込みは1件フォームと同じ `saveOvertimeReport`（status を条件に update・0件なら「要確認」）。修正の記録の言葉も同じ
     （`computeGridRow` が変わった項目 `changedAxes` を返すようにした＝「実績報告（変更あり：時間帯・休憩）」）
   ・🚨 カレンダーに載せるか（show_on_calendar）は**実績報告・再提出とも元の値を引き継ぐ**（計画 §10-7）。
     buildOvertimeRecord は再提出で null にするので、表の側で元の値に戻している（1件フォームの再提出は欄を出して聞く）
   ・通知は1件フォームと同じ条件：ベルは元の申請先へ1件ずつ。**残業なし（差分0）はその場で確定するので送らない**。メールは申請先ごとに1通（OFF）
   ・送ったあとはその日の入力を**消す**（空で上書きしない。再提出を事前申請として出した日は「実績報告」の行に変わるため、読み直した状態から作り直す）
   ・検算：行の計算16通り（予定どおり／時間変更で理由なし→エラー・理由あり→送れる／残業なし／休憩の変更／再提出／sameGridReport の一致・不一致）
   ・✅ **実機で確認（2026-09-25）**：
     ① 新しい行：長岡さん宛の事前申請 → ベル1件＋push_queue の notification_ids=1（🚨 **(117) のベル連動の復旧を本物の通知で初めて確認**）＋カレンダーに【申請中】
     ② ［残業なし］の実績報告：試しの申請を DB で 9/18・事前受理済みにしてから送信 → confirmed（確定者＝本人）・差分0・種別なし・
        **長岡さんへのベルは増えない**・修正の記録「残業なし（通常どおり）で報告」・カレンダー0件（9/27 の予定も同期で消えた）
     ③ 試しの2件（9/18・9/24）と時間帯・記録・ベルは、ユーザーの許可を得て DB から削除（取り消し版で2件だけ減ることを確かめてから。削除後も実測）
     🚧 まだ：［予定どおり］で「実績 確認待ち」＋申請先にベル／時間を変えて「実績報告（変更あり：時間帯）」
   ・**狭い画面で時刻の欄が縮んで見えなくなっていた**（ユーザー発見・push `f5e0095b`）。共用の TimeInput は空いた幅いっぱいに伸びて0まで縮む。
     🚨 部品は触らず、表の側で幅（104px・縮ませない）を渡した
   ・🚧 残り：申請の依頼がある日を行に出して結び付ける（計画 §10-10・**まだ作っていない**）／Slack をまとめる作り（ON にする前に）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (128) 残業の表入力：「申請の依頼」がある日を表に出して結び付ける（2026-09-25・計画 §10-10）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・✅ **ユーザー確定（案A）**：状態の欄に「📩 ◯◯さんから申請の依頼」＋メモ＋依頼の時間。初めて触ると依頼の時間を入れる／
     申請先の既定は**依頼した人**（表の上の申請先より先）／送ると「事前申請（依頼に答える）」で、依頼を **applied＋linked_id** にする
     （見送り：B＝出さずに裏で結び付けるだけ／C＝その日はフォームで）
   ・中身は1件フォームの「依頼から申請」と同じ：kind=overtime・status=open の自分宛だけ／update は status=open を条件に件数を見る／
     失敗しても送信は成功のまま（依頼が open のまま残るだけ・console に出す）。許可は `appreq_update`（宛先本人は更新できる）を本番で確認
   ・🚨 依頼を付けるのは**新しく出す日の行だけ**（申請が既にある日は、1件フォームの依頼カードから）
   ・🚨 表の勤務地は1か所しか持てない。依頼の校が時間帯で変わるときは勤務地を入れず本人に選ばせる（1件フォームは「A→B」で入れる）
   ・🚨 依頼が読めなくても表は止めない。表の上に「依頼とは結び付きません」と黄色で出す
   ・本番の依頼は少ない（これまで6件・残業は1件＝対応しないで閉じた）。🚧 実機未確認（いま open の残業の依頼は0件なので、試すなら上長に1件作ってもらう）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (129) 残業の表入力：Slack を種類ごとに1通にまとめた（2026-09-25・計画 §10-3）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・🚨 **Slack を ON にする前に必要だった作り**。これまで表からは1行ずつ呼んでいた（ON にすると1か月で最大31通）
   ・Edge Function `send-overtime-slack`（共用9本ではない）が `report_ids[]` も受け取るようにした。
     🚨 `report_id`（1件）の呼び方は今までどおり＝ほかの5か所（1件フォーム・overtime-approve・管理画面の取消）は触っていない
   ・✅ **ユーザー確定（案A）**：1日1行の一覧。見出しに件数
       `🕐 残業・時間｜申請（3件）` ／ `対象者： 西村 ◯◯` ／ `・9月16日（火）　残業　18:00〜19:30　四条本校`
     （見送り：B＝今の1件の形を並べる（長い）／C＝件数だけ（中身が分からない））
   ・まとめる単位は**1回の送信につき種類ごとに1通**（申請＝new_request／自己受理＝confirmed）。
     🚨 Slack の宛先はチャンネルなので、メールと違って申請先ごとには分けない
   ・打刻ズレを除いて1件しか残らないときは今までの1件の形（1件フォームと同じ見た目）
   ・🚨 **まとめて呼べるのは本人の申請だけ**（service_role を除く）。他人の申請IDを並べて流せないように。上限62件。
     Slack の枠は1つ3,000文字までなので、長いときは枠を分ける（62件で2枠・1行も欠けないことを検算）
   ・ついでに直した食い違い：自己受理の「残業なし」（差分0）を表から送ると Slack の「受理」を送っていた（1件フォームは送らない）→ 送らない
   ・画面側は `sendOvertimeSlackBatch`（lib/overtimeNotify.ts）。invoke の error と中身を見て console に出す（送信そのものは成功のまま）
   ・本番の設定では残業の Slack は **まだ OFF**。🚧 ON にするときは、まず自分の申請を表から2件送って1通にまとまるかを見る
   ・✅ **本番反映済み**（Edge Function deploy・push `1f1754e7`・本番のチャンクのバイト数が手元と一致）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (130) 連絡板：送ったお知らせの「写し」を、あとから外せるようにした（2026-09-25・BoardPage.tsx だけ・DB変更なし）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・ユーザー依頼「管理者に共有を付けてしまったものを後で外したい」。🚨 画面に「管理者に共有」という言葉は無い。
     指していたのは作成画面の**「他の代表者の送信履歴に加える」**（緑のチェック・既定ON）＝ `board_messages.cc_user_ids`
   ・🚨 **仕組みの確認（ユーザーに一度誤って伝えた）**：チェックが出るのは代表者だけだが、既定ONなので
     **代表者以外が送ったものにも必ず写しが付く**（外せない）。これは作ったときからの意図（アーカイブ「名簿外の人が送ってもCCは記録される」）。
     実測：お知らせ71件のうち写しあり41件。代表者以外の送信は全部付いている
   ・✅ ユーザー確定：対象は**代表者（管理者を含む）が外し忘れたもの**／外せるのは**送った代表者本人と管理者**
   ・✅ ユーザー確定（見本2のあと、さらに簡単に）：送信トレイの詳細で、下のボタンの**下に小さく右寄せ**で
     `写し 3人 ▼` → 押すと `写し：林・濱口・…` と［写しを外す］（下線の文字）→ 黄色の確認［やめる］［写しを外す］→
     薄緑のカード「写しを外しました」。**写しが無いときは何も出さない**（「写し：なし」「写し：✕」は出さない。✕は閉じる・削除に使っているため）
   ・出すのは外せる人だけ（`renderCcRow`）。写しで見ているほかの代表者・代表者以外の送信者には出さない。
     数えるのは「写しに入っている かつ いまの代表者」＝実際に見られる人（`canSeeConfirmStatus` と同じ考え方）
   ・🚨 管理者がほかの人のお知らせで外すと、**管理者自身も写しから外れて送信トレイから消える**（管理者も代表者の1人のため）。
     確認文に「（送った方の送信トレイには残ります）」を足し、外したら詳細を閉じる。名前の一覧では自分は「あなた」
   ・外すのは `cc_user_ids` を null にするだけ（宛先・対応状況・返信は触らない）。付け直す操作は作っていない
   ・あわせて：写しで見ているほかの代表者にも［✏️ 修正する］［完全削除］が出ていた（押すと権限で弾かれて赤いエラー）→ 送った本人と管理者だけに
   ・確認：更新の権限は `board_messages_update`＝送った本人か管理者（新しく広げていない）。この表の書き換えで動くトリガーは無い（新規作成のものだけ）。社内FAQに写しの記述は無い
   ・🚧 実機未確認：代表者で写し付きのお知らせを送る → 送信トレイで開いて「写し ◯人 ▼」→ 外す → 別の代表者の送信トレイから消えるか／
     写しで見ている側に修正・完全削除が出ないこと／管理者でほかの人のお知らせの写しを外すと画面が閉じるか
   ・🚨 **気づいたが直していないこと**：送信トレイの「アーカイブ」は `board_messages.outbox_hidden`（お知らせ1件に1つ）なので、
     **管理者が写しで見ているほかの人のお知らせをアーカイブすると、送った本人の送信トレイでもアーカイブに移る**。
     ほかの代表者（管理者以外）がアーカイブしようとすると権限で弾かれる。直すなら人ごとの持ち方にする（要相談）
   ・✅ 本番反映済み（push `d12759bb`・本番の BoardPage のバイト数が手元と一致）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (131) 残業の表入力：実機で残りを全部確認／見つかった見た目の不具合を直した（2026-09-25）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・✅ **実機で確認できたこと**（林さん＝社長のアカウント。申請先は管理者にして本物のスタッフに通知を飛ばさない）
     ① 自己受理で 10/1・10/2 に事前申請 → DB で 9/17・9/18・申請先＝管理者・confirmed_by 空に書き換え（上長が受理した行と同じ形）
     ② 9/17［予定どおり］→ reported・修正の記録「実績報告（予定どおり）」／9/18 時間変更 → reported・「実績報告（変更あり：時間帯）」・変わった理由あり。
        管理者にベル2件・push_queue の notification_ids=1（2件とも）
     ③ 管理者から 10/5 の依頼 → 表に「📩 管理者さんから申請の依頼」「てすと」・申請先の既定＝依頼した人（管理者）・触ると通常シフト →
        送ると requested・依頼は applied＋linked_id・管理者にベル1件
     ④ 後片付け：カレンダーの予定3件（gcal-sync delete）→ 申請3件・時間帯5・修正の記録2・ベル4・依頼1 を削除。すべて0件を実測
   ・直したこと（ユーザーの実機指摘）：
     ・確認の枠・送れなかった行の知らせを開いたら**そこまで自動でスクロール**（［送信する］が画面の外に出ていた）
     ・送信後のカードを**画面中央の薄緑（🎨🔒 固定色）**に。ダークで暗い緑になっていた＝配色の決まり違反。送れなかったときだけ表の下に黄色（固定色）
     ・右端の「送る？」の列を**右に固定**（狭い画面で切れていた。日付の列を左に固定しているのと同じ作り。今日の行の半透明の色はこの列だけ不透明に）
     ・確認の枠の申請先が「（元の申請先）さん宛」→ 名前。申請先の候補（reviewers）に管理者が入っていないため。
       表を読むときに「依頼した人」「元の申請の申請先」の名前も1回で読む（peopleNames）
     ・✅ ユーザー確定：確認の枠の説明は1文だけ「1日ずつ、いつもの申請として登録されます。」
     ・表のボタンを「履歴・実績報告」タブにも出す（ユーザー指摘：実績報告も表から送れるのに、そのタブに無かった）
   ・権限：ユーザーが画面から overtime_grid を**リーダーにも ON**（フロア責任者は含めない・ユーザー確定）
   ・✅ ユーザー確定：名前を **「📋 残業申請「表入力」（パソコン専用）」**（ボタン・表の見出し。**試験中は付けない**）。
     「まとめて」は事後だけの雰囲気が出るので避けた。表の上の「試験中です」も外した。権限管理の項目名も「残業申請「表入力」」
   ・✅ 社内FAQを足した（migration `20260925140348`・**適用・実測済み**）：「残業申請の「表入力」とは？（パソコン専用）」
     （残業・時間管理 の 305＝「どこから申請する？」の下）。見える人はリーダー・マネージャー・社長・管理者。
     🚨 既存の「残業はどこから申請する？」には足していない（一般の方にも見えるので、使えない機能へ案内してしまう）。
     🚨 **表入力を使える役職を変えたら、この FAQ の対象役職（faq_answer_targets）も合わせる**
   ・画面の注意書き（残業ページの【注意事項】）は「マネージャーだけ」と書いておらず、直す所は無かった

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (132) 確認の枠が画面の外に出る件をアプリ全体で直した／権限管理の表の見出しを固定（2026-09-25）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：表入力・申請の依頼で［確認する］を押しても最後のボタンが見えなかった → ユーザー「他もないか確認して直して」
   ・調べ役に候補約30を当たらせ、指摘の場所は自分で開いて確かめてから直した。部品は **`hooks/useScrollIntoViewWhen.ts` 1つ**
     （🚨 画面ごとに scrollIntoView を書き写さない）。使っている所：
     表入力（確認・送れなかった知らせ）／申請の依頼（ApplicationRequestSheet）／残業の1件ずつの送信の確認・「対応しない」の理由（OvertimePage）／
     こどもシフト表の保存の確認・決定の確認（KidsShiftPanel）／シフト管理の保存の確認（ShiftManagementTab）／掃除当番の保存の確認（CleaningRosterPanel）
   ・勤怠カレンダーの登録の確認（CalendarPage）は中央の小窓だが高さの上限が無く、重なりが多いと［確定する］が切れた → maxHeight 90vh＋中でスクロール
   ・問題なしと判断したもの：中央の小窓で高さの上限と中のスクロールがあるもの（休暇・出張・備品・精算・交通費・残業の提案・連絡板など）／
     ボタンの位置で小さく入れ替わるもの（安否確認・打刻の問い合わせ・締め後の許可依頼など）
   ・権限管理（FeaturePermissionsTab・🚨 2人共通）：表を画面の高さの枠に入れて**役職名の行を上に固定**（案A・ユーザー確定）。
     スイッチにマウスを乗せると「リーダー：ON」。🚨 border-collapse の表では固定した見出しの下線が消えるので影で引いている
   ・🚧 どれも実機未確認（管理画面はこちらで開けない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (133) 🚨 関数の入口の門が、有効な管理者の鍵を断っていた（_shared/callerGate.ts・7本 deploy）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・発覚：後片付けでデータベースから gcal-sync を呼んだら 401（cron と同じ net.http_post＋vault の service_role_key）
   ・原因：門は「関数側の SUPABASE_SERVICE_ROLE_KEY と**文字が完全に一致するか**」だけで判定していた。
     Supabase が関数側の鍵を **2026-09-25 10:41 JST に入れ替え**（SUPABASE_* の Secrets がこの時刻に一斉更新）、
     vault の鍵（6/24 登録・JWT 形式）と文字が一致しなくなった。🚨 vault の鍵は今も有効（REST で 200 を実測）。
     指紋（sha256 の先頭）で別物と確認：vault aaecb8f6… ／関数側 2861813b…（🚨 鍵の値そのものは見ていない）
   ・業務への影響は無かった：cron 15本はどれも門のある関数を**直接は呼んでいない**。直近24時間の呼び出し752件で 401 は今回の3件だけ
   ・直した：文字が一致しなくても、**管理者の鍵の形**（sb_secret_… か role=service_role の JWT）なら、その鍵で
     `auth.admin.listUsers(1件)` を1回試し、通れば service として通す（実行環境のあいだは覚える）。ログインしたスタッフの判定は不変
   ・検算：anon の鍵 → 401 のまま／偽の service_role JWT → Supabase の入口で弾かれる／vault の鍵 → gcal-sync delete が 200
   ・deploy した7本：gcal-sync・send-email・slack-notify（🚨 **共用**）／overtime-approve・overtime-grant-request-notify・
     send-overtime-slack・shift-report-confirmed-notify。deploy 前に fetch（相手の変更0件）・7本とも deno check エラー0
   ・🚨 **今後 cron から門のある関数を直接呼ぶときも、これで通る**。vault の鍵を書き換える必要は無い

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (134) 🚨 実績報告の自己受理／自分で確定できる人を DB で締めた（migration 20260925144144・適用・実測済み）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：ユーザー「結果報告で自己受理がない。できると受理ページに行かなくてよくなる」（管理画面で太田さんの 9/24 が確認待ちに見えた）
   ・原因：実績報告の申請先は元の申請のまま（本人の uuid）。自己受理の判定は「画面で『自己受理』を選んだか」だけだったので、
     自己受理した事前申請の実績報告は**本人あての確認待ち**になり、自分あてのベルも届き、受理ページで自分を確認し直していた（本番で26件）
   ・✅ ユーザー確定：**元の事前申請を自己受理していて、本人がいまもマネージャー以上なら、実績報告は送った時点で確定**。
     1件フォーム（OvertimePage の isSelfReview）と表入力（lib/overtimeGrid の computeGridRow・selfId を追加）の2か所を同じ判定に。
     申請先の欄に「自己受理」。ベルは送らない。Slack は自己受理と同じ「受理」（OFF）
   ・🚨 **調べていて見つけた穴（ユーザー「いま合わせて直して」）**：
     ① 自己受理をマネージャー以上に絞っていたのは**画面だけ**。DB は欠勤しか止めておらず、一般でも自分の残業を confirmed / request_confirmed で保存できた
     ② 欠勤の自己確定の判定 `overtime_role_rank(auth.uid()) <= 2` が、9/10 の属性化で**管理者(1)・社長(2)だけ**になっていた
       （順位は roles.sort_order から計算。管理者が上に入りマネージャーが 3 に下がった）。画面は「マネージャー以上」＝食い違い。本番の欠勤の自己確定は0件
   ・直した：①② とも **is_manager_plus()（属性）** で判定。
     ・overtime_insert_own / overtime_update_own の欠勤の条件を is_manager_plus() に（本番の実定義から起こした）
     ・トリガー `trg_enforce_overtime_self_confirm`：**本人が自分の申請を confirmed / request_confirmed に「変える」ときだけ**見る。
       通すのはマネージャー以上／「残業なし」（差分0・種別なし）／「打刻ズレ」（差分0・clock_only）。
       サーバー（auth.uid() 空）・他人の申請の受理・状態を変えない書き換え・自動計上は見ない。
       🚨 RLS にしなかったのは、WITH CHECK では前の状態が見えず「受理済みの事前申請のカレンダー切替」まで止めてしまうため
     ・anon の実行権限なし（実測 false）
   ・検算（森本さん＝リーダー／長岡さん＝マネージャーになりすまし・13通り・適用前と適用後の2回）：
     通る＝リーダーの申請・実績報告・打刻ズレ・残業なしの確定・上長の受理・受理済み事前のカレンダー切替・マネージャーの自己受理と欠勤の自己受理／
     止まる（すべて新しい確かめで）＝リーダーの自分で確定・自分で事前受理・申請中を自分で確定・欠勤の自己受理・他人名で事前受理。
     書き換えは「0件で通った」ではないことも確かめた
   ・🚧 実機未確認：マネージャーで、自己受理した事前申請の実績報告を送る → その場で確定・ベルが来ないこと
   ・🚨 **自己受理の役職を変えるときは、画面（canSelfReview）と DB（is_manager_plus）の両方**。どちらも役職の属性 is_manager_plus を見ている

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (135) シフト調整：社内FAQを3件＋1行（migration 20260925170256・適用・実測済み）／お知らせの下書きの誤りを直した
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・これまでのFAQは 9/9 のチップ（未／調整済）の説明だけで、シフト調整タブのことが1つも無かった
   ・足した（勤怠カレンダー・740/750/760・見える人＝マネージャー・社長・管理者）：
     「「シフト調整」タブで何ができる？」／「「決定する」の「勤怠に登録する」「残業申請を依頼する」は何？」／
     「パートの方に「出勤のお願い」を送るには？」。既存「シフト 未／調整済…」の最後に「だれが入るかまで決めるときは…シフト調整タブ」を1行
   ・パートの方向けは足さない（ユーザー確定）
   ・🚨 **お知らせの下書きで「勤怠に登録する」と「残業申請を依頼する」を「選ぶ」と書いていたのは誤り**。
     決定する人が**パートなら勤怠（休日出勤）、正社員なら残業申請の依頼**が付く2つのチェック（kindOf＝employment_type）。下書きは直して提示済み
   ・🚨 **画面にまだ無い機能**：権限管理の「案を作る」（shift_adjust_plan）「案を確認した」（shift_adjust_review）は画面で使っていない。
     表 shift_adjust_plans / plan_reviews も未使用（案を保存は shift_adjust_saved_plans）。→ ユーザー「つくって」。**次にやること**（下）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (136) シフト調整：「決定を取り消す」で本人に知らせる／「この方に決定」の印を外す（2026-09-25）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・それまで：取り消しても本人に何も届かず（「出勤が決まりました」を見た人は出勤するつもりのまま）、
     出勤のお願いの picked が残って「この方に決定」が出続けていた
   ・DB（migration `20260925171111`・適用済み）：shift_adjust_undecide を**本番の実定義から起こし** picked=false の update を1つ足しただけ（機械で差分確認）
   ・画面（ShiftAdjustTab の undecide）：取り消す前の割り当てを控え、成功したら1人ずつベル。✅ ユーザー確定の文面（C案）：
     見出し「📅 9/30（火）の出勤についてのお知らせ」／本文「出勤の予定は取り消しとなりました。ご調整いただいていたところ、申し訳ございません。」
     正社員だけ「残業の申請がお済みの場合は、お手数ですが取り消しをお願いいたします。」を足す（applied の依頼は取り消しの関数が触らないため）
   ・スマホ：push-dispatch（🚨 共用・deploy 済み）の EVENT_MAP に `shift_adjust:cancelled`（→/shift-request）と `shift_adjust:cancelled:ot`（→/overtime?tab=history）。
     文は「出勤の予定についてお知らせがあります」・bell: true。設定は baseEventKey＝shift_adjust:cancelled（行は無い＝ON）。
     ベルの行き先は App.tsx の classifyNotif（source_type `shift_adjust:cancelled` / `shift_adjust:cancelled_ot`）。🚨 2か所を揃えること
   ・検算：取り消し版で決定済みを仮に作り、長岡さんになりすまして取り消し → 印 1→0・割り当て0・調整中・anon 不可。本番は元のまま
   ・✅ 林さんの端末で1通試した：ロック画面「出勤の予定についてお知らせがあります」・ベルの見出しと本文・押すと /shift-request（ユーザー確認済み）。試しのベルは削除済み

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (138) 🚨 シフト調整：案を何通りも作れる／確認した／意見の期限（1〜3段目・2026-09-25）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・仕様は **`docs/計画-シフト調整.md` §6-2 が正**（一問一答＋UI/UX とエンジニアの2体レビュー）
   ・1段目 DB（migration `20260925185551`・適用・なりすまし17通り×2回）：plans に 番号・担当・確認する方へ・更新日時・
     確認のやり直しの記録・期限の知らせ済み／slots に plan_seq／関数5つ（plan_create・update・delete・review・unreview）＋知らせの関数／
     案を消すトリガー（決定・休みの取消・日の経過・現行シフトで対応）／通知設定の行6つ／**plans・plan_reviews の直接の書き込みを止めた**
   ・2段目 push-dispatch（deploy 済み・林さんの端末で2通とも確認）：`shift_adjust:plan_created`「シフト調整案のご確認をお願いします」／
     `shift_adjust:plan_due`「意見の期限を過ぎた案があります」
   ・3段目 画面（ShiftAdjustTab・この記録と同じ push）：案のカード（確認 ◯人と名前・意見の期限・直したら確認はやり直し）／
     入力欄は押したときだけ開き、先頭にモード（新しい案／案◯を直す／案◯の内容で決定）／置き換える前に確認／
     現行シフトで対応の前に「案は消えます」／一覧「案 2件・期限過ぎ」／ベルから場をいきなり開く（App.tsx）／管理画面「通知」に2行（NotificationsTab）
   ・✅ ユーザー確定：決定の欄に「**本人にお知らせを送る**」（既定ON）。OFF なら依頼・勤怠の登録はするがベル・スマホは送らない・メモも送らない
   ・社内FAQ（migration `20260925205249`・適用済み）：タブの説明の④⑤と決定の説明を差し替え
   ・🚨 いまの「案を保存」（saved_plans・save_plan）は**画面から使わなくなった**が、DB にはまだある（0件）。5段目で片付ける
   ・✅ 4段目（migration `20260925205420`・適用済み）：`shift_adjust_notify_plan_due()` を cron `shift-adjust-plan-due`（15分ごと）で。
     期限を過ぎた案に expired_notified_at を立てた行だけ作った人へベル（＋スマホ）。在籍していない・見る権限なし・ベル OFF は印だけ。
     検算：取り消し版で1回目1件・2回目0件・期限前は対象外。anon・authenticated から実行不可
   ・🚧 5段目（数日後）：saved_plans の表・shift_adjust_save_plan・trg_zz_shift_adjust_clear_saved_plan を片付ける（0件・画面は使っていない）
   ・🚧 実機未確認：マネージャーで案を作る→別の人で確認した→直すと確認が消える→この案で決定へ→決定、の一巡

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (137) 連絡板：本文の URL を押せるリンクにした（2026-09-25・ユーザー「至急」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・連絡板は**もともと本文の URL をリンクにしていなかった**（今日の変更で壊れたのではない）
   ・部品 `components/LinkifiedText.tsx` 1つ（http/https だけ・末尾の句読点と閉じかっこは含めない・別タブ・クリックを外へ伝えない）。
     使っている所は BoardPage の全文4か所（お知らせ／グループ・DM の本文＝renderMsg、お知らせへの返信、スレッドの親と返信）
   ・🚨 一覧の2〜3行の要約には使っていない（行を押すと開く作りなので、押し間違いのもとになる）
   ・🚨 正規表現の全角スペースは `　` と書く。Edit の道具は `　` を本物の全角スペースに置き換えるので、node で書いた（ESLint の no-irregular-whitespace）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (139) 残業：差し戻しの再提出で種別（終日 ↔ 時間）を変えられるようにした（2026-09-25・DB変更なし・旧「次にやること 2c」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・2体レビュー（UI/UX・エンジニア）→ 指摘をコードで裏取り → ✅ ユーザー確定＝**案A（再提出の画面の中で切り替える）**。
     見送り＝案C（既存の「取り消して同じ内容で作り直す」cancelAndCopy へ送る導線）。理由：同じ画面で終わる・履歴1件・ベル1回。
     🚨 レビューが「重い」と言った7点は、実際は1行の直しが4つ・数行が2つ・仕様として書くだけが1つ＝30〜50行（言い過ぎだった）
   ・画面（OvertimePage.tsx・OvertimeForm）：
     ・切り替えボタンの外枠と中身の条件に `isResubmit` を足した（🚨 外枠 `date && (!editTarget || isResubmit || fullDay)` を直さないと「時間→終日」が出ない）。
       実績報告（isReportPhase）には出さない。文言は再提出だけ「…で再提出 ─ 押すと時間の申請に変えられます」（「申請中」は差し戻し中には嘘）
     ・終日→時間に戻したとき、時刻が1つも無ければ通常シフトを入れる（✅ ユーザー確定 A）。打ってあれば触らない（往復で消さない）。
       2択（lateChoice/earlyChoice）は切り替えでリセット
     ・`typeSwitched`（開いたときの終日かどうか ≠ いまの fullDay）で、切り替え直後の青い案内1行・送信確認の1行・修正の記録「再提出（終日 → 時間の申請に変更）」を出し分け
     ・再提出の見出しの下に**差し戻し理由**（return_comment）を赤い枠で出す（今まで履歴タブにしか無かった）
     ・調整休の二重計上チェック（leave_auto と同日）を `!editTarget` → `!isReportPhase` に（再提出でも効く）。🚨 DB にこの重複を止める制約は無い＝画面が唯一の網
     ・上長へのベルの phaseLabel を再提出は「再提出」に（今までは「事前申請」）。プッシュの文面は event_key 固定なので不変
   ・🚨 **欠勤の申請先の穴（新規でも同じ）**：先にリーダーを選んでから「欠勤」を押すと、選択肢からは消えるのに reviewerId は残り、リーダー宛に送れた。
     DB・Edge Function にも止める検査は無い（overtime-approve は reviewer_id 本人なら受理できる）。→ 欠勤を押した時点で外して黄色で知らせる
     （`absenceReviewerDropped`）＋ validateOvertime に任意項目 `absenceReviewerOk` を足して送信前でも止める（ERR_FIELD_BY_MSG に登録済み）。
     表入力は渡さないので不変。本番の欠勤2件は申請先マネージャー以上＝実害0
   ・lib：`saveOvertimeReport` は時間帯が空（＝終日）なら phase を問わず全部消す（実績報告後に差し戻された申請は予定＋実績があり、
     片方だけ消すと calendar_overtime_events が種別を見ずに時間帯を読んで「欠勤なのに時刻付き」で出る）／
     `buildOvertimeRecord` は **再提出かつ終日のときだけ** request_confirmed_at を null に。
     🚨 差し戻し（overtime-approve の return）は status と理由しか書き換えないので、事前受理→実績→差し戻しの申請には事前受理の日時が残る。
        一律に消すと時間のまま再提出した人の受理ページに「⚠️ 事前申請の受理をしていません」が嘘で出る
   ・仕様として書いておく（直していない）：事前／事後は編集中に変えられないので、**事後で出した欠勤を時間に戻すと必ず「実績報告」として出る**／
     終日→時間で「カレンダーに載せる」は未チェック（載せない）から始まる（新規と同じ）／往復したとき fullDayType は前の選択が残る
   ・検算：lib 35項目（overtimePhase 4・buildOvertimeRecord 24・validateOvertime 7）／本番DBで取り消し版のなりすまし4通り
     （リーダー 時間→欠勤(長岡宛) 通る・リーダー 欠勤の自己受理 42501 で止まる・マネージャー 時間→欠勤の自己受理 通る・リーダー 欠勤→時間 通る／
      終日にしたとき時間帯が両 phase とも0件）。取り消し後の件数は前と同じ（申請130・時間帯189）
   ・🚨 本番で差し戻し中の 10/17 欠勤は**林さん（社長アカウント）の申請**（申請先＝長岡さん）。「太田さん」と書いていたのは誤り
   ・🚧 実機未確認：林さんで 10/17 を「修正して再提出」→ 差し戻し理由が上に出る／🌙 ボタンで時間に変える→通常シフトが入る→再提出→
     長岡さんに「再提出が届きました」／逆に時間の申請を欠勤に変えて、申請先にリーダーを選んでから欠勤を押すと黄色で外される

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (140) 連絡板：送信トレイのアーカイブを「お知らせ × 人」ごとに持つ形に（2026-09-26・migration 20260926004919・適用・実測済み・旧「次にやること 2b」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・(130) の末尾で見つけた穴。✅ ユーザー確定「これで直して」。
     それまで `board_messages.outbox_hidden`（お知らせ1件に1つの印）だったので、管理者が写しで見ている他人のお知らせを
     アーカイブすると**送った本人の送信トレイでもアーカイブに移り**、管理者以外の代表者は権限で弾かれ、
     送信者が片付けたお知らせは写しの側から**どこにも見えなかった**（写しの読み込みが outbox_hidden=false で絞っていた）
   ・DB：新しい表 **`board_outbox_hidden`（message_id × user_id）**。受信トレイのアーカイブ（recipients.archived＝人ごと）と同じ持ち方。
     権限＝読む・付ける・外すは自分のぶんだけ（管理者も他人のぶんは触らない）／付けるときは「そのお知らせが自分に見える」が条件
     （board_messages の select 権限が副問い合わせにも効く）／anon なし・authenticated は select/insert/delete だけ
     （🚨 既定の権限で all が付いていたので `revoke all … from authenticated` してから付け直した。取り消し版で update=true と出て気づいた）。
     既存の印3件（林・長岡・管理者が自分のお知らせに付けたもの）を送信者本人のぶんとして写した。
     🚨 **`board_messages.outbox_hidden` の列は残してある**（戻すときの値。画面はもう読まない・列コメントに書いた）
   ・画面（BoardPage.tsx）：loadOutbox は自分のお知らせ＋写しを**絞らずに全部**読み、`board_outbox_hidden` の自分の行で一覧／アーカイブに分ける。
     BoardMessage.outbox_hidden は「いまの自分が片付けたか」の意味に変わった（DB の列ではない）。
     archiveOutboxMsg＝自分の行を upsert／unarchiveOutboxMsg＝自分の行を delete（🚨 この表に id 列は無い。message_id で数えて describeUpdate に渡す）。
     記録が読めなかったときは黙って「全部未整理」にせず赤で出す。検索の select から outbox_hidden を外した
   ・🚨 写しで見ている他人のお知らせもアーカイブに並ぶようになったので、**一括の「完全削除」の対象は送った本人（か管理者）のものだけ**に。
     チェックも出さない。判定は `canDeleteNotice` 1つ（詳細の「✏️ 修正する」「完全削除」と同じもの・2か所が呼ぶ）
   ・検算：tsc／build／rules-of-hooks 0／ESLint 48→47。本番DBで取り消し版（migration＋なりすまし6通り→rollback）：
     写した行3／管理者が写しのお知らせを片付ける＝通る・林さんの一覧と元の印は不変／林さんは自分の行だけ見える／
     林さんが管理者の行を消す＝0件／関係ない人・他人名の書き込み＝42501／anon 不可。適用後も同じ項目を実測
   ・見え方の変化：林さん「てすと」・長岡さん「あ」は本人がアーカイブ済みだが、写しを持つ管理者の送信トレイには**これから出る**
     （管理者が自分で片付ければ管理者側だけアーカイブへ）
   ・🚨 **実機で1回目は失敗した**（管理者がアーカイブ → 「permission denied for table board_outbox_hidden」）。
     原因＝画面が `upsert` で書いていた。upsert は DB では「あれば更新」（insert … on conflict do update）なので **UPDATE の権限が要る**が、
     この表は更新を使わない方針で authenticated に UPDATE を付けていない。取り消し版の検算は素の SQL（on conflict do nothing）で
     通していたので、**画面と同じ道を通っていなかった**。→ 素の insert にして、すでにある（23505）は成功扱い（同日 push）。
     取り消し版で ①insert=1行 ②二度目は 23505 ③upsert は 42501 を実測。失敗した1回目は DB に何も残していない
     🚨 教訓：**RLS・権限の検算は、画面が呼ぶのと同じ命令（upsert なら on conflict do update）で流す**。素の insert で通しても upsert の権限は確かめられない
   ・🚧 実機未確認：管理者で写しのお知らせをアーカイブ → 送った本人（林さん）の送信トレイに残っているか／
     アーカイブから戻す／アーカイブの一覧で他人のお知らせにチェックが出ないこと／社内FAQに送信トレイのアーカイブの記述は無い（直す所なし）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (141) FAQ集計：検索した言葉の数え上げを DB 側（RPC）に寄せ、500 件の打ち切りをなくした（2026-09-26・migration 20260926073634・適用・実測済み・旧「次にやること 3」）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・それまで FaqAnalytics は faq_query_log の行を新しい順に 500 件まで画面に読んでから数えていた（打ち切ると多い順の並びが狂うので断りは出していた）。
     本番の実測（2026-09-26）：全 106 件・多い月でも 34 件＝まだ打ち切りは起きていなかった
   ・DB：関数 **`faq_query_log_summary(p_from, p_to)`** → (is_public, raw_query, n, miss)。期間内の言葉を社外／社内で分けて group by。
     🚨 security definer にしない（faq_query_log の select は can_edit_faq() の人だけ＝その権限がそのまま効く）。既存の faq_public_event_summary と同じ形。
     anon は明示的に revoke（実測 false）
   ・画面：2本の select＋画面での数え上げ → rpc 1回。社外は miss>0 の行を miss の数で（候補0件の回数だけ・今までと同じ意味）、社内は全部（n と miss）。
     LOG_LIMIT・wordsCut・staffWordsCut と「🚨 新しい順に500件まで…」の断り2か所は削除
   ・検算：本番で取り消し版＝関数の結果と生の group by を集合で突き合わせて差分0（48行・合計106）／anon 不可／
     FAQ を編集できない一般の人が呼ぶと 0行（RLS で何も見えない・エラーにならない）。適用後も同じ値を実測
   ・🚧 実機未確認：管理画面 →「📋 利用状況の集計を見る」で「検索して見つからなかった言葉」「社内FAQで検索された言葉」が今までどおり出るか
     （数字は 9/22 に0件へ戻したあと溜まったぶん。社外の見つからなかった言葉は本番0件なので「この期間はありません」が正しい）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ (142) 連絡板：お知らせのテンプレート（個人／全体）を作っている（2026-09-26〜・①②まで push・③④はこれから）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   ・きっかけ：ユーザー「お知らせを送るときテンプレートを使えるようにしたい。個人と全体を使い分けて登録もできて、管理画面で全体の管理」
   ・✅ ユーザー確定（一問一答・蒸し返さない）：
     ①テンプレに入るのは**件名・本文だけ**（宛先・期限・確認の要求は入れない）
     ②個人テンプレ＝お知らせを送れる人なら誰でも登録・修正・削除。本人だけが見る
     ③全体テンプレ＝登録／**他の人が作ったものも含めて**修正・削除 は権限「連絡板：全体テンプレートの登録・変更」（feature `board_template_global`）。
       最初はリーダー・マネージャー・社長・管理者 ON（is_leader_plus）。権限管理で変えられる。使う（読む）のは全員
     ④分類＝管理画面「連絡板」タブで管理者が管理。最初の7つ：連絡／お願い・依頼／注意喚起／行事・イベント／勤怠・シフト／研修・勉強会／その他
     ⑤探し方＝文字検索（名前・件名・本文）＋分類の絞り込み＋［全体］［自分の］
     ⑥送信トレイ・受信トレイで開いたお知らせからも「テンプレートに保存」できる（③で入れる）
   ・2体レビュー（UI/UX・エンジニア）→ 裏取りして直した点：
     ・分類は**文字ではなく id**で結ぶ（文字だと管理者が改名しても他人の個人テンプレ＝RLS で見えない＝の分類を書き換えられない）。消すときは active=false で隠す
     ・update の with check を insert と同じ式に（自分の個人の持ち主を他人に／全体を他人の個人に押し付け を塞ぐ）
     ・owner_id は on delete set null（退職は auth.users を消す経路＝cascade だと全体テンプレが消える）。迷子の個人テンプレは cron で掃除
     ・owner_id / updated_by / updated_at はトリガーで入れる（画面から送らない）／権限の関数は (select …) で包む
     ・画面：ボタンは［クリア］と同じ最上段／一覧は送信プレビューと同じ全面シート／行を押して開く／全体＝分類順→名前順、自分の＝更新順／
       ［使う］は件名か本文があるときだけ黄色で確認／入れたら ⚙️ を開いて青で案内／権限が無い人に「全体」を出さない／💾 は使わない（検証済み一覧に無い）
   ・✅ **①DB**（migration `20260926100016`・適用・実測済み）：`board_template_categories`／`board_notice_templates`／トリガー2つ／feature_permissions の行／
     cron `purge-board-templates-orphan`（3:45 JST）／anon 不可。なりすまし18通り（一般・リーダー・管理者・anon）を取り消し版で実測（#5 #6 #11 は 42501 で止まる）
   ・✅ **②**（この記録と同じ push）：useAuth `canBoardTemplateGlobal`（既定 false）／FeaturePermissionsTab に1行（🚨 2人共通・足しただけ）／
     `lib/boardTemplates.ts`（型・filterTemplates・sortTemplates・categoryChips・categoryLabel・canEditTemplate・needsReplaceConfirm・validateTemplateInput・supabase なし）／
     `lib/boardTemplatesApi.ts`（loadTemplates 1000件で truncated・insert/update/deleteTemplate・insert/updateCategory・42501 は「権限がありません」）。lib の検算 30項目
   ・🚧 **③ 送信画面**（BoardPage）：見本HTML（ライト／ダーク上下）→ 確認 → 実装。［📋 テンプレートから］（全面シート）［テンプレートに保存］を最上段に。
     「置き換えますか」はコピーの確認と同じ枠を共用（汎用の保留アクションにする）。送信・受信トレイの詳細に「テンプレートに保存」。役職プレビュー中は保存不可
   ・🚧 **④ 管理画面**（BoardSettingsTab）：分類の管理（追加・改名・並べ替え・使わない）／全体テンプレの一覧（検索・修正・削除。送信画面と同じ部品を manage モードで）
   ・🚧 社内FAQ：「受信トレイ・グループ・DM の違い」の答えに1行足すか、新しい質問を1つ（③のあと）
   ・割り切り（レビュー指摘・承知のうえ）：個人テンプレの登録は DB では「お知らせを送れる人」に絞れない（画面の設定のため）。本人にしか見えないので実害なし

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0.【2026-09-25 のぶん・最優先】
   ▼ **お知らせを送る**（ユーザーが送る。下書きは会話で確定済み・2通）：
     ①リーダー・マネージャーあて＝表入力（パソコン専用）＋確認画面の自動スクロール＋連絡板の URL リンク
     ②マネージャーあて＝シフト調整（案・確認した・意見の期限・決定・本人にお知らせを送る・取り消しの知らせ・出勤のお願い）
       ＋自己受理した事前申請の実績報告はその場で確定＋写しを外す（代表者）。🚨 ①と②は内容を重ねない（ユーザー指示）
   ▼ **マネージャーにシフト調整の「案」を試してもらう**（ユーザー依頼済み）→ 出てきた点を直す → 問題なければ 5段目（saved_plans を片付け）
   ▼ 実機確認：表入力（中央の薄緑カード・右端の列・履歴タブのボタン・名前「残業申請「表入力」」）／権限管理の見出しの固定／
     連絡板の写しを外す・URL リンク／マネージャーの実績報告の自己受理（その場で確定・ベルが来ない）／
     🚨 一般・リーダーがいつもどおり申請・実績報告・残業なしを送れるか（DB で自己受理を締めたため）
   ▼ 権限：シフト調整の案・確認した（shift_adjust_plan / _review）はいまマネージャー・社長・管理者だけ。
     ✅ ユーザーの方針＝最終的にリーダーまで。フロア責任者は「見るだけ」か検討中
1.【実機確認】🚨 こちらでは画面を開けない。**9/21〜22 のぶんが丸ごと未確認のまま**
   ▼ 🚨 **(117) ベル連動の復旧**：受理か差し戻しの結果プッシュを1件押して、**ベルが開いて該当の行が光るか**
     （8/26 以来はじめて動くはず）。あわせて push_queue の notification_ids が埋まっているかを SELECT で
   ▼ **(117) 返信のプッシュ**：🚨 まず**社長端末に1通**（新しい文面「お知らせに返信が届いています」）→
     押すとベルが開いて返信が光る → 押すと元のお知らせが開くか
   ▼ **(119) 休暇**：リーダーかマネージャーで休暇の申請画面を開き、**申請先の候補に自分が出ない**こと／ふつうに申請できること
   ▼ **(120) シフト調整**：上の ①〜⑤（社長・管理者で）。済んだら手順9へ
   ▼ **(118) 残業のメモ**：時刻が「時」「分」の2枠になり、iPhoneでテンキーが出るか／「いま」で入るか／
     開始の分を打つと終了の時へ進むか
   ▼ **今回（連絡板の返信）**：🚨 **管理者の送信トレイの「test」に薄い札「↩ 返信 2件」**（もう読んだため）／
     **林さんの受信トレイの「test」に青いベタ「↩ 返信 2件（未読1）」**、サイドバーとナビに数字／
     林さんが開くと青いベタが薄い札に変わり、数字が消える／返信の枠の中が左寄せ／
     返信のベルを押すと元のお知らせが開く／検索で名前（例「管理者」）を入れるとその方のものが出る／
     検索結果の「↩ お知らせへの返信」を押すと元のお知らせが開く／送信予約の札（ダークで読めるか）
   ▼ **今回（FAQ集計）**：管理画面 →「📋 利用状況の集計を見る」→
     ヘッダーが `人 ／ のべ 人 ／ 回` になっているか／内訳が横棒になっているか／
     時間帯が 00時〜23時の順に並び **0の時間も出ているか**／
     「検索して見つからなかった言葉」の見出しと説明行／
     🚨 **つまずいた理由に「・休会したいのですが 1 人」が出ているか**／
     11件以上ある一覧で「残り◯件を見る」が効くか／ダークモード
   ▼ **メール**：受理か差し戻しを1件して、いつもどおり届くか（🚨 (105) の門の最終確認）
   ▼ ✅ **連絡板の返信（9/21 のぶん）は 2026-09-24 にユーザーが実機で確認**（ベルが届くことも確認）。
     そこで出た指摘が (115)(116)。以下は確認の観点として残す：
     作成画面に「↩ 返信を受け付ける」／**既定でOFF**／
     相手に［↩ 送信者に返信する］と「あと30日」／🚨 **他の宛先には何も見えないこと**／
     終了と再開／🚨 チェックを入れずに送ったお知らせには返信の枠が出ないこと
   ▼ **退職チェック表**：［対象外］／項目の［修正］［削除］／
     🚨 済みにした項目の文字を変えても、その行の文字が変わらないこと／
     メモ（🚨 **書いても必須の残りが減らないこと**）
   ▼ **交通費の却下**：退職して申請期間中の方に警告（在籍者には出ない）
   ▼ **役職プレビュー →「退職者」**：🚨 ［× 終了］で戻れること
   ▼ 🚨 **こどもシフト表**：1回目（2026-09-16）から**1度も触られていない**。
     1回目＋2回目をまとめて確認する。1回目の項目はアーカイブの (70)、2回目は (110)
   ▼ **勉強会**：「確認した」を押して、**翌日にも外れていないこと**（(109)）
   ▼ 9/20 以前の分（(93)〜(100)）の未確認項目も残っている
2.【連絡の準備・2026-09-25 ユーザー依頼】リーダー以上へ「表入力を試して」、マネージャー以上へ「シフト調整の更新」の連絡を送る。
   下書きは会話で提示済み（2通・返信を受け付けるで送る案）。🚨 **送る前に**：
   ・表入力：✅ 社内FAQ・名前は済み（(131)）。🚨 連絡文の中のボタン名は「📋 残業申請「表入力」（パソコン専用）」に直すこと
   ・シフト調整：(120) の実機確認 ①〜⑤（とくに ③決定→勤怠に本物の登録）を社長・管理者で先に
   ・実機確認：表入力の見た目の直し（(131)）と確認の枠のスクロール（(132)）
   （表入力の実機確認そのもの＝予定どおり・時間変更・依頼の結び付けは (131) で済み）
2a.【シフト調整・ユーザー「つくって」】「案を作る」「案を確認した」（意見の期限・確認した人・期限切れの知らせ）を作る。
   計画 `docs/計画-シフト調整.md` §6。いまの「案を保存」（shift_adjust_saved_plans・決める権限）との組み合わせを一問一答で決めてから。
   ✅ ユーザーの方針：**最終的にはリーダーまで見られる形**にする。フロア責任者は「見るだけ」にするか検討中
2c.【✅ 2026-09-25 に実装・push】差し戻しの再提出で種別（終日 ↔ 時間）を変えられるようにした → (139)。実機確認は (139) の 🚧
2b.【連絡板】写しを外す（(130)）の実機確認。
   ✅ **送信トレイのアーカイブを「お知らせ × 人」ごとに持つ形に直した（2026-09-26・(140)・DB 適用済み・push 済み）**。実機確認は (140) の 🚧
3.【FAQ】🚨 **不具合も見せ方も片付いた**（(107)(108)(111)〜(114)）。残っているのは
   ・✅ 検索ログの `.limit(500)` の打ち切り → RPC に寄せた（2026-09-26・(141)）
   ・/faq.html の古い15問の移行
   🚨 記録は 9/22 に0件へ戻してあるので、**数日ぶん溜まってから**数字を見ること
4.【要ユーザー作業】他校のIPを登録（各校のPCで `https://api.ipify.org` を開く）。
   四条本校 150.9.88.61 は登録済み。🚨 192.168.x.x は建物の中だけの番号なので一致しない
5.【連絡】場所予約の担当へ（下のまとめ・**事後報告でよい**）。🚨 9/24 の push-dispatch と DB トリガーのぶんも入っている
6.【繰越】シフト調整の手順9（🚨 (120) の実機確認のあと）／表示速度／有給の管理（(A)）／
   電車遅延（✅ アプリは変更しない・2026-09-22 判断）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 場所予約の担当への連絡（✅ 2026-09-26 に伝えた＝`docs/引き継ぎ-場所予約.md` の冒頭に「📩 申請系からの連絡」として書いた。
   下は伝えた内容の控え。次に共用のものを触ったら、この下に「まだ伝えていない」として足すこと）
・🚨 **まだ伝えていない（2026-09-26・(142)）**：2人共通ファイル `hooks/useAuth.ts`（`canBoardTemplateGlobal` を足した・3か所）と
  `components/admin/FeaturePermissionsTab.tsx`（FEATURES に「連絡板：全体テンプレートの登録・変更」を1行）。いずれも足しただけ・値は不変。
  DB は feature_permissions に `board_template_global` の行と、連絡板の表2つ（場所予約には関係しない）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**共用の Edge Function を4本 deploy した**（2026-09-21〜22・いずれも deploy 前に `git pull` 済み）
  - `record-signup-ip` … 地域判定を ip-api.com → MaxMind に差し替え（🚨 非商用枠を商用で使っていたため）
  - `slack-notify` / `send-email` / `gcal-sync` … 入口に「呼ぶ人が在籍者か」の門を足した。
    🚨 **service_role（cron・他の関数）は素通りする**ので、そちらの呼び出しには影響しない。
    🚨 `supabase/functions/_shared/callerGate.ts` を新しく置いた（**このリポジトリ初の `_shared`**）
・**2人共通ファイル**（いずれも足しただけ・値は変えていない）
  - `App.tsx` … 返信をナビの赤バッジに数えない1行／役職プレビューに「退職者」
    🚨 **2026-09-24 に返信の扱いを逆にした**（数えるようにした・(115)）。あわせてベルの飛び先の判定に
    `event_key` が `board:` で始まるものを足した（`classifyNotif`）。どちらも連絡板だけに効く・足しただけ
  - `types/index.ts` … `Profile` に `is_active` / `retiree_access_until`（どちらも任意）
  - `ShiftManagementTab.tsx` … 「こどもシフト表◯件が時間外になります」
・🚨 **2026-09-24：共用の `push-dispatch` を deploy した**（deploy 前に fetch して相手の変更0件を確認）。
  EVENT_MAP に `board:dm_message:reply`（お知らせへの返信・bell: true）を**1行足しただけ**。他の行は変えていない
・🚨 **2026-09-24：通知の仕組みの DB 関数 `enqueue_push_notification` を直した**（(117)）。
  8/26 の上書きで消えていた「ベル通知のIDを積む」を戻した＝**場所予約を含む全部の bell: true が本来の動きに戻る**。
  🚨 この関数を次に直すときは**本番の実定義から起こす**こと（8/26 はここで古い版を使って消した）
・🚨 **2026-09-24：残業の表入力で2人共通ファイルを触った**（いずれも足しただけ）：
  `App.tsx`（useAuth から canOvertimeGrid を受け取り OvertimePage に渡す・2か所）／`hooks/useAuth.ts`（canOvertimeGrid・3か所）／
  `components/admin/FeaturePermissionsTab.tsx`（権限の一覧に1行）／`lib/draftStorage.ts`（下書きのキーを1行）。DB は feature_permissions に overtime_grid の行を足しただけ
・🚨 **2026-09-25：共用の `gcal-sync`・`send-email`・`slack-notify` を deploy した**（(133)）。
  変えたのは共通の門 `_shared/callerGate.ts` だけ（有効な管理者の鍵なら文字が違っても通す）。deploy 前に fetch して相手の変更0件
・🚨 **2026-09-25：2人共通の `FeaturePermissionsTab.tsx` を触った**（見出しの固定・スイッチに役職名を出す。値は変えていない・(132)）
・🚨 **2026-09-25：共用の `push-dispatch` を2回 deploy した**（shift_adjust:cancelled・:ot／plan_created・plan_due を足しただけ・他の行は不変）
・🚨 **2026-09-25：2人共通ファイル**：App.tsx（ベルの行き先：shift_adjust:cancelled・cancelled_ot・plan_created・plan_due）／
  NotificationsTab.tsx（通知一覧に2行・宛先の選べる役職に plan_created）／FeaturePermissionsTab.tsx（見出しの固定・項目名「残業申請「表入力」」）。
  いずれも足しただけ・値は変えていない
・🚨 **2026-09-25：DB で自己受理をマネージャー以上に締めた**（overtime_insert_own / update_own の欠勤の条件＋トリガー）。場所予約には関係しない
・`supabase/config.toml` は**触っていない**
・2026-09-24 は `App.tsx` を触った（上のとおり）。FaqAnalytics.tsx・BoardPage.tsx は申請系だけのもの

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ ✅ ユーザーの決定（蒸し返さないこと）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・**FAQ集計の数字は行ごとに単位を書く**（2026-09-24・案い）。`人` ／ `のべ 人` ／ `回`
・**量を表すチップは横棒にする**（2026-09-24・案B）。上位10件＋折りたたみ。
  時間帯・曜日は時刻順・曜日順で**0のぶんも出す**。内訳は**人数だけ**（「件」は出さない）
・**見出しは「検索して見つからなかった言葉」**（2026-09-24・案A）。条件は説明行に逃がす
・**つまずいた理由の下に質問名を並べる**（2026-09-24・案1）。上位3件＋「ほか◯件の質問」
・**お知らせの返信はバッジに数え、元のお知らせの行に「↩ 返信 ◯件」を読んだあとも出す**（2026-09-24・案B）。
  🚨 返信を一覧に1行として並べることはしない（9/21 の「お知らせの中で往復」を崩さない）
・**連絡板の検索は送信者名にも当たる**（2026-09-24）
・**送信予約は薄紫の札**（2026-09-24・案B）
・**返信のプッシュの本文は「お知らせに返信が届いています」**（2026-09-24・案A）。押すとベル経由で元のお知らせへ
・**共用の push-dispatch は事後報告でよい**（2026-09-24・返信の飛び先について）
・**休暇の穴 第3段を適用**（2026-09-24）／**シフト調整の手順9は実機確認のあと**（2026-09-24）
・（2026-09-22 以前の決定はアーカイブの 2026-09-21〜22 の節と「ユーザーの決定」を参照）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 今日わかったこと（次も踏みやすい）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **数え方を直したら、画面の文字も一緒に直す**。9/22 に中身を「人」へ揃えたのに
  「◯ 回」の文字が残り、**5行のうち4行が嘘**のまま2日間動いていた
・🚨 **「意味を説明できない数字」は出さない**。内訳の「◯件」は説明を書こうとして初めて
  「回でも人でもない」と分かった。**画面に出す前に、その数字の説明文を書いてみる**とよい
・🚨 **ユーザーの質問は不具合の報告**。「これは入力された言葉ってこと？」「これがどれなのか
  分かるのか？」の2つとも、答えは「画面が説明できていない」だった。
  質問されたら**言葉を直す機会**だと考える
・🚨 **記録は持っているのに出していない**ことがある。(114) は DB の変更ゼロで直った。
  「出せない」と思う前に、まず**その列が既にあるか**を見る
・**チップ（枠付きの札）は量を表せない**。幅が文字数で決まる。量は必ず棒の長さで
・🚨 **通知の行き先を「文言」で判定しない**。文面に無い語は拾えず、押しても何も起きない（(115)）。`event_key` で見る
・🚨 **お知らせの返信を受け取るのは、多くの場合「送った側」**。送った側から見ると元のお知らせは送信トレイにある。
  返信まわりの印・数字は**受信と送信の両方**に出す
・🚨 **ESLint を HEAD と比べるときは同じパスで**。`eslint.config.js` にファイル名で指定したルールがあり
  （App.tsx・BoardPage.tsx も入っている）、別名の一時ファイルに写すとそのルールが外れて数が合わない。
  `git stash push -- <2ファイル>` → 数える → `git stash pop` で比べた
・🚨 **周りが `any` でも、足す行は `any` にしない**（1つ足すたびに1件増える）。`as any[]` も同じ
・🚨 **`// eslint-disable-next-line` と対象の行の間にコメントを挟まない**。挟むと効かなくなり警告が2件増える（今日踏んだ）
・**本番の返信の読み書きは、なりすまして `begin … rollback` で確かめられる**
  （`set local role authenticated` ＋ `set_config('request.jwt.claims', …)`。IDは先に `set_config('app.xxx', …)` で持つ）
・**管理画面はこちらで開けない**（ログインが要る）。**見本HTML**（実コードと同じ寸法・配色を
  node で書き出す）ならライト／ダークの見え方を確かめられる。場所予約の担当も同じ手を使っている。
  🚨 ブラウザ枠は**プロジェクト外のローカルHTMLを操作できない**ので、
     切り替えボタンを付けず**ライトとダークを上下に並べた静的HTML**にすること
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
