# 引き継ぎ（備品購入申請・残業・休暇・FAQ・連絡板ほか）

このファイルは **場所予約以外を担当する人の引き継ぎ**で、**最新の1セッション分だけ**を置きます。
（CLAUDE.md から自動で読み込まれるため、大きくしすぎない）

- 過去の記録（2026-08-31 より前の半年分すべて） … `docs/引き継ぎ-申請系-アーカイブ.md`
- 場所予約（/rooms）の引き継ぎ … `docs/引き継ぎ-場所予約.md`
- 2人で開発するときの決まりごと・毎回の作業ルール … `CLAUDE.md`

🚨 **書き方のルール**
1. 新しい記録は、この案内のすぐ下に「▶▶ 次セッション ここから開始」として書く
2. そのとき、**古くなった記録は見出しを「▶ 前セッション」に下げて、
   アーカイブ（`引き継ぎ-申請系-アーカイブ.md`）の先頭へ移す**
3. `▶▶` はこのファイルに**1つだけ**。このファイルには最新以外を残さない

🚨 アーカイブ内の古い記録には「CLAUDE.md 冒頭の引き継ぎを確認」「CLAUDE.md に
セッション記録を保存」と書かれていますが、**2026-08-31 の分割で無効になりました**。
読むのはこのファイル、書くのもこのファイルです。CLAUDE.md に日々の記録は書きません。

## 🔎 探しもの（アーカイブを Ctrl+F / grep する語）

**日付では探せない恒久的なルールが、アーカイブの中にあります。**
`docs/引き継ぎ-申請系-アーカイブ.md` を下記の語で検索してください。
（🚨 アーカイブは非常に大きく、全文を開くことはできません。必ず grep で引くこと）

| 探したいもの | 検索する語 |
|---|---|
| 配色（バナー・選択ボタン・入力欄）※例外なし | `🎨🔒` |
| UIフィードバックの標準（成功は緑カード等） | `UIフィードバック標準仕様` |
| 新規ページの作り方（上余白・タブ・説明枠の型） | `🏗️ 新規ページ` |
| 役職の序列 | `役職序列` |
| プッシュ通知の文面ルール（🚨 旧「検証済み語」の一覧は 2026-09-09 に無効） | `プッシュ文面のルール` |
| 失敗が静かに消えないようにする書き方（error と件数を見る） | `lib/statusUpdate.ts` |
| Excel・CSVの落とし穴 | `Excelが数式` |
| バックアップ・復旧 | `docs/DISASTER-RECOVERY.md` |
| FAQの運用 | `docs/FAQ運用ガイド.md` |

---

## ▶▶ 次セッション ここから開始（2026-09-09 深夜〜09-10・役職の属性化 段0〜3 を本番反映）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【状態】✅ 段0〜3 まで本番反映（master 722f075／リモート一致）。
       段4 ＝ ✅ migration 適用（本番で16項目 OK）・コミット済み・**Edge 20本 deploy 済み**（2026-09-10 00:45・1本ずつ全成功）。
       　　　起動テスト11本 4xx（起動エラー無し）。✅ **通知の実測済み**（勤怠の登録・取消を「宛先＝社長だけ」に一時切替して
       　　　実施。通知の行が社長1人だけに作られ、スマホ・ベルとも到達。設定は退避表から元に戻し、退避表は削除済み）。
       　　　段4 のコミットは **未 push**
・本番DBの変更 **3本**（すべて 取り消しテスト → 適用 → 実測 まで完了）
    20260909231618 段0：roles に acts_as＋フラグ5つ／profiles.role_id を role_title から必ず同期するトリガー
    20260909232945 段1・2：関数9本＋RLSポリシー14本を属性判定に／権限管理に「勤怠カレンダーへの登録・取消」
    （20260910001625 段4 の下地＝role_ids_for / profile_ids_for_roles は **未適用**。ファイルは未コミット）
・コミット4本：c971926（申請まわり8か所）／段0／段1・2／段3（画面22ファイル＋新規2）。**push 済み**
・tsc 通過／vite build 成功／rules-of-hooks 0件／今回のルール 0件
・✅ 本番JSで実測済み：AdminPanel-DIQTtjAE.js に「勤怠カレンダーへの登録・取消」1件／
  main-D6AMW7dT.js に旧「正社員（一般）」0件（2026-09-10 00:20・curl 1回）
・🚨 **2人共通のファイルを触った**：App.tsx／useAuth.ts／FeaturePermissionsTab.tsx／NotificationsTab.tsx／
  notificationDispatch.ts／UsersTab.tsx ほか。**相手へ連絡すること**（文面は下）
・未追跡：AGENTS.md（触らない）／supabase/migrations/20260910001625_…（段4・未適用）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 何をしたか：役職名の直書きをやめ、roles の属性で判定する（docs/計画-役職の属性化.md v2）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
きっかけ：実機確認で「社長」→「代表社長」に改名したところ**本番の権限が壊れた**（同日復旧）。
役職名が7層（コード46／RLS13／関数10／Edge 20／master_options／データ4種）に直書きされていたため。
🚨 私（Claude）の確認手順の指示ミスが原因。組み込み役職の名前を変える必要はなかった。

【決定（2026-09-09 ユーザー）】
  Q1 全段やる／Q2 属性は roles の列／Q3 序列は sort_order 1本（新設は最下位から・管理者は固定で最上位・
  社長↔管理者の同点は解消＝社長から経理の残業集計は見えなくてよい）／Q4 通知の宛先は立場 acts_as で自動／
  Q5 ユーザー作成・削除・お知らせ送信はシステム管理者（app_metadata）だけ／
  勤怠カレンダーは「表示」と「登録・取消」を別トグルに（権限管理に1行追加・名前はユーザー選択）

【属性】roles.acts_as（leader/manager/accounting/president・非一意）＋
  is_approver（承認者・フロア責任者込み）／is_leader_plus（フロア責任者なし）／is_manager_plus（管理者込み）／
  is_board_approver（決裁者・🚨 管理者＝経理を含まない）／is_org_wide（経営）。序列＝sort_order を反転。
  🚨 is_manager_plus と is_board_approver を分けるのが要点。1つに寄せると経理が3万円超の決裁に自動で入る。

【DB】中核 role_is_*(uid) / role_acts_as / role_rank ＋ RLS 用ラッパー is_approver() 等。
  既存 is_leader / is_manager_plus / overtime_role_rank は契約を変えず中身だけ差し替え。
  🚨 RLS から呼ぶ関数は anon の実行権を**外していない**（外すと未ログイン時に評価が失敗する）。
  🚨 検算：50人全員で「旧式＝新式」15項目0／序列2,500ペア0／7役職×9テーブルの見える行数63行一致。
     なりすましは set local role authenticated まで行った（検算用 rls_probe は rollback で消している）。

【画面】lib/roleAttrs.ts（supabase を読まない・検算26項目）＋ hooks/useRoles.ts（1回だけ読んで全画面で共有・
  別キーの端末キャッシュ・AUTH_CACHE_VERSION は上げていない）。useAuth が isLeaderPlus/isManagerPlus/
  isBoardApprover/isOrgWide/actsAs/roleRank/roles/previewRoles を返す。
  DB 直引き15件は roles!inner(...) で属性を条件に。序列の表5か所は rankOf に一本化。
  🚨 埋め込み roles(...) は「1件」と「配列」のどちらでも来る＝lib の embeddedRole() で読む。
  🚨 プレビューの一覧は roles から出す＝「正社員（一般）」→「一般」と表示が変わった。

【意図的に触っていないもの】
  ・Slack の宛先振り分けに役職名を渡す sendLeaveSlack（LeaveApprovals／LeaveRequestsTab／OvertimeProposalResponse）
    … send-leave-slack:80 が role名で振り分けるため、段4で Edge と同時に
  ・faq.ts:423 と OvertimePage の役職ルール照合（役職名で保存されたデータ）… 段5
  ・CalendarPage の ROLE_ORDER（「三役」＝グループ名）… 役職の序列ではない
  ・RoomBookingPage の 'パート'／'管理者'（10005-10006）… 場所予約の領域。相手へ連絡
  ・room_can_use_basic_settings（役職名で判定・場所予約の関数）… 相手へ連絡

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 🚨 次に触る人へ（今夜の教訓）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
・🚨 **組み込みの役職の名前を変える確認をさせない。** 確かめるなら「テスト用」役職を追加→改名→「0人に反映」→削除
・🚨 「role_id はどこからも使われていない」は誤りだった（has_feature_permission / overtime_role_rank が先に読む）。
  今は profiles のトリガーが role_title から必ず導出するので、二度と食い違わない
・🚨 「概念6つ」は粗すぎた。同じ「マネージャー以上」でも管理者を含む群と含まない群がある。
  **箇所ごとの対応表**（docs/対応表-役職の属性化.md・144行）を作ってから置き換えた
・🚨 Hook を `isAdmin || useRoles()` のように || の右に置くと条件付き呼び出しになる（lint が止めた）
・🚨 自動モードの分類器が「複合コマンドでの本番適用」を止めることがある。migration の適用は
  `npx.cmd supabase db query --linked --file <migration>` **単体**で流す
・🚨 supabase db query の結果は JSON の `rows`（`result` ではない）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 次にやること（優先順）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1.【実機確認・最優先／朝いちばん】
   ・ログインして**ナビが減っていない**こと（社長・マネージャー・リーダー・一般で。役職プレビューでも可）
   ・権限管理に「勤怠カレンダーへの登録・取消」の行が出て、リーダー以上 ON になっているか
   ・勤怠カレンダーで登録・取消ができるか（リーダーで）／フロア責任者にはボタンが出ないか
   ・休暇承認・備品購入の承認者候補・通知の宛先ラベルが今までどおりか
   ・役職プレビューの一覧に「パート／一般／フロア責任者／リーダー／マネージャー／社長」が並ぶか
   ・🚨 本番JS（AdminPanel-*.js）に「勤怠カレンダーへの登録・取消」が載っているか（curl 1回）
2.【要連絡】相手（場所予約）へ。文面：
   ─────────────────────────────────────────────
   お疲れさまです。2026-09-09〜10 の申請系の変更で、場所予約側に関わる点を共有します。
   ■ 役職名の直書きをやめ、roles の属性（承認者／リーダー以上／決裁者／経営／立場）で判定する形に変えました。
     DB：roles に acts_as と is_approver 等の列を追加、role_is_*() 等の関数、RLS 14本を属性判定に。
     profiles.role_id は role_title から必ずトリガーで導出されます（手で書いても上書きされます）。
   ■ 2人共通のファイルを触りました（すべて push 済み・722f075）：
     App.tsx／useAuth.ts／FeaturePermissionsTab.tsx／NotificationsTab.tsx／UsersTab.tsx／
     lib/notificationDispatch.ts。作業前に git pull をお願いします。
   ■ このあと Edge Function（push-dispatch を含む20本）を deploy する予定です。
     deploy は関数を丸ごと置き換えるので、こちらは出す前に必ず git pull します。
     そちらで push-dispatch を触る予定があれば先に教えてください。
   ■ 場所予約側に、役職名で判定している箇所が2つあります（こちらでは触っていません）：
     ・room_can_use_basic_settings（'一般,リーダー,フロア責任者,マネージャー,社長,管理者' の既定値）
     ・RoomBookingPage.tsx:10005-10006（'パート'／'管理者' の直接比較）
     改名や役職の新設で壊れる形なので、roles の属性（lib/roleAttrs.ts の attrsFor）に寄せることをお勧めします。
   ─────────────────────────────────────────────
3.【段4・deploy 済み】Edge Function 20本（宛先解決＝rpc('resolve_role_recipients')、
   役職の集合→人＝rpc('profile_ids_for_roles')、認可＝role_is_manager_plus 等／app_metadata（Q5=B））。
   🚨 **通知の実測が残っている**：勤怠登録（attendance-notify）／休暇のマネージャー受理（leave-approved-notify）／
   　 勤務変更の受理（shift-report-confirmed-notify）／時間調整（time-adjustment-notify）／
   　 備品精算の記録（purchase-reimbursement-notify）／締め後の取消アラート（overtime-approve）。
   　 届かないときは Supabase ダッシュボード → Functions → Logs で「宛先を解決できません」を探す
   🚨 attendance-notify / leave-approved-notify / shift-report-confirmed-notify に、旧処理で使っていた
   　 グループ計算（rawGroups / teamMaster / applicantGroups）が未使用のまま残っている。deploy には影響しないが、次に触るとき消す
4.【段5】faq_answer_targets / overtime_calendar_choice_rules を role_id 化、notification_settings の JSON を
   role_id に変換（退避表を残す・件数一致を実測）。room_settings.basic_settings_roles は相手へ
5.【段6】master_options の役職一覧を廃止、rename_role を「表示名だけ」に。FAQ本文・画面の注意事項の役職名
6.【段7】ESLint（形で禁止：role_title === / .in('role_title' / includes(roleTitle）、
   管理画面「役職の区分」カード（UI/UXレビューの文字モック）、新設時の「権限のもとにする役職」、
   属性化が終わるまで属性を持つ役職の改名・削除を保存できないようにする
7.【繰越】docs/残作業-失敗が消える箇所.md の残り43件（設定マスタ12／連絡板18ほか）／
   前セッションの繰越（連絡板グループのシフト調整・FAQの戻る・ベルの closeOnTap ほか）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---


