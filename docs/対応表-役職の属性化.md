# 対応表：役職名の直書き × いま含まれる役職 × 割り当てる属性（2026-09-09 棚卸し・段0）

凡例：**属性** … approver／leader_plus／manager_plus／board_approver／org_wide／acts_as=…／rank／roles一覧（選択肢）／表示のみ（触らない）／DB直引き→roles!inner

🚨 「いま含まれる役職」は実際の配列の中身。同じ名前の概念でも中身が違うものを1つに寄せない（v2 §v2）。

## client/src/App.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 669 | literal | `if (!isAdmin && !['リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'].includes(roleTitle ?? '')) { setPendingCount(0); return; }` | **approver** | 承認待ち件数の表示。useAuth.isApprover を読む |

## client/src/components/AdminPanel.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 528 | literal | `{ key: 'leader_assignments',  label: 'リーダー',      icon: '📋' },` | **表示のみ** | タブ名 |

## client/src/components/LeaveApprovals.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 65 | literal | `const isPresident = roleTitle === '社長';` | **acts_as=president** | 最終受理 |
| 149 | db-query | `.eq('role_title', 'マネージャー')` | **DB直引き→acts_as=manager** |  |
| 287 | literal | `await sendLeaveSlack('manager_approved', profileName || '受理者', 'マネージャー', undefined, undefined, undefined, {` | **acts_as** | Slack 振り分け |
| 331 | literal | `await sendLeaveSlack('accounting_approved', profileName || '経理担当者', '管理者', undefined, undefined, undefined, {` | **acts_as** |  |
| 390 | literal | `await sendLeaveSlack('leader_approved', profileName || '承認者', roleTitle || 'リーダー', selectedManager?.name || '', 'マネージャー');` | **acts_as** |  |
| 534 | literal | `const canSeeAll = isAdmin || roleTitle === 'マネージャー' || roleTitle === '社長' || roleTitle === '管理者';` | **manager_plus** | 全件閲覧 |
| 535 | literal | `const isLeader = roleTitle === 'リーダー';` | **acts_as=leader** |  |
| 876 | literal | `const isManager = roleTitle === 'マネージャー';` | **acts_as=manager** |  |

## client/src/components/LeaveRequest.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 479 | db-query | `.in('role_title', ['リーダー', 'マネージャー', 'フロア責任者'])` | **DB直引き→approver ∧ ¬org_wide** | 🚨 第一承認者の候補＝承認者のうち社長・経理を除く。要確認 |
| 485 | literal | `const ord: Record<string, number> = { 'リーダー': 0, 'マネージャー': 1, 'フロア責任者': 2 };` | **rank** |  |
| 578 | compare | `const leaveApprovers = approvers.filter(a => a.role_title !== 'フロア責任者');` | **leader_plus** | 休暇の承認者からフロア責任者を除く＝リーダー以上 |
| 675 | compare | `const apprKey = selectedApprover?.role_title === 'マネージャー' ? 'manager' : 'leader';` | **acts_as** | 通知キーの振り分け |
| 731 | literal | `const isApprover = ['リーダー', 'マネージャー', '社長', '管理者'].includes(_roleTitle);` | **leader_plus** | フロア責任者を含まない側 |

## client/src/components/OvertimeProposalResponse.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 198 | literal | `await sendLeaveSlack('new_request', proposerName || 'スタッフ', proposerRole || 'リーダー');` | **acts_as** | Slack 振り分け |

## client/src/components/PurchaseApprovals.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 377 | literal | `const ROUTE_LABEL: Record<Route, string> = { leader: 'リーダー', manager: 'マネージャー', board: '全員承認' };` | **表示のみ** | 経路ラベル |

## client/src/components/PurchaseRequestForm.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 250 | db-query | `.in('role_title', ['リーダー', 'マネージャー']).order('role_title').order('name').then(` | **DB直引き→acts_as∈(leader,manager)** | 事前確認の依頼先 |
| 254 | db-query | `supabase.from('profiles').select('id, name').eq('is_active', true).eq('role_title', 'マネージャー').order('name').then(` | **DB直引き→acts_as=manager** |  |
| 260 | db-query | `.in('role_title', ['マネージャー', '社長']).order('role_title').order('name').then(` | **DB直引き→board_approver** | 決裁者（管理者を含まない） |
| 268 | db-query | `.in('role_title', ['マネージャー', '社長', '管理者']).neq('id', user.id).then(` | **DB直引き→manager_plus** | 管理者を含む |
| 275 | db-query | `.in('role_title', ['マネージャー', '社長']).neq('id', user.id).order('role_title').order('name').then(` | **DB直引き→board_approver** |  |
| 349 | literal | `const isLeaderPlus = isAdmin || ['リーダー', 'マネージャー', '社長'].includes(roleTitle);` | **leader_plus** | Q6：管理者を除くが isAdmin で救われるので同値 |
| 350 | literal | `const isManagerPlus = isAdmin || ['マネージャー', '社長'].includes(roleTitle);` | **board_approver** | 🚨 変数名は isManagerPlus だが中身は決裁者。要確認 |
| 351 | literal | `const isPresident = !isAdmin && roleTitle === '社長';` | **acts_as=president** |  |
| 1344 | literal | `事前に確認してもらう（{tier === 'leader' ? 'リーダー・マネージャー' : 'マネージャー'}に依頼）` | **表示のみ** |  |

## client/src/components/ReimbursementForm.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 112 | db-query | `.in('role_title', ['リーダー', 'マネージャー']).order('role_title').order('name').then(` | **DB直引き→acts_as∈(leader,manager)** |  |

## client/src/components/admin/AdminPanelContext.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 1122 | literal | `const updateData = { expenses_data: editingExpenses, last_edited_at: new Date().toISOString(), last_edited_by: '管理者', edit_count: currentEditCount + 1 };` | **表示のみ** | 人名の代替文字 |

## client/src/components/admin/FaqTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 39 | literal | `const ROLE_OPTIONS = ['パート', '一般', 'フロア責任者', 'リーダー', 'マネージャー', '社長', '管理者'];` | **roles一覧** | 段5で対象を role_id 化 |

## client/src/components/admin/FeaturePermissionsTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 289 | db-query | `.eq('role_title', role.name);` | **role_id** | トリガー同期後は .eq(role_id) |
| 314 | db-query | `.eq('role_title', role.name)` | **role_id** | 同上 |

## client/src/components/admin/LeaveRequestsTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 301 | literal | `setLeaveHistory(prev => ({ ...prev, [reqId]: rows.map(r => ({ ...r, changerName: r.changed_by ? (nameMap[r.changed_by] || '不明') : '管理者' })) }));` | **表示のみ** | 人名の代替 |
| 510 | literal | `if (req.status === 'manager_approved') return { role: '③ 経理', name: '管理者', color: '#17a2b8' };` | **表示のみ** | ステップ名「③ 経理」 |
| 1557 | db-query | `const { data: mgrs } = await supabase.from('profiles').select('id, name, role_title').eq('role_title', 'マネージャー').eq('is_active', true).order('name');` | **DB直引き→acts_as=manager** | roles!inner(acts_as) |
| 1626 | literal | `await sendLeaveSlack('manager_approved', '管理者', 'マネージャー', undefined, undefined, undefined, {` | **acts_as** | 🚨 sendLeaveSlack の役職引数は send-leave-slack:80 で宛先振り分けに使われる。名前でなく acts_as を渡す |
| 1642 | literal | `await sendLeaveSlack('accounting_approved', '経理担当者', '管理者', undefined, undefined, undefined, {` | **acts_as** | 同上 |
| 1760 | literal | `<span style={{ marginLeft: 8 }}>{req.modifier?.name ?? '管理者'}</span>` | **表示のみ** |  |
| 1963 | literal | `await sendLeaveSlack('rejected', '管理者', '管理者', undefined, undefined, targetChannel ?? 'leader');` | **acts_as** | 同上 |
| 2019 | literal | `await sendLeaveSlack('cancelled', '管理者', '管理者', undefined, undefined, undefined, {` | **acts_as** | 同上 |

## client/src/components/admin/NotificationsTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 312 | literal | `const TIME_ADJ_ROLE_OPTIONS = ['申請者本人', 'リーダー', 'マネージャー', '管理者', '社長'];` | **roles一覧** | 宛先候補。段5で role_id 保存 |
| 333 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者'];` | **org_wide** | 既定値 |
| 339 | literal | `roles: Array.isArray(p.roles) ? p.roles : ['リーダー', 'マネージャー'],` | **acts_as=leader|manager** | 既定値。段5で role_id |
| 344 | literal | `return { roles: ['リーダー', 'マネージャー'], groupFilter: 'same', orgWideRoles: DEFAULT_ORG_WIDE_ROLES };` | **acts_as=leader|manager** | 同上 |
| 524 | literal | `{ value: 'leader',    label: 'リーダー' },` | **表示（rolesから）** | 宛先キーのラベル。acts_as で roles を引いて表示名を出す |
| 525 | literal | `{ value: 'manager',   label: 'マネージャー' },` | **表示（rolesから）** | 同上 |
| 531 | literal | `{ value: 'leader',    label: 'リーダー' },` | **表示（rolesから）** | 同上 |
| 532 | literal | `{ value: 'manager',   label: 'マネージャー' },` | **表示（rolesから）** | 同上 |
| 553 | literal | `leader:    'リーダー',` | **acts_as** | キー→役職は acts_as で |
| 554 | literal | `manager:   'マネージャー',` | **acts_as** |  |
| 555 | literal | `president: '社長',` | **acts_as** |  |
| 565 | literal | `return [...base, { value: 'president', label: '社長' }];` | **表示（rolesから）** |  |
| 1393 | literal | `const orgWideRoles = Array.isArray(curRecipient.orgWideRoles) ? curRecipient.orgWideRoles as string[] : ['社長', '管理者'];` | **org_wide** | 既定値 |

## client/src/components/admin/OvertimeAdminTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 76 | literal | `const ROLE_CHOICES = ['一般', 'フロア責任者', 'リーダー', 'マネージャー', '社長'];` | **roles一覧** | 段5で role_id |
| 175 | literal | `const [roleTitle, setRoleTitle] = useState('マネージャー');` | **roles一覧** | 既定の選択 |
| 229 | literal | `'社長': 1, '管理者': 1, 'マネージャー': 2, 'リーダー': 3, 'フロア責任者': 4, '一般': 5,` | **rank** | sort_order |
| 231 | literal | `const ROLE_GROUP_ORDER = ['社長', '管理者', 'マネージャー', 'リーダー', 'フロア責任者', '一般'];` | **rank** | sort_order |
| 363 | literal | `setOtHistory(prev => ({ ...prev, [reportId]: rows.map(r => ({ ...r, changerName: r.changed_by ? (nm[r.changed_by] || '不明') : '管理者' })) }));` | **表示のみ** |  |
| 1595 | compare | `const members = staff.filter(s => s.role_title === role && s.employment_type !== 'パート');` | **role_id** | 役職で束ねる表示。名前でなく role_id で束ねる |

## client/src/components/admin/UsersTab.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 134 | literal | `{(masterOptions.role_title.length > 0 ? masterOptions.role_title : ['一般', 'リーダー', 'マネージャー', '管理者', '社長']).map(v => (` | **roles一覧** | フォールバック配列を削除 |
| 478 | literal | `role_title: masterOptions.role_title.length > 0 ? masterOptions.role_title : ['一般', 'リーダー', 'マネージャー', '管理者', '社長'],` | **roles一覧** | 同上 |

## client/src/hooks/useAuth.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 7 | literal | `const APPROVER_ROLES = ['リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'] as const;` | **approver** | → roles を読んで返す |
| 44 | literal | `const PREVIEW_ROLES = ['パート', '一般', 'リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'] as const;` | **roles一覧** | プレビューは roles から（管理者を除く） |

## client/src/hooks/useFeaturePublished.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 12 | literal | `const LEADER_PLUS_ROLES = ['リーダー', 'マネージャー', '社長', '管理者'];` | **leader_plus** |  |
| 13 | literal | `const PRESIDENT_ROLES = ['社長', '管理者'];` | **org_wide** | 「社長のみ」先行公開 |

## client/src/hooks/useLeavePendingCount.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 11 | literal | `if (!isAdmin && !['リーダー', 'マネージャー', '社長', '管理者'].includes(roleTitle ?? '')) { setPendingCount(0); return; }` | **leader_plus** | フロア責任者を含まない側 |
| 15 | literal | `const { data: d3 } = roleTitle === '社長'` | **acts_as=president** |  |

## client/src/lib/faq.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 423 | compare | `if (t.role_title && t.role_title !== viewer.roleTitle) return false;` | **role_id** | 段5：対象の照合を role_id に |

## client/src/lib/notificationDispatch.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 95 | literal | `leader:    'リーダー',` | **acts_as** |  |
| 96 | literal | `manager:   'マネージャー',` | **acts_as** |  |
| 97 | literal | `president: '社長',` | **acts_as** |  |
| 100 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者'];` | **org_wide** |  |
| 144 | db-query | `let q = supabase.from('profiles').select('id, email').eq('role_title', role).eq('is_active', true);` | **DB直引き→acts_as** |  |

## client/src/lib/purchaseComments.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 171 | db-query | `.in('role_title', ['マネージャー', '社長', '管理者']);` | **DB直引き→manager_plus** |  |

## client/src/pages/BoardPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 168 | literal | `const MANAGER_LEADER_ROLES = ['リーダー', 'マネージャー', '社長'];` | **leader_plus** | Q6：管理者を除いている。意図か確認 |
| 1920 | literal | `const ROLE_ORDER = ['管理者', '社長', 'マネージャー', 'リーダー', '一般', 'その他'];` | **rank** |  |

## client/src/pages/CalendarPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 279 | literal | `const ROLE_ORDER = ['社長', '三役', 'マネージャー', 'リーダー'];` | **対象外** | 「三役」はグループ名。役職の序列ではない |
| 1536 | literal | `const forceAllTeams = viewParam === 'fyi' || focusDate !== null || isAdmin || roleTitle === '社長';` | **org_wide** | 全チーム表示 |
| 1812 | db-query | `supabase.from('profiles').select('id, name, role_title, employment_type, group_names').eq('is_active', true).neq('role_title', '管理者').then(({ data }) => {` | **DB直引き→acts_as≠accounting** | 一覧から経理を除く |

## client/src/pages/OvertimePage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 120 | literal | `const REVIEWER_ROLES = ['リーダー', 'マネージャー'];` | **acts_as∈(leader,manager)** | 確認者の候補 |
| 122 | literal | `const ROLE_RANK: Record<string, number> = { '社長': 1, '管理者': 1, 'マネージャー': 2, 'リーダー': 3, 'フロア責任者': 4, '一般': 5 };` | **rank** |  |
| 125 | literal | `const SELF_REVIEW_ROLES = ['マネージャー', '社長', '管理者'];` | **manager_plus** | 自己確定 |
| 127 | literal | `const ABSENCE_REVIEWER_ROLES = ['マネージャー'];` | **acts_as=manager** |  |
| 2584 | db-query | `supabase.from('profiles').select('id, name, role_title').in('role_title', REVIEWER_ROLES).eq('is_active', true).order('role_title').order('name'),` | **DB直引き→acts_as∈(leader,manager)** |  |
| 2675 | compare | `const byRole = rules.find(r => r.role_title === roleTitle);        // なければ役職ルール` | **role_id** | 段5：役職ルールの照合 |

## client/src/pages/PurchaseRequestPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 683 | literal | `const isManagerPlus = isAdmin || ['マネージャー', '社長'].includes(roleTitle);` | **board_approver** |  |
| 684 | literal | `const canApprovePurchase = isAdmin || ['リーダー', 'マネージャー', '社長'].includes(roleTitle);` | **leader_plus** | Q6：管理者を除いている（isAdmin で救われる） |

## client/src/pages/RoomBookingPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 10005 | literal | `const fixed = r.name === 'パート' || r.name === '管理者';` | **場所予約（触らない）** | 相手へ連絡 |
| 10006 | literal | `const on = r.name === '管理者' ? true : (r.name === 'パート' ? false : allowed.includes(r.name));` | **場所予約（触らない）** | 相手へ連絡 |

## client/src/pages/SafetyCheckPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 214 | literal | `const isManagerPlus = isAdmin || ['マネージャー', '社長', '管理者'].includes(roleTitle);` | **manager_plus** |  |
| 215 | literal | `const isLeader = roleTitle === 'リーダー';` | **acts_as=leader** |  |
| 1015 | literal | `const roleOptions = ['パート', '一般', 'フロア責任者', 'リーダー', 'マネージャー', '社長', '管理者'];` | **roles一覧** |  |

## client/src/pages/ShiftDirectoryPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 43 | literal | `const ROLE_RANK: Record<string, number> = { '社長': 1, '管理者': 1, 'マネージャー': 2, 'リーダー': 3, 'フロア責任者': 4, '一般': 5, 'パート': 6 };` | **rank** |  |

## client/src/pages/ShiftReportPage.tsx

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 72 | literal | `const REVIEWER_ROLES = ['リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'];` | **approver** | フロア責任者を含む側 |
| 1032 | compare | `{[...reviewers.filter(r => r.id !== user.id && r.role_title !== '管理者' && r.role_title !== '社長')]` | **¬org_wide** | 確認先から社長・経理を除く |
| 1034 | literal | `const ord: Record<string, number> = { 'リーダー': 0, 'マネージャー': 1, 'フロア責任者': 2 };` | **rank** |  |
| 1116 | literal | `const canSeeAll  = isAdmin || ['リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'].includes(roleTitle);` | **approver** |  |
| 1185 | literal | `: (isAdmin || ['リーダー', 'マネージャー', 'フロア責任者', '社長', '管理者'].includes(roleTitle)) ? 'all'` | **approver** |  |
| 1276 | db-query | `supabase.from('profiles').select('id, name, role_title').in('role_title', REVIEWER_ROLES).eq('is_active', true).order('role_title').order('name')` | **DB直引き→approver** |  |

## client/src/utils/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 153 | literal | `if (row.amount <= 10000) return 'リーダー';` | **表示のみ** | 金額帯の経路ラベル |
| 154 | literal | `if (row.amount <= 30000) return 'マネージャー';` | **表示のみ** |  |

## supabase/functions/announcement-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 42 | literal | `if (!profile || !['admin', '管理者'].includes(profile.role_title)) {` | **app_metadata** | Q5=B：システム管理者だけ |

## supabase/functions/attendance-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 23 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者']` | **org_wide** | 既定値 |
| 150 | literal | `let roles: string[] = ['リーダー', 'マネージャー']` | **acts_as=leader|manager** | 既定値 |
| 168 | db-query | `let query = supabase.from('profiles').select('id').in('role_title', groupRoles).eq('is_active', true)` | **rpc resolve_role_recipients** | 写しを消して DB に寄せる |
| 176 | db-query | `const { data } = await supabase.from('profiles').select('id').in('role_title', orgWideRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |

## supabase/functions/board-scheduled-send/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 93 | literal | `const senderName = senderProfile?.name || '管理者';` | **表示のみ** |  |

## supabase/functions/create-user/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 53 | literal | `const adminRoles = ['admin', '管理者'];` | **app_metadata** | Q5=B |

## supabase/functions/delete-user/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 52 | literal | `const adminRoles = ['admin', '管理者'];` | **app_metadata** | Q5=B |

## supabase/functions/leave-approved-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 18 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者']` | **org_wide** |  |
| 89 | literal | `let roles: string[] = ['リーダー', 'マネージャー', '社長']` | **acts_as∈(leader,manager,president)** | 既定値 |
| 106 | db-query | `let query = supabase.from('profiles').select('id').in('role_title', groupRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |
| 114 | db-query | `const { data } = await supabase.from('profiles').select('id').in('role_title', orgWideRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |

## supabase/functions/new-signup-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 24 | db-query | `.eq('role_title', '管理者');` | **acts_as=accounting** | 経理への連絡 |

## supabase/functions/overtime-approve/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 98 | literal | `if (!['社長', '管理者', 'マネージャー'].includes(me?.role_title ?? '')) {` | **manager_plus** |  |
| 207 | db-query | `const { data: admins } = await db.from('profiles').select('id').in('role_title', ['管理者', '社長'])` | **org_wide** | 締め後アラート |

## supabase/functions/overtime-grant-request-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 45 | literal | `let roles: string[] = ['管理者']` | **acts_as=accounting** |  |
| 50 | db-query | `const { data } = await supabase.from('profiles').select('id').in('role_title', roles).eq('is_active', true)` | **acts_as=accounting** |  |

## supabase/functions/purchase-reimbursement-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 53 | literal | `let roles: string[] = ['マネージャー', '社長']` | **board_approver** | 既定値 |
| 62 | db-query | `.in('role_title', roles)` | **rpc / roles!inner** |  |
| 82 | literal | `let pushRoles: string[] = ['社長']` | **acts_as=president** |  |
| 88 | db-query | `.from('profiles').select('id').in('role_title', pushRoles).eq('is_active', true)` | **rpc / roles!inner** |  |

## supabase/functions/receipt-bulk-zip/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 48 | compare | `if (!profile || profile.role_title !== '管理者') {` | **acts_as=accounting** | 領収書の一括DL＝経理の業務 |

## supabase/functions/receipt-signed-url/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 5 | literal | `const VIEW_ROLES = ['管理者'];` | **acts_as=accounting** |  |
| 8 | literal | `const QUOTE_VIEW_ROLES = ['リーダー', 'マネージャー', '社長', '管理者'];` | **leader_plus** | 見積の閲覧 |

## supabase/functions/remind-admin-setup/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 73 | db-query | `.eq('role_title', '社長')` | **acts_as=president** |  |

## supabase/functions/remind-leave-shift-adjust/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 143 | literal | `let recipientJson: Record<string, unknown> = { roles: ['マネージャー', '社長'], groupFilter: 'same', orgWideRoles: ['社長', '管理者'] }` | **acts_as∈(manager,president)＋org_wide** | 既定値 |

## supabase/functions/remind-overtime-threshold/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 17 | literal | `'社長': 1, '管理者': 1, 'マネージャー': 2, 'リーダー': 3,` | **rank** |  |
| 18 | literal | `'フロア責任者': 4, '一般': 5, 'パート': 6,` | **rank** |  |
| 94 | literal | `let roles: string[] = ['申請者本人', 'リーダー', 'マネージャー', '社長', '管理者']` | **acts_as／段5 role_id** | 既定値 |
| 95 | literal | `let orgWide: string[] = ['社長', '管理者']` | **org_wide** |  |

## supabase/functions/safety-check-send/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 162 | literal | `const isAuthorized = profile?.is_active && ['マネージャー', '社長', '管理者'].includes(profile.role_title || '');` | **manager_plus** |  |

## supabase/functions/send-leave-slack/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 80 | literal | `if (event === 'new_request') return approverRole === 'マネージャー' ? 'manager' : 'leader';` | **acts_as** | 🚨 役職名で Slack の宛先を振り分けている |
| 147 | literal | `lines.push(nextApproverName ? *確認先：* ${nextApproverName}（${nextApproverRole || 'マネージャー'}） : '*確認先：* マネージャー')` | **表示のみ** |  |

## supabase/functions/shift-report-confirmed-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 21 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者']` | **org_wide** |  |
| 116 | literal | `let roles: string[] = ['リーダー', 'マネージャー']` | **acts_as=leader|manager** | 既定値 |
| 134 | db-query | `let query = supabase.from('profiles').select('id').in('role_title', groupRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |
| 142 | db-query | `const { data } = await supabase.from('profiles').select('id').in('role_title', orgWideRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |

## supabase/functions/storage-usage-check/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 47 | db-query | `const { data: admins } = await supabase.from('profiles').select('id').eq('role_title', '管理者');` | **acts_as=accounting** |  |

## supabase/functions/time-adjustment-notify/index.ts

| 行 | 種別 | いまのコード | 割り当て | 備考 |
|---|---|---|---|---|
| 16 | literal | `const DEFAULT_ORG_WIDE_ROLES = ['社長', '管理者']` | **org_wide** |  |
| 109 | literal | `let roles: string[] = ['リーダー', 'マネージャー']` | **acts_as=leader|manager** | 既定値 |
| 128 | db-query | `let query = supabase.from('profiles').select('id').in('role_title', groupRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |
| 136 | db-query | `const { data } = await supabase.from('profiles').select('id').in('role_title', orgWideRoles).eq('is_active', true)` | **rpc resolve_role_recipients** |  |
| 174 | db-query | `? await supabase.from('profiles').select('id').in('role_title', ['リーダー', 'マネージャー']).eq('is_active', true).overlaps('group_names', senderGroups)` | **acts_as∈(leader,manager)** |  |
| 175 | db-query | `: await supabase.from('profiles').select('id').in('role_title', ['マネージャー', '管理者']).eq('is_active', true)` | **acts_as∈(manager,accounting)** | 🚨 「マネージャー＋管理者」は他に無い組み合わせ。要確認 |

